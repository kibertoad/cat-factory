# Base URL of the cat-factory backend (the Cloudflare worker / wrangler dev server).
# Defaults to the local dev server; copy this file to `.env` to override.
NUXT_PUBLIC_API_BASE=http://localhost:8787
