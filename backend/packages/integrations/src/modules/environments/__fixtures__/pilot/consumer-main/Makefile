# Sanitized fixture — the pilot repo also ships a Makefile. The detector prefers the
# `bin/dev-console` repo-CLI over a Makefile, so the `repoCliHint` points at the former;
# this file is here only to keep the snapshot faithful (its presence does not change the
# recommendation). The detector never parses it.
.PHONY: build test
build:
	@echo "build (fixture stub)"
test:
	@echo "test (fixture stub)"
