# Sanitized fixture — the committed env template the real consumer keeps under
# services/app/ (outside the compose dir). Its NAME is the only thing detection reads
# (`.env.dev.local-dist` → materialize `.env.dev.local` before `up`); the placeholder
# values below are never parsed. See ../../../README.md (env templates one level deep).
DATABASE_URL=
APP_SECRET=
