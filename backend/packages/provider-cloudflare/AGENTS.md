# `@cat-factory/provider-cloudflare` — opt-in Cloudflare Workers AI registry

Mix into a `CompositeModelProvider` to add the `workers-ai` provider (an in-process binding on
the Worker, OpenAI-compatible REST elsewhere). One of the opt-in `provider-*` packages.
Single-file: `src/index.ts`.
