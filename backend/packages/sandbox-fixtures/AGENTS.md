# `@cat-factory/sandbox-fixtures` — graded no-repo fixtures for the sandbox

Hand-authored inline agent inputs (requirements / clarity / code-review / architecture-proposal
review) + expectations, rated by trickiness and impact. Consumed by `@cat-factory/sandbox`.

**Entry:** `src/index.ts`; the fixtures live under `src/fixtures/`.
