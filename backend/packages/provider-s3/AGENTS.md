# `@cat-factory/provider-s3` — opt-in AWS S3 blob backend

Implements the kernel `BinaryBlobBackend` port over an S3 bucket (the alternative to the
runtime-default blob storage). Single-file: `src/index.ts`.
