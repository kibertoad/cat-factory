{
  "name": "@cat-factory/deploy-harness",
  "version": "0.2.0",
  "private": true,
  "description": "Container payload: a thin TypeScript wrapper that renders a service's Kubernetes manifests with real kubectl/kustomize/helm and applies them to a per-PR namespace. Carries no secrets (the per-job cluster token + git token arrive in the job body).",
  "type": "module",
  "main": "./dist/server.js",
  "exports": {
    ".": "./dist/server.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.typecheck.json --noEmit",
    "start": "node dist/server.js",
    "test": "vitest run",
    "test:run": "vitest run",
    "image:publish": "bash scripts/publish-image.sh"
  },
  "devDependencies": {
    "@types/node": "^26.0.0",
    "typescript": "^6.0.3",
    "vitest": "^4.1.9"
  }
}
