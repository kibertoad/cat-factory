{
  "$schema": "https://json.schemastore.org/tsconfig",
  "comment": "Self-contained (does not extend the monorepo base) so the Docker build context can be just this package directory.",
  "comment:declaration": "Declarations are emitted for the `./claude-stream` subpath: the BACKEND imports the Claude Code stream aggregator from `dist` at RUNTIME (unlike the compile-only `./embed`, which resolves to source), so it needs types beside the JS.",
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "lib": ["ES2023"],
    "types": ["node"],
    "strict": true,
    "noUncheckedIndexedAccess": true,
    "noImplicitOverride": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "verbatimModuleSyntax": true,
    "isolatedModules": true,
    "forceConsistentCasingInFileNames": true,
    "rootDir": "src",
    "outDir": "dist",
    "declaration": true,
    "sourceMap": false
  },
  "include": ["src/**/*.ts"]
}
