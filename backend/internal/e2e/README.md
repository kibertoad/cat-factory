# @cat-factory/e2e

Playwright end-to-end suite for the **assembled product**: a real Chromium drives the
real SPA (the `@cat-factory/app` Nuxt layer, served via the `deploy/frontend` consumer),
which talks to a **real Node backend**: real Postgres, real pg-boss durable execution,
and the real WebSocket push transport.

Only the **external** dependencies are faked, so the suite is deterministic, needs no
secrets, no Docker and no network:

| External dep                       | Faked with                                                                                                                                                                                                                                                                                                                                                  |
| ---------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| LLMs + per-run agent containers    | `FakeAgentExecutor` (the canonical conformance fake): no LLM HTTP, no Docker                                                                                                                                                                                                                                                                                |
| inline (non-container) LLM calls   | `E2eInlineModels` (`src/fakeInlineModel.ts`), a per-workspace `ai/test` model that answers by the SHAPE of the prompt it is handed, so the requirements-review loop's calls (review / incorporate / re-review, plus the Writer, which recommends nothing) are each scripted. See the seam below                                                             |
| judge verdicts                     | `E2eJudgeAssessor` (`src/fakeJudge.ts`) + the SHIPPED example judge (`@cat-factory/example-custom-agent`'s `scope-adherence`) registered on the container's own `judgeRegistry`, so the judge bucket is drivable at all (the registry ships empty and an unwired assessor is a pass-through)                                                                |
| repo bootstrap                     | `FakeRepoBootstrapper`                                                                                                                                                                                                                                                                                                                                      |
| CI / mergeability / release-health | per-workspace fake gate providers (`E2eGateProviders`) wired via `buildNodeContainer`'s `gateProviders` seam, so a spec can drive the `ci`/`conflicts`/`post-release-health` gate → helper-agent loop (a gate with no scripted verdict passes through, as before)                                                                                           |
| GitHub App                         | faked **on** with the canonical `FakeGitHubClient` + the real Drizzle projection repos, wired through `buildNodeContainer`'s `overrides` seam with NO real credentials (`src/fakeGitHub.ts`). `seedGitHub` connects a workspace with a deterministic repo + branches; the read endpoints serve from Postgres projections. See `github.spec.ts`.             |
| a run's own repository             | `makeE2eRunRepoResolver` (`src/fakeGitHub.ts`) as `overrides.resolveRunRepoContext`, so the engine's checkout-free repo WRITES run for real against the fake client (the deep review's `createReview`), and `review`-task creation runs its real validation against `getPullRequest`. Deliberately not the facade's production resolver: see the note below |
| email / Slack                      | left **off** (all opt-in; the board renders and the un-scripted notifications channels pass through without them)                                                                                                                                                                                                                                           |

Everything else is production code: the controllers, the (dev-open) auth gate, the
durable execution worker + sweepers, and the per-workspace real-time hub. So a run
started over REST advances durably and the SPA updates **live over the WebSocket**, just
with a fake agent doing the "work".

The same process also serves a second, **auth-enabled** HTTP surface (with a second SPA instance
pointed at it) for the specs whose subject is identity: the login screen, and any policy that names
PEOPLE. Nothing is faked there either: it is the same container with `config.auth` on. See [the
auth-enabled stack](#the-auth-enabled-stack-the-auth_frontend_url-seam).

See [`src/testServer.ts`](./src/testServer.ts) for the backend wiring (it reuses the
`buildContainer` seam of `@cat-factory/node-server`'s `start()`).

## Specs

Every spec follows the same signature pattern: **seed/trigger over REST, then assert only on
LIVE pushed UI updates** (no reloads, no fragile canvas drag/zoom). Shared setup lives in
[`tests/fixtures.ts`](./tests/fixtures.ts): a `seededBoard` fixture (seed → pin → open), an
**auto** `pageErrors` fixture that fails any test on an uncaught SPA exception, and `newSession`
for a second signed-in browser. **Take a secondary session from `newSession`, never from
`browser.newContext()`**: it joins the session to `pageErrors` (a hand-rolled context is outside
that auto fixture, so a component throwing in the very rail under test would pass) and closes the
context on teardown, failure path included (`browser` is worker-scoped, so a leaked context keeps a
live subscriber for every later test in the worker). Common helpers

- named timeouts are in [`tests/helpers.ts`](./tests/helpers.ts).

The SPA boots in **basic** interface mode, which hides the power-user nav destinations and the
less-used run options. A spec whose subject is something else (RBAC gating, the compact
drawer) must call `useAdvancedInterfaceMode(page)` before `openBoard` rather than assert
against whatever the tier happens to show, or it fails for the wrong reason;
`ui-mode.spec.ts` is the one place that exercises the default and the switch themselves.

### Assert state the product HOLDS, never a state it passes THROUGH

A web-first assertion polls, so it can only see what is still true when a poll lands. Aiming one
at a transient is not a slow test, it is a **coin flip that CI eventually loses**, and it fails as
`element(s) not found` / `unexpected value` with nothing pointing at the real cause. This is the
second flake class beside the store clobber the [SPA
README](../../../frontend/app/README.md#real-time-store-coherence-avoid-the-full-refresh-clobber)
covers, and unlike that one it is a defect in the SPEC, so no product change can fix it. Both
shapes have already shipped and been removed:

- **An in-flight control state.** `<AgentFailureCard>`'s retry button disables itself for exactly
  as long as one POST plus one snapshot refresh takes, which against a local backend is a few tens
  of milliseconds: inside a single poll interval, so `toBeDisabled()` was unobservable in
  principle. What proves the click was handled is what the click PERSISTED, here the run's
  prior-attempt failure trail (`carryForwardFailures`) gaining exactly one entry.
- **A surface the product deliberately un-renders.** A `done` task leaves the frame's three live
  swimlanes for its collapsed Done lane, so no card for it is in the DOM, and a spawned block
  reaches the
  browser only through the coarse debounced `board` refresh, so an unattended run that auto-merges
  races that refresh and usually wins. Do not widen the timeout, which only makes losing the race
  more certain: script the fake so the run settles somewhere the surface SURVIVES
  (`confidence: 0.2` makes the merger decline and park the task at `pr_ready`), then assert on
  that settled status too, so the spec also proves the run got that far.

The general move is to find the state the run WROTE and read it back where the SPA renders it. When
only a transient exists, the assertion belongs in a component or store unit test, not here.

### Click the AFFORDANCE, never a container whose centre happens to sit on it

The third flake class, and the one that reads least like a spec defect. Playwright clicks the
CENTRE of the element it is given, so `card.click()` does not mean "click the card": it means
"click whatever is at the card's midpoint". A `pr_ready` task card is ~85px tall and its midpoint
lands in the ACTION ROW, whose membership is asynchronous: `Outcome` renders only while
`outcomeReadable` holds, and that is composed from the EXECUTION INSTANCE in the store, which
arrives on a different event than the `data-status` a spec gates on. So the card reads
`Review | Merge` until the instance lands and `Outcome | Review | Merge` after, and one unchanged
click resolves to **Review** (opens the focus view) in the second case and to **Merge** (opens the
merge confirm, and no focus view) in the first. The spec then waits out its full timeout on a step
list nothing opened, and the failure points at the assertion rather than at the click. The PR chip
is a third member on the same row and moves the midpoint again.

Take the affordance by `data-testid`. `openTaskFocusView(card)` is that seam for the focus view
(`pipeline-step`); a spec that only needs the card SELECTED may still click the root, because
selection is what every path through the card does. Adding a control to a card is enough to move
a midpoint, so a spec that depends on one is a spec that a UI change breaks somewhere unrelated.

| Spec                             | Covers                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| -------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `boot.spec.ts`                   | The product boots: the real SPA renders a seeded board from the real backend, no login gate.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `run.spec.ts`                    | Flagship: start a run → it parks on a decision live → resolve it in the UI → it reaches a terminal state, all over the WebSocket.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `notifications.spec.ts`          | A merger-less run raises a `pipeline_complete` notification live; the inbox bell + item render and acting / dismissing resolves it. (The real PR merge needs GitHub and is covered by the backend conformance suites, not here.)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `swimlanes.spec.ts`              | A service frame's tasks are laid out in status swimlanes, and a card MOVES between columns on a live push as its run starts, parks and merges. Also that the lane header counts track the same derivation the cards do, that a merged task leaves the live lanes for the collapsed Done lane (whose count is what keeps finished work from vanishing), and that an empty lane states its emptiness.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `create-task.spec.ts`            | Create a task through the real add-task modal → the new card appears on the board.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `consumer-extension.spec.ts`     | The consumer-extension dogfood (frontend-extension-mechanism slices A / B / D): the `acme:security` module shipped in `deploy/frontend` contributes a nav entry (`nav-acme-security`), an inspector body panel for task blocks (`acme-incident-panel`), a bespoke run-detail window (`acme-security-report-window`) for the `security-auditor` kind, a custom `acme:incident` task type (card badge, plus its own captioned row + help text in the grouped create-task picker), and a top-level overlay (`acme-security-dashboard-window`) the nav entry opens via `useAppOverlays`; all via `registerAppModule`, with zero host edits. Drives them through the real SPA, incl. opening the paired window from a completed step and the overlay from the nav item.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `approval-gate.spec.ts`          | A per-step human **approval** gate parks the run; Approve in the full-screen step-detail rail and it advances. (Distinct from the agent-raised decision in `run.spec`.)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `reset-run.spec.ts`              | The destructive run-lifecycle control: **Reset** a parked run from the task inspector → the `cancelExecution` path discards it and the task returns to `planned` live. (The dual of resolving/approving; nothing else covers cancel/reset.)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `mobile-shell.spec.ts`           | The responsive shell at a phone viewport (390×844): the sidebar is an off-canvas drawer toggled by the hamburger (open via the trigger, close via the backdrop), the inspector opens as a bottom sheet, and the board chrome doesn't overflow horizontally.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `mobile-canvas.spec.ts`          | The board canvas at a phone viewport (390×844): the toolbar's zoom-out / zoom-in / fit-view camera controls stay reachable and no minimap is rendered (it was removed altogether). (Touch pan/pinch themselves are a Vue Flow config; not driven here.)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `board-chrome.spec.ts`           | The board's top overlay region with standing advisories raised, the one condition `pinWorkspace` seeds away for every other spec. Restores the infra-setup advisories before the first navigation, then asserts the toolbar's fit-view control (the board-basics tour's anchor) is the element at its own centre point and that the advisory stacks below it. `toBeVisible` passes on a control buried under a banner, which is what shipped: the placement rule lives in `BoardTopOverlays`, and rendered geometry is the only place a breach of it is visible.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `agent-failure-retry.spec.ts`    | A container-dispatch failure faults the run → the block goes `blocked` with the shared `<AgentFailureCard>` (banner + retry) live; the retry control re-drives it, evidenced by the inspector's prior-attempt failure trail gaining the superseded attempt. The unified failure/retry surface; nothing else covers a FAILED run through the SPA.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `merge-review.spec.ts`           | A pipeline ending in a low-confidence `merger` step declines to auto-merge → raises a `merge_review` notification live and leaves the task `pr_ready`; dismissing clears the inbox. (Complements `notifications.spec`'s merger-less `pipeline_complete`.)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `result-window-shell.spec.ts`    | Slice 5 of the modular-vue adoption: the shared `ResultWindowShell` that all agent-run result windows now render inside. Drives the pilot (`MergerResultView`): reach a low-confidence merger step, open the focus view through the card's Review affordance (`openTaskFocusView`, never a centre click on the card), assert the window mounts in the shell (`result-window` + the merger verdict body), and that it closes on all three paths the shell now owns via the upstream `useModalBehavior`: the close button, **Escape** (the shared overlay stack, not a per-window listener), and a backdrop click. A second test drives a converted `#header-extras` window (the CI `gate`) through the same shell (opening it from the run's step list, asserting its status badge renders in the shell's extras slot, and that the `manageEscape: false` handoff closes it on Escape), so the coverage isn't pilot-only. A third test covers the **nested-overlay reconciliation**: the tester report window embeds `ArtifactLightbox`, which now rides the SAME `useModalBehavior` stack. Open the lightbox from a screenshot thumbnail and assert stacked-Escape ordering (the lightbox closes first, the owning window survives, then a second Escape closes the re-topped window).                                                                                                                                                                                                                                                                                                                                                                                                           |
| `pipeline-progress.spec.ts`      | A polled async agent kind (the durable `awaiting_job` loop) surfaces live subtask counts in the inspector run panel and drives the run to a terminal state: the async path `run.spec`'s inline decision run never touches.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `bootstrap-live.spec.ts`         | The repo-bootstrap flow: a run materialises a provisional service frame with a live progress badge that then clears; a failed bootstrap surfaces the shared `<AgentFailureCard>` (banner + retry) on the frame. (Triggered over REST; the real GitHub push is off.)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `recurring-run.spec.ts`          | The recurring-pipeline round-trip: creating a schedule pushes its reused on-board task live (`block-added`, no reload), and firing it via run-now drives THAT block to a terminal status over the WebSocket. (The bug-triage step mechanics live in the conformance suite.)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `initiative-preset.spec.ts`      | The initiative-PRESET flow: create an initiative from a preset (docs-refresh) over REST → its anchor card appears live → the fake-planned run auto-plans (analyst → planner → committer, unattended) and the loop spawns a first-class DECORATED `document` task on the board, all over the WebSocket. The spawned run is scripted to park at `pr_ready` rather than auto-merge, so its card survives to be asserted on. The S9 baseline the tech-migration preset E2E extends.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `tech-migration-preset.spec.ts`  | The tech-migration PRESET flow (the second real preset): create a FULL-interview initiative over REST → the `pl_initiative` run's interviewer converges on the seeded qa (fake inline model) → the planner returns a five-phase migration plan the generic ingest normalizer accepts → approve the parked planner gate over REST → the loop spawns the phase-1 blast-zone report as a DECORATED `document` task live. Exercises the interviewer + planner-gate + template-shape + `seedMigrationPlan` decoration the docs-refresh baseline does not.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `initiative-checkpoint.spec.ts`  | The phase CHECKPOINT flow (custom-initiative D2): a planner-authored `checkpoint: true` phase pauses the initiative once its item reaches `done` (merger-tailed pipeline) → the anchor card flips to `paused` live and phase two has NOT spawned → the tracker window shows the checkpoint pause banner + "awaiting review" phase badge → **Resume (GO)** from the banner spawns phase two's task live, while a sibling spec takes the **Cancel (NO_GO)** branch (card → `cancelled`, phase two never spawns). Proves the checkpoint genuinely gates the next phase in both directions.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `initiative-plan-review.spec.ts` | The initiative PLAN-APPROVAL gate, driven entirely through the UI: a `pl_initiative` run parks on the planner's human gate → the anchor card swaps "Run planning" for its review affordance live → the button opens the tracker window, whose plan-review rail approves the plan → the committer flips the card to `executing` and the rail disappears with the park it resolved. The other initiative specs clear this gate over REST as a trigger; this is where the board + window path itself is pinned. **Second spec:** the rail REVIEWS the rendered plan: the gate parks on a markdown rendering of the ingested plan (not the planner's transcript summary), so the document, its outline and per-block click-to-comment are asserted, send-back is refused until there is something to re-plan from, and an anchored comment drives a re-plan that parks again with the drafts cleared.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `ci-gate.spec.ts`                | The CI gate → ci-fixer loop: a `ci` step probes RED via the fake CI provider (`ciStatus: [false, true]`) → dispatches the `ci-fixer` container agent → re-probes GREEN → the ci step reaches `done` live and the task settles `pr_ready`. The operational GitHub gate flow that used to pass through (GitHub off).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `conflicts-gate.spec.ts`         | The conflicts gate → conflict-resolver loop: a `conflicts` step probes `conflicted` via the fake mergeability provider (`mergeability: ['conflicted','mergeable']`) → dispatches the `conflict-resolver` → re-probes clean → the step clears `done` live and the task settles `pr_ready`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `post-release-health.spec.ts`    | The post-release-health gate → on-call escalation: the merger auto-merges (`confidence: 1`), the gate probes `regressed` via the fake release-health provider (`releaseHealth: ['regressed']`) → escalates the async `on-call` agent → raises a `release_regression` notification live (bell + item). Asserts the whole path through that pushed notification (the gate escalates only once the release actually merged), which is robust to the auto-merge relocating the task into its module. The flagship operational scenario the new mocks unlock.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `tester-fixer.spec.ts`           | The Tester→Fixer loop: a `tester-api` step returns a report that withholds its greenlight (`testReports: [notGreen, green]`) → the engine dispatches the `fixer` and re-tests → the second report greenlights → the tester step reaches `done` live and the task settles `pr_ready`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `rework-loop.spec.ts`            | The companion rework loop: a persistently-failing `reviewer` grade (`companionRating: 0.4`) loops the `coder` on its automatic budget, then parks the run at the iteration cap → the task goes `blocked` live and a `decision_required` notification lands in the inbox.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `followup-gate.spec.ts`          | The Follow-up companion gate: the async `coder` surfaces a forward-looking follow-up (`followUps: [...]`) → the run parks `blocked` at the Coder's completion → a `followup_pending` notification lands in the inbox live.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `github.spec.ts`                 | The GitHub App integration, faked ON with no real credentials (`src/fakeGitHub.ts`): `seedGitHub` connects a workspace with the deterministic `octo/demo` repo + branches, then importing it over REST (`/blocks/from-repo`) makes a service frame appear live on the board, and a task under that repo-linked frame renders the slice-4 apriori-branches picker in its inspector. Unlocks the whole GitHub surface the e2e backend used to run without.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `fork-decision.spec.ts`          | The implementation-fork decision phase: a task forcing the phase (`coder.forkDecision: always`) + a proposer returning two forks (`customResult`) parks the run `blocked` → a `fork_decision_pending` notification opens the fork window showing both approaches → the human CHATS (the grounded, no-model canned reply is pushed live, proving the chat re-park round-trips) → choosing one resumes the Coder to a terminal state, all over the WebSocket.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `rbac.spec.ts`                   | Workspace RBAC (ADR 0025) as the assembled product shows it: the SAME restricted board, booted by two authenticated users (via `seedRbacScenario` + `pinAuthedWorkspace`), degrades differently in the real SPA: a `viewer` sees it read-only (per-frame add-task + build-pipeline + settings nav absent, the inspector Run trigger disabled with the read-only reason), while an account **admin** (no member row, the escape hatch) sees the same board with those authoring/admin affordances present and Run enabled. The UI round-trip only the assembled product exposes; the port-level 404/list-filtering/floor-403s are the conformance suite's job.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `tutorial.spec.ts`               | The in-app tutorial's first-launch behaviour, which only a fresh browser can show: an un-answered launch prompt (`pinWorkspace(..., { tutorial: 'unanswered' })`) auto-opens on its own once the board is up, starting `board-basics` runs the coach-mark overlay against the REAL controls (the highlight ring resolves and measures a live `data-testid`), ending the tour tears the overlay down, and "No thanks" persists so a second launch opens straight onto the board. Every OTHER spec pre-answers the prompt in `pinWorkspace`: a first-run modal is a reka-ui dismissable layer, so leaving it open makes the whole suite's clicks unactionable. Plus the tours that key off RUN state: a real run parks on a decision and the parked-run tour goes from absent to offered with no reload, then the human answers the park THROUGH the tour and the tour survives its own success (the gate that offered it goes false, and a script re-read from the gated slot would tear the overlay down one step short of its finish card). Both halves are only reachable assembled: the unit tests drive `navSlotFilter` with a fixture gate object, while production goes through `createNavGates` reading the execution store. Plus the two surfaces that BRING a walkthrough up: the finish card's handoff (walk `board-basics` to its end, take the offered next tour, and assert the swap left no false "you missed N steps" notice behind — the overlay stays MOUNTED across it, so per-run state resetting with the script is only observable here), and the contextual offer appearing the moment a real run parks with no reload, then standing down while the tour it started runs. |
| `input-gate.spec.ts`             | The PRE-DISPATCH INPUT GATE, the first thing every run does: a task with no description parks the run before its first dispatch, and the notice names the missing input (`data-issue-code`). Both exits are driven, because a gate whose only exit is "ignore me" cannot be satisfied: **Proceed** waives it (the verdict re-renders as `waived`, since what was overruled stays part of the run's record) and **Recheck** releases it only after the task is actually fixed. A third test pins the park's ROUTING: it is the one dedicated park with no window of its own, so the generic attention affordance must open the step RAIL and not the step's usual result view (which would be a window about work that has not run). The one park that turns on the shape of the TASK rather than the pipeline, so no other spec reaches it.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `requirements-review.spec.ts`    | The REQUIREMENTS-REVIEW clarification loop, the first step of the default pipelines and the platform's main human-in-the-loop surface: scripted findings park the run → the card's attention affordance opens the review window → an answer typed into a finding persists on blur (`answered`) → **Incorporate** hands the human back to the board while the driver folds + re-reviews → the converged gate settles and the run advances. A second test takes the DISMISS branch: with nothing to fold in the rail swaps to **Proceed**, and the settled banner arrives in the still-open window. Unlocked by the prompt-shape inline mock (the whole loop is three inline LLM calls the agent-executor fake never sees).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `judge-gate.spec.ts`             | JUDGES, the fourth step-taxonomy bucket, driven through the SHIPPED example (`scope-adherence`): a failing verdict BOUNCES the coder with the findings as its rework brief and the re-judged work passes live, proved on the engine's own ROUND HISTORY (two rounds, `bounce` then `pass`) rather than inferred from the run having finished, which a run that never bounced would also do; a script that fails twice spends the bounce budget and PARKS, where the judge window renders the score, the rubric findings and the decision rail, and **Proceed** overrules the rubric. Only reachable assembled: no frontend code names the kind — it arrives through the workspace capability manifest with `resultView: 'judge'`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `run-controls.spec.ts`           | The everyday delivery loop driven entirely by hand, with no REST trigger: author a task in the real add-task modal, CHOOSE its pipeline in the picker (basic mode's only surviving run option), **Start** it from the card (so the pipeline pin has to have round-tripped), then **Stop** it from the inspector behind the shared confirm. Stop is deliberately contrasted with Reset (`reset-run.spec`): a stop KEEPS the run, so the block lands `blocked` with the failure surface on it rather than back at `planned`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `pipeline-builder.spec.ts`       | The AUTHORING half of the delivery loop, and the only spec that creates a pipeline through the real builder rather than over REST: open it from the sidebar, add `architect` → `coder` from the agent palette, put a human approval gate on the FIRST step, save, then run a task on it. The gate is what makes the drawing observable — the persisted wire shape is read back over REST, and the run then parks on the ARCHITECT step (asserted on the step rail's own `data-agent-kind`, not on its translated heading), so a draft flag written at the wrong index, dropped from the save payload, or realigned by the companion/enabled bookkeeping fails here instead of shipping a pipeline that saves cleanly and runs differently from the picture. A second test covers the other half of that surface, what the palette OFFERS before anything is drawn: picking the `review` purpose drops `coder` with its whole category AND `documenter` on its own, the per-KIND narrowing a filter keyed on the display section cannot express.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `pr-review.spec.ts`              | DEEP PR REVIEW, both dispositions: what a human curates is what reaches the pull request. A `review` task's reviewer parks on three findings; the window groups them by slice, a nit is DISMISSED (curation, run stays parked), a medium is DESELECTED, and Post publishes the rest, after which the report reads back `1 of 1 comments posted`, the published finding is badged and the deselected one is not. A second test drives the PARTIAL post: the fake refuses one anchor ONCE, so the run is HANDED BACK (re-parked carrying the report, dispositions on offer again) rather than finished, the failure list names the anchor and the reason, and the retry attempts only the comment that did not land. Both tests then assert the WIRE (`readReviewAttempts`), which is the only place at-most-once posting is visible: the window's report reads the same whether a retry skipped or re-sent what had already landed. The engine's own repo-write path runs for real (`resolveRunRepoContext` → `createReview` against the fake client), so a review that silently posts nothing fails. Needs a repo-LINKED frame, hence `seedOwnRepo` (see below).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `outcome-summary.spec.ts`        | "READ THE RESULT": the run-keyed outcome card, which in basic mode is what the card's one result affordance opens. A tester-bearing run settles at `pr_ready` and its outcome reads back the disposition, the PR, what was asked, the per-requirement verdicts (including the UNCHECKED one) with what the tester observed, the greenlight, and the advisory concern that stands despite it. The reduction is unit-tested; what only the assembled product shows is that a producer's real recorded state reaches the section that renders it — the failure mode being a section that reads CLEAN because nothing arrived (`runOutcome.ts` rule 2). Also pins that a spec-less run marks its rows as ids and claims no regression.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `board-switch.spec.ts`           | Switching boards from the sidebar switcher, the affordance every other spec bypasses by pinning a board client-side, and the one that has to MOVE the live connection. Two seeded boards; it switches to the one it means to start from (`switchBoard`, idempotent) rather than resting on which board a cold load resolves to, then authors a task on each over REST and asserts the new board's arrives live while the left board's does not. Ordered so the positive is the synchronization point for the negative (an absence assertion alone would pass on a push that had not landed yet). The stale-subscription defect it guards is invisible otherwise: the new board's blocks all render from the REST snapshot, and only later updates go missing.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `merge-policy.spec.ts`           | The MERGE POLICY a human authors is the one the merger applies. Both tests hand the merger the SAME within-`Balanced` assessment and change only the policy: under the workspace default it auto-merges (the card un-renders, the block reads `done`), and under a stricter preset typed into Workspace Settings → Merge and pinned on the task through the inspector's picker, the identical assessment is refused and raises `merge_review`. The pair is the point: the refusal alone could be a dozen unrelated things, and `merge-review.spec` reaches its refusal by making the AGENT report a bad diff, so nothing there depends on the policy. Also asserts the stored ceilings (percentages in the editor, fractions on the wire), since a panel that shipped `10` for `0.1` would refuse this assessment too.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `member-admin.spec.ts`           | WORKSPACE ACCESS ADMINISTRATION: the Members tab, where a role is changed and a member removed. `rbac.spec` proves the two ends render differently but seeds both roles into the database; this drives the act itself, and the proof is what a DIFFERENT signed-in user's browser then renders (each test spends a second context, booted AFTER the change so it resolves fresh through the real gate). Promoting a `viewer` to `member` gives that user an authoring board; removing a member from a restricted board drops it out of their reach, evidenced by their session landing on the seeded SPARE board instead and the switcher not offering the revoked one.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `quorum-gate.spec.ts`            | A human checkpoint with a POLICY: two named approvers, both required, authored in the real builder (`gate-required-approvals` + the roster-backed `gate-named-approvers`) and asserted on the persisted `stepOptions[i].gateConfig` before the run starts. Then one browser context per person: a `member` who is not named is refused in the rail (`gate-not-approver`, approve disabled), the first named approver's approval is RECORDED without releasing the run (quorum 1 of 2, card still `blocked`, the assertion a quorum that counts one person twice fails), and the second releases it. Runs on the AUTH-ENABLED stack, because a policy that names people needs a browser that has one.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `sign-in.spec.ts`                | THE FRONT DOOR, on the AUTH-ENABLED stack: the login screen gates the board (no session ⇒ no canvas), a wrong password is refused ON the form without advancing, the right one signs in and the board loads as that user (identity in the sidebar, the seeded task, and the live WebSocket connected, the one assertion that catches a session good enough for REST and not for the stream), the session survives a RELOAD (the only way to show a returning user isn't re-gated), and sign-out returns to the gate and stays there across a reload. The one surface every user of a hosted deployment passes through, and previously the only one with no coverage at all.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `ui-mode.spec.ts`                | The basic/advanced interface separation: a board opens at the SHIPPED default (basic ⇒ icon rail, advanced destinations absent from the DOM), the sidebar rail toggle expands and re-collapses independently of the tier, switching tiers from the sidebar switcher re-gates the nav LIVE (no reload, same reactive slot filter an RBAC flip uses), a stored advanced choice survives a reload, a BASIC-mode rail choice survives a reload too (the preference is per-tier, so `collapsed` is basic's default rather than a rule that re-asserts itself), the command palette can reach the tier switch from basic mode (the route back out of the default tier), and the compact drawer is never railed.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |

### Auth-enabled RBAC scenario (the `seedRbacScenario` seam)

Every other spec runs the backend **dev-open** (anonymous). RBAC only means something with a
per-user session, so the shared backend ALSO configures a session secret **without** enabling any
auth provider: an anonymous request stays dev-open (so every existing spec is byte-identical), but a
request bearing a **signed** session token resolves to its user and the workspace-RBAC gate enforces
that user's access. `seedRbacScenario` (control route `/rbac-seed`, backed by `testServer.ts`) seeds
a restricted board with an admin + a scoped `viewer` (mirroring the cross-runtime
`defineWorkspaceRbacSuite` fixture) and mints a real Bearer token per principal; `pinAuthedWorkspace`
seeds it (plus the pinned board + active account) into the SPA's persisted pinia stores, which are
**cookie-backed** (`pinia-plugin-persistedstate/nuxt` defaults to cookies, not localStorage), so the
board boots as that authenticated user on that specific restricted board. It is one call into the
generic `seedTeamScenario` below.

What this hybrid gives you is a backend that ENFORCES per-user access while the SPA still renders
anonymously, and that asymmetry is exactly its limit: the browser has a session the server honours and
no identity the SPA can see. Anything that turns on who the VIEWER is (the login screen, a gate policy
naming people) needs the auth-enabled stack instead.

### The AUTH-ENABLED stack (the `AUTH_FRONTEND_URL` seam)

The shared backend above runs `TESTING_NO_AUTH`, which is what lets 40-odd specs seed and drive over
anonymous REST. That flag also makes two things structurally untestable there, and they are the two
most consequential surfaces in the product:

- the SPA renders the board anonymously instead of the LOGIN SCREEN (`needsLogin` short-circuits on
  the flag), so the front door is unreachable; and
- having no signed-in user, the SPA never resolves one. `config.auth.enabled` is false, so the auth
  handshake skips `getMe()` even with a valid session token. Every actor is then `unattributed`, and
  a gate policy that names PEOPLE refuses all of them. Correct for a deployment with auth off, and
  useless as coverage.

So the same process serves a SECOND HTTP surface with `config.auth` flipped on
([`src/authBackend.ts`](./src/authBackend.ts)), and a SECOND instance of the same SPA build points at
it ([`src/authFrontend.mjs`](./src/authFrontend.mjs)). A spec opts in with
`test.use({ baseURL: AUTH_FRONTEND_URL })` and otherwise drives the product exactly as any other
spec does. Four facts make that cheap:

- **`authConfig(c)` reads `container.config.auth` PER REQUEST**, so an app built over a container
  whose auth config differs is a full auth-enabled deployment with no forked wiring
  (`authEnabledContainer` is a shallow clone: same services, same repositories, `config.auth` on).
  It turns `devOpen` off along with `testingNoAuth`, even though every reader tests `enabled` first
  and the surface is closed either way today: a flag that only happens to be harmless through
  statement order is not a configuration, and the sign-in spec asserts what the SPA RENDERS, so it
  would stay green if that order ever changed.
- **It is the same container**, so one Postgres, one pg-boss worker, one engine. A run started over
  the ANONYMOUS surface is driven by that worker and pushed to browsers watching over this one,
  which is why the auth specs keep using the ordinary REST helpers for setup.
- **The SPA needs no second build.** Its API base is baked in at build time, but the Nitro server
  that serves the shell re-reads `NUXT_PUBLIC_API_BASE` at STARTUP, so the second origin is one
  extra process over the same `.output`, not a second (slowest-step) build. Since Playwright starts
  `webServer` entries in parallel, the launcher waits for the PRIMARY SPA to answer before it starts:
  that entry's command is `nuxt build && nuxt preview`, so a response means the build finished and
  `.output` is complete. Waiting on the FILE instead would start on whatever an earlier run left
  there, which the concurrent build then deletes under the process (a chunk fails inside a request,
  reading like a product bug) or which is simply the previous SPA, missing whatever a new spec
  selects. Both are silent, and the entry's own readiness probe passes either way.
- **Cookies are not PORT-scoped**, so `pinAuthedWorkspace` / `pinBoardForUser` /
  `useAdvancedInterfaceMode` seed both origins and no parallel set of helpers is needed.

The one thing that does NOT come for free is the WebSocket: the hub the primary listener registers
sockets on is created inside `start()` and never handed to the container, so the auth surface brings
its own and `fanOutRealtime` tees engine events into both. Without that a board opened on the auth
stack paints once from REST and then goes deaf, which is why a missing `realtimeSink` FAILS the boot
rather than skipping the tee, and why a drop inside the tee is logged through the kernel `Logger`
port: those are the only two things that name the cause of a spec that otherwise just hangs. That
function (and the config clone) are pinned by `src/authBackend.test.ts` in the browser-free lane
rather than left to surface as a flake.

Every port and origin on both stacks comes from ONE module, [`src/ports.ts`](./src/ports.ts), read by
the Playwright config (which starts the servers and probes them), the backend, the auth SPA's launcher
and the specs' own helpers. Four ends have to agree here, and a copy of `PORT + 2` in one of them
beside an `E2E_AUTH_PORT` read in another is not a knob: the stack boots, every probe passes, and a
spec then fails on a connection error naming nothing. Add a port there, never beside it.

### Real people, real sessions (the `seedTeamScenario` / `seedPasswordUser` seams)

There is no anonymous REST path to a user, an account membership or a workspace role, so the specs
whose subject is WHO may do what seed through the built container's own services
([`src/seedTeam.ts`](./src/seedTeam.ts)) over the control channel:

- **`seedTeamScenario`** seeds an org, a board and the principals a scenario asks for, minting a
  signed session per principal. The RBAC spec's fixed admin+viewer pair is one call into it. Its
  vocabulary carries the two facts a scenario has to be able to state: a principal enrolled in the
  ACCOUNT but NOT scoped to the board (`role: null`), and whether the board starts `restricted` or is
  restricted later through the UI. `spareBoard` adds a second, EMPTY board in the account, for a
  spec that takes access away, because the SPA resolves whatever board the caller can still reach
  and CREATES one when that is none, landing the session on a fresh board's onboarding gate (a state
  that says nothing about the revoke). It carries no sample architecture precisely so the primary
  board's fixed block ids are absent there and can be asserted on.
- **`seedPasswordUser`** writes a real password credential through the identity service the signup
  endpoint calls, plus the account and board the user lands on, so the password `sign-in.spec` types
  is verified by production code.

### Per-run fake behaviour (the `setFakeProfile` seam)

The backend boots ONCE and serves every spec (one shared Node process + one production frontend
build), so the fake-agent knobs below can't be flipped globally per spec. Instead a spec sets a
**`FakeProfile`** for its OWN freshly-seeded workspace over a test-only control channel and the
fakes resolve that per-workspace behaviour on each call: the same way existing specs vary
behaviour by choosing a pipeline SHAPE over REST. Call it BEFORE starting the run:

```ts
await setFakeProfile(request, workspaceId, { decisionOnSteps: [], dispatchThrowKinds: ['coder'] })
```

A workspace's fake is built lazily on its FIRST call and cached (it owns per-workspace state (job
maps, companion/tester counters, gate verdict sequences) that has to survive a whole run), so the
profile behind it is read exactly once. A write therefore **re-arms** that workspace: the cached
fakes are dropped so the next call rebuilds them from the profile just written. That is what lets a
spec change behaviour PART-WAY through a workspace's life: `initiative-checkpoint.spec.ts` re-arms
the decision gate so the next phase's run parks instead of settling to `done`, leaving a stable card
to assert on. Without it the late write would be a silent no-op, visible only as a load-dependent
flake. Re-arming resets that workspace's counters and job map too, so only write while the workspace
is quiescent, never while one of its own async jobs is mid-poll.

`FakeProfile` (see [`tests/helpers.ts`](./tests/helpers.ts)): `confidence`, `decisionOnSteps`,
`asyncKinds`, `dispatchThrowKinds`, `asyncPolls`, `bootstrapProgress`, `bootstrapFailWith`,
`customResult`, `initiativePlan` (the plan draft the fake `initiative-planner` returns, so an
initiative planning run reaches `executing` and the loop spawns the decorated tasks; the planner
faults without one). Two families were added for the operational-gate + agent-loop specs:

- **Operational-gate verdict scripts** (consumed by `E2eGateProviders`, one queue per probe, last
  entry repeats): `ciStatus` (`boolean[]`, green/red), `mergeability`
  (`('mergeable'|'conflicted'|'unknown')[]`), `releaseHealth` (`('healthy'|'pending'|'regressed')[]`).
- **Agent-loop knobs** (forwarded straight into the fake agent): `testReports` (Tester→Fixer),
  `companionRatings` / `companionRating` / `companionMalformed` (companion rework), `followUps`
  (the Follow-up gate), `pollFailKinds` / `pollFailCause` (structured async poll failure),
  `mergeAssessment`, `onCallAssessment` (post-release-health → on-call), `pullRequest`,
  `pooledContainer`.
- **Inline-LLM scripts** (consumed by `E2eInlineModels`, see below): `reviewFindings` (what the
  requirements reviewer raises on its FIRST pass; absent ⇒ it raises nothing and the gate
  auto-passes) and `incorporatedRequirements` (the document the incorporation companion returns).
- **Judge verdicts** (consumed by `E2eJudgeAssessor`): `judgeScores`, one score per assessment with
  the last repeating. Against the default preset (threshold `DEFAULT_JUDGE_MIN_SCORE`, bounce budget
  `1`), `[0.4, 0.9]` is one bounce then a pass and `[0.4, 0.4]` spends the budget and parks. The
  queue is keyed by WORKSPACE, so one run per workspace, and never read the queue's own order as
  evidence that a bounce happened: assert the round history the engine recorded.

A workspace with no profile gets the base backend behaviour, so the
pre-existing specs are unchanged. The same channel carries the SEEDS that have no anonymous REST path
(`/github-seed`, `/github-seed-own-repo`, `/rbac-seed`, `/team-seed`, `/password-user-seed`) and the
one read-only report (`/github-review-attempts`). It is a tiny separate HTTP listener on `PORT + 1` (`E2E_CONTROL_PORT`),
posted to from Node (Playwright's `request`), never from the browser: see
[`src/testServer.ts`](./src/testServer.ts) + [`src/fakeProfile.ts`](./src/fakeProfile.ts).

### Inline LLM calls (the `E2eInlineModels` seam)

The `FakeAgentExecutor` above only fakes the CONTAINER/agent steps. Several flows run an LLM
**inline** through the `ModelProvider` port instead, and never touch the agent executor: the
initiative INTERVIEWER (`pl_initiative`), the document interviewer, and the whole
requirements-review loop. On the keyless e2e backend the real per-scope resolver would fault them,
so `testServer.ts` injects **`E2eInlineModels`** (`src/fakeInlineModel.ts`) via
`buildNodeContainer`'s `overrides.modelProviderResolver`. It is the inline-LLM sibling of the fake
agent executor, and it answers on two axes:

- **WHICH call this is comes from the SHAPE OF THE PROMPT** (`classifyInlineCall`), because that is
  the only thing an inline call carries that says which step of a flow it belongs to. The markers
  are quotes of production strings from `requirements.logic.ts`, so `src/fakeInlineModel.test.ts`
  classifies prompts built by the REAL builders rather than by hand. That guard is not optional: an
  unmatched prompt falls through to the interview answer, whose JSON carries no `items`, so a
  drifted marker silently turns the requirements reviewer into one that finds nothing — and the
  failure then reads as an engine bug. **Every call the flow can make is NAMED**, including the
  Requirement Writer (`buildRecommendationPrompt`), which no spec drives: an unnamed call is one
  that answers with interview JSON, and the whole point of the classification is that no real caller
  lands on the catch-all by accident.
- **WHAT it answers is per-workspace**, resolved from the same `FakeProfile` registry the agent and
  gate fakes read (`forScope` carries the `workspaceId`), and re-armed by a profile write the same
  way. A workspace with no profile gets the historical behaviour: a converging interview decision
  and a review that raises nothing, so every pre-existing spec is byte-identical.

The three requirements calls a spec drives are therefore scriptable as one loop: `reviewFindings` on
the first pass, an incorporation document, and a re-review that ALWAYS converges — which is what
makes `requirements-review.spec.ts` a single deterministic park → answer → settle round trip. The
fourth, the Writer, deliberately recommends NOTHING: a synthesized product recommendation is
indistinguishable from a real one in the window, so the fake answers with an empty recommendation set
and the finding stays `open`. Scripting a real one is a script field this seam does not have yet.

### Judges (the `E2eJudgeAssessor` + registered-judge seam)

A judge needs two things before a browser can reach one, and the stock backend has neither:
`defaultJudgeRegistry()` ships EMPTY, and a judge with no wired assessor is a pass-through
(`enabled: false`). `src/fakeJudge.ts` supplies both. It registers the **shipped example**
(`@cat-factory/example-custom-agent`'s `scope-adherence`, including its own valibot verdict parser)
on the built container's own `judgeRegistry` through the same public seam a deployment uses, and
wires a per-workspace assessor that replays that workspace's `judgeScores`. Registered judges reach
the SPA through the workspace capability manifest with `presentation.resultView: 'judge'`, so the
judge window opens with no frontend wiring — which is most of what `judge-gate.spec.ts` proves.

### A run's own repository (`resolveRunRepoContext`) and a spec's own repo

Three facts about the e2e board decide how the run↔repository seam is wired, and a spec that needs a
repo has to know all of them.

**The seeded sample board has no repo-linked service frame**, and the production resolver THROWS for
a block under none — on purpose: a run must never guess which repository to write to, and
`runRegisteredPreOps` propagates that throw so a misconfigured block fails loudly. Wiring the
facade's own resolver (by handing `buildNodeContainer` its `githubClient` option, the way the local
facade does) therefore fails the POLL of every seeded run whose agent kind declares repo hooks: the
whole suite breaks so one spec can write to a repo. So the backend wires
`overrides.resolveRunRepoContext` = `makeE2eRunRepoResolver` instead, which answers ONLY for the repo
a spec seeded as the workspace's own and is `null` everywhere else — byte-for-byte what an unwired
resolver means for those blocks. Its `RepoFiles` delegates to the same fake client the GitHub module
reads through, so a repo op's write lands where a spec can see it (`client.files` / `client.writes` /
`client.reviewAttempts`).

**A `Service` is ACCOUNT-owned**, so two specs cannot import the SAME repo: `addServiceFromRepo`
dedupes across every board in the account and MOUNTS the existing frame, whose block row still
belongs to the workspace that imported it first — the second spec then gets a frame it cannot start
runs under (`Block not found` for its own workspace, which is how this was found). A spec that needs
a repo-linked frame calls **`seedOwnRepo`** (control route `/github-seed-own-repo`, derivation
`ownRepoFor`) and imports THAT, which dedupes against nothing. `pr-review.spec.ts` is the worked
example.

**The faked provider serves exactly ONE pull request** (`E2E_REVIEWED_PR`, number 42) and answers
"no such PR" for anything else, `createReview` included. That is deliberate: the PR-review methods
took the number and ignored it at first, so a run that resolved the wrong PR would have posted
cleanly against the same canned diff and every assertion would still have passed. For the same
reason `getPullRequest` is wired, which is what makes `review`-task creation run its REAL validation
(an unknown number is refused, a `prUrl` naming another repo is refused, and the stored reference is
canonicalised to the provider's own URL). A spec therefore passes the NUMBER and lets creation fill
in the URL.

One anchor in that diff, `src/session.ts:21` (`E2E_TRANSIENT_REVIEW_POST_FAILURE`), is refused the
FIRST time it is posted and accepted afterwards, keyed per installation and PR so one spec cannot
spend another's refusal. A partial post is its own disposition (the run is re-parked carrying the
report, and a retry must send only what did not land), and a per-comment failure cannot be reached
any other way: the engine folds an out-of-diff finding into the summary before the write is called.
`client.reviewAttempts` records every attempt, read by a spec through `readReviewAttempts`
(`/github-review-attempts`); it is the only view of what the engine actually sent, which is what
makes at-most-once posting assertable at all.

## Mocking other externals (when a spec needs a real outbound call)

The default suite needs no network mocks. If a future spec must exercise a real outbound
code path (e.g. the real `FetchGitHubClient`, or an inline LLM call), mock at the
backend's **outbound boundary** with **MSW** (intercepts `fetch` in-process) or inject a
port fake via `buildNodeContainer`'s `githubClient` seam, not in the browser, since the
SPA only ever talks to this backend.

## Running

Needs a reachable Postgres in `DATABASE_URL` (CI provides one; locally use
`deploy/local`'s `pnpm db:up`). From the repo root:

```bash
pnpm build                               # build the workspace libraries (e2e imports their dist)
pnpm --filter @cat-factory/e2e exec playwright install chromium
DATABASE_URL=postgres://postgres:postgres@127.0.0.1:5432/cat_factory_test \
  pnpm --filter @cat-factory/e2e run test:e2e
```

Playwright's `webServer` boots the backend (`src/testServer.ts`), the SPA (`deploy/frontend`, built
once and served by `nuxt preview`, pointed at the backend via `NUXT_PUBLIC_API_BASE`) and a SECOND
SPA instance beside it (`E2E_FRONTEND_PORT + 1`) pointed at the backend's auth-enabled surface on
`PORT + 2` (see the auth-enabled stack above; the same `.output`, so no second build). Use
`test:e2e:ui` for the
interactive runner. The browser suite is **not** part of the
unit `test:run` lane: it runs in its own CI job (`.github/workflows/ci.yml` → `Test
e2e`). That job is **non-blocking**: it isn't wired into the aggregated `Test` gate, so a
browser/boot flake can't block an otherwise-green PR. Promote it into `test-gate.needs`
once it has proven stable.

`test:run` is a separate, browser-free vitest lane over the test-only backend SEAMS in
`src/` (`vitest.config.ts` scopes it to `src/**/*.test.ts`, away from the Playwright specs
in `tests/`). It rides the ordinary `Test units (no DB)` job, so a seam whose breakage
would only ever show up as an e2e flake (`FakeProfileRegistry` re-arming a workspace's
fakes is the motivating case) gets pinned deterministically instead.

### Test isolation

Specs share one Postgres datastore (`workers: 1`, `fullyParallel: false`), but each spec
**seeds its own workspace** and pins it client-side (`pinWorkspace`), so concurrent
workspaces never collide and no per-test DB teardown is needed.

What actually decides which board OPENS, though, is the product's own resolution, not that pin: the
persisted pinia stores are COOKIE-backed under `pinia-plugin-persistedstate`'s Nuxt defaults, so
`pinWorkspace`'s `localStorage` write is read back for the direct-key state it also seeds (the
tutorial answer, the infra-setup dismissal) and NOT for the active board id, which resolves to the
first of a newest-first list. A spec that seeds one board is on it either way, which is why this has
never mattered; a spec that seeds TWO cannot infer which one it is looking at, and calls
`switchBoard` to say so (see `board-switch.spec.ts`). CI runs against a fresh,
ephemeral Postgres service per run, so accumulated rows are discarded with the container;
a retried spec simply seeds a new workspace. If a future spec needs a clean global state,
add a `globalSetup` that truncates rather than relying on ordering.

### Knobs

These env vars set the **base** fake behaviour every workspace inherits. A spec overrides them
per-workspace at runtime via `setFakeProfile` (see the seam above); prefer that over the globals,
which mainly exist so a whole-suite run can shift the defaults.

- `E2E_DECISION_ON_STEPS` (default `0`): agent-step indices where the fake agent raises a
  one-shot human decision, so the decision-gate flow can be exercised. Empty disables it. (A
  spec that wants no gate passes `decisionOnSteps: []` in its profile.)
- `E2E_DISPATCH_THROW_KINDS` / `E2E_ASYNC_KINDS`: comma-separated agent kinds that drive the
  polled `awaiting_job` loop (`E2E_ASYNC_KINDS`) or throw on container dispatch
  (`E2E_DISPATCH_THROW_KINDS`). Normally set PER WORKSPACE via `setFakeProfile` instead.
- `E2E_CONFIDENCE` (default `1`): the confidence the fake reports on the final step (drives
  auto-merge vs merge-review). Normally set per-workspace via `setFakeProfile`.
- `JOB_POLL_INTERVAL` / `CI_POLL_INTERVAL` (default `1 second` in the e2e backend): how often
  the durable driver polls an async job / a gate. Shortened from the 15s/30s production cadence
  so a polled `awaiting_job` run settles within the suite timeouts; inline specs never poll.
- `INITIATIVE_LOOP_INTERVAL_MS` (default `1000` in the e2e backend; `60000` in production): how
  often the Node initiative-execution loop sweeps for `executing` initiatives to spawn the next
  wave of tasks. Shortened so a just-planned initiative spawns its first decorated task within the
  suite timeouts (the planning run's terminal doesn't poke the loop: only the sweep spawns wave 1).
- `E2E_CHROMIUM_PATH`: opt-in: launch Chromium from this path instead of a `playwright
install` download. For sandboxes that ship a preinstalled browser and block the download
  (e.g. `E2E_CHROMIUM_PATH=/opt/pw-browsers/chromium`). Unset in CI.
- `PORT` (default `8787`), `E2E_FRONTEND_PORT` (default `3000`), `E2E_BACKEND_URL`,
  `E2E_CONTROL_PORT` (default `PORT + 1`) / `E2E_CONTROL_URL`.
- `E2E_AUTH_PORT` (default `PORT + 2`) / `E2E_AUTH_BACKEND_URL`, `E2E_AUTH_FRONTEND_PORT` (default
  `E2E_FRONTEND_PORT + 1`) / `E2E_AUTH_FRONTEND_URL`: the auth-enabled stack's ends.
- All of the above resolve in ONE place, [`src/ports.ts`](./src/ports.ts), which every end reads (the
  Playwright config, the backend, the auth SPA's launcher, the specs' helpers). So overriding `PORT`
  or `E2E_FRONTEND_PORT` moves the whole set together and can never collide with it, and a `*_URL`
  override moves the LISTENER too, not just the callers.

### Promoting the e2e job to a required gate

`test-e2e` is intentionally **non-blocking** today. Promote it into `test-gate.needs` (in
`.github/workflows/ci.yml`) once it has earned trust:

1. The suite has been green on `main` for ~10+ consecutive runs with **no** retries kicking in
   (a retry that rescues a run still counts as a flake to investigate first).
2. No spec relies on a fixed sleep: only web-first assertions (`toBeVisible` / `expect.poll`).
3. The cold-start `BOOT_TIMEOUT` comfortably covers the CI runner's first board paint.

Until then a browser/boot flake stays advisory and can't block an otherwise-green PR.
