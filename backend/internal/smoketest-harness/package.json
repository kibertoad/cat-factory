{
  "name": "@cat-factory/smoketest-harness",
  "version": "0.6.0",
  "private": true,
  "description": "Headless smoketest harness for cat-factory's Pi coding agent: runs real coding tasks through the actual Pi setup against Cloudflare AI, captures the full prompt/response transcript, and analyses it for breakage, dead-ends and non-productive loops. No grading.",
  "bin": {
    "cat-smoke": "./src/cli.ts"
  },
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.typecheck.json --noEmit",
    "test": "vitest run",
    "smoke": "node --experimental-strip-types src/cli.ts"
  },
  "dependencies": {
    "@cat-factory/benchmark-harness": "workspace:*",
    "@cat-factory/executor-harness": "workspace:*",
    "@cat-factory/kernel": "workspace:*",
    "valibot": "^1.4.2"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "typescript": "7.0.2",
    "vitest": "^4.1.10"
  }
}
