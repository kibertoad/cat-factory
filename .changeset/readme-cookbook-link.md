---
---

Docs only: point the README at the product site's new task-oriented cookbook
(`https://www.catfactory.ai/guide/cookbook.html`), from the header line and from the
"Agents & pipelines" section of the documentation index. The cookbook indexes the common
pipeline edits by the operation someone wants to perform (add a review step, add a gate,
change a step's prompt, attach a skill) rather than by the subsystem they live in, which
is what the repo's own docs and the guide already do.
