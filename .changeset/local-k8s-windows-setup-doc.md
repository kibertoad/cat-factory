---
---

docs: add a Windows host-toolchain setup guide for local Kubernetes provisioning
(`backend/docs/local-kubernetes-setup-windows.md`) — installing pinned native
`kubectl`/`helm`/`kustomize`/`k3d`, bringing up a local k3d (k3s-in-Docker) cluster, and
wiring the `K8S_IT_*` integration suites. Cross-linked from `local-k3s-environments.md`.
