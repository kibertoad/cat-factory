---
---

Docs only: add `backend/docs/public-api.md` — the public API (`/api/v1`) setup & usage guide with a
full endpoint reference and the outbound-webhook delivery contract — and link it from the root and
backend READMEs, `auth.md`, `debug-api.md`, ADR 0030 and the public-api-additions tracker.
