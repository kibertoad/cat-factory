---
'@cat-factory/cli': patch
---

Refresh the scaffold library pins to the current published releases (`@cat-factory/local-server` `^0.34.0`, `@cat-factory/app` `^0.66.0`) so `templates.pins.test.ts` is green again.
