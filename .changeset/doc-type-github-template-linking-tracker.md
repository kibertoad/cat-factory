---
---

Docs-only: adjust the documentation-type task improvements initiative tracker
(`docs/initiatives/document-task-improvements.md`) to add workspace-linked template
overrides sourced from GitHub repo documents (and any other configured doc source),
reusing the existing `documents` integration's link/read mechanism instead of a new
fetch path. No code changes.
