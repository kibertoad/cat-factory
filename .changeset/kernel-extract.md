---
'@cat-factory/kernel': minor
---

Extract shared vocabulary, port interfaces, and the `requireWorkspace` guard into a new `@cat-factory/kernel` package. The `@cat-factory/core` package re-exports everything from `@cat-factory/kernel` for backward compatibility.
