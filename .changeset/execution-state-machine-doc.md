---
---

docs: add the execution state-machine lifecycle reference (run + step Mermaid diagrams) and
the recorded decision not to adopt XState for the engine (durable/persisted state, snapshot
persistence collision, no payoff over plain-TS StepGraph + RunStateMachine).
