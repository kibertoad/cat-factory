---
---

docs: document local k3s under WSL2 — apiserver reachability via WSL localhost-forwarding, the
runner-backend `PUBLIC_URL` pod→host-proxy callback, and a ready-to-apply ServiceAccount/RBAC +
token snippet. Docs-only; no package behaviour changes.
