---
'@cat-factory/app': patch
---

Move the basic/advanced interface-tier switcher to the top of the sidebar and make the choice
visible at rest. Expanded, it is now a segmented control showing both tiers rather than a
dropdown that only named the current one; in the collapsed rail it is a single button that flips
the tier, labelled with the tier it is currently on.
