---
---

Tooling only: add Stryker mutation testing (nightly, non-blocking CI) for `@cat-factory/kernel`,
`@cat-factory/gates` and `@cat-factory/spend`, and move the contributor-only docs under
`docs/internal/`. No shipped code changes, so no package is released.
