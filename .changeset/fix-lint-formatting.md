---
---

CI: apply `oxfmt` to files that landed unformatted via the consensus (#127) and
Sandbox (#128) merges, which left `main` red on the Lint & format check. No code
or behavior changes — formatting only.
