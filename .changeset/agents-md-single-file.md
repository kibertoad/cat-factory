---
---

Docs and CI only: fold the root `CLAUDE.md` into `AGENTS.md`, so Claude Code reads the per-package
`AGENTS.md` files it was switching off, and add `scripts/check-no-claude-md.mjs` to keep a
`CLAUDE.md` from coming back.
