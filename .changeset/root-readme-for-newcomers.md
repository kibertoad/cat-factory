---
---

Rewrite the root README for first-time readers. The capability catalog moves to `docs/capabilities.md`, the package catalog to `docs/repository-layout.md` (the package-catalog guard now reads it), and the feature guide and topic index to `docs/README.md`.
