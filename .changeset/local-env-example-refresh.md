---
---

Docs only: group local mode's four mandatory env values at the top of
`deploy/local/.env.example`, document the OTEL / platform-alert / mothership / native-harness
variables it was missing, and correct the README's stale claim that `ENCRYPTION_KEY` is optional.
