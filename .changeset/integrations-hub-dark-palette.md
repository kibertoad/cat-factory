---
'@cat-factory/app': patch
---

Darken the Integrations hub modal so its text has readable contrast. The modal now uses
the same dark slate palette as the agent run details overlay (a `slate-950` surface with
`slate-800` chrome and `slate-900/50` rows) instead of rendering dark slate text on the
default light modal surface.
