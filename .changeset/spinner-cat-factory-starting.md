---
"@cat-factory/app": patch
---

Prettier SPA startup screen: the early loading shell now shows an inline cat
badge inside the spinner ring plus a "Cat Factory is starting…" message, and
honors `prefers-reduced-motion`.
