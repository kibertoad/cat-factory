---
---

docs: add the per-service provisioning reference (`backend/docs/per-service-provisioning.md`) covering the Kubernetes render/deploy path (raw apiserver apply vs the kustomize/helm/Gateway deploy container), the provision types + engines, custom manifest types, the per-user override, and auto-detection; correct the stale environment docs (`local-k3s-environments.md`, `infrastructure-providers-window.md`, `environments-integration.md`) and the `backend/README.md` endpoint list to the reshaped per-type handler model.
