---
'@cat-factory/kernel': patch
'@cat-factory/gates': patch
'@cat-factory/spend': patch
---

Pin the behaviour mutation testing found unpinned in the gate verdicts, the spend
arithmetic and kernel's SSRF/host classification, and raise each package's score floor to
the measured value.
