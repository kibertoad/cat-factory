---
---

ci: bump all GitHub Actions still running on the deprecated Node 20 runtime to their Node 24 releases (`actions/cache` → v6.1.0, `actions/upload-artifact` → v7.0.1, `actions/download-artifact` → v8.0.1, `docker/login-action` → v4.2.0, `docker/setup-qemu-action` → v4.1.0, `docker/setup-buildx-action` → v4.1.0, `docker/build-push-action` → v7.2.0), resolving the Node 20 deprecation warnings in CI.
