---
---

Docs only: split the consuming-side MCP tool-server model out of `custom-agents.md` into
`backend/docs/mcp-tool-servers.md`, fold the 2026-08-05 external-servers review into
`docs/initiatives/mcp-maturation.md`, name MCP tool results as an untrusted-input source in
`security-model.md`, and refresh the cross-references (CLAUDE.md, README, glossary, AGENTS.md).
