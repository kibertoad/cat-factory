---
---

Test-only: regenerate the stale `containerAgentJobBody` inline snapshot in
`@cat-factory/server` (the spec-writer/architect/tester/blueprints/merger/on-call
system prompts gained the "no repository write access" safety clause + the
`FINAL_ANSWER_IN_REPLY` fragment after the snapshot was captured) and reformat
`llmProxyOutputCeiling.spec.ts` to satisfy `oxfmt`. No source/behaviour change.
