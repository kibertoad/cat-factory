---
'@cat-factory/cli': patch
---

Refresh the scaffolded `@cat-factory/app` pin to `^0.64.0` so `cat-factory init` generates a
frontend deployment against the current published layer (the `^0.63.1` pin no longer covered
`0.64.0`).
