---
---

docs: record ExecutionService split (take 2) phase 5 (gate-window action sub-facades) +
phase 6 partial (dead constructor-field trim) in the progress tracker, and refine the Phase 4
`RunDispatcher` scoping (the concrete method cluster it would own + the merge-subgraph boundary).
