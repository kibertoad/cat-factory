---
---

The Cloudflare OS Gatekeeper gained a live suite (`sdk/gatekeeper-worker/test/live`) run by the
cross-SDK smoketest harness against a real deployment; tests, CI and docs only, so no versioned
package changes.
