---
---

Release plumbing and changelog maintenance only: the version commit now goes up over the git CLI,
and the aged-out release history moved into per-package `CHANGELOG-ARCHIVE.md` files. No package
source changed.
