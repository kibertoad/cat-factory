---
---

Docs/comments only: remove references to a specific downstream adapter ("Kargo") from the
provider-agnostic platform's own prose (environment self-test, provisioning-status, handler-seeder,
and the `seedEnvironmentHandlers` option docs). No behavior or API change; the wording is now
neutral ("a code/REST environment adapter", "a provider that …"). No release needed.
