---
'@cat-factory/executor-harness': patch
---

Bump the pinned Pi coding agent (`@earendil-works/pi-coding-agent`) from 0.79.4 to
0.79.7 in the executor-harness image. Changes the image, so the harness version (its
GHCR/registry image tag) bumps with it.
