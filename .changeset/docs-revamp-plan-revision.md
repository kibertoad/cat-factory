---
---

Docs only: revise `docs/initiatives/documentation-revamp.md` against both checkouts. Ten "mixed"
docs have no website pointer where the classification claimed all 24 do; slice 15c is blocked on the
same manifest-depth question slice 15b handed back, so both become item 17; the audit's repo-only
row now names `auth.md`, `reusable-operations.md` and `figma-claude-design-context.md`. New item 18
puts the repo-to-website link check in the website repo, where the pages are, and item 12 becomes
rendering the OpenAPI spec rather than moving 2000 lines of prose.
