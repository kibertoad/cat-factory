---
'@cat-factory/executor-harness': patch
'@cat-factory/agents': patch
---

Record one `llm_call_metrics` row per model call instead of per stream envelope, and bound what a pr-review slice carries.
