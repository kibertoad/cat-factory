---
---

Docs only: reduce `backend/docs/environments-integration.md` to the provider-integration design,
per slice 15b of `docs/initiatives/documentation-revamp.md`. Setup, backend choice, registration
and the internal-host allow-list are owned by catfactory.ai; the code-adapter seam by
`native-environment-adapter.md`, which gains the `confirmTeardown` contract it never documented.
The manifest schema stays: the site's manifests page stops above the field level.
