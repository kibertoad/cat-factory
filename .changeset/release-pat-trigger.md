---
---

ci: use a PAT (RELEASE_TOKEN) for the changesets action + Release PR auto-merge so the merge triggers the publish run (the default GITHUB_TOKEN cannot). Workflow-only; no published package changes.
