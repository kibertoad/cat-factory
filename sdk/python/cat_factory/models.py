# GENERATED by `pnpm gen:sdk` from docs/openapi.json — DO NOT EDIT BY HAND.
# Regenerate after any change to the `/api/v1` contracts; `pnpm check:sdk` fails CI on drift.

"""Wire models for the cat-factory public API (`/api/v1`), surface version 1.32.0.

Every model is a frozen dataclass with `from_dict` / `to_dict`. Unknown fields are kept on
`extra` rather than dropped, and an unknown enum value decodes to its plain string, so an SDK
release older than the deployment it talks to degrades to "less typed", never to "raises".
"""

from __future__ import annotations

from collections.abc import Mapping
from dataclasses import dataclass, field as _dc_field
from enum import Enum, StrEnum
from typing import Any, TypeAlias


def _enum(cls: type[Enum], value: Any) -> Any:
    """Decode an enum value LENIENTLY.

    An unrecognised value comes back as the plain string rather than raising. The /api/v1
    surface is additive forever and a deployment will send values an older SDK has never heard
    of; a strict decode would turn every additive server release into an outage for anyone who
    had not upgraded. The members subclass str, so comparing against either form works.
    """
    if value is None:
        return None
    try:
        return cls(value)
    except ValueError:
        return value


def _encode(value: Any) -> Any:
    """Encode a model, enum, list or scalar back to its JSON form."""
    if hasattr(value, "to_dict"):
        return value.to_dict()
    if isinstance(value, Enum):
        return value.value
    if isinstance(value, list):
        return [_encode(item) for item in value]
    if isinstance(value, dict):
        return {key: _encode(val) for key, val in value.items()}
    return value


@dataclass(frozen=True, slots=True)
class ActPublicNotificationRequest:
    """`ActPublicNotificationRequest`, as carried on the wire."""

    #: May be absent entirely.
    review_effort: GetPublicMergeRecordResponseReviewEffort | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "ActPublicNotificationRequest":
        """Decode a `ActPublicNotificationRequest` from its JSON object."""
        known = {"reviewEffort"}
        return cls(
            review_effort=None if data.get("reviewEffort") is None else _enum(GetPublicMergeRecordResponseReviewEffort, data.get("reviewEffort")),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        if self.review_effort is not None:
            out["reviewEffort"] = _encode(self.review_effort)
        return out


@dataclass(frozen=True, slots=True)
class AddPublicTaskDependencyRequest:
    """`AddPublicTaskDependencyRequest`, as carried on the wire."""

    depends_on_task_id: str

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "AddPublicTaskDependencyRequest":
        """Decode a `AddPublicTaskDependencyRequest` from its JSON object."""
        known = {"dependsOnTaskId"}
        return cls(
            depends_on_task_id=data.get("dependsOnTaskId"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["dependsOnTaskId"] = self.depends_on_task_id
        return out


@dataclass(frozen=True, slots=True)
class AttachPublicTaskDocumentRequest:
    """`AttachPublicTaskDocumentRequest`, as carried on the wire."""

    document: PublicTaskDocument

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "AttachPublicTaskDocumentRequest":
        """Decode a `AttachPublicTaskDocumentRequest` from its JSON object."""
        known = {"document"}
        return cls(
            document=PublicTaskDocument.from_dict(data.get("document")),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["document"] = _encode(self.document)
        return out


@dataclass(frozen=True, slots=True)
class CreatedPublicApiKey:
    """`CreatedPublicApiKey`, as carried on the wire."""

    key: PublicApiKey
    secret: str

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "CreatedPublicApiKey":
        """Decode a `CreatedPublicApiKey` from its JSON object."""
        known = {"key", "secret"}
        return cls(
            key=PublicApiKey.from_dict(data.get("key")),
            secret=data.get("secret"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["key"] = _encode(self.key)
        out["secret"] = self.secret
        return out


@dataclass(frozen=True, slots=True)
class CreateHeadlessPublicApiKey:
    """`CreateHeadlessPublicApiKey`, as carried on the wire."""

    label: str
    #: May be absent entirely.
    external_identity: str | None = None
    #: May be absent entirely.
    scope: CreateHeadlessPublicApiKeyScope | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "CreateHeadlessPublicApiKey":
        """Decode a `CreateHeadlessPublicApiKey` from its JSON object."""
        known = {"label", "externalIdentity", "scope"}
        return cls(
            label=data.get("label"),
            external_identity=data.get("externalIdentity"),
            scope=None if data.get("scope") is None else _enum(CreateHeadlessPublicApiKeyScope, data.get("scope")),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["label"] = self.label
        if self.external_identity is not None:
            out["externalIdentity"] = self.external_identity
        if self.scope is not None:
            out["scope"] = _encode(self.scope)
        return out


class CreateHeadlessPublicApiKeyScope(StrEnum):
    """The `CreateHeadlessPublicApiKeyScope` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    READ = "read"
    WRITE = "write"
    DECIDE = "decide"


@dataclass(frozen=True, slots=True)
class CreatePublicJob:
    """`CreatePublicJob`, as carried on the wire."""

    input: str
    pipeline_id: str
    #: May be absent entirely.
    title: str | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "CreatePublicJob":
        """Decode a `CreatePublicJob` from its JSON object."""
        known = {"input", "pipelineId", "title"}
        return cls(
            input=data.get("input"),
            pipeline_id=data.get("pipelineId"),
            title=data.get("title"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["input"] = self.input
        out["pipelineId"] = self.pipeline_id
        if self.title is not None:
            out["title"] = self.title
        return out


@dataclass(frozen=True, slots=True)
class CreatePublicServiceRequest:
    """`CreatePublicServiceRequest`, as carried on the wire."""

    #: May be absent entirely.
    description: str | None = None
    #: May be absent entirely.
    repo: CreatePublicServiceRequestRepo | None = None
    #: May be absent entirely.
    title: str | None = None
    #: May be absent entirely.
    type_: CreatePublicServiceRequestType | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "CreatePublicServiceRequest":
        """Decode a `CreatePublicServiceRequest` from its JSON object."""
        known = {"description", "repo", "title", "type"}
        return cls(
            description=data.get("description"),
            repo=None if data.get("repo") is None else CreatePublicServiceRequestRepo.from_dict(data.get("repo")),
            title=data.get("title"),
            type_=None if data.get("type") is None else _enum(CreatePublicServiceRequestType, data.get("type")),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        if self.description is not None:
            out["description"] = self.description
        if self.repo is not None:
            out["repo"] = _encode(self.repo)
        if self.title is not None:
            out["title"] = self.title
        if self.type_ is not None:
            out["type"] = _encode(self.type_)
        return out


@dataclass(frozen=True, slots=True)
class CreatePublicServiceRequestRepo:
    """`CreatePublicServiceRequestRepo`, as carried on the wire."""

    repo_id: float
    #: May be absent entirely.
    directory: str | None = None
    #: May be absent entirely.
    monorepo: bool | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "CreatePublicServiceRequestRepo":
        """Decode a `CreatePublicServiceRequestRepo` from its JSON object."""
        known = {"repoId", "directory", "monorepo"}
        return cls(
            repo_id=data.get("repoId"),
            directory=data.get("directory"),
            monorepo=data.get("monorepo"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["repoId"] = self.repo_id
        if self.directory is not None:
            out["directory"] = self.directory
        if self.monorepo is not None:
            out["monorepo"] = self.monorepo
        return out


class CreatePublicServiceRequestType(StrEnum):
    """The `CreatePublicServiceRequestType` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    SERVICE = "service"
    FRONTEND = "frontend"
    LIBRARY = "library"
    DOCUMENT = "document"


@dataclass(frozen=True, slots=True)
class CreatePublicTask:
    """`CreatePublicTask`, as carried on the wire."""

    title: str
    #: May be absent entirely.
    description: str | None = None
    #: May be absent entirely.
    documents: list[PublicTaskDocument] | None = None
    #: May be absent entirely.
    fields: dict[str, CreatePublicTaskFieldsValue] | None = None
    #: May be absent entirely.
    task_type: str | None = None
    #: May be absent entirely.
    ticket: PublicTaskTicket | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "CreatePublicTask":
        """Decode a `CreatePublicTask` from its JSON object."""
        known = {"title", "description", "documents", "fields", "taskType", "ticket"}
        return cls(
            title=data.get("title"),
            description=data.get("description"),
            documents=None if data.get("documents") is None else [PublicTaskDocument.from_dict(item) for item in data.get("documents") or []],
            fields=None if data.get("fields") is None else {key: CreatePublicTaskFieldsValue.from_dict(val) for key, val in (data.get("fields") or {}).items()},
            task_type=data.get("taskType"),
            ticket=None if data.get("ticket") is None else PublicTaskTicket.from_dict(data.get("ticket")),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["title"] = self.title
        if self.description is not None:
            out["description"] = self.description
        if self.documents is not None:
            out["documents"] = [_encode(item) for item in self.documents]
        if self.fields is not None:
            out["fields"] = {key: _encode(val) for key, val in self.fields.items()}
        if self.task_type is not None:
            out["taskType"] = self.task_type
        if self.ticket is not None:
            out["ticket"] = _encode(self.ticket)
        return out


@dataclass(frozen=True, slots=True)
class DebugAgentContextSnapshot:
    """`DebugAgentContextSnapshot`, as carried on the wire."""

    agent_kind: str
    context_files: list[DebugContextFile]
    created_at: float
    extras: dict[str, Any]
    fragments: list[DebugContextFragment]
    run_id: str
    snapshot_id: str
    step_index: float
    system_prompt: DebugText
    user_prompt: DebugText
    #: Always present; ``None`` when the server has no value for it.
    harness: str | None = None
    #: Always present; ``None`` when the server has no value for it.
    model: str | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "DebugAgentContextSnapshot":
        """Decode a `DebugAgentContextSnapshot` from its JSON object."""
        known = {"agentKind", "contextFiles", "createdAt", "extras", "fragments", "runId", "snapshotId", "stepIndex", "systemPrompt", "userPrompt", "harness", "model"}
        return cls(
            agent_kind=data.get("agentKind"),
            context_files=[DebugContextFile.from_dict(item) for item in data.get("contextFiles") or []],
            created_at=data.get("createdAt"),
            extras={key: val for key, val in (data.get("extras") or {}).items()},
            fragments=[DebugContextFragment.from_dict(item) for item in data.get("fragments") or []],
            run_id=data.get("runId"),
            snapshot_id=data.get("snapshotId"),
            step_index=data.get("stepIndex"),
            system_prompt=DebugText.from_dict(data.get("systemPrompt")),
            user_prompt=DebugText.from_dict(data.get("userPrompt")),
            harness=data.get("harness"),
            model=data.get("model"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["agentKind"] = self.agent_kind
        out["contextFiles"] = [_encode(item) for item in self.context_files]
        out["createdAt"] = self.created_at
        out["extras"] = {key: _encode(val) for key, val in self.extras.items()}
        out["fragments"] = [_encode(item) for item in self.fragments]
        out["runId"] = self.run_id
        out["snapshotId"] = self.snapshot_id
        out["stepIndex"] = self.step_index
        out["systemPrompt"] = _encode(self.system_prompt)
        out["userPrompt"] = _encode(self.user_prompt)
        out["harness"] = self.harness
        out["model"] = self.model
        return out


@dataclass(frozen=True, slots=True)
class DebugAgentContextSummary:
    """`DebugAgentContextSummary`, as carried on the wire."""

    agent_kind: str
    context_files_chars: float
    created_at: float
    fragments_chars: float
    snapshot_id: str
    step_index: float
    system_prompt_chars: float
    user_prompt_chars: float
    #: Always present; ``None`` when the server has no value for it.
    harness: str | None = None
    #: Always present; ``None`` when the server has no value for it.
    model: str | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "DebugAgentContextSummary":
        """Decode a `DebugAgentContextSummary` from its JSON object."""
        known = {"agentKind", "contextFilesChars", "createdAt", "fragmentsChars", "snapshotId", "stepIndex", "systemPromptChars", "userPromptChars", "harness", "model"}
        return cls(
            agent_kind=data.get("agentKind"),
            context_files_chars=data.get("contextFilesChars"),
            created_at=data.get("createdAt"),
            fragments_chars=data.get("fragmentsChars"),
            snapshot_id=data.get("snapshotId"),
            step_index=data.get("stepIndex"),
            system_prompt_chars=data.get("systemPromptChars"),
            user_prompt_chars=data.get("userPromptChars"),
            harness=data.get("harness"),
            model=data.get("model"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["agentKind"] = self.agent_kind
        out["contextFilesChars"] = self.context_files_chars
        out["createdAt"] = self.created_at
        out["fragmentsChars"] = self.fragments_chars
        out["snapshotId"] = self.snapshot_id
        out["stepIndex"] = self.step_index
        out["systemPromptChars"] = self.system_prompt_chars
        out["userPromptChars"] = self.user_prompt_chars
        out["harness"] = self.harness
        out["model"] = self.model
        return out


@dataclass(frozen=True, slots=True)
class DebugContextFile:
    """`DebugContextFile`, as carried on the wire."""

    content: DebugText
    path: str
    title: str
    url: str

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "DebugContextFile":
        """Decode a `DebugContextFile` from its JSON object."""
        known = {"content", "path", "title", "url"}
        return cls(
            content=DebugText.from_dict(data.get("content")),
            path=data.get("path"),
            title=data.get("title"),
            url=data.get("url"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["content"] = _encode(self.content)
        out["path"] = self.path
        out["title"] = self.title
        out["url"] = self.url
        return out


@dataclass(frozen=True, slots=True)
class DebugContextFragment:
    """`DebugContextFragment`, as carried on the wire."""

    body: DebugText
    id: str

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "DebugContextFragment":
        """Decode a `DebugContextFragment` from its JSON object."""
        known = {"body", "id"}
        return cls(
            body=DebugText.from_dict(data.get("body")),
            id=data.get("id"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["body"] = _encode(self.body)
        out["id"] = self.id
        return out


@dataclass(frozen=True, slots=True)
class DebugInfraLogEntry:
    """`DebugInfraLogEntry`, as carried on the wire."""

    created_at: float
    id: str
    operation: DebugInfraLogEntryOperation
    outcome: DebugInfraLogEntryOutcome
    subsystem: DebugInfraLogEntrySubsystem
    workspace_id: str
    #: Always present; ``None`` when the server has no value for it.
    block_id: str | None = None
    #: Always present; ``None`` when the server has no value for it.
    detail: str | None = None
    #: Always present; ``None`` when the server has no value for it.
    error: str | None = None
    #: Always present; ``None`` when the server has no value for it.
    execution_id: str | None = None
    #: Always present; ``None`` when the server has no value for it.
    provider_id: str | None = None
    #: Always present; ``None`` when the server has no value for it.
    target_id: str | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "DebugInfraLogEntry":
        """Decode a `DebugInfraLogEntry` from its JSON object."""
        known = {"createdAt", "id", "operation", "outcome", "subsystem", "workspaceId", "blockId", "detail", "error", "executionId", "providerId", "targetId"}
        return cls(
            created_at=data.get("createdAt"),
            id=data.get("id"),
            operation=_enum(DebugInfraLogEntryOperation, data.get("operation")),
            outcome=_enum(DebugInfraLogEntryOutcome, data.get("outcome")),
            subsystem=_enum(DebugInfraLogEntrySubsystem, data.get("subsystem")),
            workspace_id=data.get("workspaceId"),
            block_id=data.get("blockId"),
            detail=data.get("detail"),
            error=data.get("error"),
            execution_id=data.get("executionId"),
            provider_id=data.get("providerId"),
            target_id=data.get("targetId"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["createdAt"] = self.created_at
        out["id"] = self.id
        out["operation"] = _encode(self.operation)
        out["outcome"] = _encode(self.outcome)
        out["subsystem"] = _encode(self.subsystem)
        out["workspaceId"] = self.workspace_id
        out["blockId"] = self.block_id
        out["detail"] = self.detail
        out["error"] = self.error
        out["executionId"] = self.execution_id
        out["providerId"] = self.provider_id
        out["targetId"] = self.target_id
        return out


class DebugInfraLogEntryOperation(StrEnum):
    """The `DebugInfraLogEntryOperation` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    PROVISION = "provision"
    TEARDOWN = "teardown"
    TEARDOWN_VERIFY = "teardown-verify"
    STATUS = "status"
    DISPATCH = "dispatch"
    RELEASE = "release"
    POLL_FAILURE = "poll-failure"


class DebugInfraLogEntryOutcome(StrEnum):
    """The `DebugInfraLogEntryOutcome` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    SUCCESS = "success"
    FAILURE = "failure"


class DebugInfraLogEntrySubsystem(StrEnum):
    """The `DebugInfraLogEntrySubsystem` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    ENVIRONMENT = "environment"
    RUNNER_POOL = "runner-pool"
    CONTAINER = "container"


@dataclass(frozen=True, slots=True)
class DebugLlmAgentKindRollup:
    """`DebugLlmAgentKindRollup`, as carried on the wire."""

    agent_kind: str
    cache_read_tokens: float
    cache_write_tokens: float
    calls: float
    completion_tokens: float
    errors: float
    overhead_ms: float
    peak_completion_tokens: float
    prompt_tokens: float
    truncated_calls: float
    upstream_ms: float
    warnings: float
    #: Always present; ``None`` when the server has no value for it.
    cache_hit_rate: float | None = None
    #: Always present; ``None`` when the server has no value for it.
    cost_estimate: float | None = None
    #: Always present; ``None`` when the server has no value for it.
    max_output_tokens: float | None = None
    #: Always present; ``None`` when the server has no value for it.
    output_headroom_ratio: float | None = None
    #: Always present; ``None`` when the server has no value for it.
    transport_overhead_ratio: float | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "DebugLlmAgentKindRollup":
        """Decode a `DebugLlmAgentKindRollup` from its JSON object."""
        known = {"agentKind", "cacheReadTokens", "cacheWriteTokens", "calls", "completionTokens", "errors", "overheadMs", "peakCompletionTokens", "promptTokens", "truncatedCalls", "upstreamMs", "warnings", "cacheHitRate", "costEstimate", "maxOutputTokens", "outputHeadroomRatio", "transportOverheadRatio"}
        return cls(
            agent_kind=data.get("agentKind"),
            cache_read_tokens=data.get("cacheReadTokens"),
            cache_write_tokens=data.get("cacheWriteTokens"),
            calls=data.get("calls"),
            completion_tokens=data.get("completionTokens"),
            errors=data.get("errors"),
            overhead_ms=data.get("overheadMs"),
            peak_completion_tokens=data.get("peakCompletionTokens"),
            prompt_tokens=data.get("promptTokens"),
            truncated_calls=data.get("truncatedCalls"),
            upstream_ms=data.get("upstreamMs"),
            warnings=data.get("warnings"),
            cache_hit_rate=data.get("cacheHitRate"),
            cost_estimate=data.get("costEstimate"),
            max_output_tokens=data.get("maxOutputTokens"),
            output_headroom_ratio=data.get("outputHeadroomRatio"),
            transport_overhead_ratio=data.get("transportOverheadRatio"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["agentKind"] = self.agent_kind
        out["cacheReadTokens"] = self.cache_read_tokens
        out["cacheWriteTokens"] = self.cache_write_tokens
        out["calls"] = self.calls
        out["completionTokens"] = self.completion_tokens
        out["errors"] = self.errors
        out["overheadMs"] = self.overhead_ms
        out["peakCompletionTokens"] = self.peak_completion_tokens
        out["promptTokens"] = self.prompt_tokens
        out["truncatedCalls"] = self.truncated_calls
        out["upstreamMs"] = self.upstream_ms
        out["warnings"] = self.warnings
        out["cacheHitRate"] = self.cache_hit_rate
        out["costEstimate"] = self.cost_estimate
        out["maxOutputTokens"] = self.max_output_tokens
        out["outputHeadroomRatio"] = self.output_headroom_ratio
        out["transportOverheadRatio"] = self.transport_overhead_ratio
        return out


@dataclass(frozen=True, slots=True)
class DebugLlmCall:
    """`DebugLlmCall`, as carried on the wire."""

    agent_kind: str
    cache_read_tokens: float
    cache_write_tokens: float
    call_id: str
    completion_tokens: float
    created_at: float
    elided_leading_messages: float
    message_count: float
    model: str
    ok: bool
    outcome: LlmCallOutcome
    overhead_ms: float
    phase: str
    prompt: DebugText
    prompt_tokens: float
    provider: str
    reasoning: DebugText
    response: DebugText
    streaming: bool
    tool_count: float
    total_ms: float
    total_tokens: float
    upstream_ms: float
    #: Always present; ``None`` when the server has no value for it.
    error_message: str | None = None
    #: Always present; ``None`` when the server has no value for it.
    finish_reason: str | None = None
    #: Always present; ``None`` when the server has no value for it.
    http_status: float | None = None
    #: May be absent entirely.
    prompt_messages: list[DebugPromptMessage] | None = None
    #: Always present; ``None`` when the server has no value for it.
    request_max_tokens: float | None = None
    #: Always present; ``None`` when the server has no value for it.
    run_id: str | None = None
    #: Always present; ``None`` when the server has no value for it.
    turn_index: float | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "DebugLlmCall":
        """Decode a `DebugLlmCall` from its JSON object."""
        known = {"agentKind", "cacheReadTokens", "cacheWriteTokens", "callId", "completionTokens", "createdAt", "elidedLeadingMessages", "messageCount", "model", "ok", "outcome", "overheadMs", "phase", "prompt", "promptTokens", "provider", "reasoning", "response", "streaming", "toolCount", "totalMs", "totalTokens", "upstreamMs", "errorMessage", "finishReason", "httpStatus", "promptMessages", "requestMaxTokens", "runId", "turnIndex"}
        return cls(
            agent_kind=data.get("agentKind"),
            cache_read_tokens=data.get("cacheReadTokens"),
            cache_write_tokens=data.get("cacheWriteTokens"),
            call_id=data.get("callId"),
            completion_tokens=data.get("completionTokens"),
            created_at=data.get("createdAt"),
            elided_leading_messages=data.get("elidedLeadingMessages"),
            message_count=data.get("messageCount"),
            model=data.get("model"),
            ok=data.get("ok"),
            outcome=_enum(LlmCallOutcome, data.get("outcome")),
            overhead_ms=data.get("overheadMs"),
            phase=data.get("phase"),
            prompt=DebugText.from_dict(data.get("prompt")),
            prompt_tokens=data.get("promptTokens"),
            provider=data.get("provider"),
            reasoning=DebugText.from_dict(data.get("reasoning")),
            response=DebugText.from_dict(data.get("response")),
            streaming=data.get("streaming"),
            tool_count=data.get("toolCount"),
            total_ms=data.get("totalMs"),
            total_tokens=data.get("totalTokens"),
            upstream_ms=data.get("upstreamMs"),
            error_message=data.get("errorMessage"),
            finish_reason=data.get("finishReason"),
            http_status=data.get("httpStatus"),
            prompt_messages=None if data.get("promptMessages") is None else [DebugPromptMessage.from_dict(item) for item in data.get("promptMessages") or []],
            request_max_tokens=data.get("requestMaxTokens"),
            run_id=data.get("runId"),
            turn_index=data.get("turnIndex"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["agentKind"] = self.agent_kind
        out["cacheReadTokens"] = self.cache_read_tokens
        out["cacheWriteTokens"] = self.cache_write_tokens
        out["callId"] = self.call_id
        out["completionTokens"] = self.completion_tokens
        out["createdAt"] = self.created_at
        out["elidedLeadingMessages"] = self.elided_leading_messages
        out["messageCount"] = self.message_count
        out["model"] = self.model
        out["ok"] = self.ok
        out["outcome"] = _encode(self.outcome)
        out["overheadMs"] = self.overhead_ms
        out["phase"] = self.phase
        out["prompt"] = _encode(self.prompt)
        out["promptTokens"] = self.prompt_tokens
        out["provider"] = self.provider
        out["reasoning"] = _encode(self.reasoning)
        out["response"] = _encode(self.response)
        out["streaming"] = self.streaming
        out["toolCount"] = self.tool_count
        out["totalMs"] = self.total_ms
        out["totalTokens"] = self.total_tokens
        out["upstreamMs"] = self.upstream_ms
        out["errorMessage"] = self.error_message
        out["finishReason"] = self.finish_reason
        out["httpStatus"] = self.http_status
        if self.prompt_messages is not None:
            out["promptMessages"] = [_encode(item) for item in self.prompt_messages]
        out["requestMaxTokens"] = self.request_max_tokens
        out["runId"] = self.run_id
        out["turnIndex"] = self.turn_index
        return out


@dataclass(frozen=True, slots=True)
class DebugLlmPhaseRollup:
    """`DebugLlmPhaseRollup`, as carried on the wire."""

    cache_read_tokens: float
    cache_write_tokens: float
    calls: float
    carry_cost_tokens: float
    completion_tokens: float
    errors: float
    overhead_ms: float
    phase: str
    prompt_tokens: float
    truncated_calls: float
    upstream_ms: float
    warnings: float
    #: Always present; ``None`` when the server has no value for it.
    cache_hit_rate: float | None = None
    #: Always present; ``None`` when the server has no value for it.
    carry_cost_share: float | None = None
    #: Always present; ``None`` when the server has no value for it.
    cost_estimate: float | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "DebugLlmPhaseRollup":
        """Decode a `DebugLlmPhaseRollup` from its JSON object."""
        known = {"cacheReadTokens", "cacheWriteTokens", "calls", "carryCostTokens", "completionTokens", "errors", "overheadMs", "phase", "promptTokens", "truncatedCalls", "upstreamMs", "warnings", "cacheHitRate", "carryCostShare", "costEstimate"}
        return cls(
            cache_read_tokens=data.get("cacheReadTokens"),
            cache_write_tokens=data.get("cacheWriteTokens"),
            calls=data.get("calls"),
            carry_cost_tokens=data.get("carryCostTokens"),
            completion_tokens=data.get("completionTokens"),
            errors=data.get("errors"),
            overhead_ms=data.get("overheadMs"),
            phase=data.get("phase"),
            prompt_tokens=data.get("promptTokens"),
            truncated_calls=data.get("truncatedCalls"),
            upstream_ms=data.get("upstreamMs"),
            warnings=data.get("warnings"),
            cache_hit_rate=data.get("cacheHitRate"),
            carry_cost_share=data.get("carryCostShare"),
            cost_estimate=data.get("costEstimate"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["cacheReadTokens"] = self.cache_read_tokens
        out["cacheWriteTokens"] = self.cache_write_tokens
        out["calls"] = self.calls
        out["carryCostTokens"] = self.carry_cost_tokens
        out["completionTokens"] = self.completion_tokens
        out["errors"] = self.errors
        out["overheadMs"] = self.overhead_ms
        out["phase"] = self.phase
        out["promptTokens"] = self.prompt_tokens
        out["truncatedCalls"] = self.truncated_calls
        out["upstreamMs"] = self.upstream_ms
        out["warnings"] = self.warnings
        out["cacheHitRate"] = self.cache_hit_rate
        out["carryCostShare"] = self.carry_cost_share
        out["costEstimate"] = self.cost_estimate
        return out


@dataclass(frozen=True, slots=True)
class DebugLlmTotals:
    """`DebugLlmTotals`, as carried on the wire."""

    cache_read_tokens: float
    cache_write_tokens: float
    calls: float
    completion_tokens: float
    errors: float
    overhead_ms: float
    prompt_tokens: float
    truncated_calls: float
    upstream_ms: float
    warnings: float
    #: Always present; ``None`` when the server has no value for it.
    cache_hit_rate: float | None = None
    #: Always present; ``None`` when the server has no value for it.
    cost_estimate: float | None = None
    #: Always present; ``None`` when the server has no value for it.
    transport_overhead_ratio: float | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "DebugLlmTotals":
        """Decode a `DebugLlmTotals` from its JSON object."""
        known = {"cacheReadTokens", "cacheWriteTokens", "calls", "completionTokens", "errors", "overheadMs", "promptTokens", "truncatedCalls", "upstreamMs", "warnings", "cacheHitRate", "costEstimate", "transportOverheadRatio"}
        return cls(
            cache_read_tokens=data.get("cacheReadTokens"),
            cache_write_tokens=data.get("cacheWriteTokens"),
            calls=data.get("calls"),
            completion_tokens=data.get("completionTokens"),
            errors=data.get("errors"),
            overhead_ms=data.get("overheadMs"),
            prompt_tokens=data.get("promptTokens"),
            truncated_calls=data.get("truncatedCalls"),
            upstream_ms=data.get("upstreamMs"),
            warnings=data.get("warnings"),
            cache_hit_rate=data.get("cacheHitRate"),
            cost_estimate=data.get("costEstimate"),
            transport_overhead_ratio=data.get("transportOverheadRatio"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["cacheReadTokens"] = self.cache_read_tokens
        out["cacheWriteTokens"] = self.cache_write_tokens
        out["calls"] = self.calls
        out["completionTokens"] = self.completion_tokens
        out["errors"] = self.errors
        out["overheadMs"] = self.overhead_ms
        out["promptTokens"] = self.prompt_tokens
        out["truncatedCalls"] = self.truncated_calls
        out["upstreamMs"] = self.upstream_ms
        out["warnings"] = self.warnings
        out["cacheHitRate"] = self.cache_hit_rate
        out["costEstimate"] = self.cost_estimate
        out["transportOverheadRatio"] = self.transport_overhead_ratio
        return out


@dataclass(frozen=True, slots=True)
class DebugObservedToolServer:
    """`DebugObservedToolServer`, as carried on the wire."""

    id: str
    status: DebugObservedToolServerStatus
    #: May be absent entirely.
    tool_count: float | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "DebugObservedToolServer":
        """Decode a `DebugObservedToolServer` from its JSON object."""
        known = {"id", "status", "toolCount"}
        return cls(
            id=data.get("id"),
            status=_enum(DebugObservedToolServerStatus, data.get("status")),
            tool_count=data.get("toolCount"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["id"] = self.id
        out["status"] = _encode(self.status)
        if self.tool_count is not None:
            out["toolCount"] = self.tool_count
        return out


class DebugObservedToolServerStatus(StrEnum):
    """The `DebugObservedToolServerStatus` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    READY = "ready"
    FAILED = "failed"
    NEEDS_AUTH = "needs_auth"
    UNKNOWN = "unknown"


@dataclass(frozen=True, slots=True)
class DebugPromptMessage:
    """`DebugPromptMessage`, as carried on the wire."""

    content: DebugText
    index: float
    role: str
    tool_calls: list[DebugToolCall]
    #: Always present; ``None`` when the server has no value for it.
    name: str | None = None
    #: Always present; ``None`` when the server has no value for it.
    tool_call_id: str | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "DebugPromptMessage":
        """Decode a `DebugPromptMessage` from its JSON object."""
        known = {"content", "index", "role", "toolCalls", "name", "toolCallId"}
        return cls(
            content=DebugText.from_dict(data.get("content")),
            index=data.get("index"),
            role=data.get("role"),
            tool_calls=[DebugToolCall.from_dict(item) for item in data.get("toolCalls") or []],
            name=data.get("name"),
            tool_call_id=data.get("toolCallId"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["content"] = _encode(self.content)
        out["index"] = self.index
        out["role"] = self.role
        out["toolCalls"] = [_encode(item) for item in self.tool_calls]
        out["name"] = self.name
        out["toolCallId"] = self.tool_call_id
        return out


@dataclass(frozen=True, slots=True)
class DebugRunFailure:
    """`DebugRunFailure`, as carried on the wire."""

    kind: DebugRunFailureKind
    message: str
    occurred_at: float
    #: Always present; ``None`` when the server has no value for it.
    detail: str | None = None
    #: Always present; ``None`` when the server has no value for it.
    hint: str | None = None
    #: Always present; ``None`` when the server has no value for it.
    last_subtasks: DebugSubtaskCounts | None = None
    #: May be absent entirely.
    reason: str | None = None
    #: May be absent entirely.
    step_index: float | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "DebugRunFailure":
        """Decode a `DebugRunFailure` from its JSON object."""
        known = {"kind", "message", "occurredAt", "detail", "hint", "lastSubtasks", "reason", "stepIndex"}
        return cls(
            kind=_enum(DebugRunFailureKind, data.get("kind")),
            message=data.get("message"),
            occurred_at=data.get("occurredAt"),
            detail=data.get("detail"),
            hint=data.get("hint"),
            last_subtasks=None if data.get("lastSubtasks") is None else DebugSubtaskCounts.from_dict(data.get("lastSubtasks")),
            reason=data.get("reason"),
            step_index=data.get("stepIndex"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["kind"] = _encode(self.kind)
        out["message"] = self.message
        out["occurredAt"] = self.occurred_at
        out["detail"] = self.detail
        out["hint"] = self.hint
        out["lastSubtasks"] = _encode(self.last_subtasks)
        if self.reason is not None:
            out["reason"] = self.reason
        if self.step_index is not None:
            out["stepIndex"] = self.step_index
        return out


class DebugRunFailureKind(StrEnum):
    """The `DebugRunFailureKind` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    PREFLIGHT = "preflight"
    DISPATCH = "dispatch"
    ENVIRONMENT = "environment"
    EVICTED = "evicted"
    TIMEOUT = "timeout"
    AGENT = "agent"
    JOB_FAILED = "job_failed"
    REJECTED = "rejected"
    COMPANION_REJECTED = "companion_rejected"
    STALLED = "stalled"
    CANCELLED = "cancelled"
    UNKNOWN = "unknown"


@dataclass(frozen=True, slots=True)
class DebugRunOverview:
    """`DebugRunOverview`, as carried on the wire."""

    generated_at: float
    kind: str
    llm: DebugRunOverviewLlm
    run: DebugRunSummary
    signals: list[DebugRunSignal]
    sinks: DebugRunOverviewSinks
    steps: list[DebugRunStep]
    tool_calls: DebugRunOverviewToolCalls
    version: Any
    #: Always present; ``None`` when the server has no value for it.
    diagnostics: DebugRunOverviewDiagnostics | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "DebugRunOverview":
        """Decode a `DebugRunOverview` from its JSON object."""
        known = {"generatedAt", "kind", "llm", "run", "signals", "sinks", "steps", "toolCalls", "version", "diagnostics"}
        return cls(
            generated_at=data.get("generatedAt"),
            kind=data.get("kind"),
            llm=DebugRunOverviewLlm.from_dict(data.get("llm")),
            run=DebugRunSummary.from_dict(data.get("run")),
            signals=[DebugRunSignal.from_dict(item) for item in data.get("signals") or []],
            sinks=DebugRunOverviewSinks.from_dict(data.get("sinks")),
            steps=[DebugRunStep.from_dict(item) for item in data.get("steps") or []],
            tool_calls=DebugRunOverviewToolCalls.from_dict(data.get("toolCalls")),
            version=data.get("version"),
            diagnostics=None if data.get("diagnostics") is None else DebugRunOverviewDiagnostics.from_dict(data.get("diagnostics")),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["generatedAt"] = self.generated_at
        out["kind"] = self.kind
        out["llm"] = _encode(self.llm)
        out["run"] = _encode(self.run)
        out["signals"] = [_encode(item) for item in self.signals]
        out["sinks"] = _encode(self.sinks)
        out["steps"] = [_encode(item) for item in self.steps]
        out["toolCalls"] = _encode(self.tool_calls)
        out["version"] = self.version
        out["diagnostics"] = _encode(self.diagnostics)
        return out


@dataclass(frozen=True, slots=True)
class DebugRunOverviewDiagnostics:
    """`DebugRunOverviewDiagnostics`, as carried on the wire."""

    #: May be absent entirely.
    host: DebugRunOverviewDiagnosticsHost | None = None
    #: May be absent entirely.
    last_dispatch: DebugRunOverviewDiagnosticsLastDispatch | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "DebugRunOverviewDiagnostics":
        """Decode a `DebugRunOverviewDiagnostics` from its JSON object."""
        known = {"host", "lastDispatch"}
        return cls(
            host=None if data.get("host") is None else DebugRunOverviewDiagnosticsHost.from_dict(data.get("host")),
            last_dispatch=None if data.get("lastDispatch") is None else DebugRunOverviewDiagnosticsLastDispatch.from_dict(data.get("lastDispatch")),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        if self.host is not None:
            out["host"] = _encode(self.host)
        if self.last_dispatch is not None:
            out["lastDispatch"] = _encode(self.last_dispatch)
        return out


@dataclass(frozen=True, slots=True)
class DebugRunOverviewDiagnosticsHost:
    """`DebugRunOverviewDiagnosticsHost`, as carried on the wire."""

    #: May be absent entirely.
    platform: str | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "DebugRunOverviewDiagnosticsHost":
        """Decode a `DebugRunOverviewDiagnosticsHost` from its JSON object."""
        known = {"platform"}
        return cls(
            platform=data.get("platform"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        if self.platform is not None:
            out["platform"] = self.platform
        return out


@dataclass(frozen=True, slots=True)
class DebugRunOverviewDiagnosticsLastDispatch:
    """`DebugRunOverviewDiagnosticsLastDispatch`, as carried on the wire."""

    agent_kind: str
    at: float
    step_index: float
    #: May be absent entirely.
    execution_backend: str | None = None
    #: May be absent entirely.
    failure: DebugRunOverviewDiagnosticsLastDispatchFailure | None = None
    #: May be absent entirely.
    model: str | None = None
    #: May be absent entirely.
    repo: DebugRunOverviewDiagnosticsLastDispatchRepo | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "DebugRunOverviewDiagnosticsLastDispatch":
        """Decode a `DebugRunOverviewDiagnosticsLastDispatch` from its JSON object."""
        known = {"agentKind", "at", "stepIndex", "executionBackend", "failure", "model", "repo"}
        return cls(
            agent_kind=data.get("agentKind"),
            at=data.get("at"),
            step_index=data.get("stepIndex"),
            execution_backend=data.get("executionBackend"),
            failure=None if data.get("failure") is None else DebugRunOverviewDiagnosticsLastDispatchFailure.from_dict(data.get("failure")),
            model=data.get("model"),
            repo=None if data.get("repo") is None else DebugRunOverviewDiagnosticsLastDispatchRepo.from_dict(data.get("repo")),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["agentKind"] = self.agent_kind
        out["at"] = self.at
        out["stepIndex"] = self.step_index
        if self.execution_backend is not None:
            out["executionBackend"] = self.execution_backend
        if self.failure is not None:
            out["failure"] = _encode(self.failure)
        if self.model is not None:
            out["model"] = self.model
        if self.repo is not None:
            out["repo"] = _encode(self.repo)
        return out


@dataclass(frozen=True, slots=True)
class DebugRunOverviewDiagnosticsLastDispatchFailure:
    """`DebugRunOverviewDiagnosticsLastDispatchFailure`, as carried on the wire."""

    at: float
    kind: str
    #: May be absent entirely.
    reason: str | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "DebugRunOverviewDiagnosticsLastDispatchFailure":
        """Decode a `DebugRunOverviewDiagnosticsLastDispatchFailure` from its JSON object."""
        known = {"at", "kind", "reason"}
        return cls(
            at=data.get("at"),
            kind=data.get("kind"),
            reason=data.get("reason"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["at"] = self.at
        out["kind"] = self.kind
        if self.reason is not None:
            out["reason"] = self.reason
        return out


@dataclass(frozen=True, slots=True)
class DebugRunOverviewDiagnosticsLastDispatchRepo:
    """`DebugRunOverviewDiagnosticsLastDispatchRepo`, as carried on the wire."""

    name: str
    owner: str
    #: May be absent entirely.
    base_branch: str | None = None
    #: May be absent entirely.
    provider: str | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "DebugRunOverviewDiagnosticsLastDispatchRepo":
        """Decode a `DebugRunOverviewDiagnosticsLastDispatchRepo` from its JSON object."""
        known = {"name", "owner", "baseBranch", "provider"}
        return cls(
            name=data.get("name"),
            owner=data.get("owner"),
            base_branch=data.get("baseBranch"),
            provider=data.get("provider"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["name"] = self.name
        out["owner"] = self.owner
        if self.base_branch is not None:
            out["baseBranch"] = self.base_branch
        if self.provider is not None:
            out["provider"] = self.provider
        return out


@dataclass(frozen=True, slots=True)
class DebugRunOverviewLlm:
    """`DebugRunOverviewLlm`, as carried on the wire."""

    by_agent_kind: list[DebugLlmAgentKindRollup]
    by_phase: list[DebugLlmPhaseRollup]
    totals: DebugLlmTotals
    #: Always present; ``None`` when the server has no value for it.
    cost_currency: str | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "DebugRunOverviewLlm":
        """Decode a `DebugRunOverviewLlm` from its JSON object."""
        known = {"byAgentKind", "byPhase", "totals", "costCurrency"}
        return cls(
            by_agent_kind=[DebugLlmAgentKindRollup.from_dict(item) for item in data.get("byAgentKind") or []],
            by_phase=[DebugLlmPhaseRollup.from_dict(item) for item in data.get("byPhase") or []],
            totals=DebugLlmTotals.from_dict(data.get("totals")),
            cost_currency=data.get("costCurrency"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["byAgentKind"] = [_encode(item) for item in self.by_agent_kind]
        out["byPhase"] = [_encode(item) for item in self.by_phase]
        out["totals"] = _encode(self.totals)
        out["costCurrency"] = self.cost_currency
        return out


@dataclass(frozen=True, slots=True)
class DebugRunOverviewSinks:
    """`DebugRunOverviewSinks`, as carried on the wire."""

    agent_context: DebugRunOverviewSinksAgentContext
    llm_calls: DebugRunOverviewSinksAgentContext
    provisioning_log: DebugRunOverviewSinksAgentContext
    search_queries: DebugRunOverviewSinksAgentContext
    tool_calls: DebugRunOverviewSinksAgentContext

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "DebugRunOverviewSinks":
        """Decode a `DebugRunOverviewSinks` from its JSON object."""
        known = {"agentContext", "llmCalls", "provisioningLog", "searchQueries", "toolCalls"}
        return cls(
            agent_context=DebugRunOverviewSinksAgentContext.from_dict(data.get("agentContext")),
            llm_calls=DebugRunOverviewSinksAgentContext.from_dict(data.get("llmCalls")),
            provisioning_log=DebugRunOverviewSinksAgentContext.from_dict(data.get("provisioningLog")),
            search_queries=DebugRunOverviewSinksAgentContext.from_dict(data.get("searchQueries")),
            tool_calls=DebugRunOverviewSinksAgentContext.from_dict(data.get("toolCalls")),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["agentContext"] = _encode(self.agent_context)
        out["llmCalls"] = _encode(self.llm_calls)
        out["provisioningLog"] = _encode(self.provisioning_log)
        out["searchQueries"] = _encode(self.search_queries)
        out["toolCalls"] = _encode(self.tool_calls)
        return out


@dataclass(frozen=True, slots=True)
class DebugRunOverviewSinksAgentContext:
    """`DebugRunOverviewSinksAgentContext`, as carried on the wire."""

    available: bool
    count: float

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "DebugRunOverviewSinksAgentContext":
        """Decode a `DebugRunOverviewSinksAgentContext` from its JSON object."""
        known = {"available", "count"}
        return cls(
            available=data.get("available"),
            count=data.get("count"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["available"] = self.available
        out["count"] = self.count
        return out


@dataclass(frozen=True, slots=True)
class DebugRunOverviewToolCalls:
    """`DebugRunOverviewToolCalls`, as carried on the wire."""

    by_agent_kind: list[DebugToolCallKindRollup]
    by_tool: list[DebugToolRollup]
    totals: DebugToolCallTotals

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "DebugRunOverviewToolCalls":
        """Decode a `DebugRunOverviewToolCalls` from its JSON object."""
        known = {"byAgentKind", "byTool", "totals"}
        return cls(
            by_agent_kind=[DebugToolCallKindRollup.from_dict(item) for item in data.get("byAgentKind") or []],
            by_tool=[DebugToolRollup.from_dict(item) for item in data.get("byTool") or []],
            totals=DebugToolCallTotals.from_dict(data.get("totals")),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["byAgentKind"] = [_encode(item) for item in self.by_agent_kind]
        out["byTool"] = [_encode(item) for item in self.by_tool]
        out["totals"] = _encode(self.totals)
        return out


@dataclass(frozen=True, slots=True)
class DebugRunSignal:
    """`DebugRunSignal`, as carried on the wire."""

    code: str
    message: str
    severity: DebugRunSignalSeverity
    #: Always present; ``None`` when the server has no value for it.
    agent_kind: str | None = None
    #: Always present; ``None`` when the server has no value for it.
    count: float | None = None
    #: Always present; ``None`` when the server has no value for it.
    step_index: float | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "DebugRunSignal":
        """Decode a `DebugRunSignal` from its JSON object."""
        known = {"code", "message", "severity", "agentKind", "count", "stepIndex"}
        return cls(
            code=data.get("code"),
            message=data.get("message"),
            severity=_enum(DebugRunSignalSeverity, data.get("severity")),
            agent_kind=data.get("agentKind"),
            count=data.get("count"),
            step_index=data.get("stepIndex"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["code"] = self.code
        out["message"] = self.message
        out["severity"] = _encode(self.severity)
        out["agentKind"] = self.agent_kind
        out["count"] = self.count
        out["stepIndex"] = self.step_index
        return out


class DebugRunSignalSeverity(StrEnum):
    """The `DebugRunSignalSeverity` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    INFO = "info"
    WARNING = "warning"
    ERROR = "error"


@dataclass(frozen=True, slots=True)
class DebugRunStep:
    """`DebugRunStep`, as carried on the wire."""

    agent_kind: str
    eviction_recoveries: float
    has_structured_result: bool
    index: float
    output_chars: float
    progress: float
    skipped: bool
    state: str
    #: Always present; ``None`` when the server has no value for it.
    finished_at: float | None = None
    #: Always present; ``None`` when the server has no value for it.
    first_eviction_detail: DebugText | None = None
    #: Always present; ``None`` when the server has no value for it.
    last_activity_at: float | None = None
    #: Always present; ``None`` when the server has no value for it.
    model: str | None = None
    #: Always present; ``None`` when the server has no value for it.
    started_at: float | None = None
    #: Always present; ``None`` when the server has no value for it.
    subtasks: RunSubtaskCounts | None = None
    #: May be absent entirely.
    tool_servers: DebugStepToolServers | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "DebugRunStep":
        """Decode a `DebugRunStep` from its JSON object."""
        known = {"agentKind", "evictionRecoveries", "hasStructuredResult", "index", "outputChars", "progress", "skipped", "state", "finishedAt", "firstEvictionDetail", "lastActivityAt", "model", "startedAt", "subtasks", "toolServers"}
        return cls(
            agent_kind=data.get("agentKind"),
            eviction_recoveries=data.get("evictionRecoveries"),
            has_structured_result=data.get("hasStructuredResult"),
            index=data.get("index"),
            output_chars=data.get("outputChars"),
            progress=data.get("progress"),
            skipped=data.get("skipped"),
            state=data.get("state"),
            finished_at=data.get("finishedAt"),
            first_eviction_detail=None if data.get("firstEvictionDetail") is None else DebugText.from_dict(data.get("firstEvictionDetail")),
            last_activity_at=data.get("lastActivityAt"),
            model=data.get("model"),
            started_at=data.get("startedAt"),
            subtasks=None if data.get("subtasks") is None else RunSubtaskCounts.from_dict(data.get("subtasks")),
            tool_servers=None if data.get("toolServers") is None else DebugStepToolServers.from_dict(data.get("toolServers")),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["agentKind"] = self.agent_kind
        out["evictionRecoveries"] = self.eviction_recoveries
        out["hasStructuredResult"] = self.has_structured_result
        out["index"] = self.index
        out["outputChars"] = self.output_chars
        out["progress"] = self.progress
        out["skipped"] = self.skipped
        out["state"] = self.state
        out["finishedAt"] = self.finished_at
        out["firstEvictionDetail"] = _encode(self.first_eviction_detail)
        out["lastActivityAt"] = self.last_activity_at
        out["model"] = self.model
        out["startedAt"] = self.started_at
        out["subtasks"] = _encode(self.subtasks)
        if self.tool_servers is not None:
            out["toolServers"] = _encode(self.tool_servers)
        return out


@dataclass(frozen=True, slots=True)
class DebugRunSummary:
    """`DebugRunSummary`, as carried on the wire."""

    block_id: str
    created_at: float
    current_step: float
    pipeline_id: str
    pipeline_name: str
    run_id: str
    status: RunStatus
    step_count: float
    #: Always present; ``None`` when the server has no value for it.
    failure: DebugRunFailure | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "DebugRunSummary":
        """Decode a `DebugRunSummary` from its JSON object."""
        known = {"blockId", "createdAt", "currentStep", "pipelineId", "pipelineName", "runId", "status", "stepCount", "failure"}
        return cls(
            block_id=data.get("blockId"),
            created_at=data.get("createdAt"),
            current_step=data.get("currentStep"),
            pipeline_id=data.get("pipelineId"),
            pipeline_name=data.get("pipelineName"),
            run_id=data.get("runId"),
            status=_enum(RunStatus, data.get("status")),
            step_count=data.get("stepCount"),
            failure=None if data.get("failure") is None else DebugRunFailure.from_dict(data.get("failure")),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["blockId"] = self.block_id
        out["createdAt"] = self.created_at
        out["currentStep"] = self.current_step
        out["pipelineId"] = self.pipeline_id
        out["pipelineName"] = self.pipeline_name
        out["runId"] = self.run_id
        out["status"] = _encode(self.status)
        out["stepCount"] = self.step_count
        out["failure"] = _encode(self.failure)
        return out


@dataclass(frozen=True, slots=True)
class DebugSearchQuery:
    """`DebugSearchQuery`, as carried on the wire."""

    agent_kind: str
    created_at: float
    execution_id: str
    id: str
    query: str
    result_count: float
    workspace_id: str
    #: Always present; ``None`` when the server has no value for it.
    provider: DebugSearchQueryProvider | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "DebugSearchQuery":
        """Decode a `DebugSearchQuery` from its JSON object."""
        known = {"agentKind", "createdAt", "executionId", "id", "query", "resultCount", "workspaceId", "provider"}
        return cls(
            agent_kind=data.get("agentKind"),
            created_at=data.get("createdAt"),
            execution_id=data.get("executionId"),
            id=data.get("id"),
            query=data.get("query"),
            result_count=data.get("resultCount"),
            workspace_id=data.get("workspaceId"),
            provider=None if data.get("provider") is None else _enum(DebugSearchQueryProvider, data.get("provider")),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["agentKind"] = self.agent_kind
        out["createdAt"] = self.created_at
        out["executionId"] = self.execution_id
        out["id"] = self.id
        out["query"] = self.query
        out["resultCount"] = self.result_count
        out["workspaceId"] = self.workspace_id
        out["provider"] = _encode(self.provider)
        return out


class DebugSearchQueryProvider(StrEnum):
    """The `DebugSearchQueryProvider` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    BRAVE = "brave"
    SEARXNG = "searxng"


@dataclass(frozen=True, slots=True)
class DebugStepToolServers:
    """`DebugStepToolServers`, as carried on the wire."""

    agent_kind: str
    unavailable: list[DebugUnavailableToolServer]
    wired: list[DebugWiredToolServer]
    #: May be absent entirely.
    observed: list[DebugObservedToolServer] | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "DebugStepToolServers":
        """Decode a `DebugStepToolServers` from its JSON object."""
        known = {"agentKind", "unavailable", "wired", "observed"}
        return cls(
            agent_kind=data.get("agentKind"),
            unavailable=[DebugUnavailableToolServer.from_dict(item) for item in data.get("unavailable") or []],
            wired=[DebugWiredToolServer.from_dict(item) for item in data.get("wired") or []],
            observed=None if data.get("observed") is None else [DebugObservedToolServer.from_dict(item) for item in data.get("observed") or []],
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["agentKind"] = self.agent_kind
        out["unavailable"] = [_encode(item) for item in self.unavailable]
        out["wired"] = [_encode(item) for item in self.wired]
        if self.observed is not None:
            out["observed"] = [_encode(item) for item in self.observed]
        return out


@dataclass(frozen=True, slots=True)
class DebugSubtaskCounts:
    """`DebugSubtaskCounts`, as carried on the wire."""

    completed: float
    in_progress: float
    total: float
    #: May be absent entirely.
    items: list[DebugSubtaskItem] | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "DebugSubtaskCounts":
        """Decode a `DebugSubtaskCounts` from its JSON object."""
        known = {"completed", "inProgress", "total", "items"}
        return cls(
            completed=data.get("completed"),
            in_progress=data.get("inProgress"),
            total=data.get("total"),
            items=None if data.get("items") is None else [DebugSubtaskItem.from_dict(item) for item in data.get("items") or []],
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["completed"] = self.completed
        out["inProgress"] = self.in_progress
        out["total"] = self.total
        if self.items is not None:
            out["items"] = [_encode(item) for item in self.items]
        return out


@dataclass(frozen=True, slots=True)
class DebugSubtaskItem:
    """`DebugSubtaskItem`, as carried on the wire."""

    label: str
    status: DebugSubtaskItemStatus

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "DebugSubtaskItem":
        """Decode a `DebugSubtaskItem` from its JSON object."""
        known = {"label", "status"}
        return cls(
            label=data.get("label"),
            status=_enum(DebugSubtaskItemStatus, data.get("status")),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["label"] = self.label
        out["status"] = _encode(self.status)
        return out


class DebugSubtaskItemStatus(StrEnum):
    """The `DebugSubtaskItemStatus` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    PENDING = "pending"
    IN_PROGRESS = "in_progress"
    COMPLETED = "completed"


@dataclass(frozen=True, slots=True)
class DebugText:
    """`DebugText`, as carried on the wire."""

    chars: float
    offset: float
    text: str
    total_chars: float
    truncated: bool
    #: May be absent entirely.
    match_offset: float | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "DebugText":
        """Decode a `DebugText` from its JSON object."""
        known = {"chars", "offset", "text", "totalChars", "truncated", "matchOffset"}
        return cls(
            chars=data.get("chars"),
            offset=data.get("offset"),
            text=data.get("text"),
            total_chars=data.get("totalChars"),
            truncated=data.get("truncated"),
            match_offset=data.get("matchOffset"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["chars"] = self.chars
        out["offset"] = self.offset
        out["text"] = self.text
        out["totalChars"] = self.total_chars
        out["truncated"] = self.truncated
        if self.match_offset is not None:
            out["matchOffset"] = self.match_offset
        return out


@dataclass(frozen=True, slots=True)
class DebugToolCall:
    """`DebugToolCall`, as carried on the wire."""

    args: DebugText
    name: str

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "DebugToolCall":
        """Decode a `DebugToolCall` from its JSON object."""
        known = {"args", "name"}
        return cls(
            args=DebugText.from_dict(data.get("args")),
            name=data.get("name"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["args"] = _encode(self.args)
        out["name"] = self.name
        return out


@dataclass(frozen=True, slots=True)
class DebugToolCallKindRollup:
    """`DebugToolCallKindRollup`, as carried on the wire."""

    agent_kind: str
    calls: float
    failures: float
    #: Always present; ``None`` when the server has no value for it.
    failure_rate: float | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "DebugToolCallKindRollup":
        """Decode a `DebugToolCallKindRollup` from its JSON object."""
        known = {"agentKind", "calls", "failures", "failureRate"}
        return cls(
            agent_kind=data.get("agentKind"),
            calls=data.get("calls"),
            failures=data.get("failures"),
            failure_rate=data.get("failureRate"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["agentKind"] = self.agent_kind
        out["calls"] = self.calls
        out["failures"] = self.failures
        out["failureRate"] = self.failure_rate
        return out


@dataclass(frozen=True, slots=True)
class DebugToolCallTotals:
    """`DebugToolCallTotals`, as carried on the wire."""

    calls: float
    failures: float
    #: Always present; ``None`` when the server has no value for it.
    failure_rate: float | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "DebugToolCallTotals":
        """Decode a `DebugToolCallTotals` from its JSON object."""
        known = {"calls", "failures", "failureRate"}
        return cls(
            calls=data.get("calls"),
            failures=data.get("failures"),
            failure_rate=data.get("failureRate"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["calls"] = self.calls
        out["failures"] = self.failures
        out["failureRate"] = self.failure_rate
        return out


@dataclass(frozen=True, slots=True)
class DebugToolRollup:
    """`DebugToolRollup`, as carried on the wire."""

    calls: float
    failures: float
    tool: str
    #: Always present; ``None`` when the server has no value for it.
    failure_rate: float | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "DebugToolRollup":
        """Decode a `DebugToolRollup` from its JSON object."""
        known = {"calls", "failures", "tool", "failureRate"}
        return cls(
            calls=data.get("calls"),
            failures=data.get("failures"),
            tool=data.get("tool"),
            failure_rate=data.get("failureRate"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["calls"] = self.calls
        out["failures"] = self.failures
        out["tool"] = self.tool
        out["failureRate"] = self.failure_rate
        return out


@dataclass(frozen=True, slots=True)
class DebugUnavailableToolServer:
    """`DebugUnavailableToolServer`, as carried on the wire."""

    id: str
    label: str
    reason: DebugUnavailableToolServerReason

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "DebugUnavailableToolServer":
        """Decode a `DebugUnavailableToolServer` from its JSON object."""
        known = {"id", "label", "reason"}
        return cls(
            id=data.get("id"),
            label=data.get("label"),
            reason=_enum(DebugUnavailableToolServerReason, data.get("reason")),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["id"] = self.id
        out["label"] = self.label
        out["reason"] = _encode(self.reason)
        return out


class DebugUnavailableToolServerReason(StrEnum):
    """The `DebugUnavailableToolServerReason` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    HARNESS_UNSUPPORTED = "harness_unsupported"
    TRANSPORT_UNSUPPORTED = "transport_unsupported"
    MISSING_SECRET = "missing_secret"
    RESERVED_SECRET = "reserved_secret"
    OAUTH_NOT_CONNECTED = "oauth_not_connected"
    OAUTH_TOKEN_FAILED = "oauth_token_failed"
    OVER_BUDGET = "over_budget"


@dataclass(frozen=True, slots=True)
class DebugWiredToolServer:
    """`DebugWiredToolServer`, as carried on the wire."""

    id: str
    label: str
    transport: DebugWiredToolServerTransport
    #: May be absent entirely.
    tools: list[str] | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "DebugWiredToolServer":
        """Decode a `DebugWiredToolServer` from its JSON object."""
        known = {"id", "label", "transport", "tools"}
        return cls(
            id=data.get("id"),
            label=data.get("label"),
            transport=_enum(DebugWiredToolServerTransport, data.get("transport")),
            tools=None if data.get("tools") is None else [item for item in data.get("tools") or []],
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["id"] = self.id
        out["label"] = self.label
        out["transport"] = _encode(self.transport)
        if self.tools is not None:
            out["tools"] = [_encode(item) for item in self.tools]
        return out


class DebugWiredToolServerTransport(StrEnum):
    """The `DebugWiredToolServerTransport` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    STDIO = "stdio"
    HTTP = "http"


@dataclass(frozen=True, slots=True)
class DetachPublicTaskDocumentRequest:
    """`DetachPublicTaskDocumentRequest`, as carried on the wire."""

    external_id: str
    source: PrReportContextDocumentOrigin

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "DetachPublicTaskDocumentRequest":
        """Decode a `DetachPublicTaskDocumentRequest` from its JSON object."""
        known = {"externalId", "source"}
        return cls(
            external_id=data.get("externalId"),
            source=_enum(PrReportContextDocumentOrigin, data.get("source")),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["externalId"] = self.external_id
        out["source"] = _encode(self.source)
        return out


@dataclass(frozen=True, slots=True)
class DocumentFreshnessVariant0:
    """`DocumentFreshnessVariant0`, as carried on the wire."""

    change: DocumentFreshnessVariant0Change
    status: str
    version: str

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "DocumentFreshnessVariant0":
        """Decode a `DocumentFreshnessVariant0` from its JSON object."""
        known = {"change", "status", "version"}
        return cls(
            change=_enum(DocumentFreshnessVariant0Change, data.get("change")),
            status=data.get("status"),
            version=data.get("version"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["change"] = _encode(self.change)
        out["status"] = self.status
        out["version"] = self.version
        return out


class DocumentFreshnessVariant0Change(StrEnum):
    """The `DocumentFreshnessVariant0Change` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    UNCHANGED = "unchanged"
    REIMPORTED = "reimported"
    REVISION_ONLY = "revision_only"


@dataclass(frozen=True, slots=True)
class DocumentFreshnessVariant1:
    """`DocumentFreshnessVariant1`, as carried on the wire."""

    status: str

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "DocumentFreshnessVariant1":
        """Decode a `DocumentFreshnessVariant1` from its JSON object."""
        known = {"status"}
        return cls(
            status=data.get("status"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["status"] = self.status
        return out


@dataclass(frozen=True, slots=True)
class DocumentFreshnessVariant2:
    """`DocumentFreshnessVariant2`, as carried on the wire."""

    reason: DocumentFreshnessVariant2Reason
    status: str

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "DocumentFreshnessVariant2":
        """Decode a `DocumentFreshnessVariant2` from its JSON object."""
        known = {"reason", "status"}
        return cls(
            reason=_enum(DocumentFreshnessVariant2Reason, data.get("reason")),
            status=data.get("status"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["reason"] = _encode(self.reason)
        out["status"] = self.status
        return out


class DocumentFreshnessVariant2Reason(StrEnum):
    """The `DocumentFreshnessVariant2Reason` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    NOT_CONNECTED = "not_connected"
    CREDENTIALS_UNREADABLE = "credentials_unreadable"
    UNVERSIONED = "unversioned"
    SOURCE_UNREACHABLE = "source_unreachable"


@dataclass(frozen=True, slots=True)
class ErrorResponse:
    """`ErrorResponse`, as carried on the wire."""

    error: ErrorResponseError

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "ErrorResponse":
        """Decode a `ErrorResponse` from its JSON object."""
        known = {"error"}
        return cls(
            error=ErrorResponseError.from_dict(data.get("error")),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["error"] = _encode(self.error)
        return out


@dataclass(frozen=True, slots=True)
class ErrorResponseError:
    """`ErrorResponseError`, as carried on the wire."""

    code: str
    message: str
    #: May be absent entirely.
    details: Any | None = None
    #: May be absent entirely.
    issues: list[ErrorResponseErrorIssue] | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "ErrorResponseError":
        """Decode a `ErrorResponseError` from its JSON object."""
        known = {"code", "message", "details", "issues"}
        return cls(
            code=data.get("code"),
            message=data.get("message"),
            details=data.get("details"),
            issues=None if data.get("issues") is None else [ErrorResponseErrorIssue.from_dict(item) for item in data.get("issues") or []],
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["code"] = self.code
        out["message"] = self.message
        if self.details is not None:
            out["details"] = self.details
        if self.issues is not None:
            out["issues"] = [_encode(item) for item in self.issues]
        return out


@dataclass(frozen=True, slots=True)
class ErrorResponseErrorIssue:
    """`ErrorResponseErrorIssue`, as carried on the wire."""

    message: str
    #: May be absent entirely.
    path: str | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "ErrorResponseErrorIssue":
        """Decode a `ErrorResponseErrorIssue` from its JSON object."""
        known = {"message", "path"}
        return cls(
            message=data.get("message"),
            path=data.get("path"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["message"] = self.message
        if self.path is not None:
            out["path"] = self.path
        return out


class GetDebugLlmCallView(StrEnum):
    """The `GetDebugLlmCallView` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    RAW = "raw"
    MESSAGES = "messages"


@dataclass(frozen=True, slots=True)
class GetPublicMergeRecordResponse:
    """`GetPublicMergeRecordResponse`, as carried on the wire."""

    change_class: NotificationPayloadChangeClass
    created_at: float
    decision: GetPublicMergeRecordResponseDecision
    record_id: str
    task_id: str
    #: Always present; ``None`` when the server has no value for it.
    changed_file_count: float | None = None
    #: Always present; ``None`` when the server has no value for it.
    complexity: float | None = None
    #: Always present; ``None`` when the server has no value for it.
    impact: float | None = None
    #: Always present; ``None`` when the server has no value for it.
    merge_preset_id: str | None = None
    #: Always present; ``None`` when the server has no value for it.
    merge_preset_name: str | None = None
    #: Always present; ``None`` when the server has no value for it.
    pr_number: float | None = None
    #: Always present; ``None`` when the server has no value for it.
    pr_url: str | None = None
    #: Always present; ``None`` when the server has no value for it.
    provider: PrReportRunProvider | None = None
    #: Always present; ``None`` when the server has no value for it.
    repo_id: str | None = None
    #: Always present; ``None`` when the server has no value for it.
    resolved_at: float | None = None
    #: Always present; ``None`` when the server has no value for it.
    review_effort: GetPublicMergeRecordResponseReviewEffort | None = None
    #: Always present; ``None`` when the server has no value for it.
    risk: float | None = None
    #: Always present; ``None`` when the server has no value for it.
    run_id: str | None = None
    #: Always present; ``None`` when the server has no value for it.
    tagged_at: float | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "GetPublicMergeRecordResponse":
        """Decode a `GetPublicMergeRecordResponse` from its JSON object."""
        known = {"changeClass", "createdAt", "decision", "recordId", "taskId", "changedFileCount", "complexity", "impact", "mergePresetId", "mergePresetName", "prNumber", "prUrl", "provider", "repoId", "resolvedAt", "reviewEffort", "risk", "runId", "taggedAt"}
        return cls(
            change_class=_enum(NotificationPayloadChangeClass, data.get("changeClass")),
            created_at=data.get("createdAt"),
            decision=_enum(GetPublicMergeRecordResponseDecision, data.get("decision")),
            record_id=data.get("recordId"),
            task_id=data.get("taskId"),
            changed_file_count=data.get("changedFileCount"),
            complexity=data.get("complexity"),
            impact=data.get("impact"),
            merge_preset_id=data.get("mergePresetId"),
            merge_preset_name=data.get("mergePresetName"),
            pr_number=data.get("prNumber"),
            pr_url=data.get("prUrl"),
            provider=None if data.get("provider") is None else _enum(PrReportRunProvider, data.get("provider")),
            repo_id=data.get("repoId"),
            resolved_at=data.get("resolvedAt"),
            review_effort=None if data.get("reviewEffort") is None else _enum(GetPublicMergeRecordResponseReviewEffort, data.get("reviewEffort")),
            risk=data.get("risk"),
            run_id=data.get("runId"),
            tagged_at=data.get("taggedAt"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["changeClass"] = _encode(self.change_class)
        out["createdAt"] = self.created_at
        out["decision"] = _encode(self.decision)
        out["recordId"] = self.record_id
        out["taskId"] = self.task_id
        out["changedFileCount"] = self.changed_file_count
        out["complexity"] = self.complexity
        out["impact"] = self.impact
        out["mergePresetId"] = self.merge_preset_id
        out["mergePresetName"] = self.merge_preset_name
        out["prNumber"] = self.pr_number
        out["prUrl"] = self.pr_url
        out["provider"] = _encode(self.provider)
        out["repoId"] = self.repo_id
        out["resolvedAt"] = self.resolved_at
        out["reviewEffort"] = _encode(self.review_effort)
        out["risk"] = self.risk
        out["runId"] = self.run_id
        out["taggedAt"] = self.tagged_at
        return out


class GetPublicMergeRecordResponseDecision(StrEnum):
    """The `GetPublicMergeRecordResponseDecision` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    PENDING_REVIEW = "pending_review"
    AUTO_MERGED = "auto_merged"
    HUMAN_MERGED = "human_merged"
    EXTERNAL_MERGED = "external_merged"
    REJECTED = "rejected"


class GetPublicMergeRecordResponseReviewEffort(StrEnum):
    """The `GetPublicMergeRecordResponseReviewEffort` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    NONE = "none"
    MINOR = "minor"
    MAJOR = "major"


@dataclass(frozen=True, slots=True)
class GetPublicRunOutcomeResponse:
    """`GetPublicRunOutcomeResponse`, as carried on the wire."""

    checks: list[GetPublicRunOutcomeResponseCheck]
    disposition: GetPublicRunOutcomeResponseDisposition
    pull_requests: list[GetPublicRunOutcomeResponsePullRequest]
    requirements: GetPublicRunOutcomeResponseRequirements
    sources: GetPublicRunOutcomeResponseSources
    tests: GetPublicRunOutcomeResponseTests
    title: str
    truncations: list[str]
    version: float
    visuals: GetPublicRunOutcomeResponseVisuals
    #: Always present; ``None`` when the server has no value for it.
    ask: str | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "GetPublicRunOutcomeResponse":
        """Decode a `GetPublicRunOutcomeResponse` from its JSON object."""
        known = {"checks", "disposition", "pullRequests", "requirements", "sources", "tests", "title", "truncations", "version", "visuals", "ask"}
        return cls(
            checks=[GetPublicRunOutcomeResponseCheck.from_dict(item) for item in data.get("checks") or []],
            disposition=_enum(GetPublicRunOutcomeResponseDisposition, data.get("disposition")),
            pull_requests=[GetPublicRunOutcomeResponsePullRequest.from_dict(item) for item in data.get("pullRequests") or []],
            requirements=GetPublicRunOutcomeResponseRequirements.from_dict(data.get("requirements")),
            sources=GetPublicRunOutcomeResponseSources.from_dict(data.get("sources")),
            tests=GetPublicRunOutcomeResponseTests.from_dict(data.get("tests")),
            title=data.get("title"),
            truncations=[item for item in data.get("truncations") or []],
            version=data.get("version"),
            visuals=GetPublicRunOutcomeResponseVisuals.from_dict(data.get("visuals")),
            ask=data.get("ask"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["checks"] = [_encode(item) for item in self.checks]
        out["disposition"] = _encode(self.disposition)
        out["pullRequests"] = [_encode(item) for item in self.pull_requests]
        out["requirements"] = _encode(self.requirements)
        out["sources"] = _encode(self.sources)
        out["tests"] = _encode(self.tests)
        out["title"] = self.title
        out["truncations"] = [_encode(item) for item in self.truncations]
        out["version"] = self.version
        out["visuals"] = _encode(self.visuals)
        out["ask"] = self.ask
        return out


@dataclass(frozen=True, slots=True)
class GetPublicRunOutcomeResponseCheck:
    """`GetPublicRunOutcomeResponseCheck`, as carried on the wire."""

    kind: GetPublicRunOutcomeResponseCheckKind
    state: GetPublicRunOutcomeResponseCheckState
    #: Always present; ``None`` when the server has no value for it.
    reproduction: PrReportReproductionVerdict | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "GetPublicRunOutcomeResponseCheck":
        """Decode a `GetPublicRunOutcomeResponseCheck` from its JSON object."""
        known = {"kind", "state", "reproduction"}
        return cls(
            kind=_enum(GetPublicRunOutcomeResponseCheckKind, data.get("kind")),
            state=_enum(GetPublicRunOutcomeResponseCheckState, data.get("state")),
            reproduction=None if data.get("reproduction") is None else _enum(PrReportReproductionVerdict, data.get("reproduction")),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["kind"] = _encode(self.kind)
        out["state"] = _encode(self.state)
        out["reproduction"] = _encode(self.reproduction)
        return out


class GetPublicRunOutcomeResponseCheckKind(StrEnum):
    """The `GetPublicRunOutcomeResponseCheckKind` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    CI = "ci"
    VALIDATION = "validation"
    REPRODUCTION = "reproduction"


class GetPublicRunOutcomeResponseCheckState(StrEnum):
    """The `GetPublicRunOutcomeResponseCheckState` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    PASS = "pass"
    FAIL = "fail"
    PENDING = "pending"
    INCONCLUSIVE = "inconclusive"


class GetPublicRunOutcomeResponseDisposition(StrEnum):
    """The `GetPublicRunOutcomeResponseDisposition` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    MERGED = "merged"
    AWAITING_MERGE = "awaiting_merge"
    IN_FLIGHT = "in_flight"
    NEEDS_ATTENTION = "needs_attention"
    NOT_RUN = "not_run"
    UNKNOWN = "unknown"


@dataclass(frozen=True, slots=True)
class GetPublicRunOutcomeResponsePullRequest:
    """`GetPublicRunOutcomeResponsePullRequest`, as carried on the wire."""

    url: str
    #: Always present; ``None`` when the server has no value for it.
    branch: str | None = None
    #: Always present; ``None`` when the server has no value for it.
    number: float | None = None
    #: Always present; ``None`` when the server has no value for it.
    repo: str | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "GetPublicRunOutcomeResponsePullRequest":
        """Decode a `GetPublicRunOutcomeResponsePullRequest` from its JSON object."""
        known = {"url", "branch", "number", "repo"}
        return cls(
            url=data.get("url"),
            branch=data.get("branch"),
            number=data.get("number"),
            repo=data.get("repo"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["url"] = self.url
        out["branch"] = self.branch
        out["number"] = self.number
        out["repo"] = self.repo
        return out


@dataclass(frozen=True, slots=True)
class GetPublicRunOutcomeResponseRequirementsVariant0:
    """`GetPublicRunOutcomeResponseRequirementsVariant0`, as carried on the wire."""

    gap: GetPublicRunOutcomeResponseRequirementsVariant0Gap
    status: str

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "GetPublicRunOutcomeResponseRequirementsVariant0":
        """Decode a `GetPublicRunOutcomeResponseRequirementsVariant0` from its JSON object."""
        known = {"gap", "status"}
        return cls(
            gap=_enum(GetPublicRunOutcomeResponseRequirementsVariant0Gap, data.get("gap")),
            status=data.get("status"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["gap"] = _encode(self.gap)
        out["status"] = self.status
        return out


class GetPublicRunOutcomeResponseRequirementsVariant0Gap(StrEnum):
    """The `GetPublicRunOutcomeResponseRequirementsVariant0Gap` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    RUN_UNAVAILABLE = "run_unavailable"
    NO_TESTER_STEP = "no_tester_step"
    TESTER_NOT_REPORTED = "tester_not_reported"
    NO_VERDICTS = "no_verdicts"
    NO_REQUIREMENTS = "no_requirements"


@dataclass(frozen=True, slots=True)
class GetPublicRunOutcomeResponseRequirementsVariant1:
    """`GetPublicRunOutcomeResponseRequirementsVariant1`, as carried on the wire."""

    entries: list[GetPublicRunOutcomeResponseRequirementsVariant1Entry]
    met: float
    not_covered: float
    not_met: float
    regressions: float
    spec: GetPublicRunOutcomeResponseRequirementsVariant1Spec
    status: str
    total: float
    unmatched_verdicts: float

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "GetPublicRunOutcomeResponseRequirementsVariant1":
        """Decode a `GetPublicRunOutcomeResponseRequirementsVariant1` from its JSON object."""
        known = {"entries", "met", "notCovered", "notMet", "regressions", "spec", "status", "total", "unmatchedVerdicts"}
        return cls(
            entries=[GetPublicRunOutcomeResponseRequirementsVariant1Entry.from_dict(item) for item in data.get("entries") or []],
            met=data.get("met"),
            not_covered=data.get("notCovered"),
            not_met=data.get("notMet"),
            regressions=data.get("regressions"),
            spec=_enum(GetPublicRunOutcomeResponseRequirementsVariant1Spec, data.get("spec")),
            status=data.get("status"),
            total=data.get("total"),
            unmatched_verdicts=data.get("unmatchedVerdicts"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["entries"] = [_encode(item) for item in self.entries]
        out["met"] = self.met
        out["notCovered"] = self.not_covered
        out["notMet"] = self.not_met
        out["regressions"] = self.regressions
        out["spec"] = _encode(self.spec)
        out["status"] = self.status
        out["total"] = self.total
        out["unmatchedVerdicts"] = self.unmatched_verdicts
        return out


@dataclass(frozen=True, slots=True)
class GetPublicRunOutcomeResponseRequirementsVariant1Entry:
    """`GetPublicRunOutcomeResponseRequirementsVariant1Entry`, as carried on the wire."""

    id: str
    regression: bool
    verdict: PrReportRequirementsEntryVerdict
    #: Always present; ``None`` when the server has no value for it.
    detail: str | None = None
    #: Always present; ``None`` when the server has no value for it.
    state: PrReportRequirementsEntryState | None = None
    #: Always present; ``None`` when the server has no value for it.
    title: str | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "GetPublicRunOutcomeResponseRequirementsVariant1Entry":
        """Decode a `GetPublicRunOutcomeResponseRequirementsVariant1Entry` from its JSON object."""
        known = {"id", "regression", "verdict", "detail", "state", "title"}
        return cls(
            id=data.get("id"),
            regression=data.get("regression"),
            verdict=_enum(PrReportRequirementsEntryVerdict, data.get("verdict")),
            detail=data.get("detail"),
            state=None if data.get("state") is None else _enum(PrReportRequirementsEntryState, data.get("state")),
            title=data.get("title"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["id"] = self.id
        out["regression"] = self.regression
        out["verdict"] = _encode(self.verdict)
        out["detail"] = self.detail
        out["state"] = _encode(self.state)
        out["title"] = self.title
        return out


class GetPublicRunOutcomeResponseRequirementsVariant1Spec(StrEnum):
    """The `GetPublicRunOutcomeResponseRequirementsVariant1Spec` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    JOINED = "joined"
    NOT_READ = "not_read"


@dataclass(frozen=True, slots=True)
class GetPublicRunOutcomeResponseSourcesVariant0:
    """`GetPublicRunOutcomeResponseSourcesVariant0`, as carried on the wire."""

    gap: GetPublicRunOutcomeResponseSourcesVariant0Gap
    status: str

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "GetPublicRunOutcomeResponseSourcesVariant0":
        """Decode a `GetPublicRunOutcomeResponseSourcesVariant0` from its JSON object."""
        known = {"gap", "status"}
        return cls(
            gap=_enum(GetPublicRunOutcomeResponseSourcesVariant0Gap, data.get("gap")),
            status=data.get("status"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["gap"] = _encode(self.gap)
        out["status"] = self.status
        return out


class GetPublicRunOutcomeResponseSourcesVariant0Gap(StrEnum):
    """The `GetPublicRunOutcomeResponseSourcesVariant0Gap` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    RUN_UNAVAILABLE = "run_unavailable"
    NONE_LINKED = "none_linked"


@dataclass(frozen=True, slots=True)
class GetPublicRunOutcomeResponseSourcesVariant1:
    """`GetPublicRunOutcomeResponseSourcesVariant1`, as carried on the wire."""

    sources: list[GetPublicRunOutcomeResponseSourcesVariant1Source]
    status: str

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "GetPublicRunOutcomeResponseSourcesVariant1":
        """Decode a `GetPublicRunOutcomeResponseSourcesVariant1` from its JSON object."""
        known = {"sources", "status"}
        return cls(
            sources=[GetPublicRunOutcomeResponseSourcesVariant1Source.from_dict(item) for item in data.get("sources") or []],
            status=data.get("status"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["sources"] = [_encode(item) for item in self.sources]
        out["status"] = self.status
        return out


@dataclass(frozen=True, slots=True)
class GetPublicRunOutcomeResponseSourcesVariant1Source:
    """`GetPublicRunOutcomeResponseSourcesVariant1Source`, as carried on the wire."""

    moved_during_run: bool
    origin: PrReportContextDocumentOrigin
    title: str
    #: Always present; ``None`` when the server has no value for it.
    freshness: DocumentFreshness | None = None
    #: Always present; ``None`` when the server has no value for it.
    url: str | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "GetPublicRunOutcomeResponseSourcesVariant1Source":
        """Decode a `GetPublicRunOutcomeResponseSourcesVariant1Source` from its JSON object."""
        known = {"movedDuringRun", "origin", "title", "freshness", "url"}
        return cls(
            moved_during_run=data.get("movedDuringRun"),
            origin=_enum(PrReportContextDocumentOrigin, data.get("origin")),
            title=data.get("title"),
            freshness=None if data.get("freshness") is None else DocumentFreshness.from_dict(data.get("freshness")),
            url=data.get("url"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["movedDuringRun"] = self.moved_during_run
        out["origin"] = _encode(self.origin)
        out["title"] = self.title
        out["freshness"] = _encode(self.freshness)
        out["url"] = self.url
        return out


@dataclass(frozen=True, slots=True)
class GetPublicRunOutcomeResponseTestsVariant0:
    """`GetPublicRunOutcomeResponseTestsVariant0`, as carried on the wire."""

    gap: GetPublicRunOutcomeResponseTestsVariant0Gap
    status: str

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "GetPublicRunOutcomeResponseTestsVariant0":
        """Decode a `GetPublicRunOutcomeResponseTestsVariant0` from its JSON object."""
        known = {"gap", "status"}
        return cls(
            gap=_enum(GetPublicRunOutcomeResponseTestsVariant0Gap, data.get("gap")),
            status=data.get("status"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["gap"] = _encode(self.gap)
        out["status"] = self.status
        return out


class GetPublicRunOutcomeResponseTestsVariant0Gap(StrEnum):
    """The `GetPublicRunOutcomeResponseTestsVariant0Gap` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    RUN_UNAVAILABLE = "run_unavailable"
    NO_TESTER_STEP = "no_tester_step"
    TESTER_NOT_REPORTED = "tester_not_reported"


@dataclass(frozen=True, slots=True)
class GetPublicRunOutcomeResponseTestsVariant1:
    """`GetPublicRunOutcomeResponseTestsVariant1`, as carried on the wire."""

    areas: list[str]
    concerns: list[GetPublicRunOutcomeResponseTestsVariant1Concern]
    failed: float
    passed: float
    skipped: float
    status: str
    verdict: GetPublicRunOutcomeResponseTestsVariant1Verdict
    #: Always present; ``None`` when the server has no value for it.
    abort_reason: str | None = None
    #: Always present; ``None`` when the server has no value for it.
    environment: PrReportEnvironmentsEvidenceRanAgainst | None = None
    #: Always present; ``None`` when the server has no value for it.
    summary: str | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "GetPublicRunOutcomeResponseTestsVariant1":
        """Decode a `GetPublicRunOutcomeResponseTestsVariant1` from its JSON object."""
        known = {"areas", "concerns", "failed", "passed", "skipped", "status", "verdict", "abortReason", "environment", "summary"}
        return cls(
            areas=[item for item in data.get("areas") or []],
            concerns=[GetPublicRunOutcomeResponseTestsVariant1Concern.from_dict(item) for item in data.get("concerns") or []],
            failed=data.get("failed"),
            passed=data.get("passed"),
            skipped=data.get("skipped"),
            status=data.get("status"),
            verdict=_enum(GetPublicRunOutcomeResponseTestsVariant1Verdict, data.get("verdict")),
            abort_reason=data.get("abortReason"),
            environment=None if data.get("environment") is None else _enum(PrReportEnvironmentsEvidenceRanAgainst, data.get("environment")),
            summary=data.get("summary"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["areas"] = [_encode(item) for item in self.areas]
        out["concerns"] = [_encode(item) for item in self.concerns]
        out["failed"] = self.failed
        out["passed"] = self.passed
        out["skipped"] = self.skipped
        out["status"] = self.status
        out["verdict"] = _encode(self.verdict)
        out["abortReason"] = self.abort_reason
        out["environment"] = _encode(self.environment)
        out["summary"] = self.summary
        return out


@dataclass(frozen=True, slots=True)
class GetPublicRunOutcomeResponseTestsVariant1Concern:
    """`GetPublicRunOutcomeResponseTestsVariant1Concern`, as carried on the wire."""

    severity: Severity
    title: str

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "GetPublicRunOutcomeResponseTestsVariant1Concern":
        """Decode a `GetPublicRunOutcomeResponseTestsVariant1Concern` from its JSON object."""
        known = {"severity", "title"}
        return cls(
            severity=_enum(Severity, data.get("severity")),
            title=data.get("title"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["severity"] = _encode(self.severity)
        out["title"] = self.title
        return out


class GetPublicRunOutcomeResponseTestsVariant1Verdict(StrEnum):
    """The `GetPublicRunOutcomeResponseTestsVariant1Verdict` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    GREENLIT = "greenlit"
    CONCERNS = "concerns"
    COULD_NOT_RUN = "could_not_run"


@dataclass(frozen=True, slots=True)
class GetPublicRunOutcomeResponseVisualsVariant0:
    """`GetPublicRunOutcomeResponseVisualsVariant0`, as carried on the wire."""

    gap: GetPublicRunOutcomeResponseVisualsVariant0Gap
    status: str
    #: Always present; ``None`` when the server has no value for it.
    detail: str | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "GetPublicRunOutcomeResponseVisualsVariant0":
        """Decode a `GetPublicRunOutcomeResponseVisualsVariant0` from its JSON object."""
        known = {"gap", "status", "detail"}
        return cls(
            gap=_enum(GetPublicRunOutcomeResponseVisualsVariant0Gap, data.get("gap")),
            status=data.get("status"),
            detail=data.get("detail"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["gap"] = _encode(self.gap)
        out["status"] = self.status
        out["detail"] = self.detail
        return out


class GetPublicRunOutcomeResponseVisualsVariant0Gap(StrEnum):
    """The `GetPublicRunOutcomeResponseVisualsVariant0Gap` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    RUN_UNAVAILABLE = "run_unavailable"
    NO_VISUAL_STEP = "no_visual_step"
    NONE_CAPTURED = "none_captured"


@dataclass(frozen=True, slots=True)
class GetPublicRunOutcomeResponseVisualsVariant1:
    """`GetPublicRunOutcomeResponseVisualsVariant1`, as carried on the wire."""

    source: GetPublicRunOutcomeResponseVisualsVariant1Source
    status: str
    views: list[GetPublicRunOutcomeResponseVisualsVariant1View]
    #: Always present; ``None`` when the server has no value for it.
    phase: PublicVisualConfirmDecisionPhase | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "GetPublicRunOutcomeResponseVisualsVariant1":
        """Decode a `GetPublicRunOutcomeResponseVisualsVariant1` from its JSON object."""
        known = {"source", "status", "views", "phase"}
        return cls(
            source=_enum(GetPublicRunOutcomeResponseVisualsVariant1Source, data.get("source")),
            status=data.get("status"),
            views=[GetPublicRunOutcomeResponseVisualsVariant1View.from_dict(item) for item in data.get("views") or []],
            phase=None if data.get("phase") is None else _enum(PublicVisualConfirmDecisionPhase, data.get("phase")),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["source"] = _encode(self.source)
        out["status"] = self.status
        out["views"] = [_encode(item) for item in self.views]
        out["phase"] = _encode(self.phase)
        return out


class GetPublicRunOutcomeResponseVisualsVariant1Source(StrEnum):
    """The `GetPublicRunOutcomeResponseVisualsVariant1Source` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    VISUAL_CONFIRM = "visual_confirm"
    TESTER = "tester"


@dataclass(frozen=True, slots=True)
class GetPublicRunOutcomeResponseVisualsVariant1View:
    """`GetPublicRunOutcomeResponseVisualsVariant1View`, as carried on the wire."""

    view: str
    #: Always present; ``None`` when the server has no value for it.
    artifact_id: str | None = None
    #: Always present; ``None`` when the server has no value for it.
    reference_artifact_id: str | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "GetPublicRunOutcomeResponseVisualsVariant1View":
        """Decode a `GetPublicRunOutcomeResponseVisualsVariant1View` from its JSON object."""
        known = {"view", "artifactId", "referenceArtifactId"}
        return cls(
            view=data.get("view"),
            artifact_id=data.get("artifactId"),
            reference_artifact_id=data.get("referenceArtifactId"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["view"] = self.view
        out["artifactId"] = self.artifact_id
        out["referenceArtifactId"] = self.reference_artifact_id
        return out


@dataclass(frozen=True, slots=True)
class ListDebugAgentContextResponse:
    """`ListDebugAgentContextResponse`, as carried on the wire."""

    snapshots: list[DebugAgentContextSummary]
    #: Always present; ``None`` when the server has no value for it.
    next_cursor: str | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "ListDebugAgentContextResponse":
        """Decode a `ListDebugAgentContextResponse` from its JSON object."""
        known = {"snapshots", "nextCursor"}
        return cls(
            snapshots=[DebugAgentContextSummary.from_dict(item) for item in data.get("snapshots") or []],
            next_cursor=data.get("nextCursor"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["snapshots"] = [_encode(item) for item in self.snapshots]
        out["nextCursor"] = self.next_cursor
        return out


class ListDebugLlmCallsOrder(StrEnum):
    """The `ListDebugLlmCallsOrder` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    NEWEST = "newest"
    OLDEST = "oldest"


@dataclass(frozen=True, slots=True)
class ListDebugLlmCallsResponse:
    """`ListDebugLlmCallsResponse`, as carried on the wire."""

    calls: list[DebugLlmCall]
    #: Always present; ``None`` when the server has no value for it.
    next_cursor: str | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "ListDebugLlmCallsResponse":
        """Decode a `ListDebugLlmCallsResponse` from its JSON object."""
        known = {"calls", "nextCursor"}
        return cls(
            calls=[DebugLlmCall.from_dict(item) for item in data.get("calls") or []],
            next_cursor=data.get("nextCursor"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["calls"] = [_encode(item) for item in self.calls]
        out["nextCursor"] = self.next_cursor
        return out


@dataclass(frozen=True, slots=True)
class ListDebugLogsResponse:
    """`ListDebugLogsResponse`, as carried on the wire."""

    entries: list[DebugInfraLogEntry]
    #: Always present; ``None`` when the server has no value for it.
    next_cursor: str | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "ListDebugLogsResponse":
        """Decode a `ListDebugLogsResponse` from its JSON object."""
        known = {"entries", "nextCursor"}
        return cls(
            entries=[DebugInfraLogEntry.from_dict(item) for item in data.get("entries") or []],
            next_cursor=data.get("nextCursor"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["entries"] = [_encode(item) for item in self.entries]
        out["nextCursor"] = self.next_cursor
        return out


@dataclass(frozen=True, slots=True)
class ListDebugRunsResponse:
    """`ListDebugRunsResponse`, as carried on the wire."""

    runs: list[DebugRunSummary]
    #: Always present; ``None`` when the server has no value for it.
    next_cursor: str | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "ListDebugRunsResponse":
        """Decode a `ListDebugRunsResponse` from its JSON object."""
        known = {"runs", "nextCursor"}
        return cls(
            runs=[DebugRunSummary.from_dict(item) for item in data.get("runs") or []],
            next_cursor=data.get("nextCursor"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["runs"] = [_encode(item) for item in self.runs]
        out["nextCursor"] = self.next_cursor
        return out


@dataclass(frozen=True, slots=True)
class ListDebugSearchQueriesResponse:
    """`ListDebugSearchQueriesResponse`, as carried on the wire."""

    queries: list[DebugSearchQuery]
    #: Always present; ``None`` when the server has no value for it.
    next_cursor: str | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "ListDebugSearchQueriesResponse":
        """Decode a `ListDebugSearchQueriesResponse` from its JSON object."""
        known = {"queries", "nextCursor"}
        return cls(
            queries=[DebugSearchQuery.from_dict(item) for item in data.get("queries") or []],
            next_cursor=data.get("nextCursor"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["queries"] = [_encode(item) for item in self.queries]
        out["nextCursor"] = self.next_cursor
        return out


class ListDebugToolCallsOrder(StrEnum):
    """The `ListDebugToolCallsOrder` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    RECENT = "recent"
    TRAJECTORY = "trajectory"


class ListDebugToolCallsOutcome(StrEnum):
    """The `ListDebugToolCallsOutcome` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    OK = "ok"
    ERROR = "error"


@dataclass(frozen=True, slots=True)
class ListDebugToolCallsResponse:
    """`ListDebugToolCallsResponse`, as carried on the wire."""

    tool_calls: list[ListDebugToolCallsResponseToolCall]
    #: Always present; ``None`` when the server has no value for it.
    next_cursor: str | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "ListDebugToolCallsResponse":
        """Decode a `ListDebugToolCallsResponse` from its JSON object."""
        known = {"toolCalls", "nextCursor"}
        return cls(
            tool_calls=[ListDebugToolCallsResponseToolCall.from_dict(item) for item in data.get("toolCalls") or []],
            next_cursor=data.get("nextCursor"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["toolCalls"] = [_encode(item) for item in self.tool_calls]
        out["nextCursor"] = self.next_cursor
        return out


@dataclass(frozen=True, slots=True)
class ListDebugToolCallsResponseToolCall:
    """`ListDebugToolCallsResponseToolCall`, as carried on the wire."""

    agent_kind: str
    args: str
    args_dropped: float
    bodies: ListDebugToolCallsResponseToolCallBodies
    created_at: float
    ended_at: float
    execution_id: str
    id: str
    job_id: str
    ok: bool
    result: str
    result_dropped: float
    seq: float
    started_at: float
    tool: str
    workspace_id: str

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "ListDebugToolCallsResponseToolCall":
        """Decode a `ListDebugToolCallsResponseToolCall` from its JSON object."""
        known = {"agentKind", "args", "argsDropped", "bodies", "createdAt", "endedAt", "executionId", "id", "jobId", "ok", "result", "resultDropped", "seq", "startedAt", "tool", "workspaceId"}
        return cls(
            agent_kind=data.get("agentKind"),
            args=data.get("args"),
            args_dropped=data.get("argsDropped"),
            bodies=_enum(ListDebugToolCallsResponseToolCallBodies, data.get("bodies")),
            created_at=data.get("createdAt"),
            ended_at=data.get("endedAt"),
            execution_id=data.get("executionId"),
            id=data.get("id"),
            job_id=data.get("jobId"),
            ok=data.get("ok"),
            result=data.get("result"),
            result_dropped=data.get("resultDropped"),
            seq=data.get("seq"),
            started_at=data.get("startedAt"),
            tool=data.get("tool"),
            workspace_id=data.get("workspaceId"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["agentKind"] = self.agent_kind
        out["args"] = self.args
        out["argsDropped"] = self.args_dropped
        out["bodies"] = _encode(self.bodies)
        out["createdAt"] = self.created_at
        out["endedAt"] = self.ended_at
        out["executionId"] = self.execution_id
        out["id"] = self.id
        out["jobId"] = self.job_id
        out["ok"] = self.ok
        out["result"] = self.result
        out["resultDropped"] = self.result_dropped
        out["seq"] = self.seq
        out["startedAt"] = self.started_at
        out["tool"] = self.tool
        out["workspaceId"] = self.workspace_id
        return out


class ListDebugToolCallsResponseToolCallBodies(StrEnum):
    """The `ListDebugToolCallsResponseToolCallBodies` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    STORED = "stored"
    WITHHELD = "withheld"


@dataclass(frozen=True, slots=True)
class ListPublicJobsResponse:
    """`ListPublicJobsResponse`, as carried on the wire."""

    jobs: list[PublicJob]
    #: Always present; ``None`` when the server has no value for it.
    next_cursor: str | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "ListPublicJobsResponse":
        """Decode a `ListPublicJobsResponse` from its JSON object."""
        known = {"jobs", "nextCursor"}
        return cls(
            jobs=[PublicJob.from_dict(item) for item in data.get("jobs") or []],
            next_cursor=data.get("nextCursor"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["jobs"] = [_encode(item) for item in self.jobs]
        out["nextCursor"] = self.next_cursor
        return out


@dataclass(frozen=True, slots=True)
class ListPublicMergeClassRollupsResponse:
    """`ListPublicMergeClassRollupsResponse`, as carried on the wire."""

    rollups: list[ListPublicMergeClassRollupsResponseRollup]

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "ListPublicMergeClassRollupsResponse":
        """Decode a `ListPublicMergeClassRollupsResponse` from its JSON object."""
        known = {"rollups"}
        return cls(
            rollups=[ListPublicMergeClassRollupsResponseRollup.from_dict(item) for item in data.get("rollups") or []],
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["rollups"] = [_encode(item) for item in self.rollups]
        return out


@dataclass(frozen=True, slots=True)
class ListPublicMergeClassRollupsResponseRollup:
    """`ListPublicMergeClassRollupsResponseRollup`, as carried on the wire."""

    auto_merged: float
    change_class: NotificationPayloadChangeClass
    effort: ListPublicMergeClassRollupsResponseRollupEffort
    external_merged: float
    human_merged: float
    merged: float
    pending_review: float
    rejected: float
    total: float

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "ListPublicMergeClassRollupsResponseRollup":
        """Decode a `ListPublicMergeClassRollupsResponseRollup` from its JSON object."""
        known = {"autoMerged", "changeClass", "effort", "externalMerged", "humanMerged", "merged", "pendingReview", "rejected", "total"}
        return cls(
            auto_merged=data.get("autoMerged"),
            change_class=_enum(NotificationPayloadChangeClass, data.get("changeClass")),
            effort=ListPublicMergeClassRollupsResponseRollupEffort.from_dict(data.get("effort")),
            external_merged=data.get("externalMerged"),
            human_merged=data.get("humanMerged"),
            merged=data.get("merged"),
            pending_review=data.get("pendingReview"),
            rejected=data.get("rejected"),
            total=data.get("total"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["autoMerged"] = self.auto_merged
        out["changeClass"] = _encode(self.change_class)
        out["effort"] = _encode(self.effort)
        out["externalMerged"] = self.external_merged
        out["humanMerged"] = self.human_merged
        out["merged"] = self.merged
        out["pendingReview"] = self.pending_review
        out["rejected"] = self.rejected
        out["total"] = self.total
        return out


@dataclass(frozen=True, slots=True)
class ListPublicMergeClassRollupsResponseRollupEffort:
    """`ListPublicMergeClassRollupsResponseRollupEffort`, as carried on the wire."""

    major: float
    minor: float
    none: float
    untagged: float

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "ListPublicMergeClassRollupsResponseRollupEffort":
        """Decode a `ListPublicMergeClassRollupsResponseRollupEffort` from its JSON object."""
        known = {"major", "minor", "none", "untagged"}
        return cls(
            major=data.get("major"),
            minor=data.get("minor"),
            none=data.get("none"),
            untagged=data.get("untagged"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["major"] = self.major
        out["minor"] = self.minor
        out["none"] = self.none
        out["untagged"] = self.untagged
        return out


@dataclass(frozen=True, slots=True)
class ListPublicReposResponse:
    """`ListPublicReposResponse`, as carried on the wire."""

    repos: list[ListPublicReposResponseRepo]

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "ListPublicReposResponse":
        """Decode a `ListPublicReposResponse` from its JSON object."""
        known = {"repos"}
        return cls(
            repos=[ListPublicReposResponseRepo.from_dict(item) for item in data.get("repos") or []],
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["repos"] = [_encode(item) for item in self.repos]
        return out


@dataclass(frozen=True, slots=True)
class ListPublicReposResponseRepo:
    """`ListPublicReposResponseRepo`, as carried on the wire."""

    default_branch: str
    linked_elsewhere: bool
    monorepo: bool
    name: str
    owner: str
    private: bool
    provider: str
    repo_id: float
    #: Always present; ``None`` when the server has no value for it.
    service_id: str | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "ListPublicReposResponseRepo":
        """Decode a `ListPublicReposResponseRepo` from its JSON object."""
        known = {"defaultBranch", "linkedElsewhere", "monorepo", "name", "owner", "private", "provider", "repoId", "serviceId"}
        return cls(
            default_branch=data.get("defaultBranch"),
            linked_elsewhere=data.get("linkedElsewhere"),
            monorepo=data.get("monorepo"),
            name=data.get("name"),
            owner=data.get("owner"),
            private=data.get("private"),
            provider=data.get("provider"),
            repo_id=data.get("repoId"),
            service_id=data.get("serviceId"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["defaultBranch"] = self.default_branch
        out["linkedElsewhere"] = self.linked_elsewhere
        out["monorepo"] = self.monorepo
        out["name"] = self.name
        out["owner"] = self.owner
        out["private"] = self.private
        out["provider"] = self.provider
        out["repoId"] = self.repo_id
        out["serviceId"] = self.service_id
        return out


@dataclass(frozen=True, slots=True)
class ListPublicTaskDocumentsResponse:
    """`ListPublicTaskDocumentsResponse`, as carried on the wire."""

    documents: list[ListPublicTaskDocumentsResponseDocument]

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "ListPublicTaskDocumentsResponse":
        """Decode a `ListPublicTaskDocumentsResponse` from its JSON object."""
        known = {"documents"}
        return cls(
            documents=[ListPublicTaskDocumentsResponseDocument.from_dict(item) for item in data.get("documents") or []],
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["documents"] = [_encode(item) for item in self.documents]
        return out


@dataclass(frozen=True, slots=True)
class ListPublicTaskDocumentsResponseDocument:
    """`ListPublicTaskDocumentsResponseDocument`, as carried on the wire."""

    excerpt: str
    external_id: str
    source: PrReportContextDocumentOrigin
    title: str
    url: str

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "ListPublicTaskDocumentsResponseDocument":
        """Decode a `ListPublicTaskDocumentsResponseDocument` from its JSON object."""
        known = {"excerpt", "externalId", "source", "title", "url"}
        return cls(
            excerpt=data.get("excerpt"),
            external_id=data.get("externalId"),
            source=_enum(PrReportContextDocumentOrigin, data.get("source")),
            title=data.get("title"),
            url=data.get("url"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["excerpt"] = self.excerpt
        out["externalId"] = self.external_id
        out["source"] = _encode(self.source)
        out["title"] = self.title
        out["url"] = self.url
        return out


@dataclass(frozen=True, slots=True)
class ListPublicTaskTypesResponse:
    """`ListPublicTaskTypesResponse`, as carried on the wire."""

    task_types: list[ListPublicTaskTypesResponseTaskType]

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "ListPublicTaskTypesResponse":
        """Decode a `ListPublicTaskTypesResponse` from its JSON object."""
        known = {"taskTypes"}
        return cls(
            task_types=[ListPublicTaskTypesResponseTaskType.from_dict(item) for item in data.get("taskTypes") or []],
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["taskTypes"] = [_encode(item) for item in self.task_types]
        return out


@dataclass(frozen=True, slots=True)
class ListPublicTaskTypesResponseTaskType:
    """`ListPublicTaskTypesResponseTaskType`, as carried on the wire."""

    builtin: bool
    fields: list[ListPublicTaskTypesResponseTaskTypeField]
    label: str
    task_type: str
    #: May be absent entirely.
    category: str | None = None
    #: May be absent entirely.
    default_pipeline_id: str | None = None
    #: May be absent entirely.
    description: str | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "ListPublicTaskTypesResponseTaskType":
        """Decode a `ListPublicTaskTypesResponseTaskType` from its JSON object."""
        known = {"builtin", "fields", "label", "taskType", "category", "defaultPipelineId", "description"}
        return cls(
            builtin=data.get("builtin"),
            fields=[ListPublicTaskTypesResponseTaskTypeField.from_dict(item) for item in data.get("fields") or []],
            label=data.get("label"),
            task_type=data.get("taskType"),
            category=data.get("category"),
            default_pipeline_id=data.get("defaultPipelineId"),
            description=data.get("description"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["builtin"] = self.builtin
        out["fields"] = [_encode(item) for item in self.fields]
        out["label"] = self.label
        out["taskType"] = self.task_type
        if self.category is not None:
            out["category"] = self.category
        if self.default_pipeline_id is not None:
            out["defaultPipelineId"] = self.default_pipeline_id
        if self.description is not None:
            out["description"] = self.description
        return out


@dataclass(frozen=True, slots=True)
class ListPublicTaskTypesResponseTaskTypeField:
    """`ListPublicTaskTypesResponseTaskTypeField`, as carried on the wire."""

    key: str
    label: str
    #: May be absent entirely.
    default: str | None = None
    #: May be absent entirely.
    default_values: list[str] | None = None
    #: May be absent entirely.
    help: str | None = None
    #: May be absent entirely.
    max: float | None = None
    #: May be absent entirely.
    max_length: int | None = None
    #: May be absent entirely.
    min: float | None = None
    #: May be absent entirely.
    options: list[ListPublicTaskTypesResponseTaskTypeFieldOption] | None = None
    #: May be absent entirely.
    placeholder: str | None = None
    #: May be absent entirely.
    required: bool | None = None
    #: May be absent entirely.
    section: str | None = None
    #: May be absent entirely.
    show_when: ListPublicTaskTypesResponseTaskTypeFieldShowWhen | None = None
    #: May be absent entirely.
    type_: ListPublicTaskTypesResponseTaskTypeFieldType | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "ListPublicTaskTypesResponseTaskTypeField":
        """Decode a `ListPublicTaskTypesResponseTaskTypeField` from its JSON object."""
        known = {"key", "label", "default", "defaultValues", "help", "max", "maxLength", "min", "options", "placeholder", "required", "section", "showWhen", "type"}
        return cls(
            key=data.get("key"),
            label=data.get("label"),
            default=data.get("default"),
            default_values=None if data.get("defaultValues") is None else [item for item in data.get("defaultValues") or []],
            help=data.get("help"),
            max=data.get("max"),
            max_length=data.get("maxLength"),
            min=data.get("min"),
            options=None if data.get("options") is None else [ListPublicTaskTypesResponseTaskTypeFieldOption.from_dict(item) for item in data.get("options") or []],
            placeholder=data.get("placeholder"),
            required=data.get("required"),
            section=data.get("section"),
            show_when=None if data.get("showWhen") is None else ListPublicTaskTypesResponseTaskTypeFieldShowWhen.from_dict(data.get("showWhen")),
            type_=None if data.get("type") is None else _enum(ListPublicTaskTypesResponseTaskTypeFieldType, data.get("type")),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["key"] = self.key
        out["label"] = self.label
        if self.default is not None:
            out["default"] = self.default
        if self.default_values is not None:
            out["defaultValues"] = [_encode(item) for item in self.default_values]
        if self.help is not None:
            out["help"] = self.help
        if self.max is not None:
            out["max"] = self.max
        if self.max_length is not None:
            out["maxLength"] = self.max_length
        if self.min is not None:
            out["min"] = self.min
        if self.options is not None:
            out["options"] = [_encode(item) for item in self.options]
        if self.placeholder is not None:
            out["placeholder"] = self.placeholder
        if self.required is not None:
            out["required"] = self.required
        if self.section is not None:
            out["section"] = self.section
        if self.show_when is not None:
            out["showWhen"] = _encode(self.show_when)
        if self.type_ is not None:
            out["type"] = _encode(self.type_)
        return out


@dataclass(frozen=True, slots=True)
class ListPublicTaskTypesResponseTaskTypeFieldOption:
    """`ListPublicTaskTypesResponseTaskTypeFieldOption`, as carried on the wire."""

    label: str
    value: str

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "ListPublicTaskTypesResponseTaskTypeFieldOption":
        """Decode a `ListPublicTaskTypesResponseTaskTypeFieldOption` from its JSON object."""
        known = {"label", "value"}
        return cls(
            label=data.get("label"),
            value=data.get("value"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["label"] = self.label
        out["value"] = self.value
        return out


@dataclass(frozen=True, slots=True)
class ListPublicTaskTypesResponseTaskTypeFieldShowWhen:
    """`ListPublicTaskTypesResponseTaskTypeFieldShowWhen`, as carried on the wire."""

    key: str
    #: May be absent entirely.
    equals: ListPublicTaskTypesResponseTaskTypeFieldShowWhenEquals | None = None
    #: May be absent entirely.
    includes: str | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "ListPublicTaskTypesResponseTaskTypeFieldShowWhen":
        """Decode a `ListPublicTaskTypesResponseTaskTypeFieldShowWhen` from its JSON object."""
        known = {"key", "equals", "includes"}
        return cls(
            key=data.get("key"),
            equals=None if data.get("equals") is None else ListPublicTaskTypesResponseTaskTypeFieldShowWhenEquals.from_dict(data.get("equals")),
            includes=data.get("includes"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["key"] = self.key
        if self.equals is not None:
            out["equals"] = _encode(self.equals)
        if self.includes is not None:
            out["includes"] = self.includes
        return out


class ListPublicTaskTypesResponseTaskTypeFieldType(StrEnum):
    """The `ListPublicTaskTypesResponseTaskTypeFieldType` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    TEXT = "text"
    PASSWORD = "password"
    SELECT = "select"
    NUMBER = "number"
    CHECKBOX = "checkbox"
    TEXTAREA = "textarea"
    CHECKBOX_GROUP = "checkbox-group"
    PATH = "path"


class LlmCallOutcome(StrEnum):
    """The `LlmCallOutcome` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    OK = "ok"
    WARNING = "warning"
    ERROR = "error"


@dataclass(frozen=True, slots=True)
class Notification:
    """`Notification`, as carried on the wire."""

    body: str
    created_at: float
    id: str
    status: NotificationStatus
    title: str
    type_: NotificationType
    #: Always present; ``None`` when the server has no value for it.
    block_id: str | None = None
    #: Always present; ``None`` when the server has no value for it.
    execution_id: str | None = None
    #: May be absent entirely.
    payload: NotificationPayload | None = None
    #: Always present; ``None`` when the server has no value for it.
    resolved_at: float | None = None
    #: May be absent entirely.
    severity: NotificationSeverity | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "Notification":
        """Decode a `Notification` from its JSON object."""
        known = {"body", "createdAt", "id", "status", "title", "type", "blockId", "executionId", "payload", "resolvedAt", "severity"}
        return cls(
            body=data.get("body"),
            created_at=data.get("createdAt"),
            id=data.get("id"),
            status=_enum(NotificationStatus, data.get("status")),
            title=data.get("title"),
            type_=_enum(NotificationType, data.get("type")),
            block_id=data.get("blockId"),
            execution_id=data.get("executionId"),
            payload=None if data.get("payload") is None else NotificationPayload.from_dict(data.get("payload")),
            resolved_at=data.get("resolvedAt"),
            severity=None if data.get("severity") is None else _enum(NotificationSeverity, data.get("severity")),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["body"] = self.body
        out["createdAt"] = self.created_at
        out["id"] = self.id
        out["status"] = _encode(self.status)
        out["title"] = self.title
        out["type"] = _encode(self.type_)
        out["blockId"] = self.block_id
        out["executionId"] = self.execution_id
        if self.payload is not None:
            out["payload"] = _encode(self.payload)
        out["resolvedAt"] = self.resolved_at
        if self.severity is not None:
            out["severity"] = _encode(self.severity)
        return out


@dataclass(frozen=True, slots=True)
class NotificationPayload:
    """`NotificationPayload`, as carried on the wire."""

    #: May be absent entirely.
    assessment: NotificationPayloadAssessment | None = None
    #: May be absent entirely.
    budget_alerts: list[NotificationPayloadBudgetAlert] | None = None
    #: May be absent entirely.
    budget_period_start: float | None = None
    #: May be absent entirely.
    change_class: NotificationPayloadChangeClass | None = None
    #: May be absent entirely.
    drift_affected: list[NotificationPayloadDriftAffected] | None = None
    #: May be absent entirely.
    finding_count: float | None = None
    #: May be absent entirely.
    fork_count: float | None = None
    #: May be absent entirely.
    initiative_reason: NotificationPayloadInitiativeReason | None = None
    #: May be absent entirely.
    merge_track_record_id: str | None = None
    #: May be absent entirely.
    merged_repos: list[str] | None = None
    #: May be absent entirely.
    on_call_assessment: NotificationPayloadOnCallAssessment | None = None
    #: May be absent entirely.
    pipeline_name: str | None = None
    #: May be absent entirely.
    platform_alert_failure_kinds: list[str] | None = None
    #: May be absent entirely.
    platform_alert_transition: float | None = None
    #: May be absent entirely.
    platform_alerts: list[NotificationPayloadPlatformAlert] | None = None
    #: May be absent entirely.
    platform_failed_total: float | None = None
    #: May be absent entirely.
    platform_failing_runs: list[NotificationPayloadPlatformFailingRun] | None = None
    #: May be absent entirely.
    platform_window: NotificationPayloadPlatformWindow | None = None
    #: May be absent entirely.
    pr_url: str | None = None
    #: May be absent entirely.
    release_signals: list[NotificationPayloadReleaseSignal] | None = None
    #: May be absent entirely.
    revert_url: str | None = None
    #: May be absent entirely.
    slice_count: float | None = None
    #: May be absent entirely.
    target_user_id: str | None = None
    #: May be absent entirely.
    unmerged_repos: list[str] | None = None
    #: May be absent entirely.
    unreachable_areas: list[NotificationPayloadUnreachableArea] | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "NotificationPayload":
        """Decode a `NotificationPayload` from its JSON object."""
        known = {"assessment", "budgetAlerts", "budgetPeriodStart", "changeClass", "driftAffected", "findingCount", "forkCount", "initiativeReason", "mergeTrackRecordId", "mergedRepos", "onCallAssessment", "pipelineName", "platformAlertFailureKinds", "platformAlertTransition", "platformAlerts", "platformFailedTotal", "platformFailingRuns", "platformWindow", "prUrl", "releaseSignals", "revertUrl", "sliceCount", "targetUserId", "unmergedRepos", "unreachableAreas"}
        return cls(
            assessment=None if data.get("assessment") is None else NotificationPayloadAssessment.from_dict(data.get("assessment")),
            budget_alerts=None if data.get("budgetAlerts") is None else [NotificationPayloadBudgetAlert.from_dict(item) for item in data.get("budgetAlerts") or []],
            budget_period_start=data.get("budgetPeriodStart"),
            change_class=None if data.get("changeClass") is None else _enum(NotificationPayloadChangeClass, data.get("changeClass")),
            drift_affected=None if data.get("driftAffected") is None else [NotificationPayloadDriftAffected.from_dict(item) for item in data.get("driftAffected") or []],
            finding_count=data.get("findingCount"),
            fork_count=data.get("forkCount"),
            initiative_reason=None if data.get("initiativeReason") is None else _enum(NotificationPayloadInitiativeReason, data.get("initiativeReason")),
            merge_track_record_id=data.get("mergeTrackRecordId"),
            merged_repos=None if data.get("mergedRepos") is None else [item for item in data.get("mergedRepos") or []],
            on_call_assessment=None if data.get("onCallAssessment") is None else NotificationPayloadOnCallAssessment.from_dict(data.get("onCallAssessment")),
            pipeline_name=data.get("pipelineName"),
            platform_alert_failure_kinds=None if data.get("platformAlertFailureKinds") is None else [item for item in data.get("platformAlertFailureKinds") or []],
            platform_alert_transition=data.get("platformAlertTransition"),
            platform_alerts=None if data.get("platformAlerts") is None else [_enum(NotificationPayloadPlatformAlert, item) for item in data.get("platformAlerts") or []],
            platform_failed_total=data.get("platformFailedTotal"),
            platform_failing_runs=None if data.get("platformFailingRuns") is None else [NotificationPayloadPlatformFailingRun.from_dict(item) for item in data.get("platformFailingRuns") or []],
            platform_window=None if data.get("platformWindow") is None else _enum(NotificationPayloadPlatformWindow, data.get("platformWindow")),
            pr_url=data.get("prUrl"),
            release_signals=None if data.get("releaseSignals") is None else [NotificationPayloadReleaseSignal.from_dict(item) for item in data.get("releaseSignals") or []],
            revert_url=data.get("revertUrl"),
            slice_count=data.get("sliceCount"),
            target_user_id=data.get("targetUserId"),
            unmerged_repos=None if data.get("unmergedRepos") is None else [item for item in data.get("unmergedRepos") or []],
            unreachable_areas=None if data.get("unreachableAreas") is None else [_enum(NotificationPayloadUnreachableArea, item) for item in data.get("unreachableAreas") or []],
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        if self.assessment is not None:
            out["assessment"] = _encode(self.assessment)
        if self.budget_alerts is not None:
            out["budgetAlerts"] = [_encode(item) for item in self.budget_alerts]
        if self.budget_period_start is not None:
            out["budgetPeriodStart"] = self.budget_period_start
        if self.change_class is not None:
            out["changeClass"] = _encode(self.change_class)
        if self.drift_affected is not None:
            out["driftAffected"] = [_encode(item) for item in self.drift_affected]
        if self.finding_count is not None:
            out["findingCount"] = self.finding_count
        if self.fork_count is not None:
            out["forkCount"] = self.fork_count
        if self.initiative_reason is not None:
            out["initiativeReason"] = _encode(self.initiative_reason)
        if self.merge_track_record_id is not None:
            out["mergeTrackRecordId"] = self.merge_track_record_id
        if self.merged_repos is not None:
            out["mergedRepos"] = [_encode(item) for item in self.merged_repos]
        if self.on_call_assessment is not None:
            out["onCallAssessment"] = _encode(self.on_call_assessment)
        if self.pipeline_name is not None:
            out["pipelineName"] = self.pipeline_name
        if self.platform_alert_failure_kinds is not None:
            out["platformAlertFailureKinds"] = [_encode(item) for item in self.platform_alert_failure_kinds]
        if self.platform_alert_transition is not None:
            out["platformAlertTransition"] = self.platform_alert_transition
        if self.platform_alerts is not None:
            out["platformAlerts"] = [_encode(item) for item in self.platform_alerts]
        if self.platform_failed_total is not None:
            out["platformFailedTotal"] = self.platform_failed_total
        if self.platform_failing_runs is not None:
            out["platformFailingRuns"] = [_encode(item) for item in self.platform_failing_runs]
        if self.platform_window is not None:
            out["platformWindow"] = _encode(self.platform_window)
        if self.pr_url is not None:
            out["prUrl"] = self.pr_url
        if self.release_signals is not None:
            out["releaseSignals"] = [_encode(item) for item in self.release_signals]
        if self.revert_url is not None:
            out["revertUrl"] = self.revert_url
        if self.slice_count is not None:
            out["sliceCount"] = self.slice_count
        if self.target_user_id is not None:
            out["targetUserId"] = self.target_user_id
        if self.unmerged_repos is not None:
            out["unmergedRepos"] = [_encode(item) for item in self.unmerged_repos]
        if self.unreachable_areas is not None:
            out["unreachableAreas"] = [_encode(item) for item in self.unreachable_areas]
        return out


@dataclass(frozen=True, slots=True)
class NotificationPayloadAssessment:
    """`NotificationPayloadAssessment`, as carried on the wire."""

    complexity: float
    impact: float
    rationale: str
    risk: float

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "NotificationPayloadAssessment":
        """Decode a `NotificationPayloadAssessment` from its JSON object."""
        known = {"complexity", "impact", "rationale", "risk"}
        return cls(
            complexity=data.get("complexity"),
            impact=data.get("impact"),
            rationale=data.get("rationale"),
            risk=data.get("risk"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["complexity"] = self.complexity
        out["impact"] = self.impact
        out["rationale"] = self.rationale
        out["risk"] = self.risk
        return out


@dataclass(frozen=True, slots=True)
class NotificationPayloadBudgetAlert:
    """`NotificationPayloadBudgetAlert`, as carried on the wire."""

    projected_overrun: bool
    tier: NotificationPayloadBudgetAlertTier
    #: Always present; ``None`` when the server has no value for it.
    threshold: float | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "NotificationPayloadBudgetAlert":
        """Decode a `NotificationPayloadBudgetAlert` from its JSON object."""
        known = {"projectedOverrun", "tier", "threshold"}
        return cls(
            projected_overrun=data.get("projectedOverrun"),
            tier=_enum(NotificationPayloadBudgetAlertTier, data.get("tier")),
            threshold=data.get("threshold"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["projectedOverrun"] = self.projected_overrun
        out["tier"] = _encode(self.tier)
        out["threshold"] = self.threshold
        return out


class NotificationPayloadBudgetAlertTier(StrEnum):
    """The `NotificationPayloadBudgetAlertTier` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    WORKSPACE = "workspace"
    ACCOUNT = "account"


class NotificationPayloadChangeClass(StrEnum):
    """The `NotificationPayloadChangeClass` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    DOCS = "docs"
    TEST = "test"
    DEPENDENCY = "dependency"
    CONFIG = "config"
    SOURCE = "source"
    SCHEMA = "schema"
    UNKNOWN = "unknown"


@dataclass(frozen=True, slots=True)
class NotificationPayloadDriftAffected:
    """`NotificationPayloadDriftAffected`, as carried on the wire."""

    id: str
    label: str
    reason: NotificationPayloadDriftAffectedReason
    source: str
    #: Always present; ``None`` when the server has no value for it.
    sealed_at: float | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "NotificationPayloadDriftAffected":
        """Decode a `NotificationPayloadDriftAffected` from its JSON object."""
        known = {"id", "label", "reason", "source", "sealedAt"}
        return cls(
            id=data.get("id"),
            label=data.get("label"),
            reason=_enum(NotificationPayloadDriftAffectedReason, data.get("reason")),
            source=data.get("source"),
            sealed_at=data.get("sealedAt"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["id"] = self.id
        out["label"] = self.label
        out["reason"] = _encode(self.reason)
        out["source"] = self.source
        out["sealedAt"] = self.sealed_at
        return out


class NotificationPayloadDriftAffectedReason(StrEnum):
    """The `NotificationPayloadDriftAffectedReason` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    KEY_MISMATCH = "key-mismatch"
    CORRUPT = "corrupt"


class NotificationPayloadInitiativeReason(StrEnum):
    """The `NotificationPayloadInitiativeReason` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    ITEM_BLOCKED = "item_blocked"
    COMPLETE = "complete"
    CHECKPOINT = "checkpoint"


@dataclass(frozen=True, slots=True)
class NotificationPayloadOnCallAssessment:
    """`NotificationPayloadOnCallAssessment`, as carried on the wire."""

    culprit_confidence: float
    evidence: list[str]
    rationale: str
    recommendation: NotificationPayloadOnCallAssessmentRecommendation

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "NotificationPayloadOnCallAssessment":
        """Decode a `NotificationPayloadOnCallAssessment` from its JSON object."""
        known = {"culpritConfidence", "evidence", "rationale", "recommendation"}
        return cls(
            culprit_confidence=data.get("culpritConfidence"),
            evidence=[item for item in data.get("evidence") or []],
            rationale=data.get("rationale"),
            recommendation=_enum(NotificationPayloadOnCallAssessmentRecommendation, data.get("recommendation")),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["culpritConfidence"] = self.culprit_confidence
        out["evidence"] = [_encode(item) for item in self.evidence]
        out["rationale"] = self.rationale
        out["recommendation"] = _encode(self.recommendation)
        return out


class NotificationPayloadOnCallAssessmentRecommendation(StrEnum):
    """The `NotificationPayloadOnCallAssessmentRecommendation` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    REVERT = "revert"
    HOLD = "hold"
    MONITOR = "monitor"


class NotificationPayloadPlatformAlert(StrEnum):
    """The `NotificationPayloadPlatformAlert` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    FAILURE_RATE_HIGH = "failure_rate_high"
    DURATION_P99_HIGH = "duration_p99_high"
    BACKLOG_HIGH = "backlog_high"
    THROUGHPUT_STALLED = "throughput_stalled"
    FAILURE_KIND_DOMINANT = "failure_kind_dominant"
    FAILURE_KIND_RATE_HIGH = "failure_kind_rate_high"
    SWEEP_DEGRADED = "sweep_degraded"


@dataclass(frozen=True, slots=True)
class NotificationPayloadPlatformFailingRun:
    """`NotificationPayloadPlatformFailingRun`, as carried on the wire."""

    created_at: float
    execution_id: str
    failure_kind: str
    #: Always present; ``None`` when the server has no value for it.
    block_id: str | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "NotificationPayloadPlatformFailingRun":
        """Decode a `NotificationPayloadPlatformFailingRun` from its JSON object."""
        known = {"createdAt", "executionId", "failureKind", "blockId"}
        return cls(
            created_at=data.get("createdAt"),
            execution_id=data.get("executionId"),
            failure_kind=data.get("failureKind"),
            block_id=data.get("blockId"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["createdAt"] = self.created_at
        out["executionId"] = self.execution_id
        out["failureKind"] = self.failure_kind
        out["blockId"] = self.block_id
        return out


class NotificationPayloadPlatformWindow(StrEnum):
    """The `NotificationPayloadPlatformWindow` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    VALUE_1H = "1h"
    VALUE_24H = "24h"
    VALUE_7D = "7d"


@dataclass(frozen=True, slots=True)
class NotificationPayloadReleaseSignal:
    """`NotificationPayloadReleaseSignal`, as carried on the wire."""

    id: str
    kind: NotificationPayloadReleaseSignalKind
    name: str
    state: NotificationPayloadReleaseSignalState
    #: May be absent entirely.
    detail: str | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "NotificationPayloadReleaseSignal":
        """Decode a `NotificationPayloadReleaseSignal` from its JSON object."""
        known = {"id", "kind", "name", "state", "detail"}
        return cls(
            id=data.get("id"),
            kind=_enum(NotificationPayloadReleaseSignalKind, data.get("kind")),
            name=data.get("name"),
            state=_enum(NotificationPayloadReleaseSignalState, data.get("state")),
            detail=data.get("detail"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["id"] = self.id
        out["kind"] = _encode(self.kind)
        out["name"] = self.name
        out["state"] = _encode(self.state)
        if self.detail is not None:
            out["detail"] = self.detail
        return out


class NotificationPayloadReleaseSignalKind(StrEnum):
    """The `NotificationPayloadReleaseSignalKind` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    MONITOR = "monitor"
    SLO = "slo"


class NotificationPayloadReleaseSignalState(StrEnum):
    """The `NotificationPayloadReleaseSignalState` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    OK = "ok"
    WARN = "warn"
    ALERT = "alert"
    NO_DATA = "no_data"


class NotificationPayloadUnreachableArea(StrEnum):
    """The `NotificationPayloadUnreachableArea` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    EPHEMERAL_ENVIRONMENTS = "ephemeralEnvironments"
    AGENT_EXECUTOR = "agentExecutor"
    BINARY_STORAGE = "binaryStorage"


class NotificationSeverity(StrEnum):
    """The `NotificationSeverity` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    NORMAL = "normal"
    URGENT = "urgent"


class NotificationStatus(StrEnum):
    """The `NotificationStatus` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    OPEN = "open"
    ACTED = "acted"
    DISMISSED = "dismissed"


class NotificationType(StrEnum):
    """The `NotificationType` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    MERGE_REVIEW = "merge_review"
    PIPELINE_COMPLETE = "pipeline_complete"
    CI_FAILED = "ci_failed"
    TEST_FAILED = "test_failed"
    REQUIREMENT_REVIEW = "requirement_review"
    CLARITY_REVIEW = "clarity_review"
    RELEASE_REGRESSION = "release_regression"
    DECISION_REQUIRED = "decision_required"
    HUMAN_TEST_READY = "human_test_ready"
    VISUAL_CONFIRMATION_READY = "visual_confirmation_ready"
    HUMAN_REVIEW = "human_review"
    FOLLOWUP_PENDING = "followup_pending"
    FORK_DECISION_PENDING = "fork_decision_pending"
    JUDGE_REVIEW = "judge_review"
    PR_REVIEW_READY = "pr_review_ready"
    INITIATIVE = "initiative"
    PLATFORM_HEALTH = "platform_health"
    INFRA_UNREACHABLE = "infra_unreachable"
    BUDGET_PAUSED = "budget_paused"
    BUDGET_THRESHOLD = "budget_threshold"
    KEY_DRIFT = "key_drift"
    MERGE_TAG_REQUEST = "merge_tag_request"


@dataclass(frozen=True, slots=True)
class NotificationWebhook:
    """`NotificationWebhook`, as carried on the wire."""

    alert_events: list[NotificationWebhookAlertEvent]
    enabled: bool
    has_secret: bool
    id: str
    name: str
    run_events: list[NotificationWebhookRunEvent]
    types: list[NotificationType]
    updated_at: float
    url: str

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "NotificationWebhook":
        """Decode a `NotificationWebhook` from its JSON object."""
        known = {"alertEvents", "enabled", "hasSecret", "id", "name", "runEvents", "types", "updatedAt", "url"}
        return cls(
            alert_events=[_enum(NotificationWebhookAlertEvent, item) for item in data.get("alertEvents") or []],
            enabled=data.get("enabled"),
            has_secret=data.get("hasSecret"),
            id=data.get("id"),
            name=data.get("name"),
            run_events=[_enum(NotificationWebhookRunEvent, item) for item in data.get("runEvents") or []],
            types=[_enum(NotificationType, item) for item in data.get("types") or []],
            updated_at=data.get("updatedAt"),
            url=data.get("url"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["alertEvents"] = [_encode(item) for item in self.alert_events]
        out["enabled"] = self.enabled
        out["hasSecret"] = self.has_secret
        out["id"] = self.id
        out["name"] = self.name
        out["runEvents"] = [_encode(item) for item in self.run_events]
        out["types"] = [_encode(item) for item in self.types]
        out["updatedAt"] = self.updated_at
        out["url"] = self.url
        return out


class NotificationWebhookAlertEvent(StrEnum):
    """The `NotificationWebhookAlertEvent` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    PLATFORM_HEALTH_FIRING = "platform_health.firing"
    PLATFORM_HEALTH_RESOLVED = "platform_health.resolved"


class NotificationWebhookRunEvent(StrEnum):
    """The `NotificationWebhookRunEvent` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    RUN_STARTED = "run.started"
    RUN_COMPLETED = "run.completed"
    RUN_FAILED = "run.failed"


@dataclass(frozen=True, slots=True)
class PrReportCheck:
    """`PrReportCheck`, as carried on the wire."""

    name: str
    #: Always present; ``None`` when the server has no value for it.
    conclusion: str | None = None
    #: May be absent entirely.
    repo: str | None = None
    #: May be absent entirely.
    url: str | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "PrReportCheck":
        """Decode a `PrReportCheck` from its JSON object."""
        known = {"name", "conclusion", "repo", "url"}
        return cls(
            name=data.get("name"),
            conclusion=data.get("conclusion"),
            repo=data.get("repo"),
            url=data.get("url"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["name"] = self.name
        out["conclusion"] = self.conclusion
        if self.repo is not None:
            out["repo"] = self.repo
        if self.url is not None:
            out["url"] = self.url
        return out


@dataclass(frozen=True, slots=True)
class PrReportCi:
    """`PrReportCi`, as carried on the wire."""

    failing_checks: list[PrReportCheck]
    fixer_attempts: float
    status: PrReportCiStatus
    #: May be absent entirely.
    head_sha: str | None = None
    #: May be absent entirely.
    max_fixer_attempts: float | None = None
    #: May be absent entirely.
    note: str | None = None
    #: May be absent entirely.
    verdict: PrReportCiVerdict | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "PrReportCi":
        """Decode a `PrReportCi` from its JSON object."""
        known = {"failingChecks", "fixerAttempts", "status", "headSha", "maxFixerAttempts", "note", "verdict"}
        return cls(
            failing_checks=[PrReportCheck.from_dict(item) for item in data.get("failingChecks") or []],
            fixer_attempts=data.get("fixerAttempts"),
            status=_enum(PrReportCiStatus, data.get("status")),
            head_sha=data.get("headSha"),
            max_fixer_attempts=data.get("maxFixerAttempts"),
            note=data.get("note"),
            verdict=None if data.get("verdict") is None else _enum(PrReportCiVerdict, data.get("verdict")),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["failingChecks"] = [_encode(item) for item in self.failing_checks]
        out["fixerAttempts"] = self.fixer_attempts
        out["status"] = _encode(self.status)
        if self.head_sha is not None:
            out["headSha"] = self.head_sha
        if self.max_fixer_attempts is not None:
            out["maxFixerAttempts"] = self.max_fixer_attempts
        if self.note is not None:
            out["note"] = self.note
        if self.verdict is not None:
            out["verdict"] = _encode(self.verdict)
        return out


class PrReportCiStatus(StrEnum):
    """The `PrReportCiStatus` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    REPORTED = "reported"
    ABSENT = "absent"


class PrReportCiVerdict(StrEnum):
    """The `PrReportCiVerdict` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    PASS = "pass"
    PENDING = "pending"
    FAIL = "fail"


@dataclass(frozen=True, slots=True)
class PrReportContext:
    """`PrReportContext`, as carried on the wire."""

    documents: list[PrReportContextDocument]
    status: PrReportCiStatus
    #: May be absent entirely.
    note: str | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "PrReportContext":
        """Decode a `PrReportContext` from its JSON object."""
        known = {"documents", "status", "note"}
        return cls(
            documents=[PrReportContextDocument.from_dict(item) for item in data.get("documents") or []],
            status=_enum(PrReportCiStatus, data.get("status")),
            note=data.get("note"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["documents"] = [_encode(item) for item in self.documents]
        out["status"] = _encode(self.status)
        if self.note is not None:
            out["note"] = self.note
        return out


@dataclass(frozen=True, slots=True)
class PrReportContextDocument:
    """`PrReportContextDocument`, as carried on the wire."""

    moved_during_run: bool
    origin: PrReportContextDocumentOrigin
    title: str
    #: May be absent entirely.
    freshness: DocumentFreshness | None = None
    #: Always present; ``None`` when the server has no value for it.
    url: str | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "PrReportContextDocument":
        """Decode a `PrReportContextDocument` from its JSON object."""
        known = {"movedDuringRun", "origin", "title", "freshness", "url"}
        return cls(
            moved_during_run=data.get("movedDuringRun"),
            origin=_enum(PrReportContextDocumentOrigin, data.get("origin")),
            title=data.get("title"),
            freshness=None if data.get("freshness") is None else DocumentFreshness.from_dict(data.get("freshness")),
            url=data.get("url"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["movedDuringRun"] = self.moved_during_run
        out["origin"] = _encode(self.origin)
        out["title"] = self.title
        if self.freshness is not None:
            out["freshness"] = _encode(self.freshness)
        out["url"] = self.url
        return out


class PrReportContextDocumentOrigin(StrEnum):
    """The `PrReportContextDocumentOrigin` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    CONFLUENCE = "confluence"
    NOTION = "notion"
    GITHUB = "github"
    FIGMA = "figma"
    ZEPLIN = "zeplin"
    LINEAR = "linear"
    UPLOAD = "upload"


@dataclass(frozen=True, slots=True)
class PrReportEnvironments:
    """`PrReportEnvironments`, as carried on the wire."""

    entries: list[PrReportEnvironmentsEntry]
    evidence: PrReportEnvironmentsEvidence
    gaps: list[str]
    proof: PrReportEnvironmentsProof
    status: PrReportCiStatus
    teardown: PrReportEnvironmentsTeardown
    timeline: PrReportEnvironmentsTimeline
    #: May be absent entirely.
    note: str | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "PrReportEnvironments":
        """Decode a `PrReportEnvironments` from its JSON object."""
        known = {"entries", "evidence", "gaps", "proof", "status", "teardown", "timeline", "note"}
        return cls(
            entries=[PrReportEnvironmentsEntry.from_dict(item) for item in data.get("entries") or []],
            evidence=PrReportEnvironmentsEvidence.from_dict(data.get("evidence")),
            gaps=[item for item in data.get("gaps") or []],
            proof=_enum(PrReportEnvironmentsProof, data.get("proof")),
            status=_enum(PrReportCiStatus, data.get("status")),
            teardown=_enum(PrReportEnvironmentsTeardown, data.get("teardown")),
            timeline=PrReportEnvironmentsTimeline.from_dict(data.get("timeline")),
            note=data.get("note"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["entries"] = [_encode(item) for item in self.entries]
        out["evidence"] = _encode(self.evidence)
        out["gaps"] = [_encode(item) for item in self.gaps]
        out["proof"] = _encode(self.proof)
        out["status"] = _encode(self.status)
        out["teardown"] = _encode(self.teardown)
        out["timeline"] = _encode(self.timeline)
        if self.note is not None:
            out["note"] = self.note
        return out


@dataclass(frozen=True, slots=True)
class PrReportEnvironmentsEntry:
    """`PrReportEnvironmentsEntry`, as carried on the wire."""

    frame_id: str
    status: PrReportEnvironmentsEntryStatus
    #: May be absent entirely.
    error: str | None = None
    #: May be absent entirely.
    url: str | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "PrReportEnvironmentsEntry":
        """Decode a `PrReportEnvironmentsEntry` from its JSON object."""
        known = {"frameId", "status", "error", "url"}
        return cls(
            frame_id=data.get("frameId"),
            status=_enum(PrReportEnvironmentsEntryStatus, data.get("status")),
            error=data.get("error"),
            url=data.get("url"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["frameId"] = self.frame_id
        out["status"] = _encode(self.status)
        if self.error is not None:
            out["error"] = self.error
        if self.url is not None:
            out["url"] = self.url
        return out


class PrReportEnvironmentsEntryStatus(StrEnum):
    """The `PrReportEnvironmentsEntryStatus` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    READY = "ready"
    FAILED = "failed"
    SKIPPED = "skipped"


@dataclass(frozen=True, slots=True)
class PrReportEnvironmentsEvidence:
    """`PrReportEnvironmentsEvidence`, as carried on the wire."""

    outcomes: float
    requirement_verdicts: float
    screenshots: list[PrReportEnvironmentsEvidenceScreenshot]
    status: PrReportEnvironmentsEvidenceStatus
    #: Always present; ``None`` when the server has no value for it.
    captured_at: float | None = None
    #: May be absent entirely.
    note: str | None = None
    #: Always present; ``None`` when the server has no value for it.
    ran_against: PrReportEnvironmentsEvidenceRanAgainst | None = None
    #: Always present; ``None`` when the server has no value for it.
    url: str | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "PrReportEnvironmentsEvidence":
        """Decode a `PrReportEnvironmentsEvidence` from its JSON object."""
        known = {"outcomes", "requirementVerdicts", "screenshots", "status", "capturedAt", "note", "ranAgainst", "url"}
        return cls(
            outcomes=data.get("outcomes"),
            requirement_verdicts=data.get("requirementVerdicts"),
            screenshots=[PrReportEnvironmentsEvidenceScreenshot.from_dict(item) for item in data.get("screenshots") or []],
            status=_enum(PrReportEnvironmentsEvidenceStatus, data.get("status")),
            captured_at=data.get("capturedAt"),
            note=data.get("note"),
            ran_against=None if data.get("ranAgainst") is None else _enum(PrReportEnvironmentsEvidenceRanAgainst, data.get("ranAgainst")),
            url=data.get("url"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["outcomes"] = self.outcomes
        out["requirementVerdicts"] = self.requirement_verdicts
        out["screenshots"] = [_encode(item) for item in self.screenshots]
        out["status"] = _encode(self.status)
        out["capturedAt"] = self.captured_at
        if self.note is not None:
            out["note"] = self.note
        out["ranAgainst"] = _encode(self.ran_against)
        out["url"] = self.url
        return out


class PrReportEnvironmentsEvidenceRanAgainst(StrEnum):
    """The `PrReportEnvironmentsEvidenceRanAgainst` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    LOCAL = "local"
    EPHEMERAL = "ephemeral"


@dataclass(frozen=True, slots=True)
class PrReportEnvironmentsEvidenceScreenshot:
    """`PrReportEnvironmentsEvidenceScreenshot`, as carried on the wire."""

    artifact_id: str
    has_reference: bool
    view: str
    #: Always present; ``None`` when the server has no value for it.
    url: str | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "PrReportEnvironmentsEvidenceScreenshot":
        """Decode a `PrReportEnvironmentsEvidenceScreenshot` from its JSON object."""
        known = {"artifactId", "hasReference", "view", "url"}
        return cls(
            artifact_id=data.get("artifactId"),
            has_reference=data.get("hasReference"),
            view=data.get("view"),
            url=data.get("url"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["artifactId"] = self.artifact_id
        out["hasReference"] = self.has_reference
        out["view"] = self.view
        out["url"] = self.url
        return out


class PrReportEnvironmentsEvidenceStatus(StrEnum):
    """The `PrReportEnvironmentsEvidenceStatus` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    CAPTURED = "captured"
    LOCAL = "local"
    UNDECLARED = "undeclared"
    ABSENT = "absent"


class PrReportEnvironmentsProof(StrEnum):
    """The `PrReportEnvironmentsProof` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    COMPLETE = "complete"
    INCOMPLETE = "incomplete"
    NOT_APPLICABLE = "not_applicable"


class PrReportEnvironmentsTeardown(StrEnum):
    """The `PrReportEnvironmentsTeardown` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    CONFIRMED = "confirmed"
    UNCONFIRMED = "unconfirmed"
    PENDING = "pending"
    FAILED = "failed"
    NOT_APPLICABLE = "not_applicable"


@dataclass(frozen=True, slots=True)
class PrReportEnvironmentsTimeline:
    """`PrReportEnvironmentsTimeline`, as carried on the wire."""

    provision_failures: float
    teardown_failures: float
    teardowns_unconfirmed: float
    #: Always present; ``None`` when the server has no value for it.
    gap: PrReportEnvironmentsTimelineGap | None = None
    #: May be absent entirely.
    note: str | None = None
    #: Always present; ``None`` when the server has no value for it.
    provisioned_at: float | None = None
    #: Always present; ``None`` when the server has no value for it.
    torn_down_at: float | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "PrReportEnvironmentsTimeline":
        """Decode a `PrReportEnvironmentsTimeline` from its JSON object."""
        known = {"provisionFailures", "teardownFailures", "teardownsUnconfirmed", "gap", "note", "provisionedAt", "tornDownAt"}
        return cls(
            provision_failures=data.get("provisionFailures"),
            teardown_failures=data.get("teardownFailures"),
            teardowns_unconfirmed=data.get("teardownsUnconfirmed"),
            gap=None if data.get("gap") is None else _enum(PrReportEnvironmentsTimelineGap, data.get("gap")),
            note=data.get("note"),
            provisioned_at=data.get("provisionedAt"),
            torn_down_at=data.get("tornDownAt"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["provisionFailures"] = self.provision_failures
        out["teardownFailures"] = self.teardown_failures
        out["teardownsUnconfirmed"] = self.teardowns_unconfirmed
        out["gap"] = _encode(self.gap)
        if self.note is not None:
            out["note"] = self.note
        out["provisionedAt"] = self.provisioned_at
        out["tornDownAt"] = self.torn_down_at
        return out


class PrReportEnvironmentsTimelineGap(StrEnum):
    """The `PrReportEnvironmentsTimelineGap` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    UNWIRED = "unwired"
    UNREADABLE = "unreadable"
    TRUNCATED = "truncated"
    NOT_PROVISIONED = "not_provisioned"


@dataclass(frozen=True, slots=True)
class PrReportIssue:
    """`PrReportIssue`, as carried on the wire."""

    external_id: str
    source: str
    title: str
    url: str

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "PrReportIssue":
        """Decode a `PrReportIssue` from its JSON object."""
        known = {"externalId", "source", "title", "url"}
        return cls(
            external_id=data.get("externalId"),
            source=data.get("source"),
            title=data.get("title"),
            url=data.get("url"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["externalId"] = self.external_id
        out["source"] = self.source
        out["title"] = self.title
        out["url"] = self.url
        return out


@dataclass(frozen=True, slots=True)
class PrReportJudge:
    """`PrReportJudge`, as carried on the wire."""

    bounces: float
    findings: list[PublicJudgeFinding]
    max_bounces: float
    rubric_overridden: bool
    step_kind: str
    #: Always present; ``None`` when the server has no value for it.
    disposition: PrReportJudgeDisposition | None = None
    #: Always present; ``None`` when the server has no value for it.
    model: str | None = None
    #: May be absent entirely.
    model_pin: PrReportJudgeModelPin | None = None
    #: Always present; ``None`` when the server has no value for it.
    rubric_name: str | None = None
    #: Always present; ``None`` when the server has no value for it.
    score: float | None = None
    #: Always present; ``None`` when the server has no value for it.
    summary: str | None = None
    #: Always present; ``None`` when the server has no value for it.
    threshold: float | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "PrReportJudge":
        """Decode a `PrReportJudge` from its JSON object."""
        known = {"bounces", "findings", "maxBounces", "rubricOverridden", "stepKind", "disposition", "model", "modelPin", "rubricName", "score", "summary", "threshold"}
        return cls(
            bounces=data.get("bounces"),
            findings=[PublicJudgeFinding.from_dict(item) for item in data.get("findings") or []],
            max_bounces=data.get("maxBounces"),
            rubric_overridden=data.get("rubricOverridden"),
            step_kind=data.get("stepKind"),
            disposition=None if data.get("disposition") is None else _enum(PrReportJudgeDisposition, data.get("disposition")),
            model=data.get("model"),
            model_pin=None if data.get("modelPin") is None else PrReportJudgeModelPin.from_dict(data.get("modelPin")),
            rubric_name=data.get("rubricName"),
            score=data.get("score"),
            summary=data.get("summary"),
            threshold=data.get("threshold"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["bounces"] = self.bounces
        out["findings"] = [_encode(item) for item in self.findings]
        out["maxBounces"] = self.max_bounces
        out["rubricOverridden"] = self.rubric_overridden
        out["stepKind"] = self.step_kind
        out["disposition"] = _encode(self.disposition)
        out["model"] = self.model
        if self.model_pin is not None:
            out["modelPin"] = _encode(self.model_pin)
        out["rubricName"] = self.rubric_name
        out["score"] = self.score
        out["summary"] = self.summary
        out["threshold"] = self.threshold
        return out


class PrReportJudgeDisposition(StrEnum):
    """The `PrReportJudgeDisposition` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    PASS = "pass"
    PARK = "park"
    BOUNCE = "bounce"
    FAIL = "fail"


@dataclass(frozen=True, slots=True)
class PrReportJudgeModelPin:
    """`PrReportJudgeModelPin`, as carried on the wire."""

    requested: str
    status: PrReportJudgeModelPinStatus

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "PrReportJudgeModelPin":
        """Decode a `PrReportJudgeModelPin` from its JSON object."""
        known = {"requested", "status"}
        return cls(
            requested=data.get("requested"),
            status=_enum(PrReportJudgeModelPinStatus, data.get("status")),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["requested"] = self.requested
        out["status"] = _encode(self.status)
        return out


class PrReportJudgeModelPinStatus(StrEnum):
    """The `PrReportJudgeModelPinStatus` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    APPLIED = "applied"
    OVERRIDDEN = "overridden"
    UNAVAILABLE = "unavailable"


@dataclass(frozen=True, slots=True)
class PrReportJudges:
    """`PrReportJudges`, as carried on the wire."""

    status: PrReportCiStatus
    verdicts: list[PrReportJudge]
    #: May be absent entirely.
    note: str | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "PrReportJudges":
        """Decode a `PrReportJudges` from its JSON object."""
        known = {"status", "verdicts", "note"}
        return cls(
            status=_enum(PrReportCiStatus, data.get("status")),
            verdicts=[PrReportJudge.from_dict(item) for item in data.get("verdicts") or []],
            note=data.get("note"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["status"] = _encode(self.status)
        out["verdicts"] = [_encode(item) for item in self.verdicts]
        if self.note is not None:
            out["note"] = self.note
        return out


@dataclass(frozen=True, slots=True)
class PrReportMerge:
    """`PrReportMerge`, as carried on the wire."""

    status: PrReportCiStatus
    #: May be absent entirely.
    assessment: NotificationPayloadAssessment | None = None
    #: May be absent entirely.
    note: str | None = None
    #: May be absent entirely.
    outcome: str | None = None
    #: May be absent entirely.
    preset_name: str | None = None
    #: May be absent entirely.
    reason: str | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "PrReportMerge":
        """Decode a `PrReportMerge` from its JSON object."""
        known = {"status", "assessment", "note", "outcome", "presetName", "reason"}
        return cls(
            status=_enum(PrReportCiStatus, data.get("status")),
            assessment=None if data.get("assessment") is None else NotificationPayloadAssessment.from_dict(data.get("assessment")),
            note=data.get("note"),
            outcome=data.get("outcome"),
            preset_name=data.get("presetName"),
            reason=data.get("reason"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["status"] = _encode(self.status)
        if self.assessment is not None:
            out["assessment"] = _encode(self.assessment)
        if self.note is not None:
            out["note"] = self.note
        if self.outcome is not None:
            out["outcome"] = self.outcome
        if self.preset_name is not None:
            out["presetName"] = self.preset_name
        if self.reason is not None:
            out["reason"] = self.reason
        return out


@dataclass(frozen=True, slots=True)
class PrReportObservability:
    """`PrReportObservability`, as carried on the wire."""

    #: Always present; ``None`` when the server has no value for it.
    report_url: str | None = None
    #: Always present; ``None`` when the server has no value for it.
    run_url: str | None = None
    #: Always present; ``None`` when the server has no value for it.
    trajectory_url: str | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "PrReportObservability":
        """Decode a `PrReportObservability` from its JSON object."""
        known = {"reportUrl", "runUrl", "trajectoryUrl"}
        return cls(
            report_url=data.get("reportUrl"),
            run_url=data.get("runUrl"),
            trajectory_url=data.get("trajectoryUrl"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["reportUrl"] = self.report_url
        out["runUrl"] = self.run_url
        out["trajectoryUrl"] = self.trajectory_url
        return out


@dataclass(frozen=True, slots=True)
class PrReportReproduction:
    """`PrReportReproduction`, as carried on the wire."""

    attempts: float
    status: PrReportCiStatus
    test_paths: list[str]
    #: May be absent entirely.
    alternative_verification: str | None = None
    #: May be absent entirely.
    at: float | None = None
    #: May be absent entirely.
    base: PrReportReproductionBase | None = None
    #: May be absent entirely.
    command: str | None = None
    #: May be absent entirely.
    final: PrReportReproductionBase | None = None
    #: May be absent entirely.
    max_attempts: float | None = None
    #: May be absent entirely.
    note: str | None = None
    #: May be absent entirely.
    observation: str | None = None
    #: May be absent entirely.
    omitted_test_paths: float | None = None
    #: May be absent entirely.
    reason: str | None = None
    #: May be absent entirely.
    verdict: PrReportReproductionVerdict | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "PrReportReproduction":
        """Decode a `PrReportReproduction` from its JSON object."""
        known = {"attempts", "status", "testPaths", "alternativeVerification", "at", "base", "command", "final", "maxAttempts", "note", "observation", "omittedTestPaths", "reason", "verdict"}
        return cls(
            attempts=data.get("attempts"),
            status=_enum(PrReportCiStatus, data.get("status")),
            test_paths=[item for item in data.get("testPaths") or []],
            alternative_verification=data.get("alternativeVerification"),
            at=data.get("at"),
            base=None if data.get("base") is None else PrReportReproductionBase.from_dict(data.get("base")),
            command=data.get("command"),
            final=None if data.get("final") is None else PrReportReproductionBase.from_dict(data.get("final")),
            max_attempts=data.get("maxAttempts"),
            note=data.get("note"),
            observation=data.get("observation"),
            omitted_test_paths=data.get("omittedTestPaths"),
            reason=data.get("reason"),
            verdict=None if data.get("verdict") is None else _enum(PrReportReproductionVerdict, data.get("verdict")),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["attempts"] = self.attempts
        out["status"] = _encode(self.status)
        out["testPaths"] = [_encode(item) for item in self.test_paths]
        if self.alternative_verification is not None:
            out["alternativeVerification"] = self.alternative_verification
        if self.at is not None:
            out["at"] = self.at
        if self.base is not None:
            out["base"] = _encode(self.base)
        if self.command is not None:
            out["command"] = self.command
        if self.final is not None:
            out["final"] = _encode(self.final)
        if self.max_attempts is not None:
            out["maxAttempts"] = self.max_attempts
        if self.note is not None:
            out["note"] = self.note
        if self.observation is not None:
            out["observation"] = self.observation
        if self.omitted_test_paths is not None:
            out["omittedTestPaths"] = self.omitted_test_paths
        if self.reason is not None:
            out["reason"] = self.reason
        if self.verdict is not None:
            out["verdict"] = _encode(self.verdict)
        return out


@dataclass(frozen=True, slots=True)
class PrReportReproductionBase:
    """`PrReportReproductionBase`, as carried on the wire."""

    exit_code: float
    passed: bool
    #: May be absent entirely.
    duration_ms: float | None = None
    #: May be absent entirely.
    output_tail: str | None = None
    #: May be absent entirely.
    setup_failed: bool | None = None
    #: May be absent entirely.
    timed_out: bool | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "PrReportReproductionBase":
        """Decode a `PrReportReproductionBase` from its JSON object."""
        known = {"exitCode", "passed", "durationMs", "outputTail", "setupFailed", "timedOut"}
        return cls(
            exit_code=data.get("exitCode"),
            passed=data.get("passed"),
            duration_ms=data.get("durationMs"),
            output_tail=data.get("outputTail"),
            setup_failed=data.get("setupFailed"),
            timed_out=data.get("timedOut"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["exitCode"] = self.exit_code
        out["passed"] = self.passed
        if self.duration_ms is not None:
            out["durationMs"] = self.duration_ms
        if self.output_tail is not None:
            out["outputTail"] = self.output_tail
        if self.setup_failed is not None:
            out["setupFailed"] = self.setup_failed
        if self.timed_out is not None:
            out["timedOut"] = self.timed_out
        return out


class PrReportReproductionVerdict(StrEnum):
    """The `PrReportReproductionVerdict` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    REPRODUCED = "reproduced"
    INCONCLUSIVE = "inconclusive"
    DECLARED_INFEASIBLE = "declared_infeasible"


@dataclass(frozen=True, slots=True)
class PrReportRequirements:
    """`PrReportRequirements`, as carried on the wire."""

    entries: list[PrReportRequirementsEntry]
    met: float
    not_covered: float
    not_met: float
    regressions: float
    status: PrReportCiStatus
    total: float
    #: May be absent entirely.
    note: str | None = None
    #: May be absent entirely.
    unmatched_verdicts: float | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "PrReportRequirements":
        """Decode a `PrReportRequirements` from its JSON object."""
        known = {"entries", "met", "notCovered", "notMet", "regressions", "status", "total", "note", "unmatchedVerdicts"}
        return cls(
            entries=[PrReportRequirementsEntry.from_dict(item) for item in data.get("entries") or []],
            met=data.get("met"),
            not_covered=data.get("notCovered"),
            not_met=data.get("notMet"),
            regressions=data.get("regressions"),
            status=_enum(PrReportCiStatus, data.get("status")),
            total=data.get("total"),
            note=data.get("note"),
            unmatched_verdicts=data.get("unmatchedVerdicts"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["entries"] = [_encode(item) for item in self.entries]
        out["met"] = self.met
        out["notCovered"] = self.not_covered
        out["notMet"] = self.not_met
        out["regressions"] = self.regressions
        out["status"] = _encode(self.status)
        out["total"] = self.total
        if self.note is not None:
            out["note"] = self.note
        if self.unmatched_verdicts is not None:
            out["unmatchedVerdicts"] = self.unmatched_verdicts
        return out


@dataclass(frozen=True, slots=True)
class PrReportRequirementsEntry:
    """`PrReportRequirementsEntry`, as carried on the wire."""

    criteria_count: float
    group: str
    id: str
    module: str
    priority: PrReportRequirementsEntryPriority
    state: PrReportRequirementsEntryState
    title: str
    verdict: PrReportRequirementsEntryVerdict
    #: Always present; ``None`` when the server has no value for it.
    detail: str | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "PrReportRequirementsEntry":
        """Decode a `PrReportRequirementsEntry` from its JSON object."""
        known = {"criteriaCount", "group", "id", "module", "priority", "state", "title", "verdict", "detail"}
        return cls(
            criteria_count=data.get("criteriaCount"),
            group=data.get("group"),
            id=data.get("id"),
            module=data.get("module"),
            priority=_enum(PrReportRequirementsEntryPriority, data.get("priority")),
            state=_enum(PrReportRequirementsEntryState, data.get("state")),
            title=data.get("title"),
            verdict=_enum(PrReportRequirementsEntryVerdict, data.get("verdict")),
            detail=data.get("detail"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["criteriaCount"] = self.criteria_count
        out["group"] = self.group
        out["id"] = self.id
        out["module"] = self.module
        out["priority"] = _encode(self.priority)
        out["state"] = _encode(self.state)
        out["title"] = self.title
        out["verdict"] = _encode(self.verdict)
        out["detail"] = self.detail
        return out


class PrReportRequirementsEntryPriority(StrEnum):
    """The `PrReportRequirementsEntryPriority` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    MUST = "must"
    SHOULD = "should"
    COULD = "could"


class PrReportRequirementsEntryState(StrEnum):
    """The `PrReportRequirementsEntryState` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    ASPIRATIONAL = "aspirational"
    ESTABLISHED = "established"


class PrReportRequirementsEntryVerdict(StrEnum):
    """The `PrReportRequirementsEntryVerdict` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    MET = "met"
    NOT_MET = "not_met"
    NOT_COVERED = "not_covered"


@dataclass(frozen=True, slots=True)
class PrReportRun:
    """`PrReportRun`, as carried on the wire."""

    block_id: str
    block_title: str
    execution_id: str
    issues: list[PrReportIssue]
    pipeline_id: str
    pipeline_name: str
    steps: list[PrReportStep]
    #: May be absent entirely.
    provider: PrReportRunProvider | None = None
    #: May be absent entirely.
    repo: str | None = None
    #: May be absent entirely.
    started_at: float | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "PrReportRun":
        """Decode a `PrReportRun` from its JSON object."""
        known = {"blockId", "blockTitle", "executionId", "issues", "pipelineId", "pipelineName", "steps", "provider", "repo", "startedAt"}
        return cls(
            block_id=data.get("blockId"),
            block_title=data.get("blockTitle"),
            execution_id=data.get("executionId"),
            issues=[PrReportIssue.from_dict(item) for item in data.get("issues") or []],
            pipeline_id=data.get("pipelineId"),
            pipeline_name=data.get("pipelineName"),
            steps=[PrReportStep.from_dict(item) for item in data.get("steps") or []],
            provider=None if data.get("provider") is None else _enum(PrReportRunProvider, data.get("provider")),
            repo=data.get("repo"),
            started_at=data.get("startedAt"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["blockId"] = self.block_id
        out["blockTitle"] = self.block_title
        out["executionId"] = self.execution_id
        out["issues"] = [_encode(item) for item in self.issues]
        out["pipelineId"] = self.pipeline_id
        out["pipelineName"] = self.pipeline_name
        out["steps"] = [_encode(item) for item in self.steps]
        if self.provider is not None:
            out["provider"] = _encode(self.provider)
        if self.repo is not None:
            out["repo"] = self.repo
        if self.started_at is not None:
            out["startedAt"] = self.started_at
        return out


class PrReportRunProvider(StrEnum):
    """The `PrReportRunProvider` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    GITHUB = "github"
    GITLAB = "gitlab"


@dataclass(frozen=True, slots=True)
class PrReportStep:
    """`PrReportStep`, as carried on the wire."""

    agent_kind: str
    index: float
    state: str
    #: May be absent entirely.
    model: str | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "PrReportStep":
        """Decode a `PrReportStep` from its JSON object."""
        known = {"agentKind", "index", "state", "model"}
        return cls(
            agent_kind=data.get("agentKind"),
            index=data.get("index"),
            state=data.get("state"),
            model=data.get("model"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["agentKind"] = self.agent_kind
        out["index"] = self.index
        out["state"] = self.state
        if self.model is not None:
            out["model"] = self.model
        return out


@dataclass(frozen=True, slots=True)
class PrReportTestConcern:
    """`PrReportTestConcern`, as carried on the wire."""

    severity: str
    title: str

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "PrReportTestConcern":
        """Decode a `PrReportTestConcern` from its JSON object."""
        known = {"severity", "title"}
        return cls(
            severity=data.get("severity"),
            title=data.get("title"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["severity"] = self.severity
        out["title"] = self.title
        return out


@dataclass(frozen=True, slots=True)
class PrReportTestOutcome:
    """`PrReportTestOutcome`, as carried on the wire."""

    name: str
    status: str
    #: May be absent entirely.
    detail: str | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "PrReportTestOutcome":
        """Decode a `PrReportTestOutcome` from its JSON object."""
        known = {"name", "status", "detail"}
        return cls(
            name=data.get("name"),
            status=data.get("status"),
            detail=data.get("detail"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["name"] = self.name
        out["status"] = self.status
        if self.detail is not None:
            out["detail"] = self.detail
        return out


@dataclass(frozen=True, slots=True)
class PrReportTests:
    """`PrReportTests`, as carried on the wire."""

    concerns: list[PrReportTestConcern]
    fixer_attempts: float
    outcomes: list[PrReportTestOutcome]
    status: PrReportCiStatus
    tested: list[str]
    #: May be absent entirely.
    environment: str | None = None
    #: May be absent entirely.
    greenlight: bool | None = None
    #: May be absent entirely.
    max_fixer_attempts: float | None = None
    #: May be absent entirely.
    note: str | None = None
    #: May be absent entirely.
    summary: str | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "PrReportTests":
        """Decode a `PrReportTests` from its JSON object."""
        known = {"concerns", "fixerAttempts", "outcomes", "status", "tested", "environment", "greenlight", "maxFixerAttempts", "note", "summary"}
        return cls(
            concerns=[PrReportTestConcern.from_dict(item) for item in data.get("concerns") or []],
            fixer_attempts=data.get("fixerAttempts"),
            outcomes=[PrReportTestOutcome.from_dict(item) for item in data.get("outcomes") or []],
            status=_enum(PrReportCiStatus, data.get("status")),
            tested=[item for item in data.get("tested") or []],
            environment=data.get("environment"),
            greenlight=data.get("greenlight"),
            max_fixer_attempts=data.get("maxFixerAttempts"),
            note=data.get("note"),
            summary=data.get("summary"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["concerns"] = [_encode(item) for item in self.concerns]
        out["fixerAttempts"] = self.fixer_attempts
        out["outcomes"] = [_encode(item) for item in self.outcomes]
        out["status"] = _encode(self.status)
        out["tested"] = [_encode(item) for item in self.tested]
        if self.environment is not None:
            out["environment"] = self.environment
        if self.greenlight is not None:
            out["greenlight"] = self.greenlight
        if self.max_fixer_attempts is not None:
            out["maxFixerAttempts"] = self.max_fixer_attempts
        if self.note is not None:
            out["note"] = self.note
        if self.summary is not None:
            out["summary"] = self.summary
        return out


@dataclass(frozen=True, slots=True)
class PrReportValidation:
    """`PrReportValidation`, as carried on the wire."""

    attempts: float
    commands: list[PrReportValidationCommand]
    status: PrReportCiStatus
    #: May be absent entirely.
    at: float | None = None
    #: May be absent entirely.
    config_unreadable: bool | None = None
    #: May be absent entirely.
    max_attempts: float | None = None
    #: May be absent entirely.
    note: str | None = None
    #: May be absent entirely.
    passed: bool | None = None
    #: May be absent entirely.
    step_kind: str | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "PrReportValidation":
        """Decode a `PrReportValidation` from its JSON object."""
        known = {"attempts", "commands", "status", "at", "configUnreadable", "maxAttempts", "note", "passed", "stepKind"}
        return cls(
            attempts=data.get("attempts"),
            commands=[PrReportValidationCommand.from_dict(item) for item in data.get("commands") or []],
            status=_enum(PrReportCiStatus, data.get("status")),
            at=data.get("at"),
            config_unreadable=data.get("configUnreadable"),
            max_attempts=data.get("maxAttempts"),
            note=data.get("note"),
            passed=data.get("passed"),
            step_kind=data.get("stepKind"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["attempts"] = self.attempts
        out["commands"] = [_encode(item) for item in self.commands]
        out["status"] = _encode(self.status)
        if self.at is not None:
            out["at"] = self.at
        if self.config_unreadable is not None:
            out["configUnreadable"] = self.config_unreadable
        if self.max_attempts is not None:
            out["maxAttempts"] = self.max_attempts
        if self.note is not None:
            out["note"] = self.note
        if self.passed is not None:
            out["passed"] = self.passed
        if self.step_kind is not None:
            out["stepKind"] = self.step_kind
        return out


@dataclass(frozen=True, slots=True)
class PrReportValidationCommand:
    """`PrReportValidationCommand`, as carried on the wire."""

    command: str
    exit_code: float
    label: str
    passed: bool
    #: May be absent entirely.
    duration_ms: float | None = None
    #: May be absent entirely.
    output_tail: str | None = None
    #: May be absent entirely.
    timed_out: bool | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "PrReportValidationCommand":
        """Decode a `PrReportValidationCommand` from its JSON object."""
        known = {"command", "exitCode", "label", "passed", "durationMs", "outputTail", "timedOut"}
        return cls(
            command=data.get("command"),
            exit_code=data.get("exitCode"),
            label=data.get("label"),
            passed=data.get("passed"),
            duration_ms=data.get("durationMs"),
            output_tail=data.get("outputTail"),
            timed_out=data.get("timedOut"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["command"] = self.command
        out["exitCode"] = self.exit_code
        out["label"] = self.label
        out["passed"] = self.passed
        if self.duration_ms is not None:
            out["durationMs"] = self.duration_ms
        if self.output_tail is not None:
            out["outputTail"] = self.output_tail
        if self.timed_out is not None:
            out["timedOut"] = self.timed_out
        return out


@dataclass(frozen=True, slots=True)
class PrVerificationReport:
    """`PrVerificationReport`, as carried on the wire."""

    ci: PrReportCi
    context: PrReportContext
    environments: PrReportEnvironments
    generated_at: float
    judges: PrReportJudges
    merge: PrReportMerge
    observability: PrReportObservability
    reproduction: PrReportReproduction
    requirements: PrReportRequirements
    run: PrReportRun
    tests: PrReportTests
    truncations: list[str]
    validation: PrReportValidation
    version: float
    #: May be absent entirely.
    scope: PrVerificationReportScope | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "PrVerificationReport":
        """Decode a `PrVerificationReport` from its JSON object."""
        known = {"ci", "context", "environments", "generatedAt", "judges", "merge", "observability", "reproduction", "requirements", "run", "tests", "truncations", "validation", "version", "scope"}
        return cls(
            ci=PrReportCi.from_dict(data.get("ci")),
            context=PrReportContext.from_dict(data.get("context")),
            environments=PrReportEnvironments.from_dict(data.get("environments")),
            generated_at=data.get("generatedAt"),
            judges=PrReportJudges.from_dict(data.get("judges")),
            merge=PrReportMerge.from_dict(data.get("merge")),
            observability=PrReportObservability.from_dict(data.get("observability")),
            reproduction=PrReportReproduction.from_dict(data.get("reproduction")),
            requirements=PrReportRequirements.from_dict(data.get("requirements")),
            run=PrReportRun.from_dict(data.get("run")),
            tests=PrReportTests.from_dict(data.get("tests")),
            truncations=[item for item in data.get("truncations") or []],
            validation=PrReportValidation.from_dict(data.get("validation")),
            version=data.get("version"),
            scope=None if data.get("scope") is None else PrVerificationReportScope.from_dict(data.get("scope")),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["ci"] = _encode(self.ci)
        out["context"] = _encode(self.context)
        out["environments"] = _encode(self.environments)
        out["generatedAt"] = self.generated_at
        out["judges"] = _encode(self.judges)
        out["merge"] = _encode(self.merge)
        out["observability"] = _encode(self.observability)
        out["reproduction"] = _encode(self.reproduction)
        out["requirements"] = _encode(self.requirements)
        out["run"] = _encode(self.run)
        out["tests"] = _encode(self.tests)
        out["truncations"] = [_encode(item) for item in self.truncations]
        out["validation"] = _encode(self.validation)
        out["version"] = self.version
        if self.scope is not None:
            out["scope"] = _encode(self.scope)
        return out


@dataclass(frozen=True, slots=True)
class PrVerificationReportScope:
    """`PrVerificationReportScope`, as carried on the wire."""

    role: PrVerificationReportScopeRole
    #: May be absent entirely.
    frame_id: str | None = None
    #: May be absent entirely.
    own_pull_request: PrVerificationReportScopeOwnPullRequest | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "PrVerificationReportScope":
        """Decode a `PrVerificationReportScope` from its JSON object."""
        known = {"role", "frameId", "ownPullRequest"}
        return cls(
            role=_enum(PrVerificationReportScopeRole, data.get("role")),
            frame_id=data.get("frameId"),
            own_pull_request=None if data.get("ownPullRequest") is None else PrVerificationReportScopeOwnPullRequest.from_dict(data.get("ownPullRequest")),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["role"] = _encode(self.role)
        if self.frame_id is not None:
            out["frameId"] = self.frame_id
        if self.own_pull_request is not None:
            out["ownPullRequest"] = _encode(self.own_pull_request)
        return out


@dataclass(frozen=True, slots=True)
class PrVerificationReportScopeOwnPullRequest:
    """`PrVerificationReportScopeOwnPullRequest`, as carried on the wire."""

    number: float
    repo: str
    #: May be absent entirely.
    url: str | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "PrVerificationReportScopeOwnPullRequest":
        """Decode a `PrVerificationReportScopeOwnPullRequest` from its JSON object."""
        known = {"number", "repo", "url"}
        return cls(
            number=data.get("number"),
            repo=data.get("repo"),
            url=data.get("url"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["number"] = self.number
        out["repo"] = self.repo
        if self.url is not None:
            out["url"] = self.url
        return out


class PrVerificationReportScopeRole(StrEnum):
    """The `PrVerificationReportScopeRole` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    OWN = "own"
    PEER = "peer"


@dataclass(frozen=True, slots=True)
class PublicAgentDecision:
    """`PublicAgentDecision`, as carried on the wire."""

    decision_id: str
    kind: str
    options: list[str]
    question: str
    step_kind: str

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "PublicAgentDecision":
        """Decode a `PublicAgentDecision` from its JSON object."""
        known = {"decisionId", "kind", "options", "question", "stepKind"}
        return cls(
            decision_id=data.get("decisionId"),
            kind=data.get("kind"),
            options=[item for item in data.get("options") or []],
            question=data.get("question"),
            step_kind=data.get("stepKind"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["decisionId"] = self.decision_id
        out["kind"] = self.kind
        out["options"] = [_encode(item) for item in self.options]
        out["question"] = self.question
        out["stepKind"] = self.step_kind
        return out


@dataclass(frozen=True, slots=True)
class PublicAnswerFollowUp:
    """`PublicAnswerFollowUp`, as carried on the wire."""

    answer: str

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "PublicAnswerFollowUp":
        """Decode a `PublicAnswerFollowUp` from its JSON object."""
        known = {"answer"}
        return cls(
            answer=data.get("answer"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["answer"] = self.answer
        return out


@dataclass(frozen=True, slots=True)
class PublicAnswerInterview:
    """`PublicAnswerInterview`, as carried on the wire."""

    answer: str
    question_id: str

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "PublicAnswerInterview":
        """Decode a `PublicAnswerInterview` from its JSON object."""
        known = {"answer", "questionId"}
        return cls(
            answer=data.get("answer"),
            question_id=data.get("questionId"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["answer"] = self.answer
        out["questionId"] = self.question_id
        return out


@dataclass(frozen=True, slots=True)
class PublicApiKey:
    """`PublicApiKey`, as carried on the wire."""

    account_id: str
    created_at: float
    id: str
    label: str
    scope: PublicApiKeyScope
    workspace_id: str
    #: Always present; ``None`` when the server has no value for it.
    created_by_key_id: str | None = None
    #: Always present; ``None`` when the server has no value for it.
    created_by_user_id: str | None = None
    #: Always present; ``None`` when the server has no value for it.
    external_identity: str | None = None
    #: Always present; ``None`` when the server has no value for it.
    last_used_at: float | None = None
    #: Always present; ``None`` when the server has no value for it.
    revoked_at: float | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "PublicApiKey":
        """Decode a `PublicApiKey` from its JSON object."""
        known = {"accountId", "createdAt", "id", "label", "scope", "workspaceId", "createdByKeyId", "createdByUserId", "externalIdentity", "lastUsedAt", "revokedAt"}
        return cls(
            account_id=data.get("accountId"),
            created_at=data.get("createdAt"),
            id=data.get("id"),
            label=data.get("label"),
            scope=_enum(PublicApiKeyScope, data.get("scope")),
            workspace_id=data.get("workspaceId"),
            created_by_key_id=data.get("createdByKeyId"),
            created_by_user_id=data.get("createdByUserId"),
            external_identity=data.get("externalIdentity"),
            last_used_at=data.get("lastUsedAt"),
            revoked_at=data.get("revokedAt"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["accountId"] = self.account_id
        out["createdAt"] = self.created_at
        out["id"] = self.id
        out["label"] = self.label
        out["scope"] = _encode(self.scope)
        out["workspaceId"] = self.workspace_id
        out["createdByKeyId"] = self.created_by_key_id
        out["createdByUserId"] = self.created_by_user_id
        out["externalIdentity"] = self.external_identity
        out["lastUsedAt"] = self.last_used_at
        out["revokedAt"] = self.revoked_at
        return out


@dataclass(frozen=True, slots=True)
class PublicApiKeyList:
    """`PublicApiKeyList`, as carried on the wire."""

    keys: list[PublicApiKey]

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "PublicApiKeyList":
        """Decode a `PublicApiKeyList` from its JSON object."""
        known = {"keys"}
        return cls(
            keys=[PublicApiKey.from_dict(item) for item in data.get("keys") or []],
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["keys"] = [_encode(item) for item in self.keys]
        return out


class PublicApiKeyScope(StrEnum):
    """The `PublicApiKeyScope` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    READ = "read"
    WRITE = "write"
    DECIDE = "decide"
    ADMIN = "admin"


@dataclass(frozen=True, slots=True)
class PublicApprovalGateDecision:
    """`PublicApprovalGateDecision`, as carried on the wire."""

    approval_id: str
    exceeded: bool
    kind: str
    proposal: str
    recorded_approvals: float
    required_approvals: float
    status: PublicApprovalGateDecisionStatus
    step_index: float
    step_kind: str
    #: Always present; ``None`` when the server has no value for it.
    feedback: str | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "PublicApprovalGateDecision":
        """Decode a `PublicApprovalGateDecision` from its JSON object."""
        known = {"approvalId", "exceeded", "kind", "proposal", "recordedApprovals", "requiredApprovals", "status", "stepIndex", "stepKind", "feedback"}
        return cls(
            approval_id=data.get("approvalId"),
            exceeded=data.get("exceeded"),
            kind=data.get("kind"),
            proposal=data.get("proposal"),
            recorded_approvals=data.get("recordedApprovals"),
            required_approvals=data.get("requiredApprovals"),
            status=_enum(PublicApprovalGateDecisionStatus, data.get("status")),
            step_index=data.get("stepIndex"),
            step_kind=data.get("stepKind"),
            feedback=data.get("feedback"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["approvalId"] = self.approval_id
        out["exceeded"] = self.exceeded
        out["kind"] = self.kind
        out["proposal"] = self.proposal
        out["recordedApprovals"] = self.recorded_approvals
        out["requiredApprovals"] = self.required_approvals
        out["status"] = _encode(self.status)
        out["stepIndex"] = self.step_index
        out["stepKind"] = self.step_kind
        out["feedback"] = self.feedback
        return out


class PublicApprovalGateDecisionStatus(StrEnum):
    """The `PublicApprovalGateDecisionStatus` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    PENDING = "pending"
    APPROVED = "approved"
    CHANGES_REQUESTED = "changes_requested"
    REJECTED = "rejected"


@dataclass(frozen=True, slots=True)
class PublicApproveStep:
    """`PublicApproveStep`, as carried on the wire."""

    #: May be absent entirely.
    proposal: str | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "PublicApproveStep":
        """Decode a `PublicApproveStep` from its JSON object."""
        known = {"proposal"}
        return cls(
            proposal=data.get("proposal"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        if self.proposal is not None:
            out["proposal"] = self.proposal
        return out


@dataclass(frozen=True, slots=True)
class PublicBrainstormDecision:
    """`PublicBrainstormDecision`, as carried on the wire."""

    iteration: float
    kind: str
    max_iterations: float
    options: list[PublicReviewFinding]
    session_id: str
    stage: PublicBrainstormDecisionStage
    status: PublicRequirementsDecisionStatus
    task_id: str
    #: Always present; ``None`` when the server has no value for it.
    converged_direction: str | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "PublicBrainstormDecision":
        """Decode a `PublicBrainstormDecision` from its JSON object."""
        known = {"iteration", "kind", "maxIterations", "options", "sessionId", "stage", "status", "taskId", "convergedDirection"}
        return cls(
            iteration=data.get("iteration"),
            kind=data.get("kind"),
            max_iterations=data.get("maxIterations"),
            options=[PublicReviewFinding.from_dict(item) for item in data.get("options") or []],
            session_id=data.get("sessionId"),
            stage=_enum(PublicBrainstormDecisionStage, data.get("stage")),
            status=_enum(PublicRequirementsDecisionStatus, data.get("status")),
            task_id=data.get("taskId"),
            converged_direction=data.get("convergedDirection"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["iteration"] = self.iteration
        out["kind"] = self.kind
        out["maxIterations"] = self.max_iterations
        out["options"] = [_encode(item) for item in self.options]
        out["sessionId"] = self.session_id
        out["stage"] = _encode(self.stage)
        out["status"] = _encode(self.status)
        out["taskId"] = self.task_id
        out["convergedDirection"] = self.converged_direction
        return out


class PublicBrainstormDecisionStage(StrEnum):
    """The `PublicBrainstormDecisionStage` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    REQUIREMENTS = "requirements"
    ARCHITECTURE = "architecture"


@dataclass(frozen=True, slots=True)
class PublicChallengePrReviewFinding:
    """`PublicChallengePrReviewFinding`, as carried on the wire."""

    #: May be absent entirely.
    question: str | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "PublicChallengePrReviewFinding":
        """Decode a `PublicChallengePrReviewFinding` from its JSON object."""
        known = {"question"}
        return cls(
            question=data.get("question"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        if self.question is not None:
            out["question"] = self.question
        return out


@dataclass(frozen=True, slots=True)
class PublicChooseFork:
    """`PublicChooseFork`, as carried on the wire."""

    #: May be absent entirely.
    custom: str | None = None
    #: May be absent entirely.
    fork_id: str | None = None
    #: May be absent entirely.
    note: str | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "PublicChooseFork":
        """Decode a `PublicChooseFork` from its JSON object."""
        known = {"custom", "forkId", "note"}
        return cls(
            custom=data.get("custom"),
            fork_id=data.get("forkId"),
            note=data.get("note"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        if self.custom is not None:
            out["custom"] = self.custom
        if self.fork_id is not None:
            out["forkId"] = self.fork_id
        if self.note is not None:
            out["note"] = self.note
        return out


@dataclass(frozen=True, slots=True)
class PublicClarityDecision:
    """`PublicClarityDecision`, as carried on the wire."""

    findings: list[PublicReviewFinding]
    iteration: float
    kind: str
    max_iterations: float
    review_id: str
    status: PublicRequirementsDecisionStatus
    task_id: str
    #: Always present; ``None`` when the server has no value for it.
    clarified_report: str | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "PublicClarityDecision":
        """Decode a `PublicClarityDecision` from its JSON object."""
        known = {"findings", "iteration", "kind", "maxIterations", "reviewId", "status", "taskId", "clarifiedReport"}
        return cls(
            findings=[PublicReviewFinding.from_dict(item) for item in data.get("findings") or []],
            iteration=data.get("iteration"),
            kind=data.get("kind"),
            max_iterations=data.get("maxIterations"),
            review_id=data.get("reviewId"),
            status=_enum(PublicRequirementsDecisionStatus, data.get("status")),
            task_id=data.get("taskId"),
            clarified_report=data.get("clarifiedReport"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["findings"] = [_encode(item) for item in self.findings]
        out["iteration"] = self.iteration
        out["kind"] = self.kind
        out["maxIterations"] = self.max_iterations
        out["reviewId"] = self.review_id
        out["status"] = _encode(self.status)
        out["taskId"] = self.task_id
        out["clarifiedReport"] = self.clarified_report
        return out


@dataclass(frozen=True, slots=True)
class PublicDecisionList:
    """`PublicDecisionList`, as carried on the wire."""

    decisions: list[PublicDecision]
    parked: bool
    run_id: str
    status: RunStatus
    task_id: str
    unanswerable: list[PublicUnanswerableWait]

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "PublicDecisionList":
        """Decode a `PublicDecisionList` from its JSON object."""
        known = {"decisions", "parked", "runId", "status", "taskId", "unanswerable"}
        return cls(
            decisions=[PublicDecision.from_dict(item) for item in data.get("decisions") or []],
            parked=data.get("parked"),
            run_id=data.get("runId"),
            status=_enum(RunStatus, data.get("status")),
            task_id=data.get("taskId"),
            unanswerable=[PublicUnanswerableWait.from_dict(item) for item in data.get("unanswerable") or []],
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["decisions"] = [_encode(item) for item in self.decisions]
        out["parked"] = self.parked
        out["runId"] = self.run_id
        out["status"] = _encode(self.status)
        out["taskId"] = self.task_id
        out["unanswerable"] = [_encode(item) for item in self.unanswerable]
        return out


@dataclass(frozen=True, slots=True)
class PublicFollowUpItem:
    """`PublicFollowUpItem`, as carried on the wire."""

    detail: str
    item_id: str
    kind: PublicFollowUpItemKind
    status: PublicFollowUpItemStatus
    title: str
    #: Always present; ``None`` when the server has no value for it.
    answer: str | None = None
    #: Always present; ``None`` when the server has no value for it.
    suggested_action: str | None = None
    #: Always present; ``None`` when the server has no value for it.
    ticket_external_id: str | None = None
    #: Always present; ``None`` when the server has no value for it.
    ticket_url: str | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "PublicFollowUpItem":
        """Decode a `PublicFollowUpItem` from its JSON object."""
        known = {"detail", "itemId", "kind", "status", "title", "answer", "suggestedAction", "ticketExternalId", "ticketUrl"}
        return cls(
            detail=data.get("detail"),
            item_id=data.get("itemId"),
            kind=_enum(PublicFollowUpItemKind, data.get("kind")),
            status=_enum(PublicFollowUpItemStatus, data.get("status")),
            title=data.get("title"),
            answer=data.get("answer"),
            suggested_action=data.get("suggestedAction"),
            ticket_external_id=data.get("ticketExternalId"),
            ticket_url=data.get("ticketUrl"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["detail"] = self.detail
        out["itemId"] = self.item_id
        out["kind"] = _encode(self.kind)
        out["status"] = _encode(self.status)
        out["title"] = self.title
        out["answer"] = self.answer
        out["suggestedAction"] = self.suggested_action
        out["ticketExternalId"] = self.ticket_external_id
        out["ticketUrl"] = self.ticket_url
        return out


class PublicFollowUpItemKind(StrEnum):
    """The `PublicFollowUpItemKind` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    FOLLOW_UP = "follow_up"
    QUESTION = "question"


class PublicFollowUpItemStatus(StrEnum):
    """The `PublicFollowUpItemStatus` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    PENDING = "pending"
    FILED = "filed"
    QUEUED = "queued"
    ANSWERED = "answered"
    DISMISSED = "dismissed"


@dataclass(frozen=True, slots=True)
class PublicFollowUpsDecision:
    """`PublicFollowUpsDecision`, as carried on the wire."""

    items: list[PublicFollowUpItem]
    kind: str
    loops: float
    max_loops: float
    step_index: float
    step_kind: str

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "PublicFollowUpsDecision":
        """Decode a `PublicFollowUpsDecision` from its JSON object."""
        known = {"items", "kind", "loops", "maxLoops", "stepIndex", "stepKind"}
        return cls(
            items=[PublicFollowUpItem.from_dict(item) for item in data.get("items") or []],
            kind=data.get("kind"),
            loops=data.get("loops"),
            max_loops=data.get("maxLoops"),
            step_index=data.get("stepIndex"),
            step_kind=data.get("stepKind"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["items"] = [_encode(item) for item in self.items]
        out["kind"] = self.kind
        out["loops"] = self.loops
        out["maxLoops"] = self.max_loops
        out["stepIndex"] = self.step_index
        out["stepKind"] = self.step_kind
        return out


@dataclass(frozen=True, slots=True)
class PublicForkDecision:
    """`PublicForkDecision`, as carried on the wire."""

    forks: list[PublicForkDecisionFork]
    kind: str
    status: PublicForkDecisionStatus
    #: Always present; ``None`` when the server has no value for it.
    seam_summary: str | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "PublicForkDecision":
        """Decode a `PublicForkDecision` from its JSON object."""
        known = {"forks", "kind", "status", "seamSummary"}
        return cls(
            forks=[PublicForkDecisionFork.from_dict(item) for item in data.get("forks") or []],
            kind=data.get("kind"),
            status=_enum(PublicForkDecisionStatus, data.get("status")),
            seam_summary=data.get("seamSummary"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["forks"] = [_encode(item) for item in self.forks]
        out["kind"] = self.kind
        out["status"] = _encode(self.status)
        out["seamSummary"] = self.seam_summary
        return out


@dataclass(frozen=True, slots=True)
class PublicForkDecisionFork:
    """`PublicForkDecisionFork`, as carried on the wire."""

    approach: str
    id: str
    summary: str
    title: str
    tradeoffs: list[str]
    #: May be absent entirely.
    recommended: bool | None = None
    #: May be absent entirely.
    risk_notes: str | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "PublicForkDecisionFork":
        """Decode a `PublicForkDecisionFork` from its JSON object."""
        known = {"approach", "id", "summary", "title", "tradeoffs", "recommended", "riskNotes"}
        return cls(
            approach=data.get("approach"),
            id=data.get("id"),
            summary=data.get("summary"),
            title=data.get("title"),
            tradeoffs=[item for item in data.get("tradeoffs") or []],
            recommended=data.get("recommended"),
            risk_notes=data.get("riskNotes"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["approach"] = self.approach
        out["id"] = self.id
        out["summary"] = self.summary
        out["title"] = self.title
        out["tradeoffs"] = [_encode(item) for item in self.tradeoffs]
        if self.recommended is not None:
            out["recommended"] = self.recommended
        if self.risk_notes is not None:
            out["riskNotes"] = self.risk_notes
        return out


class PublicForkDecisionStatus(StrEnum):
    """The `PublicForkDecisionStatus` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    PROPOSING = "proposing"
    AWAITING_CHOICE = "awaiting_choice"
    ANSWERING = "answering"
    CHOSEN = "chosen"
    SINGLE_PATH = "single_path"
    SKIPPED = "skipped"


@dataclass(frozen=True, slots=True)
class PublicHumanTestDecision:
    """`PublicHumanTestDecision`, as carried on the wire."""

    attempts: float
    kind: str
    max_attempts: float
    phase: PublicHumanTestDecisionPhase
    #: Always present; ``None`` when the server has no value for it.
    degraded_reason: str | None = None
    #: Always present; ``None`` when the server has no value for it.
    environment: PublicHumanTestEnvironment | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "PublicHumanTestDecision":
        """Decode a `PublicHumanTestDecision` from its JSON object."""
        known = {"attempts", "kind", "maxAttempts", "phase", "degradedReason", "environment"}
        return cls(
            attempts=data.get("attempts"),
            kind=data.get("kind"),
            max_attempts=data.get("maxAttempts"),
            phase=_enum(PublicHumanTestDecisionPhase, data.get("phase")),
            degraded_reason=data.get("degradedReason"),
            environment=None if data.get("environment") is None else PublicHumanTestEnvironment.from_dict(data.get("environment")),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["attempts"] = self.attempts
        out["kind"] = self.kind
        out["maxAttempts"] = self.max_attempts
        out["phase"] = _encode(self.phase)
        out["degradedReason"] = self.degraded_reason
        out["environment"] = _encode(self.environment)
        return out


class PublicHumanTestDecisionPhase(StrEnum):
    """The `PublicHumanTestDecisionPhase` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    PROVISIONING = "provisioning"
    AWAITING_HUMAN = "awaiting_human"
    FIXING = "fixing"
    RESOLVING_CONFLICTS = "resolving_conflicts"
    PASSED = "passed"


@dataclass(frozen=True, slots=True)
class PublicHumanTestEnvironment:
    """`PublicHumanTestEnvironment`, as carried on the wire."""

    status: PublicHumanTestEnvironmentStatus
    #: Always present; ``None`` when the server has no value for it.
    expires_at: float | None = None
    #: Always present; ``None`` when the server has no value for it.
    url: str | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "PublicHumanTestEnvironment":
        """Decode a `PublicHumanTestEnvironment` from its JSON object."""
        known = {"status", "expiresAt", "url"}
        return cls(
            status=_enum(PublicHumanTestEnvironmentStatus, data.get("status")),
            expires_at=data.get("expiresAt"),
            url=data.get("url"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["status"] = _encode(self.status)
        out["expiresAt"] = self.expires_at
        out["url"] = self.url
        return out


class PublicHumanTestEnvironmentStatus(StrEnum):
    """The `PublicHumanTestEnvironmentStatus` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    PROVISIONING = "provisioning"
    READY = "ready"
    FAILED = "failed"
    EXPIRED = "expired"
    TEARING_DOWN = "tearing_down"
    TORN_DOWN = "torn_down"


@dataclass(frozen=True, slots=True)
class PublicIdentity:
    """`PublicIdentity`, as carried on the wire."""

    account_id: str
    created_at: float
    key_id: str
    label: str
    scope: PublicApiKeyScope
    workspace_id: str
    #: Always present; ``None`` when the server has no value for it.
    external_identity: str | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "PublicIdentity":
        """Decode a `PublicIdentity` from its JSON object."""
        known = {"accountId", "createdAt", "keyId", "label", "scope", "workspaceId", "externalIdentity"}
        return cls(
            account_id=data.get("accountId"),
            created_at=data.get("createdAt"),
            key_id=data.get("keyId"),
            label=data.get("label"),
            scope=_enum(PublicApiKeyScope, data.get("scope")),
            workspace_id=data.get("workspaceId"),
            external_identity=data.get("externalIdentity"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["accountId"] = self.account_id
        out["createdAt"] = self.created_at
        out["keyId"] = self.key_id
        out["label"] = self.label
        out["scope"] = _encode(self.scope)
        out["workspaceId"] = self.workspace_id
        out["externalIdentity"] = self.external_identity
        return out


@dataclass(frozen=True, slots=True)
class PublicIncorporate:
    """`PublicIncorporate`, as carried on the wire."""

    #: May be absent entirely.
    feedback: str | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "PublicIncorporate":
        """Decode a `PublicIncorporate` from its JSON object."""
        known = {"feedback"}
        return cls(
            feedback=data.get("feedback"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        if self.feedback is not None:
            out["feedback"] = self.feedback
        return out


@dataclass(frozen=True, slots=True)
class PublicInputGateDecision:
    """`PublicInputGateDecision`, as carried on the wire."""

    checked_at: float
    issues: list[PublicInputGateDecisionIssue]
    kind: str
    mode: PublicInputGateDecisionMode
    status: PublicInputGateDecisionStatus

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "PublicInputGateDecision":
        """Decode a `PublicInputGateDecision` from its JSON object."""
        known = {"checkedAt", "issues", "kind", "mode", "status"}
        return cls(
            checked_at=data.get("checkedAt"),
            issues=[PublicInputGateDecisionIssue.from_dict(item) for item in data.get("issues") or []],
            kind=data.get("kind"),
            mode=_enum(PublicInputGateDecisionMode, data.get("mode")),
            status=_enum(PublicInputGateDecisionStatus, data.get("status")),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["checkedAt"] = self.checked_at
        out["issues"] = [_encode(item) for item in self.issues]
        out["kind"] = self.kind
        out["mode"] = _encode(self.mode)
        out["status"] = _encode(self.status)
        return out


@dataclass(frozen=True, slots=True)
class PublicInputGateDecisionIssue:
    """`PublicInputGateDecisionIssue`, as carried on the wire."""

    code: PublicInputGateDecisionIssueCode
    severity: PublicInputGateDecisionIssueSeverity
    #: May be absent entirely.
    field: PublicInputGateDecisionIssueField | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "PublicInputGateDecisionIssue":
        """Decode a `PublicInputGateDecisionIssue` from its JSON object."""
        known = {"code", "severity", "field"}
        return cls(
            code=_enum(PublicInputGateDecisionIssueCode, data.get("code")),
            severity=_enum(PublicInputGateDecisionIssueSeverity, data.get("severity")),
            field=None if data.get("field") is None else PublicInputGateDecisionIssueField.from_dict(data.get("field")),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["code"] = _encode(self.code)
        out["severity"] = _encode(self.severity)
        if self.field is not None:
            out["field"] = _encode(self.field)
        return out


class PublicInputGateDecisionIssueCode(StrEnum):
    """The `PublicInputGateDecisionIssueCode` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    DESCRIPTION_MISSING = "description_missing"
    DESCRIPTION_PLACEHOLDER = "description_placeholder"
    DESCRIPTION_THIN = "description_thin"
    REPRODUCTION_MISSING = "reproduction_missing"
    REVIEW_TARGET_MISSING = "review_target_missing"
    SUCCESS_CRITERIA_MISSING = "success_criteria_missing"
    REQUIRED_FIELD_MISSING = "required_field_missing"


@dataclass(frozen=True, slots=True)
class PublicInputGateDecisionIssueField:
    """`PublicInputGateDecisionIssueField`, as carried on the wire."""

    key: str
    label: str

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "PublicInputGateDecisionIssueField":
        """Decode a `PublicInputGateDecisionIssueField` from its JSON object."""
        known = {"key", "label"}
        return cls(
            key=data.get("key"),
            label=data.get("label"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["key"] = self.key
        out["label"] = self.label
        return out


class PublicInputGateDecisionIssueSeverity(StrEnum):
    """The `PublicInputGateDecisionIssueSeverity` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    BLOCKING = "blocking"
    ADVISORY = "advisory"


class PublicInputGateDecisionMode(StrEnum):
    """The `PublicInputGateDecisionMode` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    STANDARD = "standard"
    ADVISORY = "advisory"
    OFF = "off"


class PublicInputGateDecisionStatus(StrEnum):
    """The `PublicInputGateDecisionStatus` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    OFF = "off"
    NOT_APPLICABLE = "not_applicable"
    PASSED = "passed"
    BLOCKED = "blocked"
    OVERRIDDEN = "overridden"


@dataclass(frozen=True, slots=True)
class PublicInterviewDecision:
    """`PublicInterviewDecision`, as carried on the wire."""

    kind: str
    max_rounds: float
    questions: list[PublicInterviewQuestion]
    round: float
    step_kind: str
    task_id: str

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "PublicInterviewDecision":
        """Decode a `PublicInterviewDecision` from its JSON object."""
        known = {"kind", "maxRounds", "questions", "round", "stepKind", "taskId"}
        return cls(
            kind=data.get("kind"),
            max_rounds=data.get("maxRounds"),
            questions=[PublicInterviewQuestion.from_dict(item) for item in data.get("questions") or []],
            round=data.get("round"),
            step_kind=data.get("stepKind"),
            task_id=data.get("taskId"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["kind"] = self.kind
        out["maxRounds"] = self.max_rounds
        out["questions"] = [_encode(item) for item in self.questions]
        out["round"] = self.round
        out["stepKind"] = self.step_kind
        out["taskId"] = self.task_id
        return out


@dataclass(frozen=True, slots=True)
class PublicInterviewQuestion:
    """`PublicInterviewQuestion`, as carried on the wire."""

    answer: str
    question: str
    status: PublicInterviewQuestionStatus
    #: Always present; ``None`` when the server has no value for it.
    question_id: str | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "PublicInterviewQuestion":
        """Decode a `PublicInterviewQuestion` from its JSON object."""
        known = {"answer", "question", "status", "questionId"}
        return cls(
            answer=data.get("answer"),
            question=data.get("question"),
            status=_enum(PublicInterviewQuestionStatus, data.get("status")),
            question_id=data.get("questionId"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["answer"] = self.answer
        out["question"] = self.question
        out["status"] = _encode(self.status)
        out["questionId"] = self.question_id
        return out


class PublicInterviewQuestionStatus(StrEnum):
    """The `PublicInterviewQuestionStatus` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    OPEN = "open"
    ANSWERED = "answered"
    DISMISSED = "dismissed"


@dataclass(frozen=True, slots=True)
class PublicJob:
    """`PublicJob`, as carried on the wire."""

    created_at: float
    external_identity_withheld: bool
    job_id: str
    pipeline_id: str
    status: PublicJobStatus
    #: Always present; ``None`` when the server has no value for it.
    error: RunError | None = None
    #: Always present; ``None`` when the server has no value for it.
    external_identity: str | None = None
    #: Always present; ``None`` when the server has no value for it.
    result: PublicJobResult | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "PublicJob":
        """Decode a `PublicJob` from its JSON object."""
        known = {"createdAt", "externalIdentityWithheld", "jobId", "pipelineId", "status", "error", "externalIdentity", "result"}
        return cls(
            created_at=data.get("createdAt"),
            external_identity_withheld=data.get("externalIdentityWithheld"),
            job_id=data.get("jobId"),
            pipeline_id=data.get("pipelineId"),
            status=_enum(PublicJobStatus, data.get("status")),
            error=None if data.get("error") is None else RunError.from_dict(data.get("error")),
            external_identity=data.get("externalIdentity"),
            result=None if data.get("result") is None else PublicJobResult.from_dict(data.get("result")),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["createdAt"] = self.created_at
        out["externalIdentityWithheld"] = self.external_identity_withheld
        out["jobId"] = self.job_id
        out["pipelineId"] = self.pipeline_id
        out["status"] = _encode(self.status)
        out["error"] = _encode(self.error)
        out["externalIdentity"] = self.external_identity
        out["result"] = _encode(self.result)
        return out


@dataclass(frozen=True, slots=True)
class PublicJobAccepted:
    """`PublicJobAccepted`, as carried on the wire."""

    job_id: str
    links: PublicJobAcceptedLinks
    status: PublicJobStatus

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "PublicJobAccepted":
        """Decode a `PublicJobAccepted` from its JSON object."""
        known = {"jobId", "links", "status"}
        return cls(
            job_id=data.get("jobId"),
            links=PublicJobAcceptedLinks.from_dict(data.get("links")),
            status=_enum(PublicJobStatus, data.get("status")),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["jobId"] = self.job_id
        out["links"] = _encode(self.links)
        out["status"] = _encode(self.status)
        return out


@dataclass(frozen=True, slots=True)
class PublicJobAcceptedLinks:
    """`PublicJobAcceptedLinks`, as carried on the wire."""

    events: str
    self_: str

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "PublicJobAcceptedLinks":
        """Decode a `PublicJobAcceptedLinks` from its JSON object."""
        known = {"events", "self"}
        return cls(
            events=data.get("events"),
            self_=data.get("self"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["events"] = self.events
        out["self"] = self.self_
        return out


@dataclass(frozen=True, slots=True)
class PublicJobResult:
    """`PublicJobResult`, as carried on the wire."""

    output: str
    #: Always present; ``None`` when the server has no value for it.
    data: Any | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "PublicJobResult":
        """Decode a `PublicJobResult` from its JSON object."""
        known = {"output", "data"}
        return cls(
            output=data.get("output"),
            data=data.get("data"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["output"] = self.output
        out["data"] = self.data
        return out


class PublicJobStatus(StrEnum):
    """The `PublicJobStatus` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    RUNNING = "running"
    SUCCEEDED = "succeeded"
    FAILED = "failed"


@dataclass(frozen=True, slots=True)
class PublicJudgeDecision:
    """`PublicJudgeDecision`, as carried on the wire."""

    bounces: float
    kind: str
    max_bounces: float
    status: PublicJudgeDecisionStatus
    step_kind: str
    #: Always present; ``None`` when the server has no value for it.
    rubric_id: str | None = None
    #: Always present; ``None`` when the server has no value for it.
    rubric_name: str | None = None
    #: Always present; ``None`` when the server has no value for it.
    threshold: float | None = None
    #: Always present; ``None`` when the server has no value for it.
    verdict: PublicJudgeVerdict | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "PublicJudgeDecision":
        """Decode a `PublicJudgeDecision` from its JSON object."""
        known = {"bounces", "kind", "maxBounces", "status", "stepKind", "rubricId", "rubricName", "threshold", "verdict"}
        return cls(
            bounces=data.get("bounces"),
            kind=data.get("kind"),
            max_bounces=data.get("maxBounces"),
            status=_enum(PublicJudgeDecisionStatus, data.get("status")),
            step_kind=data.get("stepKind"),
            rubric_id=data.get("rubricId"),
            rubric_name=data.get("rubricName"),
            threshold=data.get("threshold"),
            verdict=None if data.get("verdict") is None else PublicJudgeVerdict.from_dict(data.get("verdict")),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["bounces"] = self.bounces
        out["kind"] = self.kind
        out["maxBounces"] = self.max_bounces
        out["status"] = _encode(self.status)
        out["stepKind"] = self.step_kind
        out["rubricId"] = self.rubric_id
        out["rubricName"] = self.rubric_name
        out["threshold"] = self.threshold
        out["verdict"] = _encode(self.verdict)
        return out


class PublicJudgeDecisionStatus(StrEnum):
    """The `PublicJudgeDecisionStatus` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    EVALUATING = "evaluating"
    AWAITING_DECISION = "awaiting_decision"
    BOUNCING = "bouncing"
    PASSED = "passed"
    FAILED = "failed"
    SKIPPED = "skipped"


@dataclass(frozen=True, slots=True)
class PublicJudgeFinding:
    """`PublicJudgeFinding`, as carried on the wire."""

    severity: Severity
    title: str
    #: May be absent entirely.
    detail: str | None = None
    #: May be absent entirely.
    where: str | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "PublicJudgeFinding":
        """Decode a `PublicJudgeFinding` from its JSON object."""
        known = {"severity", "title", "detail", "where"}
        return cls(
            severity=_enum(Severity, data.get("severity")),
            title=data.get("title"),
            detail=data.get("detail"),
            where=data.get("where"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["severity"] = _encode(self.severity)
        out["title"] = self.title
        if self.detail is not None:
            out["detail"] = self.detail
        if self.where is not None:
            out["where"] = self.where
        return out


@dataclass(frozen=True, slots=True)
class PublicJudgeVerdict:
    """`PublicJudgeVerdict`, as carried on the wire."""

    findings: list[PublicJudgeFinding]
    score: float
    summary: str

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "PublicJudgeVerdict":
        """Decode a `PublicJudgeVerdict` from its JSON object."""
        known = {"findings", "score", "summary"}
        return cls(
            findings=[PublicJudgeFinding.from_dict(item) for item in data.get("findings") or []],
            score=data.get("score"),
            summary=data.get("summary"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["findings"] = [_encode(item) for item in self.findings]
        out["score"] = self.score
        out["summary"] = self.summary
        return out


@dataclass(frozen=True, slots=True)
class PublicNotificationList:
    """`PublicNotificationList`, as carried on the wire."""

    notifications: list[Notification]

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "PublicNotificationList":
        """Decode a `PublicNotificationList` from its JSON object."""
        known = {"notifications"}
        return cls(
            notifications=[Notification.from_dict(item) for item in data.get("notifications") or []],
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["notifications"] = [_encode(item) for item in self.notifications]
        return out


@dataclass(frozen=True, slots=True)
class PublicNotificationWebhook:
    """`PublicNotificationWebhook`, as carried on the wire."""

    #: Always present; ``None`` when the server has no value for it.
    webhook: NotificationWebhook | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "PublicNotificationWebhook":
        """Decode a `PublicNotificationWebhook` from its JSON object."""
        known = {"webhook"}
        return cls(
            webhook=None if data.get("webhook") is None else NotificationWebhook.from_dict(data.get("webhook")),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["webhook"] = _encode(self.webhook)
        return out


@dataclass(frozen=True, slots=True)
class PublicNotificationWebhookList:
    """`PublicNotificationWebhookList`, as carried on the wire."""

    webhooks: list[NotificationWebhook]

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "PublicNotificationWebhookList":
        """Decode a `PublicNotificationWebhookList` from its JSON object."""
        known = {"webhooks"}
        return cls(
            webhooks=[NotificationWebhook.from_dict(item) for item in data.get("webhooks") or []],
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["webhooks"] = [_encode(item) for item in self.webhooks]
        return out


@dataclass(frozen=True, slots=True)
class PublicPipeline:
    """`PublicPipeline`, as carried on the wire."""

    headless_startable: bool
    name: str
    pipeline_id: str
    public: bool
    steps: list[str]

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "PublicPipeline":
        """Decode a `PublicPipeline` from its JSON object."""
        known = {"headlessStartable", "name", "pipelineId", "public", "steps"}
        return cls(
            headless_startable=data.get("headlessStartable"),
            name=data.get("name"),
            pipeline_id=data.get("pipelineId"),
            public=data.get("public"),
            steps=[item for item in data.get("steps") or []],
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["headlessStartable"] = self.headless_startable
        out["name"] = self.name
        out["pipelineId"] = self.pipeline_id
        out["public"] = self.public
        out["steps"] = [_encode(item) for item in self.steps]
        return out


@dataclass(frozen=True, slots=True)
class PublicPipelineList:
    """`PublicPipelineList`, as carried on the wire."""

    pipelines: list[PublicPipeline]

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "PublicPipelineList":
        """Decode a `PublicPipelineList` from its JSON object."""
        known = {"pipelines"}
        return cls(
            pipelines=[PublicPipeline.from_dict(item) for item in data.get("pipelines") or []],
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["pipelines"] = [_encode(item) for item in self.pipelines]
        return out


@dataclass(frozen=True, slots=True)
class PublicPrReviewDecision:
    """`PublicPrReviewDecision`, as carried on the wire."""

    findings: list[PublicPrReviewDecisionFinding]
    kind: str
    selected_finding_ids: list[str]
    slices: list[PublicPrReviewDecisionSlice]
    status: PublicPrReviewDecisionStatus
    #: Always present; ``None`` when the server has no value for it.
    pr_url: str | None = None
    #: Always present; ``None`` when the server has no value for it.
    summary: str | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "PublicPrReviewDecision":
        """Decode a `PublicPrReviewDecision` from its JSON object."""
        known = {"findings", "kind", "selectedFindingIds", "slices", "status", "prUrl", "summary"}
        return cls(
            findings=[PublicPrReviewDecisionFinding.from_dict(item) for item in data.get("findings") or []],
            kind=data.get("kind"),
            selected_finding_ids=[item for item in data.get("selectedFindingIds") or []],
            slices=[PublicPrReviewDecisionSlice.from_dict(item) for item in data.get("slices") or []],
            status=_enum(PublicPrReviewDecisionStatus, data.get("status")),
            pr_url=data.get("prUrl"),
            summary=data.get("summary"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["findings"] = [_encode(item) for item in self.findings]
        out["kind"] = self.kind
        out["selectedFindingIds"] = [_encode(item) for item in self.selected_finding_ids]
        out["slices"] = [_encode(item) for item in self.slices]
        out["status"] = _encode(self.status)
        out["prUrl"] = self.pr_url
        out["summary"] = self.summary
        return out


@dataclass(frozen=True, slots=True)
class PublicPrReviewDecisionFinding:
    """`PublicPrReviewDecisionFinding`, as carried on the wire."""

    category: PublicPrReviewDecisionFindingCategory
    detail: str
    finding_id: str
    path: str
    severity: PublicPrReviewDecisionFindingSeverity
    title: str
    #: Always present; ``None`` when the server has no value for it.
    challenge: PublicPrReviewDecisionFindingChallenge | None = None
    #: Always present; ``None`` when the server has no value for it.
    line: float | None = None
    #: Always present; ``None`` when the server has no value for it.
    side: PublicPrReviewDecisionFindingSide | None = None
    #: Always present; ``None`` when the server has no value for it.
    slice_id: str | None = None
    #: Always present; ``None`` when the server has no value for it.
    suggested_fix: str | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "PublicPrReviewDecisionFinding":
        """Decode a `PublicPrReviewDecisionFinding` from its JSON object."""
        known = {"category", "detail", "findingId", "path", "severity", "title", "challenge", "line", "side", "sliceId", "suggestedFix"}
        return cls(
            category=_enum(PublicPrReviewDecisionFindingCategory, data.get("category")),
            detail=data.get("detail"),
            finding_id=data.get("findingId"),
            path=data.get("path"),
            severity=_enum(PublicPrReviewDecisionFindingSeverity, data.get("severity")),
            title=data.get("title"),
            challenge=None if data.get("challenge") is None else PublicPrReviewDecisionFindingChallenge.from_dict(data.get("challenge")),
            line=data.get("line"),
            side=None if data.get("side") is None else _enum(PublicPrReviewDecisionFindingSide, data.get("side")),
            slice_id=data.get("sliceId"),
            suggested_fix=data.get("suggestedFix"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["category"] = _encode(self.category)
        out["detail"] = self.detail
        out["findingId"] = self.finding_id
        out["path"] = self.path
        out["severity"] = _encode(self.severity)
        out["title"] = self.title
        out["challenge"] = _encode(self.challenge)
        out["line"] = self.line
        out["side"] = _encode(self.side)
        out["sliceId"] = self.slice_id
        out["suggestedFix"] = self.suggested_fix
        return out


class PublicPrReviewDecisionFindingCategory(StrEnum):
    """The `PublicPrReviewDecisionFindingCategory` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    CORRECTNESS = "correctness"
    SECURITY = "security"
    PERFORMANCE = "performance"
    MAINTAINABILITY = "maintainability"
    STYLE = "style"
    TEST = "test"
    OTHER = "other"


@dataclass(frozen=True, slots=True)
class PublicPrReviewDecisionFindingChallenge:
    """`PublicPrReviewDecisionFindingChallenge`, as carried on the wire."""

    status: PublicPrReviewDecisionFindingChallengeStatus
    #: Always present; ``None`` when the server has no value for it.
    justification: str | None = None
    #: Always present; ``None`` when the server has no value for it.
    question: str | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "PublicPrReviewDecisionFindingChallenge":
        """Decode a `PublicPrReviewDecisionFindingChallenge` from its JSON object."""
        known = {"status", "justification", "question"}
        return cls(
            status=_enum(PublicPrReviewDecisionFindingChallengeStatus, data.get("status")),
            justification=data.get("justification"),
            question=data.get("question"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["status"] = _encode(self.status)
        out["justification"] = self.justification
        out["question"] = self.question
        return out


class PublicPrReviewDecisionFindingChallengeStatus(StrEnum):
    """The `PublicPrReviewDecisionFindingChallengeStatus` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    INVESTIGATING = "investigating"
    UPHELD = "upheld"
    AMENDED = "amended"
    RETRACTED = "retracted"
    FAILED = "failed"


class PublicPrReviewDecisionFindingSeverity(StrEnum):
    """The `PublicPrReviewDecisionFindingSeverity` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    BLOCKER = "blocker"
    HIGH = "high"
    MEDIUM = "medium"
    LOW = "low"
    NIT = "nit"


class PublicPrReviewDecisionFindingSide(StrEnum):
    """The `PublicPrReviewDecisionFindingSide` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    LEFT = "LEFT"
    RIGHT = "RIGHT"


@dataclass(frozen=True, slots=True)
class PublicPrReviewDecisionSlice:
    """`PublicPrReviewDecisionSlice`, as carried on the wire."""

    paths: list[str]
    rationale: str
    slice_id: str
    title: str

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "PublicPrReviewDecisionSlice":
        """Decode a `PublicPrReviewDecisionSlice` from its JSON object."""
        known = {"paths", "rationale", "sliceId", "title"}
        return cls(
            paths=[item for item in data.get("paths") or []],
            rationale=data.get("rationale"),
            slice_id=data.get("sliceId"),
            title=data.get("title"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["paths"] = [_encode(item) for item in self.paths]
        out["rationale"] = self.rationale
        out["sliceId"] = self.slice_id
        out["title"] = self.title
        return out


class PublicPrReviewDecisionStatus(StrEnum):
    """The `PublicPrReviewDecisionStatus` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    REVIEWING = "reviewing"
    AWAITING_SELECTION = "awaiting_selection"
    CHALLENGING = "challenging"
    FIXING = "fixing"
    POSTING = "posting"
    DONE = "done"
    SKIPPED = "skipped"


@dataclass(frozen=True, slots=True)
class PublicRejectStep:
    """`PublicRejectStep`, as carried on the wire."""

    #: May be absent entirely.
    reason: str | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "PublicRejectStep":
        """Decode a `PublicRejectStep` from its JSON object."""
        known = {"reason"}
        return cls(
            reason=data.get("reason"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        if self.reason is not None:
            out["reason"] = self.reason
        return out


@dataclass(frozen=True, slots=True)
class PublicReplyFinding:
    """`PublicReplyFinding`, as carried on the wire."""

    reply: str

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "PublicReplyFinding":
        """Decode a `PublicReplyFinding` from its JSON object."""
        known = {"reply"}
        return cls(
            reply=data.get("reply"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["reply"] = self.reply
        return out


@dataclass(frozen=True, slots=True)
class PublicRequestGateFix:
    """`PublicRequestGateFix`, as carried on the wire."""

    findings: str

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "PublicRequestGateFix":
        """Decode a `PublicRequestGateFix` from its JSON object."""
        known = {"findings"}
        return cls(
            findings=data.get("findings"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["findings"] = self.findings
        return out


@dataclass(frozen=True, slots=True)
class PublicRequestStepChanges:
    """`PublicRequestStepChanges`, as carried on the wire."""

    feedback: str

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "PublicRequestStepChanges":
        """Decode a `PublicRequestStepChanges` from its JSON object."""
        known = {"feedback"}
        return cls(
            feedback=data.get("feedback"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["feedback"] = self.feedback
        return out


@dataclass(frozen=True, slots=True)
class PublicRequirementsDecision:
    """`PublicRequirementsDecision`, as carried on the wire."""

    findings: list[PublicReviewFinding]
    iteration: float
    kind: str
    max_iterations: float
    review_id: str
    status: PublicRequirementsDecisionStatus
    task_id: str
    #: Always present; ``None`` when the server has no value for it.
    incorporated_requirements: str | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "PublicRequirementsDecision":
        """Decode a `PublicRequirementsDecision` from its JSON object."""
        known = {"findings", "iteration", "kind", "maxIterations", "reviewId", "status", "taskId", "incorporatedRequirements"}
        return cls(
            findings=[PublicReviewFinding.from_dict(item) for item in data.get("findings") or []],
            iteration=data.get("iteration"),
            kind=data.get("kind"),
            max_iterations=data.get("maxIterations"),
            review_id=data.get("reviewId"),
            status=_enum(PublicRequirementsDecisionStatus, data.get("status")),
            task_id=data.get("taskId"),
            incorporated_requirements=data.get("incorporatedRequirements"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["findings"] = [_encode(item) for item in self.findings]
        out["iteration"] = self.iteration
        out["kind"] = self.kind
        out["maxIterations"] = self.max_iterations
        out["reviewId"] = self.review_id
        out["status"] = _encode(self.status)
        out["taskId"] = self.task_id
        out["incorporatedRequirements"] = self.incorporated_requirements
        return out


class PublicRequirementsDecisionStatus(StrEnum):
    """The `PublicRequirementsDecisionStatus` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    READY = "ready"
    INCORPORATING = "incorporating"
    REVIEWING = "reviewing"
    MERGED = "merged"
    EXCEEDED = "exceeded"
    INCORPORATED = "incorporated"


@dataclass(frozen=True, slots=True)
class PublicResolveAgentDecision:
    """`PublicResolveAgentDecision`, as carried on the wire."""

    choice: str

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "PublicResolveAgentDecision":
        """Decode a `PublicResolveAgentDecision` from its JSON object."""
        known = {"choice"}
        return cls(
            choice=data.get("choice"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["choice"] = self.choice
        return out


@dataclass(frozen=True, slots=True)
class PublicResolveExceeded:
    """`PublicResolveExceeded`, as carried on the wire."""

    choice: PublicResolveExceededChoice

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "PublicResolveExceeded":
        """Decode a `PublicResolveExceeded` from its JSON object."""
        known = {"choice"}
        return cls(
            choice=_enum(PublicResolveExceededChoice, data.get("choice")),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["choice"] = _encode(self.choice)
        return out


class PublicResolveExceededChoice(StrEnum):
    """The `PublicResolveExceededChoice` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    EXTRA_ROUND = "extra-round"
    PROCEED = "proceed"
    STOP_RESET = "stop-reset"


@dataclass(frozen=True, slots=True)
class PublicResolveInputGate:
    """`PublicResolveInputGate`, as carried on the wire."""

    choice: PublicResolveInputGateChoice

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "PublicResolveInputGate":
        """Decode a `PublicResolveInputGate` from its JSON object."""
        known = {"choice"}
        return cls(
            choice=_enum(PublicResolveInputGateChoice, data.get("choice")),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["choice"] = _encode(self.choice)
        return out


class PublicResolveInputGateChoice(StrEnum):
    """The `PublicResolveInputGateChoice` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    RECHECK = "recheck"
    PROCEED = "proceed"


@dataclass(frozen=True, slots=True)
class PublicResolveJudge:
    """`PublicResolveJudge`, as carried on the wire."""

    choice: PublicResolveJudgeChoice
    #: May be absent entirely.
    feedback: str | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "PublicResolveJudge":
        """Decode a `PublicResolveJudge` from its JSON object."""
        known = {"choice", "feedback"}
        return cls(
            choice=_enum(PublicResolveJudgeChoice, data.get("choice")),
            feedback=data.get("feedback"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["choice"] = _encode(self.choice)
        if self.feedback is not None:
            out["feedback"] = self.feedback
        return out


class PublicResolveJudgeChoice(StrEnum):
    """The `PublicResolveJudgeChoice` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    PROCEED = "proceed"
    BOUNCE = "bounce"
    STOP = "stop"


@dataclass(frozen=True, slots=True)
class PublicResolvePrReview:
    """`PublicResolvePrReview`, as carried on the wire."""

    #: May be absent entirely.
    action: PublicResolvePrReviewAction | None = None
    #: May be absent entirely.
    finding_ids: list[str] | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "PublicResolvePrReview":
        """Decode a `PublicResolvePrReview` from its JSON object."""
        known = {"action", "findingIds"}
        return cls(
            action=None if data.get("action") is None else _enum(PublicResolvePrReviewAction, data.get("action")),
            finding_ids=None if data.get("findingIds") is None else [item for item in data.get("findingIds") or []],
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        if self.action is not None:
            out["action"] = _encode(self.action)
        if self.finding_ids is not None:
            out["findingIds"] = [_encode(item) for item in self.finding_ids]
        return out


class PublicResolvePrReviewAction(StrEnum):
    """The `PublicResolvePrReviewAction` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    FINISH = "finish"
    FIX = "fix"
    POST = "post"


@dataclass(frozen=True, slots=True)
class PublicReviewFinding:
    """`PublicReviewFinding`, as carried on the wire."""

    category: PublicReviewFindingCategory
    detail: str
    item_id: str
    severity: PublicReviewFindingSeverity
    status: PublicReviewFindingStatus
    title: str
    #: Always present; ``None`` when the server has no value for it.
    reply: str | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "PublicReviewFinding":
        """Decode a `PublicReviewFinding` from its JSON object."""
        known = {"category", "detail", "itemId", "severity", "status", "title", "reply"}
        return cls(
            category=_enum(PublicReviewFindingCategory, data.get("category")),
            detail=data.get("detail"),
            item_id=data.get("itemId"),
            severity=_enum(PublicReviewFindingSeverity, data.get("severity")),
            status=_enum(PublicReviewFindingStatus, data.get("status")),
            title=data.get("title"),
            reply=data.get("reply"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["category"] = _encode(self.category)
        out["detail"] = self.detail
        out["itemId"] = self.item_id
        out["severity"] = _encode(self.severity)
        out["status"] = _encode(self.status)
        out["title"] = self.title
        out["reply"] = self.reply
        return out


class PublicReviewFindingCategory(StrEnum):
    """The `PublicReviewFindingCategory` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    GAP = "gap"
    CLARIFICATION = "clarification"
    ASSUMPTION = "assumption"
    RISK = "risk"
    QUESTION = "question"


class PublicReviewFindingSeverity(StrEnum):
    """The `PublicReviewFindingSeverity` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    LOW = "low"
    MEDIUM = "medium"
    HIGH = "high"


class PublicReviewFindingStatus(StrEnum):
    """The `PublicReviewFindingStatus` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    OPEN = "open"
    ANSWERED = "answered"
    RESOLVED = "resolved"
    DISMISSED = "dismissed"
    RECOMMEND_REQUESTED = "recommend_requested"


@dataclass(frozen=True, slots=True)
class PublicRun:
    """`PublicRun`, as carried on the wire."""

    created_at: float
    current_step: float
    external_identity_withheld: bool
    run_id: str
    status: RunStatus
    steps: list[PublicRunStep]
    task_id: str
    #: Always present; ``None`` when the server has no value for it.
    error: RunError | None = None
    #: Always present; ``None`` when the server has no value for it.
    external_identity: str | None = None
    #: Always present; ``None`` when the server has no value for it.
    pull_request: RunPullRequest | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "PublicRun":
        """Decode a `PublicRun` from its JSON object."""
        known = {"createdAt", "currentStep", "externalIdentityWithheld", "runId", "status", "steps", "taskId", "error", "externalIdentity", "pullRequest"}
        return cls(
            created_at=data.get("createdAt"),
            current_step=data.get("currentStep"),
            external_identity_withheld=data.get("externalIdentityWithheld"),
            run_id=data.get("runId"),
            status=_enum(RunStatus, data.get("status")),
            steps=[PublicRunStep.from_dict(item) for item in data.get("steps") or []],
            task_id=data.get("taskId"),
            error=None if data.get("error") is None else RunError.from_dict(data.get("error")),
            external_identity=data.get("externalIdentity"),
            pull_request=None if data.get("pullRequest") is None else RunPullRequest.from_dict(data.get("pullRequest")),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["createdAt"] = self.created_at
        out["currentStep"] = self.current_step
        out["externalIdentityWithheld"] = self.external_identity_withheld
        out["runId"] = self.run_id
        out["status"] = _encode(self.status)
        out["steps"] = [_encode(item) for item in self.steps]
        out["taskId"] = self.task_id
        out["error"] = _encode(self.error)
        out["externalIdentity"] = self.external_identity
        out["pullRequest"] = _encode(self.pull_request)
        return out


@dataclass(frozen=True, slots=True)
class PublicRunArtifact:
    """`PublicRunArtifact`, as carried on the wire."""

    artifact_id: str
    byte_size: float
    content_type: str
    created_at: float
    hash: str
    kind: PublicRunArtifactKind
    scope: PublicRunArtifactScope
    #: Always present; ``None`` when the server has no value for it.
    view: str | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "PublicRunArtifact":
        """Decode a `PublicRunArtifact` from its JSON object."""
        known = {"artifactId", "byteSize", "contentType", "createdAt", "hash", "kind", "scope", "view"}
        return cls(
            artifact_id=data.get("artifactId"),
            byte_size=data.get("byteSize"),
            content_type=data.get("contentType"),
            created_at=data.get("createdAt"),
            hash=data.get("hash"),
            kind=_enum(PublicRunArtifactKind, data.get("kind")),
            scope=_enum(PublicRunArtifactScope, data.get("scope")),
            view=data.get("view"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["artifactId"] = self.artifact_id
        out["byteSize"] = self.byte_size
        out["contentType"] = self.content_type
        out["createdAt"] = self.created_at
        out["hash"] = self.hash
        out["kind"] = _encode(self.kind)
        out["scope"] = _encode(self.scope)
        out["view"] = self.view
        return out


class PublicRunArtifactKind(StrEnum):
    """The `PublicRunArtifactKind` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    SCREENSHOT = "screenshot"
    REFERENCE = "reference"


@dataclass(frozen=True, slots=True)
class PublicRunArtifactList:
    """`PublicRunArtifactList`, as carried on the wire."""

    artifacts: list[PublicRunArtifact]

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "PublicRunArtifactList":
        """Decode a `PublicRunArtifactList` from its JSON object."""
        known = {"artifacts"}
        return cls(
            artifacts=[PublicRunArtifact.from_dict(item) for item in data.get("artifacts") or []],
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["artifacts"] = [_encode(item) for item in self.artifacts]
        return out


class PublicRunArtifactScope(StrEnum):
    """The `PublicRunArtifactScope` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    RUN = "run"
    TASK = "task"


@dataclass(frozen=True, slots=True)
class PublicRunStep:
    """`PublicRunStep`, as carried on the wire."""

    agent_kind: str
    progress: float
    state: StepState
    #: Always present; ``None`` when the server has no value for it.
    data: Any | None = None
    #: Always present; ``None`` when the server has no value for it.
    output: str | None = None
    #: Always present; ``None`` when the server has no value for it.
    subtasks: RunSubtaskCounts | None = None
    #: May be absent entirely.
    truncated: bool | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "PublicRunStep":
        """Decode a `PublicRunStep` from its JSON object."""
        known = {"agentKind", "progress", "state", "data", "output", "subtasks", "truncated"}
        return cls(
            agent_kind=data.get("agentKind"),
            progress=data.get("progress"),
            state=_enum(StepState, data.get("state")),
            data=data.get("data"),
            output=data.get("output"),
            subtasks=None if data.get("subtasks") is None else RunSubtaskCounts.from_dict(data.get("subtasks")),
            truncated=data.get("truncated"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["agentKind"] = self.agent_kind
        out["progress"] = self.progress
        out["state"] = _encode(self.state)
        out["data"] = self.data
        out["output"] = self.output
        out["subtasks"] = _encode(self.subtasks)
        if self.truncated is not None:
            out["truncated"] = self.truncated
        return out


@dataclass(frozen=True, slots=True)
class PublicService:
    """`PublicService`, as carried on the wire."""

    description: str
    service_id: str
    status: TaskStatus
    title: str
    type_: PublicServiceType

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "PublicService":
        """Decode a `PublicService` from its JSON object."""
        known = {"description", "serviceId", "status", "title", "type"}
        return cls(
            description=data.get("description"),
            service_id=data.get("serviceId"),
            status=_enum(TaskStatus, data.get("status")),
            title=data.get("title"),
            type_=_enum(PublicServiceType, data.get("type")),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["description"] = self.description
        out["serviceId"] = self.service_id
        out["status"] = _encode(self.status)
        out["title"] = self.title
        out["type"] = _encode(self.type_)
        return out


@dataclass(frozen=True, slots=True)
class PublicServiceList:
    """`PublicServiceList`, as carried on the wire."""

    services: list[PublicService]

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "PublicServiceList":
        """Decode a `PublicServiceList` from its JSON object."""
        known = {"services"}
        return cls(
            services=[PublicService.from_dict(item) for item in data.get("services") or []],
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["services"] = [_encode(item) for item in self.services]
        return out


class PublicServiceType(StrEnum):
    """The `PublicServiceType` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    FRONTEND = "frontend"
    SERVICE = "service"
    LIBRARY = "library"
    DOCUMENT = "document"
    API = "api"
    DATABASE = "database"
    QUEUE = "queue"
    INTEGRATION = "integration"
    EXTERNAL = "external"
    ENVIRONMENT = "environment"


@dataclass(frozen=True, slots=True)
class PublicSetFindingStatus:
    """`PublicSetFindingStatus`, as carried on the wire."""

    status: PublicSetFindingStatusStatus

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "PublicSetFindingStatus":
        """Decode a `PublicSetFindingStatus` from its JSON object."""
        known = {"status"}
        return cls(
            status=_enum(PublicSetFindingStatusStatus, data.get("status")),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["status"] = _encode(self.status)
        return out


class PublicSetFindingStatusStatus(StrEnum):
    """The `PublicSetFindingStatusStatus` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    DISMISSED = "dismissed"
    OPEN = "open"


@dataclass(frozen=True, slots=True)
class PublicTask:
    """`PublicTask`, as carried on the wire."""

    auto_start_dependents: bool
    depends_on: list[str]
    description: str
    progress: float
    service_id: str
    status: TaskStatus
    task_id: str
    task_type: str
    title: str
    #: Always present; ``None`` when the server has no value for it.
    pull_request_url: str | None = None
    #: Always present; ``None`` when the server has no value for it.
    run_id: str | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "PublicTask":
        """Decode a `PublicTask` from its JSON object."""
        known = {"autoStartDependents", "dependsOn", "description", "progress", "serviceId", "status", "taskId", "taskType", "title", "pullRequestUrl", "runId"}
        return cls(
            auto_start_dependents=data.get("autoStartDependents"),
            depends_on=[item for item in data.get("dependsOn") or []],
            description=data.get("description"),
            progress=data.get("progress"),
            service_id=data.get("serviceId"),
            status=_enum(TaskStatus, data.get("status")),
            task_id=data.get("taskId"),
            task_type=data.get("taskType"),
            title=data.get("title"),
            pull_request_url=data.get("pullRequestUrl"),
            run_id=data.get("runId"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["autoStartDependents"] = self.auto_start_dependents
        out["dependsOn"] = [_encode(item) for item in self.depends_on]
        out["description"] = self.description
        out["progress"] = self.progress
        out["serviceId"] = self.service_id
        out["status"] = _encode(self.status)
        out["taskId"] = self.task_id
        out["taskType"] = self.task_type
        out["title"] = self.title
        out["pullRequestUrl"] = self.pull_request_url
        out["runId"] = self.run_id
        return out


@dataclass(frozen=True, slots=True)
class PublicTaskList:
    """`PublicTaskList`, as carried on the wire."""

    tasks: list[PublicTask]
    #: Always present; ``None`` when the server has no value for it.
    next_cursor: str | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "PublicTaskList":
        """Decode a `PublicTaskList` from its JSON object."""
        known = {"tasks", "nextCursor"}
        return cls(
            tasks=[PublicTask.from_dict(item) for item in data.get("tasks") or []],
            next_cursor=data.get("nextCursor"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["tasks"] = [_encode(item) for item in self.tasks]
        out["nextCursor"] = self.next_cursor
        return out


@dataclass(frozen=True, slots=True)
class PublicTaskSourceDocument:
    """`PublicTaskSourceDocument`, as carried on the wire."""

    kind: str
    ref: str
    source: PublicTaskSourceDocumentSource

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "PublicTaskSourceDocument":
        """Decode a `PublicTaskSourceDocument` from its JSON object."""
        known = {"kind", "ref", "source"}
        return cls(
            kind=data.get("kind"),
            ref=data.get("ref"),
            source=_enum(PublicTaskSourceDocumentSource, data.get("source")),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["kind"] = self.kind
        out["ref"] = self.ref
        out["source"] = _encode(self.source)
        return out


class PublicTaskSourceDocumentSource(StrEnum):
    """The `PublicTaskSourceDocumentSource` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    CONFLUENCE = "confluence"
    NOTION = "notion"
    GITHUB = "github"
    FIGMA = "figma"
    ZEPLIN = "zeplin"
    LINEAR = "linear"


@dataclass(frozen=True, slots=True)
class PublicTaskTicket:
    """`PublicTaskTicket`, as carried on the wire."""

    ref: str
    source: str

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "PublicTaskTicket":
        """Decode a `PublicTaskTicket` from its JSON object."""
        known = {"ref", "source"}
        return cls(
            ref=data.get("ref"),
            source=data.get("source"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["ref"] = self.ref
        out["source"] = self.source
        return out


@dataclass(frozen=True, slots=True)
class PublicTaskUploadedDocument:
    """`PublicTaskUploadedDocument`, as carried on the wire."""

    content: str
    kind: str
    title: str

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "PublicTaskUploadedDocument":
        """Decode a `PublicTaskUploadedDocument` from its JSON object."""
        known = {"content", "kind", "title"}
        return cls(
            content=data.get("content"),
            kind=data.get("kind"),
            title=data.get("title"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["content"] = self.content
        out["kind"] = self.kind
        out["title"] = self.title
        return out


@dataclass(frozen=True, slots=True)
class PublicUnanswerableWait:
    """`PublicUnanswerableWait`, as carried on the wire."""

    detail: str
    reason: PublicUnanswerableWaitReason
    step_index: float
    step_kind: str

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "PublicUnanswerableWait":
        """Decode a `PublicUnanswerableWait` from its JSON object."""
        known = {"detail", "reason", "stepIndex", "stepKind"}
        return cls(
            detail=data.get("detail"),
            reason=_enum(PublicUnanswerableWaitReason, data.get("reason")),
            step_index=data.get("stepIndex"),
            step_kind=data.get("stepKind"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["detail"] = self.detail
        out["reason"] = _encode(self.reason)
        out["stepIndex"] = self.step_index
        out["stepKind"] = self.step_kind
        return out


class PublicUnanswerableWaitReason(StrEnum):
    """The `PublicUnanswerableWaitReason` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    HUMAN_WAIT_GATE = "human_wait_gate"
    UNCLASSIFIED_GATE = "unclassified_gate"
    UNWIRED_INTERVIEW_GATE = "unwired_interview_gate"


@dataclass(frozen=True, slots=True)
class PublicUsage:
    """`PublicUsage`, as carried on the wire."""

    budget: PublicUsageBudget
    currency: str
    period_start: float
    rows: list[PublicUsageRow]

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "PublicUsage":
        """Decode a `PublicUsage` from its JSON object."""
        known = {"budget", "currency", "periodStart", "rows"}
        return cls(
            budget=PublicUsageBudget.from_dict(data.get("budget")),
            currency=data.get("currency"),
            period_start=data.get("periodStart"),
            rows=[PublicUsageRow.from_dict(item) for item in data.get("rows") or []],
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["budget"] = _encode(self.budget)
        out["currency"] = self.currency
        out["periodStart"] = self.period_start
        out["rows"] = [_encode(item) for item in self.rows]
        return out


@dataclass(frozen=True, slots=True)
class PublicUsageBudget:
    """`PublicUsageBudget`, as carried on the wire."""

    cost_limit: float
    cost_spent: float
    exceeded: bool
    input_tokens: float
    output_tokens: float

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "PublicUsageBudget":
        """Decode a `PublicUsageBudget` from its JSON object."""
        known = {"costLimit", "costSpent", "exceeded", "inputTokens", "outputTokens"}
        return cls(
            cost_limit=data.get("costLimit"),
            cost_spent=data.get("costSpent"),
            exceeded=data.get("exceeded"),
            input_tokens=data.get("inputTokens"),
            output_tokens=data.get("outputTokens"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["costLimit"] = self.cost_limit
        out["costSpent"] = self.cost_spent
        out["exceeded"] = self.exceeded
        out["inputTokens"] = self.input_tokens
        out["outputTokens"] = self.output_tokens
        return out


@dataclass(frozen=True, slots=True)
class PublicUsageRow:
    """`PublicUsageRow`, as carried on the wire."""

    billing: PublicUsageRowBilling
    calls: float
    cost_estimate: float
    input_tokens: float
    model: str
    output_tokens: float
    provider: str
    #: Always present; ``None`` when the server has no value for it.
    vendor: str | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "PublicUsageRow":
        """Decode a `PublicUsageRow` from its JSON object."""
        known = {"billing", "calls", "costEstimate", "inputTokens", "model", "outputTokens", "provider", "vendor"}
        return cls(
            billing=_enum(PublicUsageRowBilling, data.get("billing")),
            calls=data.get("calls"),
            cost_estimate=data.get("costEstimate"),
            input_tokens=data.get("inputTokens"),
            model=data.get("model"),
            output_tokens=data.get("outputTokens"),
            provider=data.get("provider"),
            vendor=data.get("vendor"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["billing"] = _encode(self.billing)
        out["calls"] = self.calls
        out["costEstimate"] = self.cost_estimate
        out["inputTokens"] = self.input_tokens
        out["model"] = self.model
        out["outputTokens"] = self.output_tokens
        out["provider"] = self.provider
        out["vendor"] = self.vendor
        return out


class PublicUsageRowBilling(StrEnum):
    """The `PublicUsageRowBilling` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    METERED = "metered"
    SUBSCRIPTION = "subscription"


@dataclass(frozen=True, slots=True)
class PublicVisualConfirmDecision:
    """`PublicVisualConfirmDecision`, as carried on the wire."""

    attempts: float
    kind: str
    max_attempts: float
    pairs: list[PublicVisualConfirmDecisionPair]
    phase: PublicVisualConfirmDecisionPhase
    #: Always present; ``None`` when the server has no value for it.
    degraded_reason: str | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "PublicVisualConfirmDecision":
        """Decode a `PublicVisualConfirmDecision` from its JSON object."""
        known = {"attempts", "kind", "maxAttempts", "pairs", "phase", "degradedReason"}
        return cls(
            attempts=data.get("attempts"),
            kind=data.get("kind"),
            max_attempts=data.get("maxAttempts"),
            pairs=[PublicVisualConfirmDecisionPair.from_dict(item) for item in data.get("pairs") or []],
            phase=_enum(PublicVisualConfirmDecisionPhase, data.get("phase")),
            degraded_reason=data.get("degradedReason"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["attempts"] = self.attempts
        out["kind"] = self.kind
        out["maxAttempts"] = self.max_attempts
        out["pairs"] = [_encode(item) for item in self.pairs]
        out["phase"] = _encode(self.phase)
        out["degradedReason"] = self.degraded_reason
        return out


@dataclass(frozen=True, slots=True)
class PublicVisualConfirmDecisionPair:
    """`PublicVisualConfirmDecisionPair`, as carried on the wire."""

    view: str
    #: Always present; ``None`` when the server has no value for it.
    actual_artifact_id: str | None = None
    #: Always present; ``None`` when the server has no value for it.
    reference_artifact_id: str | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "PublicVisualConfirmDecisionPair":
        """Decode a `PublicVisualConfirmDecisionPair` from its JSON object."""
        known = {"view", "actualArtifactId", "referenceArtifactId"}
        return cls(
            view=data.get("view"),
            actual_artifact_id=data.get("actualArtifactId"),
            reference_artifact_id=data.get("referenceArtifactId"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["view"] = self.view
        out["actualArtifactId"] = self.actual_artifact_id
        out["referenceArtifactId"] = self.reference_artifact_id
        return out


class PublicVisualConfirmDecisionPhase(StrEnum):
    """The `PublicVisualConfirmDecisionPhase` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    AWAITING_HUMAN = "awaiting_human"
    FIXING = "fixing"
    APPROVED = "approved"


@dataclass(frozen=True, slots=True)
class PutNotificationWebhook:
    """`PutNotificationWebhook`, as carried on the wire."""

    #: May be absent entirely.
    alert_events: list[NotificationWebhookAlertEvent] | None = None
    #: May be absent entirely.
    enabled: bool | None = None
    #: May be absent entirely.
    name: str | None = None
    #: May be absent entirely.
    run_events: list[NotificationWebhookRunEvent] | None = None
    #: May be absent entirely.
    secret: str | None = None
    #: May be absent entirely.
    types: list[NotificationType] | None = None
    #: May be absent entirely.
    url: str | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "PutNotificationWebhook":
        """Decode a `PutNotificationWebhook` from its JSON object."""
        known = {"alertEvents", "enabled", "name", "runEvents", "secret", "types", "url"}
        return cls(
            alert_events=None if data.get("alertEvents") is None else [_enum(NotificationWebhookAlertEvent, item) for item in data.get("alertEvents") or []],
            enabled=data.get("enabled"),
            name=data.get("name"),
            run_events=None if data.get("runEvents") is None else [_enum(NotificationWebhookRunEvent, item) for item in data.get("runEvents") or []],
            secret=data.get("secret"),
            types=None if data.get("types") is None else [_enum(NotificationType, item) for item in data.get("types") or []],
            url=data.get("url"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        if self.alert_events is not None:
            out["alertEvents"] = [_encode(item) for item in self.alert_events]
        if self.enabled is not None:
            out["enabled"] = self.enabled
        if self.name is not None:
            out["name"] = self.name
        if self.run_events is not None:
            out["runEvents"] = [_encode(item) for item in self.run_events]
        if self.secret is not None:
            out["secret"] = self.secret
        if self.types is not None:
            out["types"] = [_encode(item) for item in self.types]
        if self.url is not None:
            out["url"] = self.url
        return out


@dataclass(frozen=True, slots=True)
class RunError:
    """`RunError`, as carried on the wire."""

    code: str
    message: str

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "RunError":
        """Decode a `RunError` from its JSON object."""
        known = {"code", "message"}
        return cls(
            code=data.get("code"),
            message=data.get("message"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["code"] = self.code
        out["message"] = self.message
        return out


@dataclass(frozen=True, slots=True)
class RunPullRequest:
    """`RunPullRequest`, as carried on the wire."""

    url: str
    #: Always present; ``None`` when the server has no value for it.
    branch: str | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "RunPullRequest":
        """Decode a `RunPullRequest` from its JSON object."""
        known = {"url", "branch"}
        return cls(
            url=data.get("url"),
            branch=data.get("branch"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["url"] = self.url
        out["branch"] = self.branch
        return out


class RunStatus(StrEnum):
    """The `RunStatus` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    RUNNING = "running"
    BLOCKED = "blocked"
    PAUSED = "paused"
    DONE = "done"
    FAILED = "failed"


@dataclass(frozen=True, slots=True)
class RunSubtaskCounts:
    """`RunSubtaskCounts`, as carried on the wire."""

    completed: float
    in_progress: float
    total: float

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "RunSubtaskCounts":
        """Decode a `RunSubtaskCounts` from its JSON object."""
        known = {"completed", "inProgress", "total"}
        return cls(
            completed=data.get("completed"),
            in_progress=data.get("inProgress"),
            total=data.get("total"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["completed"] = self.completed
        out["inProgress"] = self.in_progress
        out["total"] = self.total
        return out


class Severity(StrEnum):
    """The `Severity` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    LOW = "low"
    MEDIUM = "medium"
    HIGH = "high"
    CRITICAL = "critical"


@dataclass(frozen=True, slots=True)
class StartPublicTask:
    """`StartPublicTask`, as carried on the wire."""

    #: May be absent entirely.
    pipeline_id: str | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "StartPublicTask":
        """Decode a `StartPublicTask` from its JSON object."""
        known = {"pipelineId"}
        return cls(
            pipeline_id=data.get("pipelineId"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        if self.pipeline_id is not None:
            out["pipelineId"] = self.pipeline_id
        return out


class StepState(StrEnum):
    """The `StepState` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    PENDING = "pending"
    WORKING = "working"
    WAITING_DECISION = "waiting_decision"
    DONE = "done"


@dataclass(frozen=True, slots=True)
class TagPublicMergeReviewEffortRequest:
    """`TagPublicMergeReviewEffortRequest`, as carried on the wire."""

    #: Always present; ``None`` when the server has no value for it.
    review_effort: GetPublicMergeRecordResponseReviewEffort | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "TagPublicMergeReviewEffortRequest":
        """Decode a `TagPublicMergeReviewEffortRequest` from its JSON object."""
        known = {"reviewEffort"}
        return cls(
            review_effort=None if data.get("reviewEffort") is None else _enum(GetPublicMergeRecordResponseReviewEffort, data.get("reviewEffort")),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["reviewEffort"] = _encode(self.review_effort)
        return out


class TaskStatus(StrEnum):
    """The `TaskStatus` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    PLANNED = "planned"
    READY = "ready"
    IN_PROGRESS = "in_progress"
    BLOCKED = "blocked"
    PR_READY = "pr_ready"
    DONE = "done"


@dataclass(frozen=True, slots=True)
class UpdatePublicTask:
    """`UpdatePublicTask`, as carried on the wire."""

    #: May be absent entirely.
    auto_start_dependents: bool | None = None
    #: May be absent entirely.
    description: str | None = None
    #: May be absent entirely.
    fields: dict[str, CreatePublicTaskFieldsValue] | None = None
    #: May be absent entirely.
    title: str | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped: a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = _dc_field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "UpdatePublicTask":
        """Decode a `UpdatePublicTask` from its JSON object."""
        known = {"autoStartDependents", "description", "fields", "title"}
        return cls(
            auto_start_dependents=data.get("autoStartDependents"),
            description=data.get("description"),
            fields=None if data.get("fields") is None else {key: CreatePublicTaskFieldsValue.from_dict(val) for key, val in (data.get("fields") or {}).items()},
            title=data.get("title"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        if self.auto_start_dependents is not None:
            out["autoStartDependents"] = self.auto_start_dependents
        if self.description is not None:
            out["description"] = self.description
        if self.fields is not None:
            out["fields"] = {key: _encode(val) for key, val in self.fields.items()}
        if self.title is not None:
            out["title"] = self.title
        return out


#: `CreatePublicTaskFieldsValue` — one of: str, list[str], bool, float.
CreatePublicTaskFieldsValue: TypeAlias = str | list[str] | bool | float


#: `DocumentFreshness` — one of: DocumentFreshnessVariant0, DocumentFreshnessVariant1, DocumentFreshnessVariant2. Discriminated by `status`; decode with `decode_document_freshness`.
DocumentFreshness: TypeAlias = DocumentFreshnessVariant0 | DocumentFreshnessVariant1 | DocumentFreshnessVariant2

def decode_document_freshness(data: Mapping[str, Any]) -> DocumentFreshness:
    """Decode a `DocumentFreshness` by its `status` tag.
    An unrecognised tag raises rather than guessing a variant: every variant here has a
    different shape, so picking the wrong one would hand back a value whose fields are
    silently absent instead of an error naming what was not understood.
    """
    tag = data.get("status")
    if tag == "confirmed":
        return DocumentFreshnessVariant0.from_dict(data)
    if tag == "not-applicable":
        return DocumentFreshnessVariant1.from_dict(data)
    if tag == "unconfirmed":
        return DocumentFreshnessVariant2.from_dict(data)
    raise ValueError(f"Unknown DocumentFreshness status: {tag!r}")


#: `GetPublicRunOutcomeResponseRequirements` — one of: GetPublicRunOutcomeResponseRequirementsVariant0, GetPublicRunOutcomeResponseRequirementsVariant1. Discriminated by `status`; decode with `decode_get_public_run_outcome_response_requirements`.
GetPublicRunOutcomeResponseRequirements: TypeAlias = GetPublicRunOutcomeResponseRequirementsVariant0 | GetPublicRunOutcomeResponseRequirementsVariant1

def decode_get_public_run_outcome_response_requirements(data: Mapping[str, Any]) -> GetPublicRunOutcomeResponseRequirements:
    """Decode a `GetPublicRunOutcomeResponseRequirements` by its `status` tag.
    An unrecognised tag raises rather than guessing a variant: every variant here has a
    different shape, so picking the wrong one would hand back a value whose fields are
    silently absent instead of an error naming what was not understood.
    """
    tag = data.get("status")
    if tag == "absent":
        return GetPublicRunOutcomeResponseRequirementsVariant0.from_dict(data)
    if tag == "reported":
        return GetPublicRunOutcomeResponseRequirementsVariant1.from_dict(data)
    raise ValueError(f"Unknown GetPublicRunOutcomeResponseRequirements status: {tag!r}")


#: `GetPublicRunOutcomeResponseSources` — one of: GetPublicRunOutcomeResponseSourcesVariant0, GetPublicRunOutcomeResponseSourcesVariant1. Discriminated by `status`; decode with `decode_get_public_run_outcome_response_sources`.
GetPublicRunOutcomeResponseSources: TypeAlias = GetPublicRunOutcomeResponseSourcesVariant0 | GetPublicRunOutcomeResponseSourcesVariant1

def decode_get_public_run_outcome_response_sources(data: Mapping[str, Any]) -> GetPublicRunOutcomeResponseSources:
    """Decode a `GetPublicRunOutcomeResponseSources` by its `status` tag.
    An unrecognised tag raises rather than guessing a variant: every variant here has a
    different shape, so picking the wrong one would hand back a value whose fields are
    silently absent instead of an error naming what was not understood.
    """
    tag = data.get("status")
    if tag == "absent":
        return GetPublicRunOutcomeResponseSourcesVariant0.from_dict(data)
    if tag == "reported":
        return GetPublicRunOutcomeResponseSourcesVariant1.from_dict(data)
    raise ValueError(f"Unknown GetPublicRunOutcomeResponseSources status: {tag!r}")


#: `GetPublicRunOutcomeResponseTests` — one of: GetPublicRunOutcomeResponseTestsVariant0, GetPublicRunOutcomeResponseTestsVariant1. Discriminated by `status`; decode with `decode_get_public_run_outcome_response_tests`.
GetPublicRunOutcomeResponseTests: TypeAlias = GetPublicRunOutcomeResponseTestsVariant0 | GetPublicRunOutcomeResponseTestsVariant1

def decode_get_public_run_outcome_response_tests(data: Mapping[str, Any]) -> GetPublicRunOutcomeResponseTests:
    """Decode a `GetPublicRunOutcomeResponseTests` by its `status` tag.
    An unrecognised tag raises rather than guessing a variant: every variant here has a
    different shape, so picking the wrong one would hand back a value whose fields are
    silently absent instead of an error naming what was not understood.
    """
    tag = data.get("status")
    if tag == "absent":
        return GetPublicRunOutcomeResponseTestsVariant0.from_dict(data)
    if tag == "reported":
        return GetPublicRunOutcomeResponseTestsVariant1.from_dict(data)
    raise ValueError(f"Unknown GetPublicRunOutcomeResponseTests status: {tag!r}")


#: `GetPublicRunOutcomeResponseVisuals` — one of: GetPublicRunOutcomeResponseVisualsVariant0, GetPublicRunOutcomeResponseVisualsVariant1. Discriminated by `status`; decode with `decode_get_public_run_outcome_response_visuals`.
GetPublicRunOutcomeResponseVisuals: TypeAlias = GetPublicRunOutcomeResponseVisualsVariant0 | GetPublicRunOutcomeResponseVisualsVariant1

def decode_get_public_run_outcome_response_visuals(data: Mapping[str, Any]) -> GetPublicRunOutcomeResponseVisuals:
    """Decode a `GetPublicRunOutcomeResponseVisuals` by its `status` tag.
    An unrecognised tag raises rather than guessing a variant: every variant here has a
    different shape, so picking the wrong one would hand back a value whose fields are
    silently absent instead of an error naming what was not understood.
    """
    tag = data.get("status")
    if tag == "absent":
        return GetPublicRunOutcomeResponseVisualsVariant0.from_dict(data)
    if tag == "reported":
        return GetPublicRunOutcomeResponseVisualsVariant1.from_dict(data)
    raise ValueError(f"Unknown GetPublicRunOutcomeResponseVisuals status: {tag!r}")


#: `ListPublicTaskTypesResponseTaskTypeFieldShowWhenEquals` — one of: str, bool, float.
ListPublicTaskTypesResponseTaskTypeFieldShowWhenEquals: TypeAlias = str | bool | float


#: `PublicDecision` — one of: PublicRequirementsDecision, PublicForkDecision, PublicJudgeDecision, PublicInputGateDecision, PublicApprovalGateDecision, PublicAgentDecision, PublicClarityDecision, PublicBrainstormDecision, PublicPrReviewDecision, PublicHumanTestDecision, PublicVisualConfirmDecision, PublicFollowUpsDecision, PublicInterviewDecision. Discriminated by `kind`; decode with `decode_public_decision`.
PublicDecision: TypeAlias = PublicRequirementsDecision | PublicForkDecision | PublicJudgeDecision | PublicInputGateDecision | PublicApprovalGateDecision | PublicAgentDecision | PublicClarityDecision | PublicBrainstormDecision | PublicPrReviewDecision | PublicHumanTestDecision | PublicVisualConfirmDecision | PublicFollowUpsDecision | PublicInterviewDecision

def decode_public_decision(data: Mapping[str, Any]) -> PublicDecision:
    """Decode a `PublicDecision` by its `kind` tag.
    An unrecognised tag raises rather than guessing a variant: every variant here has a
    different shape, so picking the wrong one would hand back a value whose fields are
    silently absent instead of an error naming what was not understood.
    """
    tag = data.get("kind")
    if tag == "requirements-review":
        return PublicRequirementsDecision.from_dict(data)
    if tag == "fork":
        return PublicForkDecision.from_dict(data)
    if tag == "judge":
        return PublicJudgeDecision.from_dict(data)
    if tag == "input-gate":
        return PublicInputGateDecision.from_dict(data)
    if tag == "approval-gate":
        return PublicApprovalGateDecision.from_dict(data)
    if tag == "agent-decision":
        return PublicAgentDecision.from_dict(data)
    if tag == "clarity-review":
        return PublicClarityDecision.from_dict(data)
    if tag == "brainstorm":
        return PublicBrainstormDecision.from_dict(data)
    if tag == "pr-review":
        return PublicPrReviewDecision.from_dict(data)
    if tag == "human-test":
        return PublicHumanTestDecision.from_dict(data)
    if tag == "visual-confirmation":
        return PublicVisualConfirmDecision.from_dict(data)
    if tag == "follow-ups":
        return PublicFollowUpsDecision.from_dict(data)
    if tag == "interview":
        return PublicInterviewDecision.from_dict(data)
    raise ValueError(f"Unknown PublicDecision kind: {tag!r}")


#: `PublicTaskDocument` — one of: PublicTaskSourceDocument, PublicTaskUploadedDocument. Discriminated by `kind`; decode with `decode_public_task_document`.
PublicTaskDocument: TypeAlias = PublicTaskSourceDocument | PublicTaskUploadedDocument

def decode_public_task_document(data: Mapping[str, Any]) -> PublicTaskDocument:
    """Decode a `PublicTaskDocument` by its `kind` tag.
    An unrecognised tag raises rather than guessing a variant: every variant here has a
    different shape, so picking the wrong one would hand back a value whose fields are
    silently absent instead of an error naming what was not understood.
    """
    tag = data.get("kind")
    if tag == "source":
        return PublicTaskSourceDocument.from_dict(data)
    if tag == "upload":
        return PublicTaskUploadedDocument.from_dict(data)
    raise ValueError(f"Unknown PublicTaskDocument kind: {tag!r}")
