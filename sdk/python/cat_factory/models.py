# GENERATED by `pnpm gen:sdk` from docs/openapi.json — DO NOT EDIT BY HAND.
# Regenerate after any change to the `/api/v1` contracts; `pnpm check:sdk` fails CI on drift.

"""Wire models for the cat-factory public API (`/api/v1`), surface version 1.1.0.

Every model is a frozen dataclass with `from_dict` / `to_dict`. Unknown fields are kept on
`extra` rather than dropped, and an unknown enum value decodes to its plain string, so an SDK
release older than the deployment it talks to degrades to "less typed", never to "raises".
"""

from __future__ import annotations

from collections.abc import Mapping
from dataclasses import dataclass, field
from enum import Enum, StrEnum
from typing import Any, TypeAlias


def _enum(cls: type[Enum], value: Any) -> Any:
    """Decode an enum value LENIENTLY.

    An unrecognised value comes back as the plain string rather than raising. The /api/v1
    surface is additive forever and a deployment will send values an older SDK has never heard
    of; a strict decode would turn every additive server release into an outage for anyone who
    had not upgraded. The members subclass str, so comparing against either form works.
    """
    if value is None:
        return None
    try:
        return cls(value)
    except ValueError:
        return value


def _encode(value: Any) -> Any:
    """Encode a model, enum, list or scalar back to its JSON form."""
    if hasattr(value, "to_dict"):
        return value.to_dict()
    if isinstance(value, Enum):
        return value.value
    if isinstance(value, list):
        return [_encode(item) for item in value]
    if isinstance(value, dict):
        return {key: _encode(val) for key, val in value.items()}
    return value


@dataclass(frozen=True, slots=True)
class CreatePublicJob:
    """`CreatePublicJob`, as carried on the wire."""

    input: str
    pipeline_id: str
    #: May be absent entirely.
    title: str | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped — a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "CreatePublicJob":
        """Decode a `CreatePublicJob` from its JSON object."""
        known = {"input", "pipelineId", "title"}
        return cls(
            input=data.get("input"),
            pipeline_id=data.get("pipelineId"),
            title=data.get("title"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["input"] = self.input
        out["pipelineId"] = self.pipeline_id
        if self.title is not None:
            out["title"] = self.title
        return out


@dataclass(frozen=True, slots=True)
class CreatePublicTask:
    """`CreatePublicTask`, as carried on the wire."""

    title: str
    #: May be absent entirely.
    description: str | None = None
    #: May be absent entirely.
    task_type: str | None = None
    #: May be absent entirely.
    ticket: PublicTaskTicket | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped — a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "CreatePublicTask":
        """Decode a `CreatePublicTask` from its JSON object."""
        known = {"title", "description", "taskType", "ticket"}
        return cls(
            title=data.get("title"),
            description=data.get("description"),
            task_type=data.get("taskType"),
            ticket=None if data.get("ticket") is None else PublicTaskTicket.from_dict(data.get("ticket")),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["title"] = self.title
        if self.description is not None:
            out["description"] = self.description
        if self.task_type is not None:
            out["taskType"] = self.task_type
        if self.ticket is not None:
            out["ticket"] = _encode(self.ticket)
        return out


@dataclass(frozen=True, slots=True)
class DebugAgentContextSnapshot:
    """`DebugAgentContextSnapshot`, as carried on the wire."""

    agent_kind: str
    context_files: list[DebugContextFile]
    created_at: float
    extras: dict[str, Any]
    fragments: list[DebugContextFragment]
    run_id: str
    snapshot_id: str
    step_index: float
    system_prompt: DebugText
    user_prompt: DebugText
    #: Always present; ``None`` when the server has no value for it.
    harness: str | None = None
    #: Always present; ``None`` when the server has no value for it.
    model: str | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped — a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "DebugAgentContextSnapshot":
        """Decode a `DebugAgentContextSnapshot` from its JSON object."""
        known = {"agentKind", "contextFiles", "createdAt", "extras", "fragments", "runId", "snapshotId", "stepIndex", "systemPrompt", "userPrompt", "harness", "model"}
        return cls(
            agent_kind=data.get("agentKind"),
            context_files=[DebugContextFile.from_dict(item) for item in data.get("contextFiles") or []],
            created_at=data.get("createdAt"),
            extras={key: val for key, val in (data.get("extras") or {}).items()},
            fragments=[DebugContextFragment.from_dict(item) for item in data.get("fragments") or []],
            run_id=data.get("runId"),
            snapshot_id=data.get("snapshotId"),
            step_index=data.get("stepIndex"),
            system_prompt=DebugText.from_dict(data.get("systemPrompt")),
            user_prompt=DebugText.from_dict(data.get("userPrompt")),
            harness=data.get("harness"),
            model=data.get("model"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["agentKind"] = self.agent_kind
        out["contextFiles"] = [_encode(item) for item in self.context_files]
        out["createdAt"] = self.created_at
        out["extras"] = {key: _encode(val) for key, val in self.extras.items()}
        out["fragments"] = [_encode(item) for item in self.fragments]
        out["runId"] = self.run_id
        out["snapshotId"] = self.snapshot_id
        out["stepIndex"] = self.step_index
        out["systemPrompt"] = _encode(self.system_prompt)
        out["userPrompt"] = _encode(self.user_prompt)
        out["harness"] = self.harness
        out["model"] = self.model
        return out


@dataclass(frozen=True, slots=True)
class DebugAgentContextSummary:
    """`DebugAgentContextSummary`, as carried on the wire."""

    agent_kind: str
    context_files_chars: float
    created_at: float
    fragments_chars: float
    snapshot_id: str
    step_index: float
    system_prompt_chars: float
    user_prompt_chars: float
    #: Always present; ``None`` when the server has no value for it.
    harness: str | None = None
    #: Always present; ``None`` when the server has no value for it.
    model: str | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped — a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "DebugAgentContextSummary":
        """Decode a `DebugAgentContextSummary` from its JSON object."""
        known = {"agentKind", "contextFilesChars", "createdAt", "fragmentsChars", "snapshotId", "stepIndex", "systemPromptChars", "userPromptChars", "harness", "model"}
        return cls(
            agent_kind=data.get("agentKind"),
            context_files_chars=data.get("contextFilesChars"),
            created_at=data.get("createdAt"),
            fragments_chars=data.get("fragmentsChars"),
            snapshot_id=data.get("snapshotId"),
            step_index=data.get("stepIndex"),
            system_prompt_chars=data.get("systemPromptChars"),
            user_prompt_chars=data.get("userPromptChars"),
            harness=data.get("harness"),
            model=data.get("model"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["agentKind"] = self.agent_kind
        out["contextFilesChars"] = self.context_files_chars
        out["createdAt"] = self.created_at
        out["fragmentsChars"] = self.fragments_chars
        out["snapshotId"] = self.snapshot_id
        out["stepIndex"] = self.step_index
        out["systemPromptChars"] = self.system_prompt_chars
        out["userPromptChars"] = self.user_prompt_chars
        out["harness"] = self.harness
        out["model"] = self.model
        return out


@dataclass(frozen=True, slots=True)
class DebugContextFile:
    """`DebugContextFile`, as carried on the wire."""

    content: DebugText
    path: str
    title: str
    url: str

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped — a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "DebugContextFile":
        """Decode a `DebugContextFile` from its JSON object."""
        known = {"content", "path", "title", "url"}
        return cls(
            content=DebugText.from_dict(data.get("content")),
            path=data.get("path"),
            title=data.get("title"),
            url=data.get("url"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["content"] = _encode(self.content)
        out["path"] = self.path
        out["title"] = self.title
        out["url"] = self.url
        return out


@dataclass(frozen=True, slots=True)
class DebugContextFragment:
    """`DebugContextFragment`, as carried on the wire."""

    body: DebugText
    id: str

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped — a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "DebugContextFragment":
        """Decode a `DebugContextFragment` from its JSON object."""
        known = {"body", "id"}
        return cls(
            body=DebugText.from_dict(data.get("body")),
            id=data.get("id"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["body"] = _encode(self.body)
        out["id"] = self.id
        return out


@dataclass(frozen=True, slots=True)
class DebugInfraLogEntry:
    """`DebugInfraLogEntry`, as carried on the wire."""

    created_at: float
    id: str
    operation: DebugInfraLogEntryOperation
    outcome: DebugInfraLogEntryOutcome
    subsystem: DebugInfraLogEntrySubsystem
    workspace_id: str
    #: Always present; ``None`` when the server has no value for it.
    block_id: str | None = None
    #: Always present; ``None`` when the server has no value for it.
    detail: str | None = None
    #: Always present; ``None`` when the server has no value for it.
    error: str | None = None
    #: Always present; ``None`` when the server has no value for it.
    execution_id: str | None = None
    #: Always present; ``None`` when the server has no value for it.
    provider_id: str | None = None
    #: Always present; ``None`` when the server has no value for it.
    target_id: str | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped — a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "DebugInfraLogEntry":
        """Decode a `DebugInfraLogEntry` from its JSON object."""
        known = {"createdAt", "id", "operation", "outcome", "subsystem", "workspaceId", "blockId", "detail", "error", "executionId", "providerId", "targetId"}
        return cls(
            created_at=data.get("createdAt"),
            id=data.get("id"),
            operation=_enum(DebugInfraLogEntryOperation, data.get("operation")),
            outcome=_enum(DebugInfraLogEntryOutcome, data.get("outcome")),
            subsystem=_enum(DebugInfraLogEntrySubsystem, data.get("subsystem")),
            workspace_id=data.get("workspaceId"),
            block_id=data.get("blockId"),
            detail=data.get("detail"),
            error=data.get("error"),
            execution_id=data.get("executionId"),
            provider_id=data.get("providerId"),
            target_id=data.get("targetId"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["createdAt"] = self.created_at
        out["id"] = self.id
        out["operation"] = _encode(self.operation)
        out["outcome"] = _encode(self.outcome)
        out["subsystem"] = _encode(self.subsystem)
        out["workspaceId"] = self.workspace_id
        out["blockId"] = self.block_id
        out["detail"] = self.detail
        out["error"] = self.error
        out["executionId"] = self.execution_id
        out["providerId"] = self.provider_id
        out["targetId"] = self.target_id
        return out


class DebugInfraLogEntryOperation(StrEnum):
    """The `DebugInfraLogEntryOperation` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    PROVISION = "provision"
    TEARDOWN = "teardown"
    STATUS = "status"
    DISPATCH = "dispatch"
    RELEASE = "release"
    POLL_FAILURE = "poll-failure"


class DebugInfraLogEntryOutcome(StrEnum):
    """The `DebugInfraLogEntryOutcome` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    SUCCESS = "success"
    FAILURE = "failure"


class DebugInfraLogEntrySubsystem(StrEnum):
    """The `DebugInfraLogEntrySubsystem` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    ENVIRONMENT = "environment"
    RUNNER_POOL = "runner-pool"
    CONTAINER = "container"


@dataclass(frozen=True, slots=True)
class DebugLlmAgentKindRollup:
    """`DebugLlmAgentKindRollup`, as carried on the wire."""

    agent_kind: str
    cache_read_tokens: float
    cache_write_tokens: float
    calls: float
    completion_tokens: float
    errors: float
    overhead_ms: float
    peak_completion_tokens: float
    prompt_tokens: float
    truncated_calls: float
    upstream_ms: float
    warnings: float
    #: Always present; ``None`` when the server has no value for it.
    cache_hit_rate: float | None = None
    #: Always present; ``None`` when the server has no value for it.
    max_output_tokens: float | None = None
    #: Always present; ``None`` when the server has no value for it.
    output_headroom_ratio: float | None = None
    #: Always present; ``None`` when the server has no value for it.
    transport_overhead_ratio: float | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped — a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "DebugLlmAgentKindRollup":
        """Decode a `DebugLlmAgentKindRollup` from its JSON object."""
        known = {"agentKind", "cacheReadTokens", "cacheWriteTokens", "calls", "completionTokens", "errors", "overheadMs", "peakCompletionTokens", "promptTokens", "truncatedCalls", "upstreamMs", "warnings", "cacheHitRate", "maxOutputTokens", "outputHeadroomRatio", "transportOverheadRatio"}
        return cls(
            agent_kind=data.get("agentKind"),
            cache_read_tokens=data.get("cacheReadTokens"),
            cache_write_tokens=data.get("cacheWriteTokens"),
            calls=data.get("calls"),
            completion_tokens=data.get("completionTokens"),
            errors=data.get("errors"),
            overhead_ms=data.get("overheadMs"),
            peak_completion_tokens=data.get("peakCompletionTokens"),
            prompt_tokens=data.get("promptTokens"),
            truncated_calls=data.get("truncatedCalls"),
            upstream_ms=data.get("upstreamMs"),
            warnings=data.get("warnings"),
            cache_hit_rate=data.get("cacheHitRate"),
            max_output_tokens=data.get("maxOutputTokens"),
            output_headroom_ratio=data.get("outputHeadroomRatio"),
            transport_overhead_ratio=data.get("transportOverheadRatio"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["agentKind"] = self.agent_kind
        out["cacheReadTokens"] = self.cache_read_tokens
        out["cacheWriteTokens"] = self.cache_write_tokens
        out["calls"] = self.calls
        out["completionTokens"] = self.completion_tokens
        out["errors"] = self.errors
        out["overheadMs"] = self.overhead_ms
        out["peakCompletionTokens"] = self.peak_completion_tokens
        out["promptTokens"] = self.prompt_tokens
        out["truncatedCalls"] = self.truncated_calls
        out["upstreamMs"] = self.upstream_ms
        out["warnings"] = self.warnings
        out["cacheHitRate"] = self.cache_hit_rate
        out["maxOutputTokens"] = self.max_output_tokens
        out["outputHeadroomRatio"] = self.output_headroom_ratio
        out["transportOverheadRatio"] = self.transport_overhead_ratio
        return out


@dataclass(frozen=True, slots=True)
class DebugLlmCall:
    """`DebugLlmCall`, as carried on the wire."""

    agent_kind: str
    cache_read_tokens: float
    cache_write_tokens: float
    call_id: str
    completion_tokens: float
    created_at: float
    elided_leading_messages: float
    message_count: float
    model: str
    ok: bool
    outcome: LlmCallOutcome
    overhead_ms: float
    phase: str
    prompt: DebugText
    prompt_tokens: float
    provider: str
    reasoning: DebugText
    response: DebugText
    streaming: bool
    tool_count: float
    total_ms: float
    total_tokens: float
    upstream_ms: float
    #: Always present; ``None`` when the server has no value for it.
    error_message: str | None = None
    #: Always present; ``None`` when the server has no value for it.
    finish_reason: str | None = None
    #: Always present; ``None`` when the server has no value for it.
    http_status: float | None = None
    #: May be absent entirely.
    prompt_messages: list[DebugPromptMessage] | None = None
    #: Always present; ``None`` when the server has no value for it.
    request_max_tokens: float | None = None
    #: Always present; ``None`` when the server has no value for it.
    run_id: str | None = None
    #: Always present; ``None`` when the server has no value for it.
    turn_index: float | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped — a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "DebugLlmCall":
        """Decode a `DebugLlmCall` from its JSON object."""
        known = {"agentKind", "cacheReadTokens", "cacheWriteTokens", "callId", "completionTokens", "createdAt", "elidedLeadingMessages", "messageCount", "model", "ok", "outcome", "overheadMs", "phase", "prompt", "promptTokens", "provider", "reasoning", "response", "streaming", "toolCount", "totalMs", "totalTokens", "upstreamMs", "errorMessage", "finishReason", "httpStatus", "promptMessages", "requestMaxTokens", "runId", "turnIndex"}
        return cls(
            agent_kind=data.get("agentKind"),
            cache_read_tokens=data.get("cacheReadTokens"),
            cache_write_tokens=data.get("cacheWriteTokens"),
            call_id=data.get("callId"),
            completion_tokens=data.get("completionTokens"),
            created_at=data.get("createdAt"),
            elided_leading_messages=data.get("elidedLeadingMessages"),
            message_count=data.get("messageCount"),
            model=data.get("model"),
            ok=data.get("ok"),
            outcome=_enum(LlmCallOutcome, data.get("outcome")),
            overhead_ms=data.get("overheadMs"),
            phase=data.get("phase"),
            prompt=DebugText.from_dict(data.get("prompt")),
            prompt_tokens=data.get("promptTokens"),
            provider=data.get("provider"),
            reasoning=DebugText.from_dict(data.get("reasoning")),
            response=DebugText.from_dict(data.get("response")),
            streaming=data.get("streaming"),
            tool_count=data.get("toolCount"),
            total_ms=data.get("totalMs"),
            total_tokens=data.get("totalTokens"),
            upstream_ms=data.get("upstreamMs"),
            error_message=data.get("errorMessage"),
            finish_reason=data.get("finishReason"),
            http_status=data.get("httpStatus"),
            prompt_messages=None if data.get("promptMessages") is None else [DebugPromptMessage.from_dict(item) for item in data.get("promptMessages") or []],
            request_max_tokens=data.get("requestMaxTokens"),
            run_id=data.get("runId"),
            turn_index=data.get("turnIndex"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["agentKind"] = self.agent_kind
        out["cacheReadTokens"] = self.cache_read_tokens
        out["cacheWriteTokens"] = self.cache_write_tokens
        out["callId"] = self.call_id
        out["completionTokens"] = self.completion_tokens
        out["createdAt"] = self.created_at
        out["elidedLeadingMessages"] = self.elided_leading_messages
        out["messageCount"] = self.message_count
        out["model"] = self.model
        out["ok"] = self.ok
        out["outcome"] = _encode(self.outcome)
        out["overheadMs"] = self.overhead_ms
        out["phase"] = self.phase
        out["prompt"] = _encode(self.prompt)
        out["promptTokens"] = self.prompt_tokens
        out["provider"] = self.provider
        out["reasoning"] = _encode(self.reasoning)
        out["response"] = _encode(self.response)
        out["streaming"] = self.streaming
        out["toolCount"] = self.tool_count
        out["totalMs"] = self.total_ms
        out["totalTokens"] = self.total_tokens
        out["upstreamMs"] = self.upstream_ms
        out["errorMessage"] = self.error_message
        out["finishReason"] = self.finish_reason
        out["httpStatus"] = self.http_status
        if self.prompt_messages is not None:
            out["promptMessages"] = [_encode(item) for item in self.prompt_messages]
        out["requestMaxTokens"] = self.request_max_tokens
        out["runId"] = self.run_id
        out["turnIndex"] = self.turn_index
        return out


@dataclass(frozen=True, slots=True)
class DebugLlmPhaseRollup:
    """`DebugLlmPhaseRollup`, as carried on the wire."""

    cache_read_tokens: float
    cache_write_tokens: float
    calls: float
    carry_cost_tokens: float
    completion_tokens: float
    errors: float
    overhead_ms: float
    phase: str
    prompt_tokens: float
    truncated_calls: float
    upstream_ms: float
    warnings: float
    #: Always present; ``None`` when the server has no value for it.
    cache_hit_rate: float | None = None
    #: Always present; ``None`` when the server has no value for it.
    carry_cost_share: float | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped — a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "DebugLlmPhaseRollup":
        """Decode a `DebugLlmPhaseRollup` from its JSON object."""
        known = {"cacheReadTokens", "cacheWriteTokens", "calls", "carryCostTokens", "completionTokens", "errors", "overheadMs", "phase", "promptTokens", "truncatedCalls", "upstreamMs", "warnings", "cacheHitRate", "carryCostShare"}
        return cls(
            cache_read_tokens=data.get("cacheReadTokens"),
            cache_write_tokens=data.get("cacheWriteTokens"),
            calls=data.get("calls"),
            carry_cost_tokens=data.get("carryCostTokens"),
            completion_tokens=data.get("completionTokens"),
            errors=data.get("errors"),
            overhead_ms=data.get("overheadMs"),
            phase=data.get("phase"),
            prompt_tokens=data.get("promptTokens"),
            truncated_calls=data.get("truncatedCalls"),
            upstream_ms=data.get("upstreamMs"),
            warnings=data.get("warnings"),
            cache_hit_rate=data.get("cacheHitRate"),
            carry_cost_share=data.get("carryCostShare"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["cacheReadTokens"] = self.cache_read_tokens
        out["cacheWriteTokens"] = self.cache_write_tokens
        out["calls"] = self.calls
        out["carryCostTokens"] = self.carry_cost_tokens
        out["completionTokens"] = self.completion_tokens
        out["errors"] = self.errors
        out["overheadMs"] = self.overhead_ms
        out["phase"] = self.phase
        out["promptTokens"] = self.prompt_tokens
        out["truncatedCalls"] = self.truncated_calls
        out["upstreamMs"] = self.upstream_ms
        out["warnings"] = self.warnings
        out["cacheHitRate"] = self.cache_hit_rate
        out["carryCostShare"] = self.carry_cost_share
        return out


@dataclass(frozen=True, slots=True)
class DebugLlmTotals:
    """`DebugLlmTotals`, as carried on the wire."""

    cache_read_tokens: float
    cache_write_tokens: float
    calls: float
    completion_tokens: float
    errors: float
    overhead_ms: float
    prompt_tokens: float
    truncated_calls: float
    upstream_ms: float
    warnings: float
    #: Always present; ``None`` when the server has no value for it.
    cache_hit_rate: float | None = None
    #: Always present; ``None`` when the server has no value for it.
    transport_overhead_ratio: float | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped — a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "DebugLlmTotals":
        """Decode a `DebugLlmTotals` from its JSON object."""
        known = {"cacheReadTokens", "cacheWriteTokens", "calls", "completionTokens", "errors", "overheadMs", "promptTokens", "truncatedCalls", "upstreamMs", "warnings", "cacheHitRate", "transportOverheadRatio"}
        return cls(
            cache_read_tokens=data.get("cacheReadTokens"),
            cache_write_tokens=data.get("cacheWriteTokens"),
            calls=data.get("calls"),
            completion_tokens=data.get("completionTokens"),
            errors=data.get("errors"),
            overhead_ms=data.get("overheadMs"),
            prompt_tokens=data.get("promptTokens"),
            truncated_calls=data.get("truncatedCalls"),
            upstream_ms=data.get("upstreamMs"),
            warnings=data.get("warnings"),
            cache_hit_rate=data.get("cacheHitRate"),
            transport_overhead_ratio=data.get("transportOverheadRatio"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["cacheReadTokens"] = self.cache_read_tokens
        out["cacheWriteTokens"] = self.cache_write_tokens
        out["calls"] = self.calls
        out["completionTokens"] = self.completion_tokens
        out["errors"] = self.errors
        out["overheadMs"] = self.overhead_ms
        out["promptTokens"] = self.prompt_tokens
        out["truncatedCalls"] = self.truncated_calls
        out["upstreamMs"] = self.upstream_ms
        out["warnings"] = self.warnings
        out["cacheHitRate"] = self.cache_hit_rate
        out["transportOverheadRatio"] = self.transport_overhead_ratio
        return out


@dataclass(frozen=True, slots=True)
class DebugPromptMessage:
    """`DebugPromptMessage`, as carried on the wire."""

    content: DebugText
    index: float
    role: str
    tool_calls: list[DebugToolCall]
    #: Always present; ``None`` when the server has no value for it.
    name: str | None = None
    #: Always present; ``None`` when the server has no value for it.
    tool_call_id: str | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped — a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "DebugPromptMessage":
        """Decode a `DebugPromptMessage` from its JSON object."""
        known = {"content", "index", "role", "toolCalls", "name", "toolCallId"}
        return cls(
            content=DebugText.from_dict(data.get("content")),
            index=data.get("index"),
            role=data.get("role"),
            tool_calls=[DebugToolCall.from_dict(item) for item in data.get("toolCalls") or []],
            name=data.get("name"),
            tool_call_id=data.get("toolCallId"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["content"] = _encode(self.content)
        out["index"] = self.index
        out["role"] = self.role
        out["toolCalls"] = [_encode(item) for item in self.tool_calls]
        out["name"] = self.name
        out["toolCallId"] = self.tool_call_id
        return out


@dataclass(frozen=True, slots=True)
class DebugRunFailure:
    """`DebugRunFailure`, as carried on the wire."""

    kind: DebugRunFailureKind
    message: str
    occurred_at: float
    #: Always present; ``None`` when the server has no value for it.
    detail: str | None = None
    #: Always present; ``None`` when the server has no value for it.
    hint: str | None = None
    #: Always present; ``None`` when the server has no value for it.
    last_subtasks: DebugSubtaskCounts | None = None
    #: May be absent entirely.
    reason: str | None = None
    #: May be absent entirely.
    step_index: float | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped — a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "DebugRunFailure":
        """Decode a `DebugRunFailure` from its JSON object."""
        known = {"kind", "message", "occurredAt", "detail", "hint", "lastSubtasks", "reason", "stepIndex"}
        return cls(
            kind=_enum(DebugRunFailureKind, data.get("kind")),
            message=data.get("message"),
            occurred_at=data.get("occurredAt"),
            detail=data.get("detail"),
            hint=data.get("hint"),
            last_subtasks=None if data.get("lastSubtasks") is None else DebugSubtaskCounts.from_dict(data.get("lastSubtasks")),
            reason=data.get("reason"),
            step_index=data.get("stepIndex"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["kind"] = _encode(self.kind)
        out["message"] = self.message
        out["occurredAt"] = self.occurred_at
        out["detail"] = self.detail
        out["hint"] = self.hint
        out["lastSubtasks"] = _encode(self.last_subtasks)
        if self.reason is not None:
            out["reason"] = self.reason
        if self.step_index is not None:
            out["stepIndex"] = self.step_index
        return out


class DebugRunFailureKind(StrEnum):
    """The `DebugRunFailureKind` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    PREFLIGHT = "preflight"
    DISPATCH = "dispatch"
    ENVIRONMENT = "environment"
    EVICTED = "evicted"
    TIMEOUT = "timeout"
    AGENT = "agent"
    JOB_FAILED = "job_failed"
    REJECTED = "rejected"
    COMPANION_REJECTED = "companion_rejected"
    STALLED = "stalled"
    CANCELLED = "cancelled"
    UNKNOWN = "unknown"


@dataclass(frozen=True, slots=True)
class DebugRunOverview:
    """`DebugRunOverview`, as carried on the wire."""

    generated_at: float
    kind: str
    llm: DebugRunOverviewLlm
    run: DebugRunSummary
    signals: list[DebugRunSignal]
    sinks: DebugRunOverviewSinks
    steps: list[DebugRunStep]
    version: Any
    #: Always present; ``None`` when the server has no value for it.
    diagnostics: DebugRunOverviewDiagnostics | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped — a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "DebugRunOverview":
        """Decode a `DebugRunOverview` from its JSON object."""
        known = {"generatedAt", "kind", "llm", "run", "signals", "sinks", "steps", "version", "diagnostics"}
        return cls(
            generated_at=data.get("generatedAt"),
            kind=data.get("kind"),
            llm=DebugRunOverviewLlm.from_dict(data.get("llm")),
            run=DebugRunSummary.from_dict(data.get("run")),
            signals=[DebugRunSignal.from_dict(item) for item in data.get("signals") or []],
            sinks=DebugRunOverviewSinks.from_dict(data.get("sinks")),
            steps=[DebugRunStep.from_dict(item) for item in data.get("steps") or []],
            version=data.get("version"),
            diagnostics=None if data.get("diagnostics") is None else DebugRunOverviewDiagnostics.from_dict(data.get("diagnostics")),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["generatedAt"] = self.generated_at
        out["kind"] = self.kind
        out["llm"] = _encode(self.llm)
        out["run"] = _encode(self.run)
        out["signals"] = [_encode(item) for item in self.signals]
        out["sinks"] = _encode(self.sinks)
        out["steps"] = [_encode(item) for item in self.steps]
        out["version"] = self.version
        out["diagnostics"] = _encode(self.diagnostics)
        return out


@dataclass(frozen=True, slots=True)
class DebugRunOverviewDiagnostics:
    """`DebugRunOverviewDiagnostics`, as carried on the wire."""

    #: May be absent entirely.
    host: DebugRunOverviewDiagnosticsHost | None = None
    #: May be absent entirely.
    last_dispatch: DebugRunOverviewDiagnosticsLastDispatch | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped — a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "DebugRunOverviewDiagnostics":
        """Decode a `DebugRunOverviewDiagnostics` from its JSON object."""
        known = {"host", "lastDispatch"}
        return cls(
            host=None if data.get("host") is None else DebugRunOverviewDiagnosticsHost.from_dict(data.get("host")),
            last_dispatch=None if data.get("lastDispatch") is None else DebugRunOverviewDiagnosticsLastDispatch.from_dict(data.get("lastDispatch")),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        if self.host is not None:
            out["host"] = _encode(self.host)
        if self.last_dispatch is not None:
            out["lastDispatch"] = _encode(self.last_dispatch)
        return out


@dataclass(frozen=True, slots=True)
class DebugRunOverviewDiagnosticsHost:
    """`DebugRunOverviewDiagnosticsHost`, as carried on the wire."""

    #: May be absent entirely.
    platform: str | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped — a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "DebugRunOverviewDiagnosticsHost":
        """Decode a `DebugRunOverviewDiagnosticsHost` from its JSON object."""
        known = {"platform"}
        return cls(
            platform=data.get("platform"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        if self.platform is not None:
            out["platform"] = self.platform
        return out


@dataclass(frozen=True, slots=True)
class DebugRunOverviewDiagnosticsLastDispatch:
    """`DebugRunOverviewDiagnosticsLastDispatch`, as carried on the wire."""

    agent_kind: str
    at: float
    step_index: float
    #: May be absent entirely.
    execution_backend: str | None = None
    #: May be absent entirely.
    model: str | None = None
    #: May be absent entirely.
    repo: DebugRunOverviewDiagnosticsLastDispatchRepo | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped — a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "DebugRunOverviewDiagnosticsLastDispatch":
        """Decode a `DebugRunOverviewDiagnosticsLastDispatch` from its JSON object."""
        known = {"agentKind", "at", "stepIndex", "executionBackend", "model", "repo"}
        return cls(
            agent_kind=data.get("agentKind"),
            at=data.get("at"),
            step_index=data.get("stepIndex"),
            execution_backend=data.get("executionBackend"),
            model=data.get("model"),
            repo=None if data.get("repo") is None else DebugRunOverviewDiagnosticsLastDispatchRepo.from_dict(data.get("repo")),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["agentKind"] = self.agent_kind
        out["at"] = self.at
        out["stepIndex"] = self.step_index
        if self.execution_backend is not None:
            out["executionBackend"] = self.execution_backend
        if self.model is not None:
            out["model"] = self.model
        if self.repo is not None:
            out["repo"] = _encode(self.repo)
        return out


@dataclass(frozen=True, slots=True)
class DebugRunOverviewDiagnosticsLastDispatchRepo:
    """`DebugRunOverviewDiagnosticsLastDispatchRepo`, as carried on the wire."""

    name: str
    owner: str
    #: May be absent entirely.
    base_branch: str | None = None
    #: May be absent entirely.
    provider: str | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped — a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "DebugRunOverviewDiagnosticsLastDispatchRepo":
        """Decode a `DebugRunOverviewDiagnosticsLastDispatchRepo` from its JSON object."""
        known = {"name", "owner", "baseBranch", "provider"}
        return cls(
            name=data.get("name"),
            owner=data.get("owner"),
            base_branch=data.get("baseBranch"),
            provider=data.get("provider"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["name"] = self.name
        out["owner"] = self.owner
        if self.base_branch is not None:
            out["baseBranch"] = self.base_branch
        if self.provider is not None:
            out["provider"] = self.provider
        return out


@dataclass(frozen=True, slots=True)
class DebugRunOverviewLlm:
    """`DebugRunOverviewLlm`, as carried on the wire."""

    by_agent_kind: list[DebugLlmAgentKindRollup]
    by_phase: list[DebugLlmPhaseRollup]
    totals: DebugLlmTotals

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped — a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "DebugRunOverviewLlm":
        """Decode a `DebugRunOverviewLlm` from its JSON object."""
        known = {"byAgentKind", "byPhase", "totals"}
        return cls(
            by_agent_kind=[DebugLlmAgentKindRollup.from_dict(item) for item in data.get("byAgentKind") or []],
            by_phase=[DebugLlmPhaseRollup.from_dict(item) for item in data.get("byPhase") or []],
            totals=DebugLlmTotals.from_dict(data.get("totals")),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["byAgentKind"] = [_encode(item) for item in self.by_agent_kind]
        out["byPhase"] = [_encode(item) for item in self.by_phase]
        out["totals"] = _encode(self.totals)
        return out


@dataclass(frozen=True, slots=True)
class DebugRunOverviewSinks:
    """`DebugRunOverviewSinks`, as carried on the wire."""

    agent_context: DebugRunOverviewSinksAgentContext
    llm_calls: DebugRunOverviewSinksAgentContext
    provisioning_log: DebugRunOverviewSinksAgentContext
    search_queries: DebugRunOverviewSinksAgentContext

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped — a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "DebugRunOverviewSinks":
        """Decode a `DebugRunOverviewSinks` from its JSON object."""
        known = {"agentContext", "llmCalls", "provisioningLog", "searchQueries"}
        return cls(
            agent_context=DebugRunOverviewSinksAgentContext.from_dict(data.get("agentContext")),
            llm_calls=DebugRunOverviewSinksAgentContext.from_dict(data.get("llmCalls")),
            provisioning_log=DebugRunOverviewSinksAgentContext.from_dict(data.get("provisioningLog")),
            search_queries=DebugRunOverviewSinksAgentContext.from_dict(data.get("searchQueries")),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["agentContext"] = _encode(self.agent_context)
        out["llmCalls"] = _encode(self.llm_calls)
        out["provisioningLog"] = _encode(self.provisioning_log)
        out["searchQueries"] = _encode(self.search_queries)
        return out


@dataclass(frozen=True, slots=True)
class DebugRunOverviewSinksAgentContext:
    """`DebugRunOverviewSinksAgentContext`, as carried on the wire."""

    available: bool
    count: float

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped — a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "DebugRunOverviewSinksAgentContext":
        """Decode a `DebugRunOverviewSinksAgentContext` from its JSON object."""
        known = {"available", "count"}
        return cls(
            available=data.get("available"),
            count=data.get("count"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["available"] = self.available
        out["count"] = self.count
        return out


@dataclass(frozen=True, slots=True)
class DebugRunSignal:
    """`DebugRunSignal`, as carried on the wire."""

    code: str
    message: str
    severity: DebugRunSignalSeverity
    #: Always present; ``None`` when the server has no value for it.
    agent_kind: str | None = None
    #: Always present; ``None`` when the server has no value for it.
    count: float | None = None
    #: Always present; ``None`` when the server has no value for it.
    step_index: float | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped — a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "DebugRunSignal":
        """Decode a `DebugRunSignal` from its JSON object."""
        known = {"code", "message", "severity", "agentKind", "count", "stepIndex"}
        return cls(
            code=data.get("code"),
            message=data.get("message"),
            severity=_enum(DebugRunSignalSeverity, data.get("severity")),
            agent_kind=data.get("agentKind"),
            count=data.get("count"),
            step_index=data.get("stepIndex"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["code"] = self.code
        out["message"] = self.message
        out["severity"] = _encode(self.severity)
        out["agentKind"] = self.agent_kind
        out["count"] = self.count
        out["stepIndex"] = self.step_index
        return out


class DebugRunSignalSeverity(StrEnum):
    """The `DebugRunSignalSeverity` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    INFO = "info"
    WARNING = "warning"
    ERROR = "error"


@dataclass(frozen=True, slots=True)
class DebugRunStep:
    """`DebugRunStep`, as carried on the wire."""

    agent_kind: str
    eviction_recoveries: float
    has_structured_result: bool
    index: float
    output_chars: float
    progress: float
    skipped: bool
    state: str
    #: Always present; ``None`` when the server has no value for it.
    finished_at: float | None = None
    #: Always present; ``None`` when the server has no value for it.
    first_eviction_detail: DebugText | None = None
    #: Always present; ``None`` when the server has no value for it.
    last_activity_at: float | None = None
    #: Always present; ``None`` when the server has no value for it.
    model: str | None = None
    #: Always present; ``None`` when the server has no value for it.
    started_at: float | None = None
    #: Always present; ``None`` when the server has no value for it.
    subtasks: RunSubtaskCounts | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped — a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "DebugRunStep":
        """Decode a `DebugRunStep` from its JSON object."""
        known = {"agentKind", "evictionRecoveries", "hasStructuredResult", "index", "outputChars", "progress", "skipped", "state", "finishedAt", "firstEvictionDetail", "lastActivityAt", "model", "startedAt", "subtasks"}
        return cls(
            agent_kind=data.get("agentKind"),
            eviction_recoveries=data.get("evictionRecoveries"),
            has_structured_result=data.get("hasStructuredResult"),
            index=data.get("index"),
            output_chars=data.get("outputChars"),
            progress=data.get("progress"),
            skipped=data.get("skipped"),
            state=data.get("state"),
            finished_at=data.get("finishedAt"),
            first_eviction_detail=None if data.get("firstEvictionDetail") is None else DebugText.from_dict(data.get("firstEvictionDetail")),
            last_activity_at=data.get("lastActivityAt"),
            model=data.get("model"),
            started_at=data.get("startedAt"),
            subtasks=None if data.get("subtasks") is None else RunSubtaskCounts.from_dict(data.get("subtasks")),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["agentKind"] = self.agent_kind
        out["evictionRecoveries"] = self.eviction_recoveries
        out["hasStructuredResult"] = self.has_structured_result
        out["index"] = self.index
        out["outputChars"] = self.output_chars
        out["progress"] = self.progress
        out["skipped"] = self.skipped
        out["state"] = self.state
        out["finishedAt"] = self.finished_at
        out["firstEvictionDetail"] = _encode(self.first_eviction_detail)
        out["lastActivityAt"] = self.last_activity_at
        out["model"] = self.model
        out["startedAt"] = self.started_at
        out["subtasks"] = _encode(self.subtasks)
        return out


@dataclass(frozen=True, slots=True)
class DebugRunSummary:
    """`DebugRunSummary`, as carried on the wire."""

    block_id: str
    created_at: float
    current_step: float
    pipeline_id: str
    pipeline_name: str
    run_id: str
    status: RunStatus
    step_count: float
    #: Always present; ``None`` when the server has no value for it.
    failure: DebugRunFailure | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped — a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "DebugRunSummary":
        """Decode a `DebugRunSummary` from its JSON object."""
        known = {"blockId", "createdAt", "currentStep", "pipelineId", "pipelineName", "runId", "status", "stepCount", "failure"}
        return cls(
            block_id=data.get("blockId"),
            created_at=data.get("createdAt"),
            current_step=data.get("currentStep"),
            pipeline_id=data.get("pipelineId"),
            pipeline_name=data.get("pipelineName"),
            run_id=data.get("runId"),
            status=_enum(RunStatus, data.get("status")),
            step_count=data.get("stepCount"),
            failure=None if data.get("failure") is None else DebugRunFailure.from_dict(data.get("failure")),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["blockId"] = self.block_id
        out["createdAt"] = self.created_at
        out["currentStep"] = self.current_step
        out["pipelineId"] = self.pipeline_id
        out["pipelineName"] = self.pipeline_name
        out["runId"] = self.run_id
        out["status"] = _encode(self.status)
        out["stepCount"] = self.step_count
        out["failure"] = _encode(self.failure)
        return out


@dataclass(frozen=True, slots=True)
class DebugSearchQuery:
    """`DebugSearchQuery`, as carried on the wire."""

    agent_kind: str
    created_at: float
    execution_id: str
    id: str
    query: str
    result_count: float
    workspace_id: str
    #: Always present; ``None`` when the server has no value for it.
    provider: DebugSearchQueryProvider | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped — a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "DebugSearchQuery":
        """Decode a `DebugSearchQuery` from its JSON object."""
        known = {"agentKind", "createdAt", "executionId", "id", "query", "resultCount", "workspaceId", "provider"}
        return cls(
            agent_kind=data.get("agentKind"),
            created_at=data.get("createdAt"),
            execution_id=data.get("executionId"),
            id=data.get("id"),
            query=data.get("query"),
            result_count=data.get("resultCount"),
            workspace_id=data.get("workspaceId"),
            provider=None if data.get("provider") is None else _enum(DebugSearchQueryProvider, data.get("provider")),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["agentKind"] = self.agent_kind
        out["createdAt"] = self.created_at
        out["executionId"] = self.execution_id
        out["id"] = self.id
        out["query"] = self.query
        out["resultCount"] = self.result_count
        out["workspaceId"] = self.workspace_id
        out["provider"] = _encode(self.provider)
        return out


class DebugSearchQueryProvider(StrEnum):
    """The `DebugSearchQueryProvider` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    BRAVE = "brave"
    SEARXNG = "searxng"


@dataclass(frozen=True, slots=True)
class DebugSubtaskCounts:
    """`DebugSubtaskCounts`, as carried on the wire."""

    completed: float
    in_progress: float
    total: float
    #: May be absent entirely.
    items: list[DebugSubtaskItem] | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped — a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "DebugSubtaskCounts":
        """Decode a `DebugSubtaskCounts` from its JSON object."""
        known = {"completed", "inProgress", "total", "items"}
        return cls(
            completed=data.get("completed"),
            in_progress=data.get("inProgress"),
            total=data.get("total"),
            items=None if data.get("items") is None else [DebugSubtaskItem.from_dict(item) for item in data.get("items") or []],
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["completed"] = self.completed
        out["inProgress"] = self.in_progress
        out["total"] = self.total
        if self.items is not None:
            out["items"] = [_encode(item) for item in self.items]
        return out


@dataclass(frozen=True, slots=True)
class DebugSubtaskItem:
    """`DebugSubtaskItem`, as carried on the wire."""

    label: str
    status: DebugSubtaskItemStatus

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped — a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "DebugSubtaskItem":
        """Decode a `DebugSubtaskItem` from its JSON object."""
        known = {"label", "status"}
        return cls(
            label=data.get("label"),
            status=_enum(DebugSubtaskItemStatus, data.get("status")),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["label"] = self.label
        out["status"] = _encode(self.status)
        return out


class DebugSubtaskItemStatus(StrEnum):
    """The `DebugSubtaskItemStatus` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    PENDING = "pending"
    IN_PROGRESS = "in_progress"
    COMPLETED = "completed"


@dataclass(frozen=True, slots=True)
class DebugText:
    """`DebugText`, as carried on the wire."""

    chars: float
    offset: float
    text: str
    total_chars: float
    truncated: bool
    #: May be absent entirely.
    match_offset: float | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped — a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "DebugText":
        """Decode a `DebugText` from its JSON object."""
        known = {"chars", "offset", "text", "totalChars", "truncated", "matchOffset"}
        return cls(
            chars=data.get("chars"),
            offset=data.get("offset"),
            text=data.get("text"),
            total_chars=data.get("totalChars"),
            truncated=data.get("truncated"),
            match_offset=data.get("matchOffset"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["chars"] = self.chars
        out["offset"] = self.offset
        out["text"] = self.text
        out["totalChars"] = self.total_chars
        out["truncated"] = self.truncated
        if self.match_offset is not None:
            out["matchOffset"] = self.match_offset
        return out


@dataclass(frozen=True, slots=True)
class DebugToolCall:
    """`DebugToolCall`, as carried on the wire."""

    args: DebugText
    name: str

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped — a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "DebugToolCall":
        """Decode a `DebugToolCall` from its JSON object."""
        known = {"args", "name"}
        return cls(
            args=DebugText.from_dict(data.get("args")),
            name=data.get("name"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["args"] = _encode(self.args)
        out["name"] = self.name
        return out


@dataclass(frozen=True, slots=True)
class ErrorResponse:
    """`ErrorResponse`, as carried on the wire."""

    error: ErrorResponseError

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped — a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "ErrorResponse":
        """Decode a `ErrorResponse` from its JSON object."""
        known = {"error"}
        return cls(
            error=ErrorResponseError.from_dict(data.get("error")),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["error"] = _encode(self.error)
        return out


@dataclass(frozen=True, slots=True)
class ErrorResponseError:
    """`ErrorResponseError`, as carried on the wire."""

    code: str
    message: str
    #: May be absent entirely.
    details: Any | None = None
    #: May be absent entirely.
    issues: list[ErrorResponseErrorIssue] | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped — a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "ErrorResponseError":
        """Decode a `ErrorResponseError` from its JSON object."""
        known = {"code", "message", "details", "issues"}
        return cls(
            code=data.get("code"),
            message=data.get("message"),
            details=data.get("details"),
            issues=None if data.get("issues") is None else [ErrorResponseErrorIssue.from_dict(item) for item in data.get("issues") or []],
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["code"] = self.code
        out["message"] = self.message
        if self.details is not None:
            out["details"] = self.details
        if self.issues is not None:
            out["issues"] = [_encode(item) for item in self.issues]
        return out


@dataclass(frozen=True, slots=True)
class ErrorResponseErrorIssue:
    """`ErrorResponseErrorIssue`, as carried on the wire."""

    message: str
    #: May be absent entirely.
    path: str | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped — a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "ErrorResponseErrorIssue":
        """Decode a `ErrorResponseErrorIssue` from its JSON object."""
        known = {"message", "path"}
        return cls(
            message=data.get("message"),
            path=data.get("path"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["message"] = self.message
        if self.path is not None:
            out["path"] = self.path
        return out


class GetDebugLlmCallView(StrEnum):
    """The `GetDebugLlmCallView` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    RAW = "raw"
    MESSAGES = "messages"


@dataclass(frozen=True, slots=True)
class ListDebugAgentContextResponse:
    """`ListDebugAgentContextResponse`, as carried on the wire."""

    snapshots: list[DebugAgentContextSummary]
    #: Always present; ``None`` when the server has no value for it.
    next_cursor: str | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped — a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "ListDebugAgentContextResponse":
        """Decode a `ListDebugAgentContextResponse` from its JSON object."""
        known = {"snapshots", "nextCursor"}
        return cls(
            snapshots=[DebugAgentContextSummary.from_dict(item) for item in data.get("snapshots") or []],
            next_cursor=data.get("nextCursor"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["snapshots"] = [_encode(item) for item in self.snapshots]
        out["nextCursor"] = self.next_cursor
        return out


class ListDebugLlmCallsOrder(StrEnum):
    """The `ListDebugLlmCallsOrder` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    NEWEST = "newest"
    OLDEST = "oldest"


@dataclass(frozen=True, slots=True)
class ListDebugLlmCallsResponse:
    """`ListDebugLlmCallsResponse`, as carried on the wire."""

    calls: list[DebugLlmCall]
    #: Always present; ``None`` when the server has no value for it.
    next_cursor: str | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped — a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "ListDebugLlmCallsResponse":
        """Decode a `ListDebugLlmCallsResponse` from its JSON object."""
        known = {"calls", "nextCursor"}
        return cls(
            calls=[DebugLlmCall.from_dict(item) for item in data.get("calls") or []],
            next_cursor=data.get("nextCursor"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["calls"] = [_encode(item) for item in self.calls]
        out["nextCursor"] = self.next_cursor
        return out


@dataclass(frozen=True, slots=True)
class ListDebugLogsResponse:
    """`ListDebugLogsResponse`, as carried on the wire."""

    entries: list[DebugInfraLogEntry]
    #: Always present; ``None`` when the server has no value for it.
    next_cursor: str | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped — a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "ListDebugLogsResponse":
        """Decode a `ListDebugLogsResponse` from its JSON object."""
        known = {"entries", "nextCursor"}
        return cls(
            entries=[DebugInfraLogEntry.from_dict(item) for item in data.get("entries") or []],
            next_cursor=data.get("nextCursor"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["entries"] = [_encode(item) for item in self.entries]
        out["nextCursor"] = self.next_cursor
        return out


@dataclass(frozen=True, slots=True)
class ListDebugRunsResponse:
    """`ListDebugRunsResponse`, as carried on the wire."""

    runs: list[DebugRunSummary]
    #: Always present; ``None`` when the server has no value for it.
    next_cursor: str | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped — a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "ListDebugRunsResponse":
        """Decode a `ListDebugRunsResponse` from its JSON object."""
        known = {"runs", "nextCursor"}
        return cls(
            runs=[DebugRunSummary.from_dict(item) for item in data.get("runs") or []],
            next_cursor=data.get("nextCursor"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["runs"] = [_encode(item) for item in self.runs]
        out["nextCursor"] = self.next_cursor
        return out


@dataclass(frozen=True, slots=True)
class ListDebugSearchQueriesResponse:
    """`ListDebugSearchQueriesResponse`, as carried on the wire."""

    queries: list[DebugSearchQuery]
    #: Always present; ``None`` when the server has no value for it.
    next_cursor: str | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped — a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "ListDebugSearchQueriesResponse":
        """Decode a `ListDebugSearchQueriesResponse` from its JSON object."""
        known = {"queries", "nextCursor"}
        return cls(
            queries=[DebugSearchQuery.from_dict(item) for item in data.get("queries") or []],
            next_cursor=data.get("nextCursor"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["queries"] = [_encode(item) for item in self.queries]
        out["nextCursor"] = self.next_cursor
        return out


@dataclass(frozen=True, slots=True)
class ListPublicJobsResponse:
    """`ListPublicJobsResponse`, as carried on the wire."""

    jobs: list[PublicJob]
    #: Always present; ``None`` when the server has no value for it.
    next_cursor: str | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped — a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "ListPublicJobsResponse":
        """Decode a `ListPublicJobsResponse` from its JSON object."""
        known = {"jobs", "nextCursor"}
        return cls(
            jobs=[PublicJob.from_dict(item) for item in data.get("jobs") or []],
            next_cursor=data.get("nextCursor"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["jobs"] = [_encode(item) for item in self.jobs]
        out["nextCursor"] = self.next_cursor
        return out


class LlmCallOutcome(StrEnum):
    """The `LlmCallOutcome` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    OK = "ok"
    WARNING = "warning"
    ERROR = "error"


@dataclass(frozen=True, slots=True)
class Notification:
    """`Notification`, as carried on the wire."""

    body: str
    created_at: float
    id: str
    status: NotificationStatus
    title: str
    type_: NotificationType
    #: Always present; ``None`` when the server has no value for it.
    block_id: str | None = None
    #: Always present; ``None`` when the server has no value for it.
    execution_id: str | None = None
    #: May be absent entirely.
    payload: NotificationPayload | None = None
    #: Always present; ``None`` when the server has no value for it.
    resolved_at: float | None = None
    #: May be absent entirely.
    severity: NotificationSeverity | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped — a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "Notification":
        """Decode a `Notification` from its JSON object."""
        known = {"body", "createdAt", "id", "status", "title", "type", "blockId", "executionId", "payload", "resolvedAt", "severity"}
        return cls(
            body=data.get("body"),
            created_at=data.get("createdAt"),
            id=data.get("id"),
            status=_enum(NotificationStatus, data.get("status")),
            title=data.get("title"),
            type_=_enum(NotificationType, data.get("type")),
            block_id=data.get("blockId"),
            execution_id=data.get("executionId"),
            payload=None if data.get("payload") is None else NotificationPayload.from_dict(data.get("payload")),
            resolved_at=data.get("resolvedAt"),
            severity=None if data.get("severity") is None else _enum(NotificationSeverity, data.get("severity")),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["body"] = self.body
        out["createdAt"] = self.created_at
        out["id"] = self.id
        out["status"] = _encode(self.status)
        out["title"] = self.title
        out["type"] = _encode(self.type_)
        out["blockId"] = self.block_id
        out["executionId"] = self.execution_id
        if self.payload is not None:
            out["payload"] = _encode(self.payload)
        out["resolvedAt"] = self.resolved_at
        if self.severity is not None:
            out["severity"] = _encode(self.severity)
        return out


@dataclass(frozen=True, slots=True)
class NotificationPayload:
    """`NotificationPayload`, as carried on the wire."""

    #: May be absent entirely.
    assessment: NotificationPayloadAssessment | None = None
    #: May be absent entirely.
    change_class: NotificationPayloadChangeClass | None = None
    #: May be absent entirely.
    drift_affected: list[NotificationPayloadDriftAffected] | None = None
    #: May be absent entirely.
    finding_count: float | None = None
    #: May be absent entirely.
    fork_count: float | None = None
    #: May be absent entirely.
    initiative_reason: NotificationPayloadInitiativeReason | None = None
    #: May be absent entirely.
    merge_track_record_id: str | None = None
    #: May be absent entirely.
    merged_repos: list[str] | None = None
    #: May be absent entirely.
    on_call_assessment: NotificationPayloadOnCallAssessment | None = None
    #: May be absent entirely.
    pipeline_name: str | None = None
    #: May be absent entirely.
    platform_alert_transition: float | None = None
    #: May be absent entirely.
    platform_alerts: list[NotificationPayloadPlatformAlert] | None = None
    #: May be absent entirely.
    platform_failed_total: float | None = None
    #: May be absent entirely.
    platform_failing_runs: list[NotificationPayloadPlatformFailingRun] | None = None
    #: May be absent entirely.
    platform_window: NotificationPayloadPlatformWindow | None = None
    #: May be absent entirely.
    pr_url: str | None = None
    #: May be absent entirely.
    release_signals: list[NotificationPayloadReleaseSignal] | None = None
    #: May be absent entirely.
    revert_url: str | None = None
    #: May be absent entirely.
    slice_count: float | None = None
    #: May be absent entirely.
    target_user_id: str | None = None
    #: May be absent entirely.
    unmerged_repos: list[str] | None = None
    #: May be absent entirely.
    unreachable_areas: list[NotificationPayloadUnreachableArea] | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped — a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "NotificationPayload":
        """Decode a `NotificationPayload` from its JSON object."""
        known = {"assessment", "changeClass", "driftAffected", "findingCount", "forkCount", "initiativeReason", "mergeTrackRecordId", "mergedRepos", "onCallAssessment", "pipelineName", "platformAlertTransition", "platformAlerts", "platformFailedTotal", "platformFailingRuns", "platformWindow", "prUrl", "releaseSignals", "revertUrl", "sliceCount", "targetUserId", "unmergedRepos", "unreachableAreas"}
        return cls(
            assessment=None if data.get("assessment") is None else NotificationPayloadAssessment.from_dict(data.get("assessment")),
            change_class=None if data.get("changeClass") is None else _enum(NotificationPayloadChangeClass, data.get("changeClass")),
            drift_affected=None if data.get("driftAffected") is None else [NotificationPayloadDriftAffected.from_dict(item) for item in data.get("driftAffected") or []],
            finding_count=data.get("findingCount"),
            fork_count=data.get("forkCount"),
            initiative_reason=None if data.get("initiativeReason") is None else _enum(NotificationPayloadInitiativeReason, data.get("initiativeReason")),
            merge_track_record_id=data.get("mergeTrackRecordId"),
            merged_repos=None if data.get("mergedRepos") is None else [item for item in data.get("mergedRepos") or []],
            on_call_assessment=None if data.get("onCallAssessment") is None else NotificationPayloadOnCallAssessment.from_dict(data.get("onCallAssessment")),
            pipeline_name=data.get("pipelineName"),
            platform_alert_transition=data.get("platformAlertTransition"),
            platform_alerts=None if data.get("platformAlerts") is None else [_enum(NotificationPayloadPlatformAlert, item) for item in data.get("platformAlerts") or []],
            platform_failed_total=data.get("platformFailedTotal"),
            platform_failing_runs=None if data.get("platformFailingRuns") is None else [NotificationPayloadPlatformFailingRun.from_dict(item) for item in data.get("platformFailingRuns") or []],
            platform_window=None if data.get("platformWindow") is None else _enum(NotificationPayloadPlatformWindow, data.get("platformWindow")),
            pr_url=data.get("prUrl"),
            release_signals=None if data.get("releaseSignals") is None else [NotificationPayloadReleaseSignal.from_dict(item) for item in data.get("releaseSignals") or []],
            revert_url=data.get("revertUrl"),
            slice_count=data.get("sliceCount"),
            target_user_id=data.get("targetUserId"),
            unmerged_repos=None if data.get("unmergedRepos") is None else [item for item in data.get("unmergedRepos") or []],
            unreachable_areas=None if data.get("unreachableAreas") is None else [_enum(NotificationPayloadUnreachableArea, item) for item in data.get("unreachableAreas") or []],
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        if self.assessment is not None:
            out["assessment"] = _encode(self.assessment)
        if self.change_class is not None:
            out["changeClass"] = _encode(self.change_class)
        if self.drift_affected is not None:
            out["driftAffected"] = [_encode(item) for item in self.drift_affected]
        if self.finding_count is not None:
            out["findingCount"] = self.finding_count
        if self.fork_count is not None:
            out["forkCount"] = self.fork_count
        if self.initiative_reason is not None:
            out["initiativeReason"] = _encode(self.initiative_reason)
        if self.merge_track_record_id is not None:
            out["mergeTrackRecordId"] = self.merge_track_record_id
        if self.merged_repos is not None:
            out["mergedRepos"] = [_encode(item) for item in self.merged_repos]
        if self.on_call_assessment is not None:
            out["onCallAssessment"] = _encode(self.on_call_assessment)
        if self.pipeline_name is not None:
            out["pipelineName"] = self.pipeline_name
        if self.platform_alert_transition is not None:
            out["platformAlertTransition"] = self.platform_alert_transition
        if self.platform_alerts is not None:
            out["platformAlerts"] = [_encode(item) for item in self.platform_alerts]
        if self.platform_failed_total is not None:
            out["platformFailedTotal"] = self.platform_failed_total
        if self.platform_failing_runs is not None:
            out["platformFailingRuns"] = [_encode(item) for item in self.platform_failing_runs]
        if self.platform_window is not None:
            out["platformWindow"] = _encode(self.platform_window)
        if self.pr_url is not None:
            out["prUrl"] = self.pr_url
        if self.release_signals is not None:
            out["releaseSignals"] = [_encode(item) for item in self.release_signals]
        if self.revert_url is not None:
            out["revertUrl"] = self.revert_url
        if self.slice_count is not None:
            out["sliceCount"] = self.slice_count
        if self.target_user_id is not None:
            out["targetUserId"] = self.target_user_id
        if self.unmerged_repos is not None:
            out["unmergedRepos"] = [_encode(item) for item in self.unmerged_repos]
        if self.unreachable_areas is not None:
            out["unreachableAreas"] = [_encode(item) for item in self.unreachable_areas]
        return out


@dataclass(frozen=True, slots=True)
class NotificationPayloadAssessment:
    """`NotificationPayloadAssessment`, as carried on the wire."""

    complexity: float
    impact: float
    rationale: str
    risk: float

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped — a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "NotificationPayloadAssessment":
        """Decode a `NotificationPayloadAssessment` from its JSON object."""
        known = {"complexity", "impact", "rationale", "risk"}
        return cls(
            complexity=data.get("complexity"),
            impact=data.get("impact"),
            rationale=data.get("rationale"),
            risk=data.get("risk"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["complexity"] = self.complexity
        out["impact"] = self.impact
        out["rationale"] = self.rationale
        out["risk"] = self.risk
        return out


class NotificationPayloadChangeClass(StrEnum):
    """The `NotificationPayloadChangeClass` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    DOCS = "docs"
    TEST = "test"
    DEPENDENCY = "dependency"
    CONFIG = "config"
    SOURCE = "source"
    SCHEMA = "schema"
    UNKNOWN = "unknown"


@dataclass(frozen=True, slots=True)
class NotificationPayloadDriftAffected:
    """`NotificationPayloadDriftAffected`, as carried on the wire."""

    id: str
    label: str
    reason: NotificationPayloadDriftAffectedReason
    source: str
    #: Always present; ``None`` when the server has no value for it.
    sealed_at: float | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped — a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "NotificationPayloadDriftAffected":
        """Decode a `NotificationPayloadDriftAffected` from its JSON object."""
        known = {"id", "label", "reason", "source", "sealedAt"}
        return cls(
            id=data.get("id"),
            label=data.get("label"),
            reason=_enum(NotificationPayloadDriftAffectedReason, data.get("reason")),
            source=data.get("source"),
            sealed_at=data.get("sealedAt"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["id"] = self.id
        out["label"] = self.label
        out["reason"] = _encode(self.reason)
        out["source"] = self.source
        out["sealedAt"] = self.sealed_at
        return out


class NotificationPayloadDriftAffectedReason(StrEnum):
    """The `NotificationPayloadDriftAffectedReason` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    KEY_MISMATCH = "key-mismatch"
    CORRUPT = "corrupt"


class NotificationPayloadInitiativeReason(StrEnum):
    """The `NotificationPayloadInitiativeReason` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    ITEM_BLOCKED = "item_blocked"
    COMPLETE = "complete"
    CHECKPOINT = "checkpoint"


@dataclass(frozen=True, slots=True)
class NotificationPayloadOnCallAssessment:
    """`NotificationPayloadOnCallAssessment`, as carried on the wire."""

    culprit_confidence: float
    evidence: list[str]
    rationale: str
    recommendation: NotificationPayloadOnCallAssessmentRecommendation

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped — a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "NotificationPayloadOnCallAssessment":
        """Decode a `NotificationPayloadOnCallAssessment` from its JSON object."""
        known = {"culpritConfidence", "evidence", "rationale", "recommendation"}
        return cls(
            culprit_confidence=data.get("culpritConfidence"),
            evidence=[item for item in data.get("evidence") or []],
            rationale=data.get("rationale"),
            recommendation=_enum(NotificationPayloadOnCallAssessmentRecommendation, data.get("recommendation")),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["culpritConfidence"] = self.culprit_confidence
        out["evidence"] = [_encode(item) for item in self.evidence]
        out["rationale"] = self.rationale
        out["recommendation"] = _encode(self.recommendation)
        return out


class NotificationPayloadOnCallAssessmentRecommendation(StrEnum):
    """The `NotificationPayloadOnCallAssessmentRecommendation` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    REVERT = "revert"
    HOLD = "hold"
    MONITOR = "monitor"


class NotificationPayloadPlatformAlert(StrEnum):
    """The `NotificationPayloadPlatformAlert` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    FAILURE_RATE_HIGH = "failure_rate_high"
    DURATION_P99_HIGH = "duration_p99_high"
    BACKLOG_HIGH = "backlog_high"
    THROUGHPUT_STALLED = "throughput_stalled"
    FAILURE_KIND_DOMINANT = "failure_kind_dominant"
    SWEEP_DEGRADED = "sweep_degraded"


@dataclass(frozen=True, slots=True)
class NotificationPayloadPlatformFailingRun:
    """`NotificationPayloadPlatformFailingRun`, as carried on the wire."""

    created_at: float
    execution_id: str
    failure_kind: str
    #: Always present; ``None`` when the server has no value for it.
    block_id: str | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped — a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "NotificationPayloadPlatformFailingRun":
        """Decode a `NotificationPayloadPlatformFailingRun` from its JSON object."""
        known = {"createdAt", "executionId", "failureKind", "blockId"}
        return cls(
            created_at=data.get("createdAt"),
            execution_id=data.get("executionId"),
            failure_kind=data.get("failureKind"),
            block_id=data.get("blockId"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["createdAt"] = self.created_at
        out["executionId"] = self.execution_id
        out["failureKind"] = self.failure_kind
        out["blockId"] = self.block_id
        return out


class NotificationPayloadPlatformWindow(StrEnum):
    """The `NotificationPayloadPlatformWindow` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    VALUE_1H = "1h"
    VALUE_24H = "24h"
    VALUE_7D = "7d"


@dataclass(frozen=True, slots=True)
class NotificationPayloadReleaseSignal:
    """`NotificationPayloadReleaseSignal`, as carried on the wire."""

    id: str
    kind: NotificationPayloadReleaseSignalKind
    name: str
    state: NotificationPayloadReleaseSignalState
    #: May be absent entirely.
    detail: str | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped — a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "NotificationPayloadReleaseSignal":
        """Decode a `NotificationPayloadReleaseSignal` from its JSON object."""
        known = {"id", "kind", "name", "state", "detail"}
        return cls(
            id=data.get("id"),
            kind=_enum(NotificationPayloadReleaseSignalKind, data.get("kind")),
            name=data.get("name"),
            state=_enum(NotificationPayloadReleaseSignalState, data.get("state")),
            detail=data.get("detail"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["id"] = self.id
        out["kind"] = _encode(self.kind)
        out["name"] = self.name
        out["state"] = _encode(self.state)
        if self.detail is not None:
            out["detail"] = self.detail
        return out


class NotificationPayloadReleaseSignalKind(StrEnum):
    """The `NotificationPayloadReleaseSignalKind` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    MONITOR = "monitor"
    SLO = "slo"


class NotificationPayloadReleaseSignalState(StrEnum):
    """The `NotificationPayloadReleaseSignalState` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    OK = "ok"
    WARN = "warn"
    ALERT = "alert"
    NO_DATA = "no_data"


class NotificationPayloadUnreachableArea(StrEnum):
    """The `NotificationPayloadUnreachableArea` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    EPHEMERAL_ENVIRONMENTS = "ephemeralEnvironments"
    AGENT_EXECUTOR = "agentExecutor"
    BINARY_STORAGE = "binaryStorage"


class NotificationSeverity(StrEnum):
    """The `NotificationSeverity` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    NORMAL = "normal"
    URGENT = "urgent"


class NotificationStatus(StrEnum):
    """The `NotificationStatus` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    OPEN = "open"
    ACTED = "acted"
    DISMISSED = "dismissed"


class NotificationType(StrEnum):
    """The `NotificationType` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    MERGE_REVIEW = "merge_review"
    PIPELINE_COMPLETE = "pipeline_complete"
    CI_FAILED = "ci_failed"
    TEST_FAILED = "test_failed"
    REQUIREMENT_REVIEW = "requirement_review"
    CLARITY_REVIEW = "clarity_review"
    RELEASE_REGRESSION = "release_regression"
    DECISION_REQUIRED = "decision_required"
    HUMAN_TEST_READY = "human_test_ready"
    VISUAL_CONFIRMATION_READY = "visual_confirmation_ready"
    HUMAN_REVIEW = "human_review"
    FOLLOWUP_PENDING = "followup_pending"
    FORK_DECISION_PENDING = "fork_decision_pending"
    JUDGE_REVIEW = "judge_review"
    PR_REVIEW_READY = "pr_review_ready"
    INITIATIVE = "initiative"
    PLATFORM_HEALTH = "platform_health"
    INFRA_UNREACHABLE = "infra_unreachable"
    BUDGET_PAUSED = "budget_paused"
    KEY_DRIFT = "key_drift"
    MERGE_TAG_REQUEST = "merge_tag_request"


@dataclass(frozen=True, slots=True)
class PublicChooseFork:
    """`PublicChooseFork`, as carried on the wire."""

    #: May be absent entirely.
    custom: str | None = None
    #: May be absent entirely.
    fork_id: str | None = None
    #: May be absent entirely.
    note: str | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped — a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "PublicChooseFork":
        """Decode a `PublicChooseFork` from its JSON object."""
        known = {"custom", "forkId", "note"}
        return cls(
            custom=data.get("custom"),
            fork_id=data.get("forkId"),
            note=data.get("note"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        if self.custom is not None:
            out["custom"] = self.custom
        if self.fork_id is not None:
            out["forkId"] = self.fork_id
        if self.note is not None:
            out["note"] = self.note
        return out


@dataclass(frozen=True, slots=True)
class PublicDecisionList:
    """`PublicDecisionList`, as carried on the wire."""

    decisions: list[PublicDecision]
    parked: bool
    run_id: str
    status: RunStatus
    task_id: str

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped — a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "PublicDecisionList":
        """Decode a `PublicDecisionList` from its JSON object."""
        known = {"decisions", "parked", "runId", "status", "taskId"}
        return cls(
            decisions=[PublicDecision.from_dict(item) for item in data.get("decisions") or []],
            parked=data.get("parked"),
            run_id=data.get("runId"),
            status=_enum(RunStatus, data.get("status")),
            task_id=data.get("taskId"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["decisions"] = [_encode(item) for item in self.decisions]
        out["parked"] = self.parked
        out["runId"] = self.run_id
        out["status"] = _encode(self.status)
        out["taskId"] = self.task_id
        return out


@dataclass(frozen=True, slots=True)
class PublicForkDecision:
    """`PublicForkDecision`, as carried on the wire."""

    forks: list[PublicForkDecisionFork]
    kind: str
    status: PublicForkDecisionStatus
    #: Always present; ``None`` when the server has no value for it.
    seam_summary: str | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped — a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "PublicForkDecision":
        """Decode a `PublicForkDecision` from its JSON object."""
        known = {"forks", "kind", "status", "seamSummary"}
        return cls(
            forks=[PublicForkDecisionFork.from_dict(item) for item in data.get("forks") or []],
            kind=data.get("kind"),
            status=_enum(PublicForkDecisionStatus, data.get("status")),
            seam_summary=data.get("seamSummary"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["forks"] = [_encode(item) for item in self.forks]
        out["kind"] = self.kind
        out["status"] = _encode(self.status)
        out["seamSummary"] = self.seam_summary
        return out


@dataclass(frozen=True, slots=True)
class PublicForkDecisionFork:
    """`PublicForkDecisionFork`, as carried on the wire."""

    approach: str
    id: str
    summary: str
    title: str
    tradeoffs: list[str]
    #: May be absent entirely.
    recommended: bool | None = None
    #: May be absent entirely.
    risk_notes: str | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped — a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "PublicForkDecisionFork":
        """Decode a `PublicForkDecisionFork` from its JSON object."""
        known = {"approach", "id", "summary", "title", "tradeoffs", "recommended", "riskNotes"}
        return cls(
            approach=data.get("approach"),
            id=data.get("id"),
            summary=data.get("summary"),
            title=data.get("title"),
            tradeoffs=[item for item in data.get("tradeoffs") or []],
            recommended=data.get("recommended"),
            risk_notes=data.get("riskNotes"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["approach"] = self.approach
        out["id"] = self.id
        out["summary"] = self.summary
        out["title"] = self.title
        out["tradeoffs"] = [_encode(item) for item in self.tradeoffs]
        if self.recommended is not None:
            out["recommended"] = self.recommended
        if self.risk_notes is not None:
            out["riskNotes"] = self.risk_notes
        return out


class PublicForkDecisionStatus(StrEnum):
    """The `PublicForkDecisionStatus` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    PROPOSING = "proposing"
    AWAITING_CHOICE = "awaiting_choice"
    ANSWERING = "answering"
    CHOSEN = "chosen"
    SINGLE_PATH = "single_path"
    SKIPPED = "skipped"


@dataclass(frozen=True, slots=True)
class PublicIncorporate:
    """`PublicIncorporate`, as carried on the wire."""

    #: May be absent entirely.
    feedback: str | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped — a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "PublicIncorporate":
        """Decode a `PublicIncorporate` from its JSON object."""
        known = {"feedback"}
        return cls(
            feedback=data.get("feedback"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        if self.feedback is not None:
            out["feedback"] = self.feedback
        return out


@dataclass(frozen=True, slots=True)
class PublicJob:
    """`PublicJob`, as carried on the wire."""

    created_at: float
    job_id: str
    pipeline_id: str
    status: PublicJobStatus
    #: Always present; ``None`` when the server has no value for it.
    error: RunError | None = None
    #: Always present; ``None`` when the server has no value for it.
    result: PublicJobResult | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped — a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "PublicJob":
        """Decode a `PublicJob` from its JSON object."""
        known = {"createdAt", "jobId", "pipelineId", "status", "error", "result"}
        return cls(
            created_at=data.get("createdAt"),
            job_id=data.get("jobId"),
            pipeline_id=data.get("pipelineId"),
            status=_enum(PublicJobStatus, data.get("status")),
            error=None if data.get("error") is None else RunError.from_dict(data.get("error")),
            result=None if data.get("result") is None else PublicJobResult.from_dict(data.get("result")),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["createdAt"] = self.created_at
        out["jobId"] = self.job_id
        out["pipelineId"] = self.pipeline_id
        out["status"] = _encode(self.status)
        out["error"] = _encode(self.error)
        out["result"] = _encode(self.result)
        return out


@dataclass(frozen=True, slots=True)
class PublicJobAccepted:
    """`PublicJobAccepted`, as carried on the wire."""

    job_id: str
    links: PublicJobAcceptedLinks
    status: PublicJobStatus

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped — a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "PublicJobAccepted":
        """Decode a `PublicJobAccepted` from its JSON object."""
        known = {"jobId", "links", "status"}
        return cls(
            job_id=data.get("jobId"),
            links=PublicJobAcceptedLinks.from_dict(data.get("links")),
            status=_enum(PublicJobStatus, data.get("status")),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["jobId"] = self.job_id
        out["links"] = _encode(self.links)
        out["status"] = _encode(self.status)
        return out


@dataclass(frozen=True, slots=True)
class PublicJobAcceptedLinks:
    """`PublicJobAcceptedLinks`, as carried on the wire."""

    events: str
    self_: str

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped — a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "PublicJobAcceptedLinks":
        """Decode a `PublicJobAcceptedLinks` from its JSON object."""
        known = {"events", "self"}
        return cls(
            events=data.get("events"),
            self_=data.get("self"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["events"] = self.events
        out["self"] = self.self_
        return out


@dataclass(frozen=True, slots=True)
class PublicJobResult:
    """`PublicJobResult`, as carried on the wire."""

    output: str
    #: Always present; ``None`` when the server has no value for it.
    data: Any | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped — a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "PublicJobResult":
        """Decode a `PublicJobResult` from its JSON object."""
        known = {"output", "data"}
        return cls(
            output=data.get("output"),
            data=data.get("data"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["output"] = self.output
        out["data"] = self.data
        return out


class PublicJobStatus(StrEnum):
    """The `PublicJobStatus` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    RUNNING = "running"
    SUCCEEDED = "succeeded"
    FAILED = "failed"


@dataclass(frozen=True, slots=True)
class PublicJudgeDecision:
    """`PublicJudgeDecision`, as carried on the wire."""

    bounces: float
    kind: str
    max_bounces: float
    status: PublicJudgeDecisionStatus
    step_kind: str
    #: Always present; ``None`` when the server has no value for it.
    rubric_id: str | None = None
    #: Always present; ``None`` when the server has no value for it.
    rubric_name: str | None = None
    #: Always present; ``None`` when the server has no value for it.
    threshold: float | None = None
    #: Always present; ``None`` when the server has no value for it.
    verdict: PublicJudgeVerdict | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped — a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "PublicJudgeDecision":
        """Decode a `PublicJudgeDecision` from its JSON object."""
        known = {"bounces", "kind", "maxBounces", "status", "stepKind", "rubricId", "rubricName", "threshold", "verdict"}
        return cls(
            bounces=data.get("bounces"),
            kind=data.get("kind"),
            max_bounces=data.get("maxBounces"),
            status=_enum(PublicJudgeDecisionStatus, data.get("status")),
            step_kind=data.get("stepKind"),
            rubric_id=data.get("rubricId"),
            rubric_name=data.get("rubricName"),
            threshold=data.get("threshold"),
            verdict=None if data.get("verdict") is None else PublicJudgeVerdict.from_dict(data.get("verdict")),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["bounces"] = self.bounces
        out["kind"] = self.kind
        out["maxBounces"] = self.max_bounces
        out["status"] = _encode(self.status)
        out["stepKind"] = self.step_kind
        out["rubricId"] = self.rubric_id
        out["rubricName"] = self.rubric_name
        out["threshold"] = self.threshold
        out["verdict"] = _encode(self.verdict)
        return out


class PublicJudgeDecisionStatus(StrEnum):
    """The `PublicJudgeDecisionStatus` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    EVALUATING = "evaluating"
    AWAITING_DECISION = "awaiting_decision"
    BOUNCING = "bouncing"
    PASSED = "passed"
    FAILED = "failed"
    SKIPPED = "skipped"


@dataclass(frozen=True, slots=True)
class PublicJudgeFinding:
    """`PublicJudgeFinding`, as carried on the wire."""

    severity: Severity
    title: str
    #: May be absent entirely.
    detail: str | None = None
    #: May be absent entirely.
    where: str | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped — a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "PublicJudgeFinding":
        """Decode a `PublicJudgeFinding` from its JSON object."""
        known = {"severity", "title", "detail", "where"}
        return cls(
            severity=_enum(Severity, data.get("severity")),
            title=data.get("title"),
            detail=data.get("detail"),
            where=data.get("where"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["severity"] = _encode(self.severity)
        out["title"] = self.title
        if self.detail is not None:
            out["detail"] = self.detail
        if self.where is not None:
            out["where"] = self.where
        return out


@dataclass(frozen=True, slots=True)
class PublicJudgeVerdict:
    """`PublicJudgeVerdict`, as carried on the wire."""

    findings: list[PublicJudgeFinding]
    score: float
    summary: str

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped — a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "PublicJudgeVerdict":
        """Decode a `PublicJudgeVerdict` from its JSON object."""
        known = {"findings", "score", "summary"}
        return cls(
            findings=[PublicJudgeFinding.from_dict(item) for item in data.get("findings") or []],
            score=data.get("score"),
            summary=data.get("summary"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["findings"] = [_encode(item) for item in self.findings]
        out["score"] = self.score
        out["summary"] = self.summary
        return out


@dataclass(frozen=True, slots=True)
class PublicNotificationList:
    """`PublicNotificationList`, as carried on the wire."""

    notifications: list[Notification]

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped — a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "PublicNotificationList":
        """Decode a `PublicNotificationList` from its JSON object."""
        known = {"notifications"}
        return cls(
            notifications=[Notification.from_dict(item) for item in data.get("notifications") or []],
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["notifications"] = [_encode(item) for item in self.notifications]
        return out


@dataclass(frozen=True, slots=True)
class PublicPipeline:
    """`PublicPipeline`, as carried on the wire."""

    headless_startable: bool
    name: str
    pipeline_id: str
    public: bool
    steps: list[str]

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped — a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "PublicPipeline":
        """Decode a `PublicPipeline` from its JSON object."""
        known = {"headlessStartable", "name", "pipelineId", "public", "steps"}
        return cls(
            headless_startable=data.get("headlessStartable"),
            name=data.get("name"),
            pipeline_id=data.get("pipelineId"),
            public=data.get("public"),
            steps=[item for item in data.get("steps") or []],
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["headlessStartable"] = self.headless_startable
        out["name"] = self.name
        out["pipelineId"] = self.pipeline_id
        out["public"] = self.public
        out["steps"] = [_encode(item) for item in self.steps]
        return out


@dataclass(frozen=True, slots=True)
class PublicPipelineList:
    """`PublicPipelineList`, as carried on the wire."""

    pipelines: list[PublicPipeline]

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped — a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "PublicPipelineList":
        """Decode a `PublicPipelineList` from its JSON object."""
        known = {"pipelines"}
        return cls(
            pipelines=[PublicPipeline.from_dict(item) for item in data.get("pipelines") or []],
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["pipelines"] = [_encode(item) for item in self.pipelines]
        return out


@dataclass(frozen=True, slots=True)
class PublicReplyFinding:
    """`PublicReplyFinding`, as carried on the wire."""

    reply: str

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped — a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "PublicReplyFinding":
        """Decode a `PublicReplyFinding` from its JSON object."""
        known = {"reply"}
        return cls(
            reply=data.get("reply"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["reply"] = self.reply
        return out


@dataclass(frozen=True, slots=True)
class PublicRequirementsDecision:
    """`PublicRequirementsDecision`, as carried on the wire."""

    findings: list[PublicReviewFinding]
    iteration: float
    kind: str
    max_iterations: float
    review_id: str
    status: PublicRequirementsDecisionStatus
    task_id: str
    #: Always present; ``None`` when the server has no value for it.
    incorporated_requirements: str | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped — a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "PublicRequirementsDecision":
        """Decode a `PublicRequirementsDecision` from its JSON object."""
        known = {"findings", "iteration", "kind", "maxIterations", "reviewId", "status", "taskId", "incorporatedRequirements"}
        return cls(
            findings=[PublicReviewFinding.from_dict(item) for item in data.get("findings") or []],
            iteration=data.get("iteration"),
            kind=data.get("kind"),
            max_iterations=data.get("maxIterations"),
            review_id=data.get("reviewId"),
            status=_enum(PublicRequirementsDecisionStatus, data.get("status")),
            task_id=data.get("taskId"),
            incorporated_requirements=data.get("incorporatedRequirements"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["findings"] = [_encode(item) for item in self.findings]
        out["iteration"] = self.iteration
        out["kind"] = self.kind
        out["maxIterations"] = self.max_iterations
        out["reviewId"] = self.review_id
        out["status"] = _encode(self.status)
        out["taskId"] = self.task_id
        out["incorporatedRequirements"] = self.incorporated_requirements
        return out


class PublicRequirementsDecisionStatus(StrEnum):
    """The `PublicRequirementsDecisionStatus` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    READY = "ready"
    INCORPORATING = "incorporating"
    REVIEWING = "reviewing"
    MERGED = "merged"
    EXCEEDED = "exceeded"
    INCORPORATED = "incorporated"


@dataclass(frozen=True, slots=True)
class PublicResolveExceeded:
    """`PublicResolveExceeded`, as carried on the wire."""

    choice: PublicResolveExceededChoice

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped — a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "PublicResolveExceeded":
        """Decode a `PublicResolveExceeded` from its JSON object."""
        known = {"choice"}
        return cls(
            choice=_enum(PublicResolveExceededChoice, data.get("choice")),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["choice"] = _encode(self.choice)
        return out


class PublicResolveExceededChoice(StrEnum):
    """The `PublicResolveExceededChoice` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    EXTRA_ROUND = "extra-round"
    PROCEED = "proceed"
    STOP_RESET = "stop-reset"


@dataclass(frozen=True, slots=True)
class PublicResolveJudge:
    """`PublicResolveJudge`, as carried on the wire."""

    choice: PublicResolveJudgeChoice
    #: May be absent entirely.
    feedback: str | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped — a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "PublicResolveJudge":
        """Decode a `PublicResolveJudge` from its JSON object."""
        known = {"choice", "feedback"}
        return cls(
            choice=_enum(PublicResolveJudgeChoice, data.get("choice")),
            feedback=data.get("feedback"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["choice"] = _encode(self.choice)
        if self.feedback is not None:
            out["feedback"] = self.feedback
        return out


class PublicResolveJudgeChoice(StrEnum):
    """The `PublicResolveJudgeChoice` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    PROCEED = "proceed"
    BOUNCE = "bounce"
    STOP = "stop"


@dataclass(frozen=True, slots=True)
class PublicReviewFinding:
    """`PublicReviewFinding`, as carried on the wire."""

    category: PublicReviewFindingCategory
    detail: str
    item_id: str
    severity: PublicReviewFindingSeverity
    status: PublicReviewFindingStatus
    title: str
    #: Always present; ``None`` when the server has no value for it.
    reply: str | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped — a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "PublicReviewFinding":
        """Decode a `PublicReviewFinding` from its JSON object."""
        known = {"category", "detail", "itemId", "severity", "status", "title", "reply"}
        return cls(
            category=_enum(PublicReviewFindingCategory, data.get("category")),
            detail=data.get("detail"),
            item_id=data.get("itemId"),
            severity=_enum(PublicReviewFindingSeverity, data.get("severity")),
            status=_enum(PublicReviewFindingStatus, data.get("status")),
            title=data.get("title"),
            reply=data.get("reply"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["category"] = _encode(self.category)
        out["detail"] = self.detail
        out["itemId"] = self.item_id
        out["severity"] = _encode(self.severity)
        out["status"] = _encode(self.status)
        out["title"] = self.title
        out["reply"] = self.reply
        return out


class PublicReviewFindingCategory(StrEnum):
    """The `PublicReviewFindingCategory` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    GAP = "gap"
    CLARIFICATION = "clarification"
    ASSUMPTION = "assumption"
    RISK = "risk"
    QUESTION = "question"


class PublicReviewFindingSeverity(StrEnum):
    """The `PublicReviewFindingSeverity` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    LOW = "low"
    MEDIUM = "medium"
    HIGH = "high"


class PublicReviewFindingStatus(StrEnum):
    """The `PublicReviewFindingStatus` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    OPEN = "open"
    ANSWERED = "answered"
    RESOLVED = "resolved"
    DISMISSED = "dismissed"
    RECOMMEND_REQUESTED = "recommend_requested"


@dataclass(frozen=True, slots=True)
class PublicRun:
    """`PublicRun`, as carried on the wire."""

    created_at: float
    current_step: float
    run_id: str
    status: RunStatus
    steps: list[PublicRunStep]
    task_id: str
    #: Always present; ``None`` when the server has no value for it.
    error: RunError | None = None
    #: Always present; ``None`` when the server has no value for it.
    pull_request: RunPullRequest | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped — a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "PublicRun":
        """Decode a `PublicRun` from its JSON object."""
        known = {"createdAt", "currentStep", "runId", "status", "steps", "taskId", "error", "pullRequest"}
        return cls(
            created_at=data.get("createdAt"),
            current_step=data.get("currentStep"),
            run_id=data.get("runId"),
            status=_enum(RunStatus, data.get("status")),
            steps=[PublicRunStep.from_dict(item) for item in data.get("steps") or []],
            task_id=data.get("taskId"),
            error=None if data.get("error") is None else RunError.from_dict(data.get("error")),
            pull_request=None if data.get("pullRequest") is None else RunPullRequest.from_dict(data.get("pullRequest")),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["createdAt"] = self.created_at
        out["currentStep"] = self.current_step
        out["runId"] = self.run_id
        out["status"] = _encode(self.status)
        out["steps"] = [_encode(item) for item in self.steps]
        out["taskId"] = self.task_id
        out["error"] = _encode(self.error)
        out["pullRequest"] = _encode(self.pull_request)
        return out


@dataclass(frozen=True, slots=True)
class PublicRunStep:
    """`PublicRunStep`, as carried on the wire."""

    agent_kind: str
    progress: float
    state: StepState
    #: Always present; ``None`` when the server has no value for it.
    subtasks: RunSubtaskCounts | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped — a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "PublicRunStep":
        """Decode a `PublicRunStep` from its JSON object."""
        known = {"agentKind", "progress", "state", "subtasks"}
        return cls(
            agent_kind=data.get("agentKind"),
            progress=data.get("progress"),
            state=_enum(StepState, data.get("state")),
            subtasks=None if data.get("subtasks") is None else RunSubtaskCounts.from_dict(data.get("subtasks")),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["agentKind"] = self.agent_kind
        out["progress"] = self.progress
        out["state"] = _encode(self.state)
        out["subtasks"] = _encode(self.subtasks)
        return out


@dataclass(frozen=True, slots=True)
class PublicService:
    """`PublicService`, as carried on the wire."""

    description: str
    service_id: str
    status: TaskStatus
    title: str
    type_: PublicServiceType

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped — a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "PublicService":
        """Decode a `PublicService` from its JSON object."""
        known = {"description", "serviceId", "status", "title", "type"}
        return cls(
            description=data.get("description"),
            service_id=data.get("serviceId"),
            status=_enum(TaskStatus, data.get("status")),
            title=data.get("title"),
            type_=_enum(PublicServiceType, data.get("type")),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["description"] = self.description
        out["serviceId"] = self.service_id
        out["status"] = _encode(self.status)
        out["title"] = self.title
        out["type"] = _encode(self.type_)
        return out


@dataclass(frozen=True, slots=True)
class PublicServiceList:
    """`PublicServiceList`, as carried on the wire."""

    services: list[PublicService]

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped — a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "PublicServiceList":
        """Decode a `PublicServiceList` from its JSON object."""
        known = {"services"}
        return cls(
            services=[PublicService.from_dict(item) for item in data.get("services") or []],
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["services"] = [_encode(item) for item in self.services]
        return out


class PublicServiceType(StrEnum):
    """The `PublicServiceType` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    FRONTEND = "frontend"
    SERVICE = "service"
    LIBRARY = "library"
    DOCUMENT = "document"
    API = "api"
    DATABASE = "database"
    QUEUE = "queue"
    INTEGRATION = "integration"
    EXTERNAL = "external"
    ENVIRONMENT = "environment"


@dataclass(frozen=True, slots=True)
class PublicSetFindingStatus:
    """`PublicSetFindingStatus`, as carried on the wire."""

    status: PublicSetFindingStatusStatus

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped — a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "PublicSetFindingStatus":
        """Decode a `PublicSetFindingStatus` from its JSON object."""
        known = {"status"}
        return cls(
            status=_enum(PublicSetFindingStatusStatus, data.get("status")),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["status"] = _encode(self.status)
        return out


class PublicSetFindingStatusStatus(StrEnum):
    """The `PublicSetFindingStatusStatus` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    DISMISSED = "dismissed"
    OPEN = "open"


@dataclass(frozen=True, slots=True)
class PublicTask:
    """`PublicTask`, as carried on the wire."""

    description: str
    progress: float
    service_id: str
    status: TaskStatus
    task_id: str
    task_type: str
    title: str
    #: Always present; ``None`` when the server has no value for it.
    pull_request_url: str | None = None
    #: Always present; ``None`` when the server has no value for it.
    run_id: str | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped — a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "PublicTask":
        """Decode a `PublicTask` from its JSON object."""
        known = {"description", "progress", "serviceId", "status", "taskId", "taskType", "title", "pullRequestUrl", "runId"}
        return cls(
            description=data.get("description"),
            progress=data.get("progress"),
            service_id=data.get("serviceId"),
            status=_enum(TaskStatus, data.get("status")),
            task_id=data.get("taskId"),
            task_type=data.get("taskType"),
            title=data.get("title"),
            pull_request_url=data.get("pullRequestUrl"),
            run_id=data.get("runId"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["description"] = self.description
        out["progress"] = self.progress
        out["serviceId"] = self.service_id
        out["status"] = _encode(self.status)
        out["taskId"] = self.task_id
        out["taskType"] = self.task_type
        out["title"] = self.title
        out["pullRequestUrl"] = self.pull_request_url
        out["runId"] = self.run_id
        return out


@dataclass(frozen=True, slots=True)
class PublicTaskList:
    """`PublicTaskList`, as carried on the wire."""

    tasks: list[PublicTask]
    #: Always present; ``None`` when the server has no value for it.
    next_cursor: str | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped — a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "PublicTaskList":
        """Decode a `PublicTaskList` from its JSON object."""
        known = {"tasks", "nextCursor"}
        return cls(
            tasks=[PublicTask.from_dict(item) for item in data.get("tasks") or []],
            next_cursor=data.get("nextCursor"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["tasks"] = [_encode(item) for item in self.tasks]
        out["nextCursor"] = self.next_cursor
        return out


@dataclass(frozen=True, slots=True)
class PublicTaskTicket:
    """`PublicTaskTicket`, as carried on the wire."""

    ref: str
    source: str

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped — a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "PublicTaskTicket":
        """Decode a `PublicTaskTicket` from its JSON object."""
        known = {"ref", "source"}
        return cls(
            ref=data.get("ref"),
            source=data.get("source"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["ref"] = self.ref
        out["source"] = self.source
        return out


@dataclass(frozen=True, slots=True)
class PublicUsage:
    """`PublicUsage`, as carried on the wire."""

    budget: PublicUsageBudget
    currency: str
    period_start: float
    rows: list[PublicUsageRow]

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped — a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "PublicUsage":
        """Decode a `PublicUsage` from its JSON object."""
        known = {"budget", "currency", "periodStart", "rows"}
        return cls(
            budget=PublicUsageBudget.from_dict(data.get("budget")),
            currency=data.get("currency"),
            period_start=data.get("periodStart"),
            rows=[PublicUsageRow.from_dict(item) for item in data.get("rows") or []],
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["budget"] = _encode(self.budget)
        out["currency"] = self.currency
        out["periodStart"] = self.period_start
        out["rows"] = [_encode(item) for item in self.rows]
        return out


@dataclass(frozen=True, slots=True)
class PublicUsageBudget:
    """`PublicUsageBudget`, as carried on the wire."""

    cost_limit: float
    cost_spent: float
    exceeded: bool
    input_tokens: float
    output_tokens: float

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped — a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "PublicUsageBudget":
        """Decode a `PublicUsageBudget` from its JSON object."""
        known = {"costLimit", "costSpent", "exceeded", "inputTokens", "outputTokens"}
        return cls(
            cost_limit=data.get("costLimit"),
            cost_spent=data.get("costSpent"),
            exceeded=data.get("exceeded"),
            input_tokens=data.get("inputTokens"),
            output_tokens=data.get("outputTokens"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["costLimit"] = self.cost_limit
        out["costSpent"] = self.cost_spent
        out["exceeded"] = self.exceeded
        out["inputTokens"] = self.input_tokens
        out["outputTokens"] = self.output_tokens
        return out


@dataclass(frozen=True, slots=True)
class PublicUsageRow:
    """`PublicUsageRow`, as carried on the wire."""

    billing: PublicUsageRowBilling
    calls: float
    cost_estimate: float
    input_tokens: float
    model: str
    output_tokens: float
    provider: str
    #: Always present; ``None`` when the server has no value for it.
    vendor: str | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped — a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "PublicUsageRow":
        """Decode a `PublicUsageRow` from its JSON object."""
        known = {"billing", "calls", "costEstimate", "inputTokens", "model", "outputTokens", "provider", "vendor"}
        return cls(
            billing=_enum(PublicUsageRowBilling, data.get("billing")),
            calls=data.get("calls"),
            cost_estimate=data.get("costEstimate"),
            input_tokens=data.get("inputTokens"),
            model=data.get("model"),
            output_tokens=data.get("outputTokens"),
            provider=data.get("provider"),
            vendor=data.get("vendor"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["billing"] = _encode(self.billing)
        out["calls"] = self.calls
        out["costEstimate"] = self.cost_estimate
        out["inputTokens"] = self.input_tokens
        out["model"] = self.model
        out["outputTokens"] = self.output_tokens
        out["provider"] = self.provider
        out["vendor"] = self.vendor
        return out


class PublicUsageRowBilling(StrEnum):
    """The `PublicUsageRowBilling` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    METERED = "metered"
    SUBSCRIPTION = "subscription"


@dataclass(frozen=True, slots=True)
class RunError:
    """`RunError`, as carried on the wire."""

    code: str
    message: str

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped — a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "RunError":
        """Decode a `RunError` from its JSON object."""
        known = {"code", "message"}
        return cls(
            code=data.get("code"),
            message=data.get("message"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["code"] = self.code
        out["message"] = self.message
        return out


@dataclass(frozen=True, slots=True)
class RunPullRequest:
    """`RunPullRequest`, as carried on the wire."""

    url: str
    #: Always present; ``None`` when the server has no value for it.
    branch: str | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped — a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "RunPullRequest":
        """Decode a `RunPullRequest` from its JSON object."""
        known = {"url", "branch"}
        return cls(
            url=data.get("url"),
            branch=data.get("branch"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["url"] = self.url
        out["branch"] = self.branch
        return out


class RunStatus(StrEnum):
    """The `RunStatus` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    RUNNING = "running"
    BLOCKED = "blocked"
    PAUSED = "paused"
    DONE = "done"
    FAILED = "failed"


@dataclass(frozen=True, slots=True)
class RunSubtaskCounts:
    """`RunSubtaskCounts`, as carried on the wire."""

    completed: float
    in_progress: float
    total: float

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped — a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "RunSubtaskCounts":
        """Decode a `RunSubtaskCounts` from its JSON object."""
        known = {"completed", "inProgress", "total"}
        return cls(
            completed=data.get("completed"),
            in_progress=data.get("inProgress"),
            total=data.get("total"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        out["completed"] = self.completed
        out["inProgress"] = self.in_progress
        out["total"] = self.total
        return out


class Severity(StrEnum):
    """The `Severity` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    LOW = "low"
    MEDIUM = "medium"
    HIGH = "high"
    CRITICAL = "critical"


@dataclass(frozen=True, slots=True)
class StartPublicTask:
    """`StartPublicTask`, as carried on the wire."""

    #: May be absent entirely.
    pipeline_id: str | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped — a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "StartPublicTask":
        """Decode a `StartPublicTask` from its JSON object."""
        known = {"pipelineId"}
        return cls(
            pipeline_id=data.get("pipelineId"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        if self.pipeline_id is not None:
            out["pipelineId"] = self.pipeline_id
        return out


class StepState(StrEnum):
    """The `StepState` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    PENDING = "pending"
    WORKING = "working"
    WAITING_DECISION = "waiting_decision"
    DONE = "done"


class TaskStatus(StrEnum):
    """The `TaskStatus` vocabulary.
    A `StrEnum`, so a member IS its wire string: it compares equal to it, formats as it in
    an f-string, and serialises as it. A plain `(str, Enum)` would satisfy the first of
    those and silently fail the other two — `str(TaskStatus.PLANNED)` is
    "TaskStatus.PLANNED", which is the value that ends up in a log line or a report.
    An UNKNOWN value decodes to the plain string rather than raising: this surface is
    additive, and a client that refused a value the server legitimately added would break on
    a release it was never told about.
    """
    PLANNED = "planned"
    READY = "ready"
    IN_PROGRESS = "in_progress"
    BLOCKED = "blocked"
    PR_READY = "pr_ready"
    DONE = "done"


@dataclass(frozen=True, slots=True)
class UpdatePublicTask:
    """`UpdatePublicTask`, as carried on the wire."""

    #: May be absent entirely.
    description: str | None = None
    #: May be absent entirely.
    title: str | None = None

    #: Fields the server sent that this SDK release has no attribute for. `/api/v1` is
    #: additive, so these are RETAINED rather than dropped — a caller on an older SDK can
    #: still reach a newly added field instead of having to upgrade first.
    extra: dict[str, Any] = field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> "UpdatePublicTask":
        """Decode a `UpdatePublicTask` from its JSON object."""
        known = {"description", "title"}
        return cls(
            description=data.get("description"),
            title=data.get("title"),
            extra={k: v for k, v in data.items() if k not in known},
        )

    def to_dict(self) -> dict[str, Any]:
        """Encode back to the JSON object shape the API expects."""
        out: dict[str, Any] = dict(self.extra)
        if self.description is not None:
            out["description"] = self.description
        if self.title is not None:
            out["title"] = self.title
        return out


#: `PublicDecision` — one of: PublicRequirementsDecision, PublicForkDecision, PublicJudgeDecision. Discriminated by `kind`; decode with `decode_public_decision`.
PublicDecision: TypeAlias = PublicRequirementsDecision | PublicForkDecision | PublicJudgeDecision

def decode_public_decision(data: Mapping[str, Any]) -> PublicDecision:
    """Decode a `PublicDecision` by its `kind` tag.
    An unrecognised tag raises rather than guessing a variant: every variant here has a
    different shape, so picking the wrong one would hand back a value whose fields are
    silently absent instead of an error naming what was not understood.
    """
    tag = data.get("kind")
    if tag == "requirements-review":
        return PublicRequirementsDecision.from_dict(data)
    if tag == "fork":
        return PublicForkDecision.from_dict(data)
    if tag == "judge":
        return PublicJudgeDecision.from_dict(data)
    raise ValueError(f"Unknown PublicDecision kind: {tag!r}")
