// GENERATED by `pnpm gen:sdk` from docs/openapi.json — DO NOT EDIT BY HAND.
// Regenerate after any change to the `/api/v1` contracts; `pnpm check:sdk` fails CI on drift.

package ai.catfactory.sdk.model;

import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonProperty;
import org.jspecify.annotations.Nullable;

/**
 * The {@code PublicJobResult} wire model.
 * @param data Always present; {@code null} when the server has no value for it.
 * @param output the {@code output} field.
 */
@JsonIgnoreProperties(ignoreUnknown = true)
public record PublicJobResult(
    /** Always present; {@code null} when the server has no value for it. */
    @JsonProperty("data") @Nullable Object data,

    @JsonProperty("output") String output
) {

    /** A new builder for {@link PublicJobResult}. */
    public static Builder builder() {
        return new Builder();
    }

    /**
     * Fluent builder for {@link PublicJobResult}.
     * Every setter is nullable so a caller supplies only what it means to send. Java has no
     * default arguments and Kotlin cannot synthesise them for a Java constructor, so this is the
     * shape that reads naturally from both languages.
     */
    public static final class Builder {
        private @Nullable Object data;
        private @Nullable String output;

        /** Set {@code data}. */
        public Builder data(@Nullable Object data) {
            this.data = data;
            return this;
        }

        /** Set {@code output}. */
        public Builder output(@Nullable String output) {
            this.output = output;
            return this;
        }

        /** Build the {@link PublicJobResult}. */
        public PublicJobResult build() {
            return new PublicJobResult(data, output);
        }
    }
}
