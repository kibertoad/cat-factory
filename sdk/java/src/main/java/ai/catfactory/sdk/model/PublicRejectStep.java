// GENERATED by `pnpm gen:sdk` from docs/openapi.json — DO NOT EDIT BY HAND.
// Regenerate after any change to the `/api/v1` contracts; `pnpm check:sdk` fails CI on drift.

package ai.catfactory.sdk.model;

import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import org.jspecify.annotations.Nullable;

/**
 * The {@code PublicRejectStep} wire model.
 * @param reason May be absent entirely. Length 0..2000.
 */
@JsonIgnoreProperties(ignoreUnknown = true)
public record PublicRejectStep(
    /** May be absent entirely. Length 0..2000. */
    @JsonInclude(JsonInclude.Include.NON_NULL) @JsonProperty("reason") @Nullable String reason
) {

    /** A new builder for {@link PublicRejectStep}. */
    public static Builder builder() {
        return new Builder();
    }

    /**
     * Fluent builder for {@link PublicRejectStep}.
     * Every setter is nullable so a caller supplies only what it means to send. Java has no
     * default arguments and Kotlin cannot synthesise them for a Java constructor, so this is the
     * shape that reads naturally from both languages.
     */
    public static final class Builder {
        private @Nullable String reason;

        /** Set {@code reason}. */
        public Builder reason(@Nullable String reason) {
            this.reason = reason;
            return this;
        }

        /** Build the {@link PublicRejectStep}. */
        public PublicRejectStep build() {
            return new PublicRejectStep(reason);
        }
    }
}
