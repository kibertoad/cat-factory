// GENERATED by `pnpm gen:sdk` from docs/openapi.json — DO NOT EDIT BY HAND.
// Regenerate after any change to the `/api/v1` contracts; `pnpm check:sdk` fails CI on drift.

package ai.catfactory.sdk.model;

import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonProperty;
import org.jspecify.annotations.Nullable;

/**
 * The {@code ErrorResponse} wire model.
 * @param error the {@code error} field.
 */
@JsonIgnoreProperties(ignoreUnknown = true)
public record ErrorResponse(
    @JsonProperty("error") ErrorResponseError error
) {

    /** A new builder for {@link ErrorResponse}. */
    public static Builder builder() {
        return new Builder();
    }

    /**
     * Fluent builder for {@link ErrorResponse}.
     * Every setter is nullable so a caller supplies only what it means to send. Java has no
     * default arguments and Kotlin cannot synthesise them for a Java constructor, so this is the
     * shape that reads naturally from both languages.
     */
    public static final class Builder {
        private @Nullable ErrorResponseError error;

        /** Set {@code error}. */
        public Builder error(@Nullable ErrorResponseError error) {
            this.error = error;
            return this;
        }

        /** Build the {@link ErrorResponse}. */
        public ErrorResponse build() {
            return new ErrorResponse(error);
        }
    }
}
