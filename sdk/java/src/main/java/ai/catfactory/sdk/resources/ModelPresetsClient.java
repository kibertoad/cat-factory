// GENERATED by `pnpm gen:sdk` from docs/openapi.json — DO NOT EDIT BY HAND.
// Regenerate after any change to the `/api/v1` contracts; `pnpm check:sdk` fails CI on drift.

package ai.catfactory.sdk.resources;

import ai.catfactory.sdk.EventStream;
import ai.catfactory.sdk.PageIterator;
import ai.catfactory.sdk.Transport;
import ai.catfactory.sdk.model.*;
import com.fasterxml.jackson.core.type.TypeReference;
import java.util.Iterator;
import java.util.Map;
import org.jspecify.annotations.Nullable;

/**
 * The model presets a task can pin, including which is the workspace default: what decides which
 * model each agent step runs on, and so what a run costs. Availability is not repeated here; join
 * `baseModelId` against the models group, which keeps unconfigured and policy-refused apart.
 * Reached from {@link CatFactoryClient}; not constructed directly.
 */
public final class ModelPresetsClient {
    private final Transport transport;

    ModelPresetsClient(Transport transport) {
        this.transport = transport;
    }

    /**
     * List the workspace’s model presets
     * The preset library, including which row is the workspace default that a task pinning none
     * resolves. `baseModelId` is the model every agent step runs on under the preset, and
     * `overrides` names the agent kinds that run on something else, which is usually the one that
     * matters: two presets often differ only in what the CODER gets. Whether a preset can actually
     * be dispatched to is NOT repeated here, because the models endpoint already answers it while
     * keeping unconfigured apart from refused-by-policy; join on `baseModelId`.
     * {@code GET /api/v1/model-presets} (operation {@code listPublicModelPresets}).
     */
    public ListPublicModelPresetsResponse list() {
        return transport.request("GET", "/api/v1/model-presets", null, Map.of(), new TypeReference<ListPublicModelPresetsResponse>() {});
    }
}
