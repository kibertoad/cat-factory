// GENERATED by `pnpm gen:sdk` from docs/openapi.json — DO NOT EDIT BY HAND.
// Regenerate after any change to the `/api/v1` contracts; `pnpm check:sdk` fails CI on drift.

package ai.catfactory.sdk.model;

import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonProperty;
import java.util.List;
import org.jspecify.annotations.Nullable;

/**
 * The {@code SpecModule} wire model.
 * @param groups the {@code groups} field.
 * @param name Length 1..120.
 * @param summary Length 0..2000.
 */
@JsonIgnoreProperties(ignoreUnknown = true)
public record SpecModule(
    @JsonProperty("groups") List<RequirementGroup> groups,

    /** Length 1..120. */
    @JsonProperty("name") String name,

    /** Length 0..2000. */
    @JsonProperty("summary") String summary
) {

    /** A new builder for {@link SpecModule}. */
    public static Builder builder() {
        return new Builder();
    }

    /**
     * Fluent builder for {@link SpecModule}.
     * Every setter is nullable so a caller supplies only what it means to send. Java has no
     * default arguments and Kotlin cannot synthesise them for a Java constructor, so this is the
     * shape that reads naturally from both languages.
     */
    public static final class Builder {
        private @Nullable List<RequirementGroup> groups;
        private @Nullable String name;
        private @Nullable String summary;

        /** Set {@code groups}. */
        public Builder groups(@Nullable List<RequirementGroup> groups) {
            this.groups = groups;
            return this;
        }

        /** Set {@code name}. */
        public Builder name(@Nullable String name) {
            this.name = name;
            return this;
        }

        /** Set {@code summary}. */
        public Builder summary(@Nullable String summary) {
            this.summary = summary;
            return this;
        }

        /** Build the {@link SpecModule}. */
        public SpecModule build() {
            return new SpecModule(groups, name, summary);
        }
    }
}
