// GENERATED by `pnpm gen:sdk` from docs/openapi.json — DO NOT EDIT BY HAND.
// Regenerate after any change to the `/api/v1` contracts; `pnpm check:sdk` fails CI on drift.

package ai.catfactory.sdk.model;

import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonProperty;
import java.util.List;
import org.jspecify.annotations.Nullable;

/**
 * The {@code PublicVisualConfirmDecision} wire model.
 * @param attempts the {@code attempts} field.
 * @param degradedReason Always present; {@code null} when the server has no value for it.
 * @param kind the {@code kind} field.
 * @param maxAttempts the {@code maxAttempts} field.
 * @param pairs the {@code pairs} field.
 * @param phase the {@code phase} field.
 */
@JsonIgnoreProperties(ignoreUnknown = true)
public record PublicVisualConfirmDecision(
    @JsonProperty("attempts") Double attempts,

    /** Always present; {@code null} when the server has no value for it. */
    @JsonProperty("degradedReason") @Nullable String degradedReason,

    @JsonProperty("kind") String kind,

    @JsonProperty("maxAttempts") Double maxAttempts,

    @JsonProperty("pairs") List<PublicVisualConfirmDecisionPair> pairs,

    @JsonProperty("phase") PublicVisualConfirmDecisionPhase phase
) implements PublicDecision {

    /** A new builder for {@link PublicVisualConfirmDecision}. */
    public static Builder builder() {
        return new Builder();
    }

    /**
     * Fluent builder for {@link PublicVisualConfirmDecision}.
     * Every setter is nullable so a caller supplies only what it means to send. Java has no
     * default arguments and Kotlin cannot synthesise them for a Java constructor, so this is the
     * shape that reads naturally from both languages.
     */
    public static final class Builder {
        private @Nullable Double attempts;
        private @Nullable String degradedReason;
        private @Nullable String kind;
        private @Nullable Double maxAttempts;
        private @Nullable List<PublicVisualConfirmDecisionPair> pairs;
        private @Nullable PublicVisualConfirmDecisionPhase phase;

        /** Set {@code attempts}. */
        public Builder attempts(@Nullable Double attempts) {
            this.attempts = attempts;
            return this;
        }

        /** Set {@code degradedReason}. */
        public Builder degradedReason(@Nullable String degradedReason) {
            this.degradedReason = degradedReason;
            return this;
        }

        /** Set {@code kind}. */
        public Builder kind(@Nullable String kind) {
            this.kind = kind;
            return this;
        }

        /** Set {@code maxAttempts}. */
        public Builder maxAttempts(@Nullable Double maxAttempts) {
            this.maxAttempts = maxAttempts;
            return this;
        }

        /** Set {@code pairs}. */
        public Builder pairs(@Nullable List<PublicVisualConfirmDecisionPair> pairs) {
            this.pairs = pairs;
            return this;
        }

        /** Set {@code phase}. */
        public Builder phase(@Nullable PublicVisualConfirmDecisionPhase phase) {
            this.phase = phase;
            return this;
        }

        /** Build the {@link PublicVisualConfirmDecision}. */
        public PublicVisualConfirmDecision build() {
            return new PublicVisualConfirmDecision(attempts, degradedReason, kind, maxAttempts, pairs, phase);
        }
    }
}
