// GENERATED by `pnpm gen:sdk` from docs/openapi.json — DO NOT EDIT BY HAND.
// Regenerate after any change to the `/api/v1` contracts; `pnpm check:sdk` fails CI on drift.

package ai.catfactory.sdk.resources;

import ai.catfactory.sdk.EventStream;
import ai.catfactory.sdk.PageIterator;
import ai.catfactory.sdk.Transport;
import ai.catfactory.sdk.model.*;
import com.fasterxml.jackson.core.type.TypeReference;
import java.util.Iterator;
import java.util.Map;
import org.jspecify.annotations.Nullable;

/**
 * The risk policies a task can pin, including which is the workspace default: what decides whether
 * a run can land its pull request without a person, and how many attempts its CI fixer,
 * requirement rounds and release watch are given. Broader than merging, which is why it is not
 * called a merge preset.
 * Reached from {@link CatFactoryClient}; not constructed directly.
 */
public final class RiskPoliciesClient {
    private final Transport transport;

    RiskPoliciesClient(Transport transport) {
        this.transport = transport;
    }

    /**
     * List the workspace’s risk policies
     * The policy library, including which row is the workspace default that a task pinning none
     * resolves. `autoMergeEnabled` is the master switch that decides whether a run can land its
     * pull request without a person; `dryRunRoles` names the roles whose runs the policy forces
     * into dry-run mode, which is the difference between “this policy merges” and “this policy
     * merges for everyone except one role”. A policy also caps CI-fixer attempts, requirement and
     * tester iteration rounds and the release-health watch, which is why it is not called a merge
     * preset; the id is what a task pins as `riskPolicyId`.
     * {@code GET /api/v1/risk-policies} (operation {@code listPublicRiskPolicies}).
     */
    public ListPublicRiskPoliciesResponse list() {
        return transport.request("GET", "/api/v1/risk-policies", null, Map.of(), new TypeReference<ListPublicRiskPoliciesResponse>() {});
    }
}
