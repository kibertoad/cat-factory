// GENERATED by `pnpm gen:sdk` from docs/openapi.json — DO NOT EDIT BY HAND.
// Regenerate after any change to the `/api/v1` contracts; `pnpm check:sdk` fails CI on drift.

package ai.catfactory.sdk.model;

import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonProperty;
import java.util.List;
import org.jspecify.annotations.Nullable;

/**
 * The {@code PublicNotificationList} wire model.
 * @param notifications the {@code notifications} field.
 */
@JsonIgnoreProperties(ignoreUnknown = true)
public record PublicNotificationList(
    @JsonProperty("notifications") List<Notification> notifications
) {

    /** A new builder for {@link PublicNotificationList}. */
    public static Builder builder() {
        return new Builder();
    }

    /**
     * Fluent builder for {@link PublicNotificationList}.
     * Every setter is nullable so a caller supplies only what it means to send. Java has no
     * default arguments and Kotlin cannot synthesise them for a Java constructor, so this is the
     * shape that reads naturally from both languages.
     */
    public static final class Builder {
        private @Nullable List<Notification> notifications;

        /** Set {@code notifications}. */
        public Builder notifications(@Nullable List<Notification> notifications) {
            this.notifications = notifications;
            return this;
        }

        /** Build the {@link PublicNotificationList}. */
        public PublicNotificationList build() {
            return new PublicNotificationList(notifications);
        }
    }
}
