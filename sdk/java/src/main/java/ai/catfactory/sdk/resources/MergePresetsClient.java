// GENERATED by `pnpm gen:sdk` from docs/openapi.json — DO NOT EDIT BY HAND.
// Regenerate after any change to the `/api/v1` contracts; `pnpm check:sdk` fails CI on drift.

package ai.catfactory.sdk.resources;

import ai.catfactory.sdk.EventStream;
import ai.catfactory.sdk.PageIterator;
import ai.catfactory.sdk.Transport;
import ai.catfactory.sdk.model.*;
import com.fasterxml.jackson.core.type.TypeReference;
import java.util.Iterator;
import java.util.Map;
import org.jspecify.annotations.Nullable;

/**
 * The merge-threshold presets a task can resolve, including which is the workspace default: what
 * decides whether a run can land its pull request without a person.
 * Reached from {@link CatFactoryClient}; not constructed directly.
 */
public final class MergePresetsClient {
    private final Transport transport;

    MergePresetsClient(Transport transport) {
        this.transport = transport;
    }

    /**
     * List the workspace’s merge-threshold presets
     * The preset library, including which row is the workspace default that a task pinning none
     * resolves. `autoMergeEnabled` is the master switch that decides whether a run can land its
     * pull request without a person; `dryRunRoles` names the roles whose runs the preset forces
     * into dry-run mode, which is the difference between “this preset merges” and “this preset
     * merges for everyone except one role”.
     * {@code GET /api/v1/merge-presets} (operation {@code listPublicMergePresets}).
     */
    public ListPublicMergePresetsResponse list() {
        return transport.request("GET", "/api/v1/merge-presets", null, Map.of(), new TypeReference<ListPublicMergePresetsResponse>() {});
    }
}
