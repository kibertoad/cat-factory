// GENERATED by `pnpm gen:sdk` from docs/openapi.json — DO NOT EDIT BY HAND.
// Regenerate after any change to the `/api/v1` contracts; `pnpm check:sdk` fails CI on drift.

package ai.catfactory.sdk.model;

import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonProperty;
import org.jspecify.annotations.Nullable;

/**
 * The {@code RunError} wire model.
 * @param code the {@code code} field.
 * @param message the {@code message} field.
 */
@JsonIgnoreProperties(ignoreUnknown = true)
public record RunError(
    @JsonProperty("code") String code,

    @JsonProperty("message") String message
) {

    /** A new builder for {@link RunError}. */
    public static Builder builder() {
        return new Builder();
    }

    /**
     * Fluent builder for {@link RunError}.
     * Every setter is nullable so a caller supplies only what it means to send. Java has no
     * default arguments and Kotlin cannot synthesise them for a Java constructor, so this is the
     * shape that reads naturally from both languages.
     */
    public static final class Builder {
        private @Nullable String code;
        private @Nullable String message;

        /** Set {@code code}. */
        public Builder code(@Nullable String code) {
            this.code = code;
            return this;
        }

        /** Set {@code message}. */
        public Builder message(@Nullable String message) {
            this.message = message;
            return this;
        }

        /** Build the {@link RunError}. */
        public RunError build() {
            return new RunError(code, message);
        }
    }
}
