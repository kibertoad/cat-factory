// GENERATED by `pnpm gen:sdk` from docs/openapi.json — DO NOT EDIT BY HAND.
// Regenerate after any change to the `/api/v1` contracts; `pnpm check:sdk` fails CI on drift.

package ai.catfactory.sdk.model;

import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonProperty;
import org.jspecify.annotations.Nullable;

/**
 * The {@code PublicResolveExceeded} wire model.
 * @param choice the {@code choice} field.
 */
@JsonIgnoreProperties(ignoreUnknown = true)
public record PublicResolveExceeded(
    @JsonProperty("choice") PublicResolveExceededChoice choice
) {

    /** A new builder for {@link PublicResolveExceeded}. */
    public static Builder builder() {
        return new Builder();
    }

    /**
     * Fluent builder for {@link PublicResolveExceeded}.
     * Every setter is nullable so a caller supplies only what it means to send. Java has no
     * default arguments and Kotlin cannot synthesise them for a Java constructor, so this is the
     * shape that reads naturally from both languages.
     */
    public static final class Builder {
        private @Nullable PublicResolveExceededChoice choice;

        /** Set {@code choice}. */
        public Builder choice(@Nullable PublicResolveExceededChoice choice) {
            this.choice = choice;
            return this;
        }

        /** Build the {@link PublicResolveExceeded}. */
        public PublicResolveExceeded build() {
            return new PublicResolveExceeded(choice);
        }
    }
}
