// GENERATED by `pnpm gen:sdk` from docs/openapi.json — DO NOT EDIT BY HAND.
// Regenerate after any change to the `/api/v1` contracts; `pnpm check:sdk` fails CI on drift.

package ai.catfactory.sdk.model;

import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonProperty;
import org.jspecify.annotations.Nullable;

/**
 * The {@code SpecReadIssue} wire model.
 * @param dropped the {@code dropped} field.
 * @param kind the {@code kind} field.
 * @param path the {@code path} field.
 */
@JsonIgnoreProperties(ignoreUnknown = true)
public record SpecReadIssue(
    @JsonProperty("dropped") Double dropped,

    @JsonProperty("kind") SpecReadIssueKind kind,

    @JsonProperty("path") String path
) {

    /** A new builder for {@link SpecReadIssue}. */
    public static Builder builder() {
        return new Builder();
    }

    /**
     * Fluent builder for {@link SpecReadIssue}.
     * Every setter is nullable so a caller supplies only what it means to send. Java has no
     * default arguments and Kotlin cannot synthesise them for a Java constructor, so this is the
     * shape that reads naturally from both languages.
     */
    public static final class Builder {
        private @Nullable Double dropped;
        private @Nullable SpecReadIssueKind kind;
        private @Nullable String path;

        /** Set {@code dropped}. */
        public Builder dropped(@Nullable Double dropped) {
            this.dropped = dropped;
            return this;
        }

        /** Set {@code kind}. */
        public Builder kind(@Nullable SpecReadIssueKind kind) {
            this.kind = kind;
            return this;
        }

        /** Set {@code path}. */
        public Builder path(@Nullable String path) {
            this.path = path;
            return this;
        }

        /** Build the {@link SpecReadIssue}. */
        public SpecReadIssue build() {
            return new SpecReadIssue(dropped, kind, path);
        }
    }
}
