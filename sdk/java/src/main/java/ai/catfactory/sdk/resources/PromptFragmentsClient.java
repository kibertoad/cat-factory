// GENERATED by `pnpm gen:sdk` from docs/openapi.json — DO NOT EDIT BY HAND.
// Regenerate after any change to the `/api/v1` contracts; `pnpm check:sdk` fails CI on drift.

package ai.catfactory.sdk.resources;

import ai.catfactory.sdk.EventStream;
import ai.catfactory.sdk.PageIterator;
import ai.catfactory.sdk.Transport;
import ai.catfactory.sdk.model.*;
import com.fasterxml.jackson.core.type.TypeReference;
import java.util.Iterator;
import java.util.Map;
import org.jspecify.annotations.Nullable;

/**
 * The best-practice standards this workspace holds its agents to: the deployment's shipped catalog
 * merged with the account's library and this board's own, each with the title an agent cites it
 * by, its one-line summary, its tags and which tier it came from. The id is what a task pins as a
 * `fragmentIds` member, and a review reports how closely it judged the change to follow each
 * standard it was given. The guidance text itself is not served: what a caller needs in order to
 * name a standard is its identity.
 * Reached from {@link CatFactoryClient}; not constructed directly.
 */
public final class PromptFragmentsClient {
    private final Transport transport;

    PromptFragmentsClient(Transport transport) {
        this.transport = transport;
    }

    /**
     * List the workspace's best-practice standards
     * List the best-practice standards the key’s workspace resolves: the deployment’s shipped
     * catalog merged with the account’s library and this board’s own, with later tiers overriding
     * earlier ones by id and a tombstoned entry absent. The discovery half of `fragmentIds` on
     * task creation, so the `fragmentId` read here is what a task pins, and an id this list does
     * not carry is refused by the create rather than dropped. Each entry carries what a picker (or
     * a model) decides from (title, category, one-line summary, tags, the `appliesTo` hint and
     * which tier it won on) and deliberately NOT the guidance body, which is the authored text of
     * the org’s standards rather than something a caller has to read in order to name one. A
     * `review` task’s reviewer additionally reports its ADHERENCE to every standard it was given,
     * so what is named on the create comes back rated on the run.
     * {@code GET /api/v1/prompt-fragments} (operation {@code listPublicPromptFragments}).
     */
    public PublicPromptFragmentList list() {
        return transport.request("GET", "/api/v1/prompt-fragments", null, Map.of(), new TypeReference<PublicPromptFragmentList>() {});
    }
}
