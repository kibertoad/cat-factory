// GENERATED by `pnpm gen:sdk` from docs/openapi.json — DO NOT EDIT BY HAND.
// Regenerate after any change to the `/api/v1` contracts; `pnpm check:sdk` fails CI on drift.

package ai.catfactory.sdk.model;

import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonProperty;
import java.util.List;
import org.jspecify.annotations.Nullable;

/**
 * The {@code PublicTaskList} wire model.
 * @param nextCursor Always present; {@code null} when the server has no value for it.
 * @param tasks the {@code tasks} field.
 */
@JsonIgnoreProperties(ignoreUnknown = true)
public record PublicTaskList(
    /** Always present; {@code null} when the server has no value for it. */
    @JsonProperty("nextCursor") @Nullable String nextCursor,

    @JsonProperty("tasks") List<PublicTask> tasks
) {

    /** A new builder for {@link PublicTaskList}. */
    public static Builder builder() {
        return new Builder();
    }

    /**
     * Fluent builder for {@link PublicTaskList}.
     * Every setter is nullable so a caller supplies only what it means to send. Java has no
     * default arguments and Kotlin cannot synthesise them for a Java constructor, so this is the
     * shape that reads naturally from both languages.
     */
    public static final class Builder {
        private @Nullable String nextCursor;
        private @Nullable List<PublicTask> tasks;

        /** Set {@code nextCursor}. */
        public Builder nextCursor(@Nullable String nextCursor) {
            this.nextCursor = nextCursor;
            return this;
        }

        /** Set {@code tasks}. */
        public Builder tasks(@Nullable List<PublicTask> tasks) {
            this.tasks = tasks;
            return this;
        }

        /** Build the {@link PublicTaskList}. */
        public PublicTaskList build() {
            return new PublicTaskList(nextCursor, tasks);
        }
    }
}
