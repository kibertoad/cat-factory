// GENERATED by `pnpm gen:sdk` from docs/openapi.json — DO NOT EDIT BY HAND.
// Regenerate after any change to the `/api/v1` contracts; `pnpm check:sdk` fails CI on drift.

package ai.catfactory.sdk.model;

import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonProperty;
import java.util.List;
import org.jspecify.annotations.Nullable;

/**
 * The {@code PublicServiceList} wire model.
 * @param services the {@code services} field.
 */
@JsonIgnoreProperties(ignoreUnknown = true)
public record PublicServiceList(
    @JsonProperty("services") List<PublicService> services
) {

    /** A new builder for {@link PublicServiceList}. */
    public static Builder builder() {
        return new Builder();
    }

    /**
     * Fluent builder for {@link PublicServiceList}.
     * Every setter is nullable so a caller supplies only what it means to send. Java has no
     * default arguments and Kotlin cannot synthesise them for a Java constructor, so this is the
     * shape that reads naturally from both languages.
     */
    public static final class Builder {
        private @Nullable List<PublicService> services;

        /** Set {@code services}. */
        public Builder services(@Nullable List<PublicService> services) {
            this.services = services;
            return this;
        }

        /** Build the {@link PublicServiceList}. */
        public PublicServiceList build() {
            return new PublicServiceList(services);
        }
    }
}
