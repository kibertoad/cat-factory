// GENERATED by `pnpm gen:sdk` from docs/openapi.json — DO NOT EDIT BY HAND.
// Regenerate after any change to the `/api/v1` contracts; `pnpm check:sdk` fails CI on drift.

package ai.catfactory.sdk.model;

import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonProperty;
import java.util.List;
import org.jspecify.annotations.Nullable;

/**
 * The {@code PublicUsage} wire model.
 * @param budget the {@code budget} field.
 * @param currency the {@code currency} field.
 * @param periodStart the {@code periodStart} field.
 * @param rows the {@code rows} field.
 */
@JsonIgnoreProperties(ignoreUnknown = true)
public record PublicUsage(
    @JsonProperty("budget") PublicUsageBudget budget,

    @JsonProperty("currency") String currency,

    @JsonProperty("periodStart") Double periodStart,

    @JsonProperty("rows") List<PublicUsageRow> rows
) {

    /** A new builder for {@link PublicUsage}. */
    public static Builder builder() {
        return new Builder();
    }

    /**
     * Fluent builder for {@link PublicUsage}.
     * Every setter is nullable so a caller supplies only what it means to send. Java has no
     * default arguments and Kotlin cannot synthesise them for a Java constructor, so this is the
     * shape that reads naturally from both languages.
     */
    public static final class Builder {
        private @Nullable PublicUsageBudget budget;
        private @Nullable String currency;
        private @Nullable Double periodStart;
        private @Nullable List<PublicUsageRow> rows;

        /** Set {@code budget}. */
        public Builder budget(@Nullable PublicUsageBudget budget) {
            this.budget = budget;
            return this;
        }

        /** Set {@code currency}. */
        public Builder currency(@Nullable String currency) {
            this.currency = currency;
            return this;
        }

        /** Set {@code periodStart}. */
        public Builder periodStart(@Nullable Double periodStart) {
            this.periodStart = periodStart;
            return this;
        }

        /** Set {@code rows}. */
        public Builder rows(@Nullable List<PublicUsageRow> rows) {
            this.rows = rows;
            return this;
        }

        /** Build the {@link PublicUsage}. */
        public PublicUsage build() {
            return new PublicUsage(budget, currency, periodStart, rows);
        }
    }
}
