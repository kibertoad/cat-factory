// Hand-written (NOT generated).

/**
 * The cat-factory public-API client for Java <b>and Kotlin</b>.
 *
 * <p>The package is {@link org.jspecify.annotations.NullMarked}: every reference type here is
 * non-null unless annotated {@link org.jspecify.annotations.Nullable}. That is what lets a Kotlin
 * consumer see real nullability instead of unchecked platform types, and is the main reason one
 * artifact serves both languages — see the SDK README for the full story and the residual gaps.
 */
@NullMarked
package ai.catfactory.sdk;

import org.jspecify.annotations.NullMarked;
