// GENERATED by `pnpm gen:sdk` from docs/openapi.json — DO NOT EDIT BY HAND.
// Regenerate after any change to the `/api/v1` contracts; `pnpm check:sdk` fails CI on drift.

package ai.catfactory.sdk.resources;

import ai.catfactory.sdk.EventStream;
import ai.catfactory.sdk.PageIterator;
import ai.catfactory.sdk.Transport;
import ai.catfactory.sdk.model.*;
import com.fasterxml.jackson.core.type.TypeReference;
import java.util.Iterator;
import java.util.Map;
import org.jspecify.annotations.Nullable;

/**
 * A service's in-repo specification: the structured requirement tree (modules → feature groups →
 * requirements, with their acceptance criteria and domain rules), the Gherkin rendered from it,
 * and the branch and commit the read describes. Read-only; the requirement ids are the join key
 * onto a run's report and outcome.
 * Reached from {@link CatFactoryClient}; not constructed directly.
 */
public final class SpecClient {
    private final Transport transport;

    SpecClient(Transport transport) {
        this.transport = transport;
    }

    /**
     * Get a service's in-repo specification
     * The prescriptive specification stored in the service’s own repository under `spec/`: modules
     * → feature groups → requirement items, each with its MoSCoW priority, its
     * `aspirational`/`established` implementation state and its Given/When/Then acceptance
     * criteria, plus the domain rules scoped to each group and the Gherkin `.feature` files
     * rendered from the same tree. `provenance` names the branch and commit the read describes,
     * because the default branch is not what a run with an open pull request is working against.
     * The requirement ids here are the join key onto `requirements` on a run’s report and outcome,
     * so criterion → evidence is a map lookup. Four outcomes are kept apart rather than folded:
     * `present: false` means the default branch holds no spec, a `503` with `reason:
     * "spec_read_failed"` means the repository could not be read, a `503` with `reason:
     * "vcs_not_configured"` means the deployment or workspace wired no version control, and a
     * partially readable spec is SERVED with `issues` naming each file that did not survive.
     * Read-only: the spec’s write path is a reviewed commit.
     * {@code GET /api/v1/services/{serviceId}/spec} (operation {@code getPublicServiceSpec}).
     */
    public PublicServiceSpec get(String serviceId) {
        return transport.request("GET", "/api/v1/services/" + Transport.pathSegment(serviceId) + "/spec", null, Map.of(), new TypeReference<PublicServiceSpec>() {});
    }

    /**
     * Get the specification one run was judged against
     * The same in-repo specification the service read serves, read at the branch THIS RUN pushed
     * its work to rather than at the repository default. That is the tree a run’s verdicts were
     * made against: while its pull request is open, every requirement the run itself ADDED is
     * absent from the default branch, so joining `requirements` rows from `GET
     * /api/v1/runs/{runId}/report` or `…/outcome` against the service read leaves exactly those
     * rows without a criterion. `provenance` names the branch and the commit, so a caller can see
     * which tree it got. `anchor` carries one value the service read cannot answer, `not_read`:
     * nothing was read, because the run’s spec read is gated on a tester having reported so that
     * the tree served is the one the verdicts were made against, and `provenance` is null there
     * and only there. The refusals are the service read’s: a `503` with `reason:
     * "spec_read_failed"` for a repository that could not be read, `"spec_ref_unresolved"` for a
     * branch that would not resolve, `"vcs_not_configured"` for a deployment or workspace that
     * wired no version control. An outage never reaches a `200`.
     * {@code GET /api/v1/runs/{runId}/spec} (operation {@code getPublicRunSpec}).
     */
    public PublicRunSpec getForRun(String runId) {
        return transport.request("GET", "/api/v1/runs/" + Transport.pathSegment(runId) + "/spec", null, Map.of(), new TypeReference<PublicRunSpec>() {});
    }
}
