// GENERATED by `pnpm gen:sdk` from docs/openapi.json — DO NOT EDIT BY HAND.
// Regenerate after any change to the `/api/v1` contracts; `pnpm check:sdk` fails CI on drift.

package ai.catfactory.sdk.model;

import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonProperty;
import java.util.List;
import org.jspecify.annotations.Nullable;

/**
 * The {@code PublicRunArtifactList} wire model.
 * @param artifacts the {@code artifacts} field.
 */
@JsonIgnoreProperties(ignoreUnknown = true)
public record PublicRunArtifactList(
    @JsonProperty("artifacts") List<PublicRunArtifact> artifacts
) {

    /** A new builder for {@link PublicRunArtifactList}. */
    public static Builder builder() {
        return new Builder();
    }

    /**
     * Fluent builder for {@link PublicRunArtifactList}.
     * Every setter is nullable so a caller supplies only what it means to send. Java has no
     * default arguments and Kotlin cannot synthesise them for a Java constructor, so this is the
     * shape that reads naturally from both languages.
     */
    public static final class Builder {
        private @Nullable List<PublicRunArtifact> artifacts;

        /** Set {@code artifacts}. */
        public Builder artifacts(@Nullable List<PublicRunArtifact> artifacts) {
            this.artifacts = artifacts;
            return this;
        }

        /** Build the {@link PublicRunArtifactList}. */
        public PublicRunArtifactList build() {
            return new PublicRunArtifactList(artifacts);
        }
    }
}
