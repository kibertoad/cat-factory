// GENERATED by `pnpm gen:sdk` from docs/openapi.json — DO NOT EDIT BY HAND.
// Regenerate after any change to the `/api/v1` contracts; `pnpm check:sdk` fails CI on drift.

package ai.catfactory.sdk.resources;

import ai.catfactory.sdk.EventStream;
import ai.catfactory.sdk.PageIterator;
import ai.catfactory.sdk.Transport;
import ai.catfactory.sdk.model.*;
import com.fasterxml.jackson.core.type.TypeReference;
import java.util.Iterator;
import java.util.Map;
import org.jspecify.annotations.Nullable;

/**
 * The workspace's human-actionable inbox: list, act on, or dismiss a run tail.
 * Reached from {@link CatFactoryClient}; not constructed directly.
 */
public final class NotificationsClient {
    private final Transport transport;

    NotificationsClient(Transport transport) {
        this.transport = transport;
    }

    /**
     * Act on a notification (no body).
     */
    public Notification act(String id) {
        return act(id, ActPublicNotificationRequest.builder().build());
    }

    /**
     * Act on a notification
     * Run a notification’s typed side-effect and resolve it: merge the PR (merge_review /
     * pipeline_complete) or retry the run (ci_failed / test_failed). Performs a real GitHub merge,
     * so it requires an admin-scoped key. Only these automated-action types are actionable through
     * the API — a notification that parks a run on an interactive human decision cannot be acted
     * on headlessly (dismiss it instead). A card that would retry a run on an individual-usage
     * model likewise cannot be acted on through the API. To record how much review a merged pull
     * request needed, call `POST /api/v1/merge-records/{recordId}/effort` (a `write` key) before
     * or after this; a `merge_tag_request` card carries its record id on the payload and is
     * resolved by tagging that record and dismissing the card.
     * {@code POST /api/v1/notifications/{id}/act} (operation {@code actPublicNotification}).
     */
    public Notification act(String id, ActPublicNotificationRequest body) {
        return transport.request("POST", "/api/v1/notifications/" + Transport.pathSegment(id) + "/act", body, Map.of(), new TypeReference<Notification>() {});
    }

    /**
     * Dismiss a notification
     * Dismiss a notification without acting on it.
     * {@code POST /api/v1/notifications/{id}/dismiss} (operation {@code
     * dismissPublicNotification}).
     */
    public Notification dismiss(String id) {
        return transport.request("POST", "/api/v1/notifications/" + Transport.pathSegment(id) + "/dismiss", null, Map.of(), new TypeReference<Notification>() {});
    }

    /**
     * List the workspace's open notifications
     * List the open, human-actionable notifications in the key’s workspace (merge reviews,
     * pipeline-complete confirmations, CI/test failures, and informational cards).
     * {@code GET /api/v1/notifications} (operation {@code listPublicNotifications}).
     */
    public PublicNotificationList list() {
        return transport.request("GET", "/api/v1/notifications", null, Map.of(), new TypeReference<PublicNotificationList>() {});
    }
}
