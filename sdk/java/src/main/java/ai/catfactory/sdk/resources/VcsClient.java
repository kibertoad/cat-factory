// GENERATED by `pnpm gen:sdk` from docs/openapi.json — DO NOT EDIT BY HAND.
// Regenerate after any change to the `/api/v1` contracts; `pnpm check:sdk` fails CI on drift.

package ai.catfactory.sdk.resources;

import ai.catfactory.sdk.EventStream;
import ai.catfactory.sdk.PageIterator;
import ai.catfactory.sdk.Transport;
import ai.catfactory.sdk.model.*;
import com.fasterxml.jackson.core.type.TypeReference;
import java.util.Iterator;
import java.util.Map;
import org.jspecify.annotations.Nullable;

/**
 * The workspace's source-control connection: which account it talks to, how it authenticates, and
 * whether it may create repositories and write workflow files. Both permissions are enforced by
 * the provider at push time, so reading them beats discovering one missing halfway through an
 * automated setup.
 * Reached from {@link CatFactoryClient}; not constructed directly.
 */
public final class VcsClient {
    private final Transport transport;

    VcsClient(Transport transport) {
        this.transport = transport;
    }

    /**
     * Read the workspace’s source-control connection and what it may do
     * The connected account, how the workspace authenticates to it, and the two permissions that
     * decide whether an automated flow can complete: whether the platform may create repositories,
     * and whether it may write workflow files. Both are enforced by the provider at push time, so
     * a caller that cannot read them discovers a missing workflow permission as a repository that
     * bootstrapped and then failed to gain its CI workflow. Provider-neutral: a GitLab-connected
     * workspace answers here too. `connection` is null when nothing is connected, which is a state
     * rather than an error.
     * {@code GET /api/v1/vcs/connection} (operation {@code getPublicVcsConnection}).
     */
    public GetPublicVcsConnectionResponse getConnection() {
        return transport.request("GET", "/api/v1/vcs/connection", null, Map.of(), new TypeReference<GetPublicVcsConnectionResponse>() {});
    }
}
