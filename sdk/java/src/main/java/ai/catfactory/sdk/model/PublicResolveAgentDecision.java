// GENERATED by `pnpm gen:sdk` from docs/openapi.json — DO NOT EDIT BY HAND.
// Regenerate after any change to the `/api/v1` contracts; `pnpm check:sdk` fails CI on drift.

package ai.catfactory.sdk.model;

import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonProperty;
import org.jspecify.annotations.Nullable;

/**
 * The {@code PublicResolveAgentDecision} wire model.
 * @param choice Length 1..4000.
 */
@JsonIgnoreProperties(ignoreUnknown = true)
public record PublicResolveAgentDecision(
    /** Length 1..4000. */
    @JsonProperty("choice") String choice
) {

    /** A new builder for {@link PublicResolveAgentDecision}. */
    public static Builder builder() {
        return new Builder();
    }

    /**
     * Fluent builder for {@link PublicResolveAgentDecision}.
     * Every setter is nullable so a caller supplies only what it means to send. Java has no
     * default arguments and Kotlin cannot synthesise them for a Java constructor, so this is the
     * shape that reads naturally from both languages.
     */
    public static final class Builder {
        private @Nullable String choice;

        /** Set {@code choice}. */
        public Builder choice(@Nullable String choice) {
            this.choice = choice;
            return this;
        }

        /** Build the {@link PublicResolveAgentDecision}. */
        public PublicResolveAgentDecision build() {
            return new PublicResolveAgentDecision(choice);
        }
    }
}
