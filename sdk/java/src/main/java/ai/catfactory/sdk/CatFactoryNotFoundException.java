// Hand-written (NOT generated).

package ai.catfactory.sdk;

import java.util.List;
import java.util.Map;
import org.jspecify.annotations.Nullable;

/** 404 — no such resource, or one outside this key's workspace (the two are indistinguishable, on purpose). */
public final class CatFactoryNotFoundException extends CatFactoryApiException {
    CatFactoryNotFoundException(
            int status,
            String code,
            String apiMessage,
            @Nullable Object details,
            List<Map<String, Object>> issues,
            @Nullable String requestId,
            @Nullable String rawBody) {
        super(status, code, apiMessage, details, issues, requestId, rawBody);
    }
}
