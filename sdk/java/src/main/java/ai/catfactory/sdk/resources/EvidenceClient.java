// GENERATED by `pnpm gen:sdk` from docs/openapi.json — DO NOT EDIT BY HAND.
// Regenerate after any change to the `/api/v1` contracts; `pnpm check:sdk` fails CI on drift.

package ai.catfactory.sdk.resources;

import ai.catfactory.sdk.EventStream;
import ai.catfactory.sdk.PageIterator;
import ai.catfactory.sdk.Transport;
import ai.catfactory.sdk.model.*;
import com.fasterxml.jackson.core.type.TypeReference;
import java.util.Iterator;
import java.util.Map;
import org.jspecify.annotations.Nullable;

/**
 * What a run proved: the engine's verification report, the outcome summary behind it, and the
 * artifacts it captured, bytes included.
 * Reached from {@link CatFactoryClient}; not constructed directly.
 */
public final class EvidenceClient {
    private final Transport transport;

    EvidenceClient(Transport transport) {
        this.transport = transport;
    }

    /**
     * Download an artifact's bytes
     * The stored bytes of one artifact listed by the run-artifacts endpoint, served with the
     * recorded image content type (`nosniff`, never inline active content). Authenticated like
     * every other call: the bytes are workspace-scoped, so a report that links here on a public
     * repository leaks nothing to a reader without a key. 404 when the id is unknown to the key’s
     * workspace, and separately when the metadata row survives but its bytes are gone from the
     * blob backend.
     * {@code GET /api/v1/artifacts/{artifactId}/blob} (operation {@code getPublicArtifactBlob}).
     */
    public byte[] downloadArtifact(String artifactId) {
        return transport.requestBytes("GET", "/api/v1/artifacts/" + Transport.pathSegment(artifactId) + "/blob", null, Map.of());
    }

    /**
     * Get a run's outcome summary
     * What the run changed and what backs that up, in product language, for a reader who will not
     * open the diff: the run’s disposition, the pull requests it opened, requirement coverage
     * joined to the service’s `spec/`, the tester’s verdict and concerns, the views it captured,
     * and the machine checks that ran. The same reduction the app’s outcome card renders, over the
     * same evidence the verification report is built from, so the two cannot state different
     * totals for one run. Nothing here is asserted by a model: every count is derived from
     * recorded verdicts. Prefer the verification report when you need a reviewer’s full bundle;
     * prefer this when you need to say what shipped. Sections state `reported` or `absent` with a
     * machine-readable gap code, and `truncations` names any list the response had to bound.
     * {@code GET /api/v1/runs/{runId}/outcome} (operation {@code getPublicRunOutcome}).
     */
    public GetPublicRunOutcomeResponse getOutcome(String runId) {
        return transport.request("GET", "/api/v1/runs/" + Transport.pathSegment(runId) + "/outcome", null, Map.of(), new TypeReference<GetPublicRunOutcomeResponse>() {});
    }

    /**
     * Get a run's verification report
     * The engine’s bundle of CAPTURED FACTS about a run: the CI gate’s verdict and failing checks,
     * the platform’s own run of the service’s lint/test/build commands (with the failing output),
     * the red-then-green reproduction proof for a bugfix, the tester’s structured report,
     * requirement coverage, the throwaway-environment lifecycle, judge verdicts and the merge
     * decision. Byte-for-byte the JSON block the pull-request body carries, composed on read, so
     * it also answers for a run that never opened a pull request. Each section states `reported`
     * or `absent` with a note, so a step that did not run never looks like a step that found
     * nothing.
     * {@code GET /api/v1/runs/{runId}/report} (operation {@code getPublicRunReport}).
     */
    public PrVerificationReport getReport(String runId) {
        return transport.request("GET", "/api/v1/runs/" + Transport.pathSegment(runId) + "/report", null, Map.of(), new TypeReference<PrVerificationReport>() {});
    }

    /**
     * List a run's captured artifacts
     * The binary artifacts the run captured (UI screenshots) plus the reference images they were
     * reviewed against: id, kind, view, content type, exact byte size and content hash. Unpaged:
     * the capture path caps how many one run may store, so the response size is bounded before the
     * request. Fetch the bytes with the blob endpoint.
     * {@code GET /api/v1/runs/{runId}/artifacts} (operation {@code listPublicRunArtifacts}).
     */
    public PublicRunArtifactList listArtifacts(String runId) {
        return transport.request("GET", "/api/v1/runs/" + Transport.pathSegment(runId) + "/artifacts", null, Map.of(), new TypeReference<PublicRunArtifactList>() {});
    }
}
