// GENERATED by `pnpm gen:sdk` from docs/openapi.json — DO NOT EDIT BY HAND.
// Regenerate after any change to the `/api/v1` contracts; `pnpm check:sdk` fails CI on drift.

package ai.catfactory.sdk.resources;

import ai.catfactory.sdk.EventStream;
import ai.catfactory.sdk.PageIterator;
import ai.catfactory.sdk.Transport;
import ai.catfactory.sdk.model.*;
import com.fasterxml.jackson.core.type.TypeReference;
import java.util.Iterator;
import java.util.Map;
import org.jspecify.annotations.Nullable;

/**
 * The workspace's own API keys: provision one headlessly, list them, revoke one (and what it
 * minted).
 * Reached from {@link CatFactoryClient}; not constructed directly.
 */
public final class KeysClient {
    private final Transport transport;

    KeysClient(Transport transport) {
        this.transport = transport;
    }

    /**
     * Provision an API key
     * Mint a key for the calling key’s own workspace and return its raw secret EXACTLY ONCE —
     * store it now, it is not recoverable. Omitting `scope` mints a `write` key. `admin` cannot be
     * minted here: a key provisioned over the API can never itself provision, which keeps the
     * chain one link long. Requires an `admin`-scope key.
     * {@code POST /api/v1/keys} (operation {@code createPublicKey}).
     */
    public CreatePublicKeyResponse create(CreatePublicKeyRequest body) {
        return transport.request("POST", "/api/v1/keys", body, Map.of(), new TypeReference<CreatePublicKeyResponse>() {});
    }

    /**
     * List the workspace's API keys
     * The live (non-revoked) keys for the calling key’s workspace, metadata only — a secret is
     * never readable back. `createdByKeyId` names the key that provisioned a key headlessly;
     * `createdByUserId` names the person who minted one in the app.
     * {@code GET /api/v1/keys} (operation {@code listPublicKeys}).
     */
    public ListPublicKeysResponse list() {
        return transport.request("GET", "/api/v1/keys", null, Map.of(), new TypeReference<ListPublicKeysResponse>() {});
    }

    /**
     * Revoke an API key
     * Revoke a key AND every key it minted, so a leaked provisioning key cannot outlive its own
     * revocation through the credentials it left behind. Idempotent, and it may name the calling
     * key. Requires an `admin`-scope key.
     * {@code DELETE /api/v1/keys/{keyId}} (operation {@code revokePublicKey}).
     */
    public void revoke(String keyId) {
        transport.requestNoContent("DELETE", "/api/v1/keys/" + Transport.pathSegment(keyId), null, Map.of());
    }
}
