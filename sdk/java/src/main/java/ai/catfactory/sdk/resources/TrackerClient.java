// GENERATED by `pnpm gen:sdk` from docs/openapi.json — DO NOT EDIT BY HAND.
// Regenerate after any change to the `/api/v1` contracts; `pnpm check:sdk` fails CI on drift.

package ai.catfactory.sdk.resources;

import ai.catfactory.sdk.EventStream;
import ai.catfactory.sdk.PageIterator;
import ai.catfactory.sdk.Transport;
import ai.catfactory.sdk.model.*;
import com.fasterxml.jackson.core.type.TypeReference;
import java.util.Iterator;
import java.util.Map;
import org.jspecify.annotations.Nullable;

/**
 * What this workspace does to a task's LINKED tracker issue as its pull request progresses:
 * comment when it opens, comment and close the issue when it merges, and post a headless run's
 * parked review findings so the reporter can answer where they filed. The write MERGES, so turning
 * one action on leaves the other two as they were. It is the writeback half of the workspace's
 * tracker configuration; the filing selection (which tracker a tech-debt ticket is raised on) is
 * not published yet.
 * Reached from {@link CatFactoryClient}; not constructed directly.
 */
public final class TrackerClient {
    private final Transport transport;

    TrackerClient(Transport transport) {
        this.transport = transport;
    }

    /**
     * Read the workspace’s tracker writeback disposition
     * What this workspace does to a task’s LINKED tracker issue as its pull request progresses:
     * comment when the pull request opens, comment and close the issue when it merges, and post a
     * headless run’s parked requirements-review findings so the reporter can answer where they
     * filed. Worth reading before filing a ticket-linked task, since it decides whether the issue
     * the work came from ever hears the outcome. `updatedAt` is null when nobody has chosen a
     * disposition, in which case the values are this deployment’s defaults (all three ON).
     * Requires an `admin` key.
     * {@code GET /api/v1/tracker/writeback} (operation {@code getPublicTrackerWriteback}).
     */
    public GetPublicTrackerWritebackResponse getWriteback() {
        return transport.request("GET", "/api/v1/tracker/writeback", null, Map.of(), new TypeReference<GetPublicTrackerWritebackResponse>() {});
    }

    /**
     * Change the workspace’s tracker writeback disposition (no body).
     */
    public GetPublicTrackerWritebackResponse updateWriteback() {
        return updateWriteback(UpdatePublicTrackerWritebackRequest.builder().build());
    }

    /**
     * Change the workspace’s tracker writeback disposition
     * Turn one or more writeback actions on or off. A MERGE: an action you omit keeps its stored
     * value, so a caller acting on one decision cannot silently move the other two. This is
     * workspace-wide configuration, so it changes what happens to every task’s ticket on the
     * board; the read beside it reports `updatedAt` so a caller can see whether it is about to
     * overwrite somebody’s choice. An empty patch is a no-op and does not stamp `updatedAt`.
     * Requires an `admin` key.
     * {@code PATCH /api/v1/tracker/writeback} (operation {@code updatePublicTrackerWriteback}).
     */
    public GetPublicTrackerWritebackResponse updateWriteback(UpdatePublicTrackerWritebackRequest body) {
        return transport.request("PATCH", "/api/v1/tracker/writeback", body, Map.of(), new TypeReference<GetPublicTrackerWritebackResponse>() {});
    }
}
