{
  "name": "@cat-factory/deploy-gatekeeper",
  "version": "0.0.0",
  "private": true,
  "description": "Template Cloudflare OS Gatekeeper deployment for cat-factory: the wrangler config, the policy an operator writes, and three lines of wiring over the published @cat-factory/gatekeeper-worker. Copy this package, point it at your workspace and edit src/policy.config.ts.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest",
    "test:run": "vitest run"
  },
  "dependencies": {
    "@cat-factory/gatekeeper-worker": "workspace:*"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "5.20260904.1",
    "typescript": "7.0.2",
    "vitest": "^4.1.11",
    "wrangler": "4.124.0"
  }
}
